%% Data Flow Diagram: Guardrails Regex Detector
%% Generated by: Claude Opus 4.6
%% Source: architecture/rhoai-3.4-ea.2/guardrails-regex-detector.md

sequenceDiagram
    participant Orchestrator as FMS Guardrails<br/>Orchestrator
    participant Detector as Regex Detector<br/>8080/TCP HTTP
    participant Kubelet as Kubernetes Kubelet<br/>Readiness Probe

    Note over Orchestrator,Detector: Flow 1: PII Detection Request

    Orchestrator->>Detector: POST /api/v1/text/contents<br/>HTTP 8080/TCP | plaintext | No Auth
    Note right of Detector: Parse JSON request body<br/>Extract contents[] and<br/>detector_params.regex[]

    alt Built-in patterns requested
        Note right of Detector: Match against built-in:<br/>- email (EmailAddress)<br/>- ssn (SocialSecurity)<br/>- credit-card (CreditCard)
    end

    alt Custom regex patterns provided
        Note right of Detector: Compile and match<br/>arbitrary regex patterns
    end

    Detector-->>Orchestrator: JSON Response<br/>[[{start, end, text,<br/>detection_type, detection, score}]]

    Note over Kubelet,Detector: Flow 2: Health Check

    Kubelet->>Detector: GET /health<br/>HTTP 8080/TCP | plaintext
    Detector-->>Kubelet: 200 OK "healthy"
