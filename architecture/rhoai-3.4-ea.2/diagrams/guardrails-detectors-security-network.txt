Guardrails Detectors - Security Network Architecture
Generated by: Claude Opus 4.6
Source: architecture/rhoai-3.4-ea.2/guardrails-detectors.md

┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CALLER (Service Mesh - mTLS)                                  │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Calls all three detectors for content safety analysis                 │
│         │                                                                │
│         ├── Port: 8080/TCP (built-in)                                    │
│         │   Protocol: HTTP                                               │
│         │   Encryption: mTLS (Istio sidecar)                             │
│         │   Auth: Istio PeerAuthentication                               │
│         │                                                                │
│         ├── Port: 8000/TCP (huggingface)                                 │
│         │   Protocol: HTTP                                               │
│         │   Encryption: mTLS (Istio sidecar)                             │
│         │   Auth: Istio PeerAuthentication                               │
│         │                                                                │
│         └── Port: 8000/TCP (llm-judge)                                   │
│             Protocol: HTTP                                               │
│             Encryption: mTLS (Istio sidecar)                             │
│             Auth: Istio PeerAuthentication                               │
│             ▼                                                            │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (mTLS via Istio Sidecar Injection)                        │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ [Built-in Detector]                                              │    │
│  │ - Service: built-in-detector (ClusterIP)                         │    │
│  │ - Port: 8080/TCP                                                 │    │
│  │ - Image: quay.io/rhoai/odh-built-in-detector-rhel9               │    │
│  │ - Base: UBI9-minimal                                              │    │
│  │ - Arch: amd64, arm64, ppc64le, s390x                             │    │
│  │ - Annotations:                                                    │    │
│  │   - sidecar.istio.io/inject: 'true'                              │    │
│  │   - security.opendatahub.io/enable-auth: 'true'                  │    │
│  │ - Endpoints:                                                      │    │
│  │   - POST /api/v1/text/contents (detection)                       │    │
│  │   - GET  /health (liveness/readiness)                             │    │
│  │   - GET  /metrics (Prometheus)                                    │    │
│  │   - GET  /registry (list detectors)                               │    │
│  │ - Processing: Local only (regex, file-type, custom)               │    │
│  │ - No egress required                                              │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ [HuggingFace Detector]                                           │    │
│  │ - Service: huggingface-detector (ClusterIP via KServe)           │    │
│  │ - Port: 8000/TCP                                                 │    │
│  │ - Image: quay.io/rhoai/odh-guardrails-detector-huggingface-      │    │
│  │          runtime-rhel9                                            │    │
│  │ - Base: UBI9-minimal                                              │    │
│  │ - Arch: amd64, arm64, ppc64le, s390x                             │    │
│  │ - Deployed via: KServe InferenceService + ServingRuntime          │    │
│  │ - Annotations:                                                    │    │
│  │   - serving.kserve.io/deploymentMode: RawDeployment               │    │
│  │   - sidecar.istio.io/inject: 'true'                              │    │
│  │   - security.opendatahub.io/enable-auth: 'true'                  │    │
│  │ - Endpoints:                                                      │    │
│  │   - POST /api/v1/text/contents (detection)                       │    │
│  │   - GET  /health (liveness/readiness)                             │    │
│  │   - GET  /metrics (Prometheus)                                    │    │
│  │ - Processing: Local GPU/CPU inference                             │    │
│  │         │                                                         │    │
│  │         │ Egress: Model download at init                          │    │
│  │         │ Port: 9000/TCP                                          │    │
│  │         │ Protocol: HTTP                                          │    │
│  │         │ Encryption: None (internal)                             │    │
│  │         │ Auth: AWS access key/secret key                         │    │
│  │         ▼                                                         │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ [LLM Judge Detector]                                             │    │
│  │ - Service: llm-judge-detector (ClusterIP via KServe)             │    │
│  │ - Port: 8000/TCP                                                 │    │
│  │ - Deployed via: KServe InferenceService + ServingRuntime          │    │
│  │ - Annotations:                                                    │    │
│  │   - serving.kserve.io/deploymentMode: RawDeployment               │    │
│  │   - sidecar.istio.io/inject: 'true'                              │    │
│  │   - security.opendatahub.io/enable-auth: 'true'                  │    │
│  │ - Endpoints:                                                      │    │
│  │   - POST /api/v1/text/contents (detection)                       │    │
│  │   - POST /api/v1/text/generation (prompt+response eval)          │    │
│  │   - GET  /api/v1/metrics (list eval metrics)                     │    │
│  │   - GET  /health (liveness/readiness)                             │    │
│  │   - GET  /metrics (Prometheus)                                    │    │
│  │ - Processing: Delegates to external vLLM server                   │    │
│  │         │                                                         │    │
│  │         │ Egress: LLM evaluation requests                         │    │
│  │         │ Port: 8080/TCP (configurable)                           │    │
│  │         │ Protocol: HTTP                                          │    │
│  │         │ Encryption: Configurable                                │    │
│  │         │ Auth: Configurable                                      │    │
│  │         ▼                                                         │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [S3-compatible Storage (MinIO)]                                         │
│  - Purpose: Model weight storage for HuggingFace detector                │
│  - Port: 9000/TCP                                                        │
│  - Protocol: HTTP                                                        │
│  - Encryption: None (internal)                                           │
│  - Auth: AWS access key / secret key                                     │
│                                                                          │
│  [External vLLM Server]                                                  │
│  - Purpose: LLM evaluation for Judge detector                            │
│  - Port: 8080/TCP (configurable)                                         │
│  - Protocol: HTTP (OpenAI-compatible API)                                │
│  - Encryption: Configurable                                              │
│  - Auth: Configurable                                                    │
│                                                                          │
│  [HuggingFace Hub] (development/example only)                            │
│  - Purpose: Model download during init container                         │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: HF token (optional)                                             │
│                                                                          │
│  [Prometheus]                                                            │
│  - Purpose: Metrics collection                                           │
│  - Scrapes: /metrics on 8080/TCP or 8000/TCP                            │
│  - Protocol: HTTP                                                        │
│  - Auth: None                                                            │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- RoleBinding: user-one-view
  - Namespace: (deployment namespace)
  - Role: ClusterRole: view
  - ServiceAccount: user-one
  - Note: Only in example deployment manifests; no custom ClusterRoles defined

Secrets:
- aws-connection-minio-data-connection-guardrails-guardian (Opaque)
  - Contents: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_S3_BUCKET,
              AWS_S3_ENDPOINT, AWS_DEFAULT_REGION
  - Provisioned By: Manual / ODH Dashboard
  - Auto-Rotate: No

Authentication & Authorization:
- /api/v1/text/contents (POST): Istio mTLS + KServe auth
  - Enforcement: Istio sidecar / KServe auth proxy
  - Annotation: security.opendatahub.io/enable-auth: 'true'
- /health (GET): Open, no auth
- /metrics (GET): Open, no auth

Security Sandboxing (Custom Detectors):
- Static code analysis on user-provided Python detector code
- Forbidden imports: os (except os.environ/os.getenv), subprocess, sys, shutil
- Forbidden function calls: eval, exec, open, compile, input

Service Mesh Configuration:
- Istio sidecar injection: enabled (sidecar.istio.io/inject: 'true')
- PeerAuthentication: mTLS enforced via Istio
- All detector-to-orchestrator communication encrypted via mTLS
