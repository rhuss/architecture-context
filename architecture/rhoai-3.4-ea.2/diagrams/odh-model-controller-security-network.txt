Security Network Diagram: odh-model-controller
Generated by: Claude Opus 4.6

================================================================================

┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [User / Dashboard]                                                          │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: Bearer Token                                                 │
│         ▼                                                                    │
│  [Kubernetes API Server]                                                     │
│                                                                              │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS Edge / Reencrypt                                   │
│         │ Auth: None or TLS client cert                                      │
│         ▼                                                                    │
│  [OpenShift Router] ──► [InferenceService Pod]                               │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (Trusted)                                                  │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │  odh-model-controller Pod                                           │     │
│  │  Namespace: Controller namespace (e.g., redhat-ods-applications)    │     │
│  │  ServiceAccount: odh-model-controller                               │     │
│  │                                                                     │     │
│  │  EXPOSED PORTS:                                                     │     │
│  │  ┌──────────────────────────────────────────────────────────────┐   │     │
│  │  │ Port: 9443/TCP  │ Protocol: HTTPS  │ Encryption: TLS         │   │     │
│  │  │ Purpose: Webhook server (mutating + validating)              │   │     │
│  │  │ Auth: Kubernetes API server TLS client cert                  │   │     │
│  │  │ Endpoints:                                                   │   │     │
│  │  │   POST /mutate--v1-pod                                       │   │     │
│  │  │   POST /mutate-serving-kserve-io-v1beta1-inferenceservice    │   │     │
│  │  │   POST /mutate-serving-kserve-io-v1alpha1-inferencegraph     │   │     │
│  │  │   POST /validate-nim-opendatahub-io-v1-account               │   │     │
│  │  │   POST /validate-serving-kserve-io-v1beta1-inferenceservice  │   │     │
│  │  │   POST /validate-serving-kserve-io-v1alpha1-inferencegraph   │   │     │
│  │  └──────────────────────────────────────────────────────────────┘   │     │
│  │  ┌──────────────────────────────────────────────────────────────┐   │     │
│  │  │ Port: 8081/TCP  │ Protocol: HTTP   │ Encryption: None        │   │     │
│  │  │ Purpose: Health probes (/healthz, /readyz)                   │   │     │
│  │  │ Auth: None                                                   │   │     │
│  │  └──────────────────────────────────────────────────────────────┘   │     │
│  │  ┌──────────────────────────────────────────────────────────────┐   │     │
│  │  │ Port: 8080/TCP  │ Protocol: HTTP   │ Encryption: None        │   │     │
│  │  │ Purpose: Prometheus metrics (/metrics)                       │   │     │
│  │  │ Auth: None (TODO: switch to 8443/HTTPS)                      │   │     │
│  │  └──────────────────────────────────────────────────────────────┘   │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│                                                                              │
│  SERVICES:                                                                   │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │ odh-model-controller-webhook-service (ClusterIP)                     │    │
│  │   Port: 443/TCP → Target: 9443/TCP                                  │    │
│  │   Protocol: HTTPS │ Encryption: TLS (OpenShift serving cert)        │    │
│  │   Auth: Kubernetes API server                                        │    │
│  │   Exposure: Internal only                                            │    │
│  ├──────────────────────────────────────────────────────────────────────┤    │
│  │ odh-model-controller-metrics-service (ClusterIP)                     │    │
│  │   Port: 8080/TCP → Target: 8080/TCP                                 │    │
│  │   Protocol: HTTP │ Encryption: None                                  │    │
│  │   Auth: None                                                         │    │
│  │   Exposure: Internal only                                            │    │
│  ├──────────────────────────────────────────────────────────────────────┤    │
│  │ {isvc-name}-metrics (ClusterIP, per ISVC, dynamically created)       │    │
│  │   Port: {prometheus-port}/TCP                                        │    │
│  │   Protocol: HTTP │ Encryption: None                                  │    │
│  │   Auth: None                                                         │    │
│  │   Exposure: Internal only                                            │    │
│  └──────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
│  INGRESS (Dynamically Created):                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │ {isvc-name} Route (OpenShift, per ISVC with visibility=exposed)      │    │
│  │   Host: Auto-generated                                               │    │
│  │   Port: 443/TCP │ Protocol: HTTPS                                    │    │
│  │   TLS Mode: Edge (no auth) / Reencrypt (with auth)                  │    │
│  │   Exposure: External                                                 │    │
│  ├──────────────────────────────────────────────────────────────────────┤    │
│  │ LLMInferenceService Auth (RHOAI 3.x pattern):                        │    │
│  │   Gateway AuthPolicy → Authorino (UserDefined auth)                  │    │
│  │   HTTPRoute AuthPolicy → Authorino (anonymous when no auth)          │    │
│  │   EnvoyFilter on Gateway for Authorino TLS bootstrap                 │    │
│  └──────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
│         │                                                                    │
│  EGRESS:│                                                                    │
│         │                                                                    │
│         ├──► Kubernetes API Server                                           │
│         │    Port: 443/TCP │ Protocol: HTTPS │ Encryption: TLS 1.2+         │
│         │    Auth: ServiceAccount token                                      │
│         │    Purpose: Controller-runtime API calls (watch, create, update)   │
│         │                                                                    │
│         ├──► Model Registry Service                                          │
│         │    Port: Variable/TCP │ Protocol: HTTPS                            │
│         │    Encryption: TLS (configurable, skippable)                       │
│         │    Auth: Bearer token                                              │
│         │    Purpose: Sync InferenceService deployment status (opt-in)       │
│         │                                                                    │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  [NVIDIA NGC API - api.ngc.nvidia.com]                                       │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: NGC API Key (from NIM Account Secret)                               │
│  - Purpose: NIM API key validation and model catalog fetch                   │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

================================================================================
RBAC Summary:
================================================================================

ClusterRole: odh-model-controller-role
  ServiceAccount: odh-model-controller (bound via odh-model-controller-rolebinding-opendatahub)

  Core API Resources:
  - "" / configmaps, secrets, serviceaccounts, services:
      get, list, watch, create, update, delete, patch
  - "" / endpoints, namespaces, pods:
      get, list, watch, create, update, patch
  - "" / events:
      create, patch

  KServe Resources:
  - serving.kserve.io / inferenceservices:
      get, list, watch, update, patch
  - serving.kserve.io / inferenceservices/finalizers:
      get, list, watch, create, update, delete, patch
  - serving.kserve.io / servingruntimes:
      get, list, watch, create, update
  - serving.kserve.io / inferencegraphs, llminferenceserviceconfigs:
      get, list, watch
  - serving.kserve.io / llminferenceservices:
      get, list, watch, update, patch, post
  - serving.kserve.io / llminferenceservices/status:
      get, update, patch
  - serving.kserve.io / llminferenceservices/finalizers:
      update, patch

  NIM Resources:
  - nim.opendatahub.io / accounts:
      get, list, watch, update, patch
  - nim.opendatahub.io / accounts/status:
      get, list, watch, update
  - nim.opendatahub.io / accounts/finalizers:
      update

  OpenShift Resources:
  - route.openshift.io / routes:
      get, list, watch, create, update, patch, delete
  - route.openshift.io / routes/custom-host:
      create
  - template.openshift.io / templates:
      get, list, watch, create, update, delete, patch
  - config.openshift.io / authentications:
      get, list, watch

  Gateway API / Kuadrant / Istio:
  - gateway.networking.k8s.io / gateways:
      get, list, watch, update, patch
  - gateway.networking.k8s.io / gateways/finalizers:
      update, patch
  - gateway.networking.k8s.io / httproutes:
      get, list, watch
  - kuadrant.io / authpolicies:
      get, list, watch, create, update, patch, delete
  - kuadrant.io / authpolicies/status:
      get, update, patch
  - kuadrant.io / kuadrants:
      get, list, watch
  - operator.authorino.kuadrant.io / authorinos:
      get, list, watch
  - networking.istio.io / envoyfilters:
      get, list, watch, create, update, patch, delete

  Networking:
  - networking.k8s.io / networkpolicies:
      get, list, watch, create, update, patch, delete
  - networking.k8s.io, extensions / ingresses:
      get, list, watch

  Monitoring / RBAC / KEDA:
  - monitoring.coreos.com / servicemonitors, podmonitors:
      get, list, watch, create, update, patch, delete
  - rbac.authorization.k8s.io / clusterrolebindings, rolebindings, roles:
      get, list, watch, create, update, patch, delete
  - keda.sh / triggerauthentications:
      get, list, watch, create, update, patch, delete
  - metrics.k8s.io / pods, nodes:
      get, list, watch

  ODH Platform:
  - datasciencecluster.opendatahub.io / datascienceclusters:
      get, list, watch
  - dscinitialization.opendatahub.io / dscinitializations:
      get, list, watch

Role Bindings:
  - odh-model-controller-rolebinding-opendatahub (Cluster-scoped)
      → ClusterRole: odh-model-controller-role
      → SA: odh-model-controller
  - leader-election-rolebinding (Controller namespace)
      → Role: leader-election-role
      → SA: odh-model-controller
  - prometheus-ns-access (dynamically created, user namespaces)
      → ClusterRole: prometheus-ns-access
      → SA: prometheus-custom

================================================================================
Secrets:
================================================================================

- odh-model-controller-webhook-cert (kubernetes.io/tls)
    Purpose: Webhook server TLS certificate
    Provisioned By: OpenShift service-ca (serving-cert annotation)
    Auto-Rotate: Yes

- storage-config (Opaque, per namespace)
    Purpose: Aggregated S3 storage credentials for KServe model loading
    Provisioned By: odh-model-controller (Secret controller)
    Auto-Rotate: No

- ray-ca-tls (Opaque)
    Purpose: Self-signed CA certificate for Ray TLS
    Provisioned By: odh-model-controller (ServingRuntime controller)
    Auto-Rotate: No

- ray-tls (Opaque, per namespace)
    Purpose: Per-pod Ray TLS server certificates signed by CA
    Provisioned By: odh-model-controller (Pod controller)
    Auto-Rotate: No

- inference-prometheus-auth (ServiceAccountToken, per namespace)
    Purpose: KEDA Prometheus autoscaling bearer token
    Provisioned By: odh-model-controller (KEDA reconciler)
    Auto-Rotate: Yes (Kubernetes managed)

- NIM pull secret (kubernetes.io/dockerconfigjson, per NIM Account)
    Purpose: NVIDIA NGC container registry pull secret
    Provisioned By: odh-model-controller (NIM Account controller)
    Auto-Rotate: No

================================================================================
Authentication & Authorization:
================================================================================

- Webhook endpoints (POST):
    Auth: Kubernetes API server TLS client cert
    Enforcement: MutatingWebhookConfiguration / ValidatingWebhookConfiguration

- InferenceService Routes (auth enabled):
    Auth: TLS Reencrypt via OpenShift Router
    Enforcement: OpenShift Router (Route TLS termination with reencrypt)

- LLMInferenceService (Gateway auth):
    Auth: Kuadrant AuthPolicy → Authorino
    Enforcement: Gateway API + Authorino (UserDefined or Anonymous)

- LLMInferenceService (HTTPRoute auth):
    Auth: Kuadrant AuthPolicy → Authorino
    Enforcement: Gateway API + Authorino (anonymous when auth not UserDefined)
