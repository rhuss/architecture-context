Security Network Diagram: vllm-orchestrator-gateway
Generated by: Claude Opus 4.6
Source: architecture/rhoai-3.4-ea.2/vllm-orchestrator-gateway.md

┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Client]                                                                │
│         │                                                                │
│         │ Port: 8090/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: plaintext                                          │
│         │ Auth: Authorization header (passthrough)                       │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  GATEWAY SERVICE (vllm-orchestrator-gateway)                            │
│                                                                          │
│  [vllm-orchestrator-gateway]                                             │
│  - Runtime: Rust 1.84.0 (axum + tokio)                                  │
│  - Listen: 0.0.0.0:8090/TCP (configurable via HOST, HTTP_PORT)          │
│  - Exposure: Internal (no ingress defined)                               │
│  - Auth: None enforced (passthrough)                                     │
│  - Config: YAML via GATEWAY_CONFIG env var                               │
│                                                                          │
│  Endpoints:                                                              │
│  - POST /{route_name}/v1/chat/completions                               │
│    - Dynamic per-route registration at startup                           │
│    - Each route maps to configured detector pipeline                     │
│    - Supports stream: true (SSE) and stream: false (JSON)               │
│         │                                                                │
│         │ Port: 8085/TCP (configurable)                                  │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: Optional mTLS                                      │
│         │   - Client cert: /etc/tls/private/tls.crt                     │
│         │   - Client key:  /etc/tls/private/tls.key                     │
│         │   - CA cert:     /etc/tls/ca/service-ca.crt                   │
│         │   - Auto-detected: HTTPS enabled when certs present           │
│         │ Auth: Forwarded Authorization header + x-forwarded-* headers  │
│         │ Target: POST /api/v2/chat/completions-detection               │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  BACKEND SERVICES (Internal ODH)                                        │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Port: 8085/TCP (or 8032/TCP, configurable)                           │
│  - Protocol: HTTP/HTTPS                                                  │
│  - Purpose: Chat completions with detector-based content analysis       │
│  - Endpoint: POST /api/v2/chat/completions-detection                    │
│         │                                                                │
│         ├──── [Guardrails Detectors]                                     │
│         │     - PII detection, regex-based filtering, etc.               │
│         │     - Referenced by detector name in gateway routes            │
│         │     - Interaction via Orchestrator (indirect)                  │
│         │                                                                │
│         └──── [vLLM / LLM Backend]                                      │
│               - Language model inference                                 │
│               - Interaction via Orchestrator (indirect)                  │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined (stateless HTTP service, no Kubernetes API access)
- No RoleBindings defined

Authentication & Authorization:
- Gateway does NOT enforce authentication
- Authorization headers are forwarded unchanged to orchestrator
- x-forwarded-* headers are forwarded unchanged
- Auth enforcement delegated to orchestrator or upstream infrastructure

Secrets:
- tls.crt / tls.key (at /etc/tls/private/)
  - Type: kubernetes.io/tls
  - Purpose: mTLS client identity for orchestrator communication
  - Provisioned by: External (cert-manager or service-ca-operator)
  - Auto-Rotate: Unknown

- service-ca.crt (at /etc/tls/ca/)
  - Type: CA certificate
  - Purpose: Trusted CA for verifying orchestrator's server certificate
  - Provisioned by: External (service-ca-operator)
  - Auto-Rotate: Unknown

mTLS Configuration:
- Auto-detection: Gateway checks for TLS certs at fixed paths on startup
- When certs found: scheme switches from http:// to https://
- CA cert added as trusted root for orchestrator server verification
- Client cert/key pair used for mTLS identity
- Certificate paths:
  - /etc/tls/private/tls.crt (client certificate)
  - /etc/tls/private/tls.key (client private key)
  - /etc/tls/ca/service-ca.crt (CA certificate)

Environment Variables:
- GATEWAY_CONFIG: Path to config YAML (default: config/config.yaml)
- HTTP_PORT: Listening port (default: 8090)
- HOST: Bind address (default: 0.0.0.0)
