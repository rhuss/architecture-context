Security Network Diagram: llm-d-inference-scheduler
Generated by: Claude Opus 4.6
Source: architecture/rhoai-3.4-ea.2/llm-d-inference-scheduler.md

┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client]                                                       │
│         │                                                                │
│         │ Port: 80/TCP (dev) or 443/TCP (production)                     │
│         │ Protocol: HTTP/HTTPS                                           │
│         │ Encryption: TLS (production)                                   │
│         │ Auth: Varies                                                   │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Gateway API)                                                  │
│                                                                          │
│  [Envoy Gateway - inference-gateway]                                     │
│  - Type: gateway.networking.k8s.io/v1 Gateway                           │
│  - Port: 80/TCP (dev default, no TLS)                                   │
│  - Production: Managed by platform operator + Istio w/ TLS              │
│  - Routes via: HTTPRoute → InferencePool                                │
│         │                                                                │
│         │ Port: 9002/TCP                                                 │
│         │ Protocol: gRPC/HTTP2 (ext-proc)                                │
│         │ Encryption: plaintext                                          │
│         │ Auth: None (cluster-internal trust)                            │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (Trusted)                                             │
│                                                                          │
│  [EPP - Endpoint Picker Plugin]                                          │
│  - Namespace: ${NAMESPACE}                                               │
│  - ServiceAccount: ${EPP_NAME}                                           │
│  - Image: odh-llm-d-inference-scheduler                                  │
│  - Ports: 9002/TCP (ext-proc), 9003/TCP (health), 9090/TCP (metrics)    │
│  - Service Type: ClusterIP                                               │
│  - Runs as: non-root (UID 65532)                                         │
│         │                                                                │
│         │ (Envoy routes to selected pod based on EPP decision)           │
│         ▼                                                                │
│  [PD Routing Sidecar]                                                    │
│  - Deployment: Co-located with decode worker pods                        │
│  - Image: odh-llm-d-routing-sidecar (FIPS-compliant TLS)               │
│  - Port: 8000/TCP                                                        │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+ (self-signed default or provided cert)          │
│  - SSRF Protection: InferencePool pod allowlist                          │
│  - Runs as: non-root (UID 65532)                                         │
│         │                                         │                      │
│         │ Prefill request                         │ Decode request        │
│         │ Port: 8000/TCP                          │ Port: 8001/TCP        │
│         │ Protocol: HTTP/HTTPS                    │ Protocol: HTTP/HTTPS  │
│         │ Encryption: TLS 1.2+ (configurable)     │ Encryption: TLS 1.2+ │
│         │ Auth: SSRF allowlist validated           │ Auth: None            │
│         ▼                                         ▼                      │
│  [Prefill Worker vLLM]                  [Local Decode vLLM]              │
│  - Port: 8000-8001/TCP                  - Port: 8001/TCP                 │
│  - Protocol: HTTP/HTTPS                 - Protocol: HTTP/HTTPS           │
│         │                                         │                      │
│         │◄───────────────────────────────────────►│                      │
│         │  KV Cache Transfer                                             │
│         │  Protocol: NIXL v2 / SGLang / shared-storage                   │
│         │  Auth: None                                                    │
│                                                                          │
│  [UDS Tokenizer Sidecar]                                                 │
│  - Socket: /tmp/tokenizer (Unix Domain Socket)                           │
│  - Protocol: gRPC over UDS                                               │
│  - Auth: None (pod-local)                                                │
│                                                                          │
│  [ZMQ KV-Events]                                                         │
│  - EPP subscribes via ZMQ SUB: 5556/TCP                                  │
│  - EPP service exposes ZMQ: 5557/TCP                                     │
│  - Protocol: ZMQ PUB/SUB                                                 │
│  - Encryption: plaintext                                                 │
│  - Auth: None                                                            │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [Kubernetes API - kubernetes.default.svc]                                │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: ServiceAccount token                                            │
│  - Purpose: Watch InferencePool and Pod resources                        │
│                                                                          │
│  [HuggingFace Hub - huggingface.co]                                      │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: HF_TOKEN (Bearer)                                               │
│  - Purpose: Download tokenizer configurations                            │
│                                                                          │
│  [Prometheus]                                                            │
│  - Port: 9090/TCP                                                        │
│  - Protocol: HTTP                                                        │
│  - Encryption: plaintext                                                 │
│  - Auth: None                                                            │
│  - Purpose: Metrics scrape                                               │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- Role: ${EPP_NAME}
  - API Groups: inference.networking.x-k8s.io
    Resources: inferencepools, inferenceobjectives, inferencemodelrewrites
    Verbs: get, watch, list
  - API Groups: inference.networking.k8s.io
    Resources: inferencepools
    Verbs: get, watch, list
  - API Groups: "" (core)
    Resources: pods
    Verbs: get, watch, list
  - API Groups: authentication.k8s.io
    Resources: tokenreviews
    Verbs: create
  - API Groups: authorization.k8s.io
    Resources: subjectaccessreviews
    Verbs: create

- Role: exec-role
  - API Groups: "" (core)
    Resources: pods/exec
    Verbs: create

Role Bindings:
- ${EPP_NAME}-binding → ${EPP_NAME} role → ${EPP_NAME} service account
- exec-rolebinding → exec-role → operator-controller-manager service account

SSRF Protection:
- PD Routing Sidecar implements InferencePool-based allowlist
- Watches InferencePool CRs and matching pods via Kubernetes informers
- Only allows prefill requests to pod IPs/names within configured InferencePool
- Enabled via --enable-ssrf-protection flag
- When disabled (default), all prefill targets allowed for backward compatibility

Secrets:
- TLS cert (--cert-path) (kubernetes.io/tls) - PD sidecar HTTPS cert, manual/cert-manager, no auto-rotate
- Self-signed TLS (in-memory) - Auto-generated at startup when no cert-path, per-restart rotation
- HF_TOKEN (Opaque env var) - HuggingFace API token for tokenizer download, manual, no auto-rotate

Container Security:
- Both images run as non-root (UID 65532)
- EPP image: UBI9 ubi-minimal base
- Sidecar image: UBI9 ubi-micro base, built with -tags strictfipsruntime (FIPS compliance)
