Security Network Diagram: Guardrails Regex Detector
Generated by: Claude Opus 4.6
Source: architecture/rhoai-3.4-ea.2/guardrails-regex-detector.md

===============================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CLUSTER (In-cluster only - No external exposure)              │
│                                                                          │
│  ┌──────────────────────────────────────┐                                │
│  │ FMS Guardrails Orchestrator          │                                │
│  │ (Caller service)                     │                                │
│  └──────────────┬───────────────────────┘                                │
│                 │                                                        │
│                 │ Port: 8080/TCP                                          │
│                 │ Protocol: HTTP                                          │
│                 │ Encryption: plaintext (in-cluster)                      │
│                 │ Auth: None                                              │
│                 │ Note: Relies on network-level isolation;                │
│                 │       mTLS via Istio sidecar in production              │
│                 ▼                                                        │
│  ┌──────────────────────────────────────┐                                │
│  │ Guardrails Regex Detector            │                                │
│  │ - Service: regex-detector (ClusterIP)│                                │
│  │ - Port: 8080/TCP → 8080/TCP          │                                │
│  │ - Image: Rust binary on ubi9-minimal │                                │
│  │ - Language: Rust 1.84.0 (Axum)       │                                │
│  │ - Runs as: non-root                  │                                │
│  │ - FIPS: enabled at platform level    │                                │
│  │                                       │                                │
│  │ Endpoints:                            │                                │
│  │   POST /api/v1/text/contents          │                                │
│  │     → PII detection (email, SSN, CC) │                                │
│  │     → Custom regex matching           │                                │
│  │   GET /health                         │                                │
│  │     → Health check                    │                                │
│  └──────────────────────────────────────┘                                │
│                 ▲                                                        │
│                 │ Port: 8080/TCP                                          │
│                 │ Protocol: HTTP                                          │
│                 │ Encryption: plaintext                                   │
│                 │ Auth: None                                              │
│                 │                                                        │
│  ┌──────────────┴───────────────────────┐                                │
│  │ Kubernetes Kubelet                   │                                │
│  │ (Readiness/Health Probe)             │                                │
│  └──────────────────────────────────────┘                                │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  None - This service has no egress connections                           │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│  INGRESS                                                                 │
│                                                                          │
│  None - No ingress resources defined; internal access only               │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  No RBAC resources — this component does not interact with the Kubernetes API.

Service Mesh Configuration:
  - No explicit PeerAuthentication or AuthorizationPolicy defined
  - In production, Istio sidecar provides mTLS between Orchestrator and Detector
  - Network policies expected to restrict access to in-cluster callers only

Secrets:
  None — no secrets consumed or generated by this component.

Authentication & Authorization:
  - Endpoint: /api/v1/text/contents (POST)
    Auth: None (plaintext)
    Policy: Open — relies on network-level isolation
  - Endpoint: /health (GET)
    Auth: None (plaintext)
    Policy: Open — health check

Container Security:
  - Base image: registry.access.redhat.com/ubi9/ubi-minimal
  - Runtime dependency: compat-openssl11
  - No privilege escalation; runs as non-root
  - FIPS-compliant operator feature enabled at platform level
  - Supported architectures: amd64, arm64, ppc64le, s390x

Configuration:
  - HTTP_PORT (default: 8080) — TCP port the HTTP server listens on
  - HOST (default: 0.0.0.0) — IP address to bind the HTTP listener to
