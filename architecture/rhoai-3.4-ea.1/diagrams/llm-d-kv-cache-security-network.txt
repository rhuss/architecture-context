Security Network Diagram: llm-d-kv-cache
Generated by: Claude Opus 4.6

================================================================================

┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL SERVICES                                                            │
│                                                                               │
│  [HuggingFace Hub - huggingface.co] (optional)                               │
│  - Purpose: Download tokenizer models                                         │
│  - Auth: Bearer Token (HF API key)                                           │
│  - Encryption: TLS 1.2+                                                      │
│                                                                               │
│  [Redis/Valkey Server] (optional)                                             │
│  - Purpose: Distributed KV-block index backend                               │
│  - Auth: Optional (URL-based auth)                                           │
│  - Encryption: Optional TLS (valkeys:// scheme)                              │
│                                                                               │
└──────────────────────────────────────────────────────────────────────────────┘
         ▲                              ▲
         │ Port: 443/TCP                │ Port: 6379/TCP
         │ Protocol: HTTPS              │ Protocol: Redis
         │ Encryption: TLS 1.2+         │ Encryption: Optional TLS
         │ Auth: Bearer Token           │ Auth: Optional URL auth
         │                              │
┌────────┼──────────────────────────────┼──────────────────────────────────────┐
│  KUBERNETES CLUSTER (Internal)        │                                       │
│        │                              │                                       │
│  ┌─────┼──────────────────────────────┼────────────────────────────────────┐ │
│  │  KV-CACHE MANAGER POD (StatefulSet)│                                     │ │
│  │     │                              │                                     │ │
│  │  ┌──┴──────────────────────────────┴──────────────────────────────────┐ │ │
│  │  │  Main Container: KV-Cache Manager (Go)                             │ │ │
│  │  │  - SA: operator-controller-manager                                 │ │ │
│  │  │                                                                    │ │ │
│  │  │  Components:                                                       │ │ │
│  │  │  - kvcache.Indexer (orchestrator)                                  │ │ │
│  │  │  - kvblock.Index (LRU/Ristretto/Redis backend)                    │ │ │
│  │  │  - kvblock.TokenProcessor (FNV-64a hashing)                       │ │ │
│  │  │  - kvblock.Scorer (prefix matching)                                │ │ │
│  │  │  - kvevents.Pool (sharded event workers)                           │ │ │
│  │  │  - kvevents.SubscriberManager (ZMQ lifecycle)                      │ │ │
│  │  │  - Pod Reconciler (controller-runtime)                             │ │ │
│  │  │  - Prometheus Metrics                                              │ │ │
│  │  │                                                                    │ │ │
│  │  │  Outbound connections:                                             │ │ │
│  │  │  → vLLM Pods: 5557/TCP ZMQ SUB (plaintext, no auth)              │ │ │
│  │  │  → K8s API: 443/TCP HTTPS (TLS 1.2+, SA Token)                   │ │ │
│  │  │  → Redis/Valkey: 6379/TCP (Optional TLS, Optional auth)          │ │ │
│  │  │  → HuggingFace: 443/TCP HTTPS (TLS 1.2+, Bearer Token)          │ │ │
│  │  │                                                                    │ │ │
│  │  │  Intra-pod communication:                                          │ │ │
│  │  │  → UDS Tokenizer: /tmp/tokenizer/tokenizer-uds.socket            │ │ │
│  │  │    Protocol: gRPC over UDS, plaintext, filesystem perms           │ │ │
│  │  └────────────────────────────────────────────────────────────────────┘ │ │
│  │                                                                         │ │
│  │  ┌────────────────────────────────────────────────────────────────────┐ │ │
│  │  │  Sidecar Container: UDS Tokenizer Service (Python)                 │ │ │
│  │  │                                                                    │ │ │
│  │  │  Listens:                                                          │ │ │
│  │  │  - UDS: /tmp/tokenizer/tokenizer-uds.socket (gRPC, plaintext)    │ │ │
│  │  │  - HTTP: 8082/TCP (health probe, plaintext, no auth)             │ │ │
│  │  │                                                                    │ │ │
│  │  │  Methods: Tokenize, RenderChatTemplate, InitializeTokenizer       │ │ │
│  │  └────────────────────────────────────────────────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  vLLM PODS (Inference Workers)                                          │ │
│  │                                                                         │ │
│  │  [vLLM Pod 1..N]                                                       │ │
│  │  - Port: 5557/TCP                                                      │ │
│  │  - Protocol: ZMQ PUB                                                   │ │
│  │  - Encryption: plaintext                                               │ │
│  │  - Auth: None (network isolation)                                      │ │
│  │  - Events: BlockStored, BlockRemoved, AllBlocksCleared (msgpack)      │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  KUBERNETES CONTROL PLANE                                               │ │
│  │                                                                         │ │
│  │  [Kubernetes API Server]                                                │ │
│  │  - Port: 443/TCP                                                       │ │
│  │  - Protocol: HTTPS                                                     │ │
│  │  - Encryption: TLS 1.2+                                                │ │
│  │  - Auth: ServiceAccount Token                                          │ │
│  │  - Purpose: Pod discovery (get, list, watch)                           │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                                                               │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  llm-d-inference-scheduler                                              │ │
│  │                                                                         │ │
│  │  - Interaction: Go library import / gRPC client                        │ │
│  │  - Protocol: Go API (in-process) or gRPC (plaintext)                  │ │
│  │  - Purpose: Consumes Indexer for KV-cache-aware pod scoring           │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────────────┘

================================================================================

RBAC Summary:
- Role: kv-cache-manager (namespace-scoped, when podNamespace is set)
  - API Groups: ""
  - Resources: pods
  - Verbs: get, list, watch

- ClusterRole: kv-cache-manager (cluster-scoped, when podNamespace is empty)
  - API Groups: ""
  - Resources: pods
  - Verbs: get, list, watch

Note: Scope depends on podNamespace config. If set, namespace-scoped Role
suffices. If empty (watch all namespaces), ClusterRole is required.

ServiceAccount: operator-controller-manager

================================================================================

Authentication & Authorization Summary:
- IndexerService gRPC: No auth (plaintext gRPC, internal library/service)
- TokenizationService gRPC: No auth (UDS-only, filesystem permissions)
- ZMQ Event Stream: No auth (relies on network policies for pod-to-pod access)
- Kubernetes API: ServiceAccount Token (auto-mounted)
- HuggingFace Hub: Bearer Token (optional, user-provisioned)
- Redis/Valkey: URL-based auth (optional, user-provisioned)

================================================================================

Secrets:
- HuggingFace Token (optional, Opaque) - Access private models, user-provisioned, no auto-rotate
- Redis/Valkey auth (optional, in URL) - Redis backend auth, user-provisioned, no auto-rotate

================================================================================

Network Policy Notes:
- No ingress from external sources
- ZMQ event transport (5557/TCP) uses plaintext - relies on network policies
- UDS tokenizer is pod-local only (Unix Domain Socket)
- Health probe (8082/TCP) is internal ClusterIP only
- All external HTTPS connections use TLS 1.2+
