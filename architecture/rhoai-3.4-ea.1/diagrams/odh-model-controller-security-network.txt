Security Network Diagram: odh-model-controller
Generated by: Claude Opus 4.6

┌──────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                    │
│                                                                          │
│  [External Client]                                                       │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS Edge or Reencrypt                              │
│         │ Auth: None / TLS (optional per annotation)                     │
│         ▼                                                                │
│  [NVIDIA NIM API - api.ngc.nvidia.com]                                   │
│  - Purpose: API key validation, model catalog fetch                      │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: API Key (Bearer)                                                │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│  INGRESS (DMZ)                                                           │
│                                                                          │
│  [OpenShift Router] ── per InferenceService Route                        │
│  - Port: 443/TCP (external) → 8080/TCP or 8443/TCP (internal)           │
│  - TLS Termination: Edge or Reencrypt                                    │
│  - Auth: None (or per annotation: security.opendatahub.io/enable-auth)  │
│         │                                                                │
│         │ Port: 8080/TCP (HTTP) or 8443/TCP (HTTPS)                      │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: None or TLS                                        │
│         ▼                                                                │
│                                                                          │
│  [Gateway API - openshift-ai-inference] (RHOAI 3.x)                     │
│  - Namespace: openshift-ingress                                          │
│  - Purpose: LLMInferenceService ingress                                  │
│  - Auth: Kuadrant AuthPolicy (Authorino)                                 │
│  - TLS Bootstrap: Istio EnvoyFilter                                      │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│  CONTROLLER NAMESPACE (Trusted)                                          │
│                                                                          │
│  [odh-model-controller]                                                  │
│  - ServiceAccount: odh-model-controller                                  │
│  - Image: odh-model-controller:v1.27.0                                   │
│                                                                          │
│  Services:                                                               │
│  ┌────────────────────────────────────────────────────────┐              │
│  │ odh-model-controller-webhook-service                   │              │
│  │ - Type: ClusterIP                                      │              │
│  │ - Port: 443/TCP → 9443/TCP                             │              │
│  │ - Protocol: HTTPS                                      │              │
│  │ - Encryption: TLS (OpenShift serving-cert)              │              │
│  │ - Auth: Kubernetes API server TLS client cert           │              │
│  └────────────────────────────────────────────────────────┘              │
│                                                                          │
│  ┌────────────────────────────────────────────────────────┐              │
│  │ odh-model-controller-metrics-service                   │              │
│  │ - Type: ClusterIP                                      │              │
│  │ - Port: 8080/TCP                                       │              │
│  │ - Protocol: HTTP                                       │              │
│  │ - Encryption: None                                     │              │
│  │ - Auth: None                                           │              │
│  └────────────────────────────────────────────────────────┘              │
│                                                                          │
│  Health Probes:                                                          │
│  - Liveness:  GET /healthz  8081/TCP HTTP                                │
│  - Readiness: GET /readyz   8081/TCP HTTP                                │
│                                                                          │
│  Webhook Endpoints (9443/TCP HTTPS TLS):                                 │
│  - POST /mutate--v1-pod                                                  │
│  - POST /mutate-serving-kserve-io-v1beta1-inferenceservice               │
│  - POST /mutate-serving-kserve-io-v1alpha1-inferencegraph                │
│  - POST /validate--v1-configmap                                          │
│  - POST /validate-nim-opendatahub-io-v1-account                          │
│  - POST /validate-serving-kserve-io-v1alpha1-llminferenceservice         │
│  - POST /validate-serving-kserve-io-v1alpha1-inferencegraph              │
│  - POST /validate-serving-kserve-io-v1beta1-inferenceservice             │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: ServiceAccount Token                                     │
│         ▼                                                                │
├──────────────────────────────────────────────────────────────────────────┤
│  CLUSTER SERVICES                                                        │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: ServiceAccount Token                                            │
│  - Purpose: All controller-runtime CRUD operations                       │
│                                                                          │
│  [Model Registry Service]                                                │
│  - Port: 443/TCP or 80/TCP                                               │
│  - Protocol: HTTPS/HTTP                                                  │
│  - Encryption: TLS (configurable, skip-tls option)                       │
│  - Auth: Bearer Token                                                    │
│  - Purpose: Bidirectional InferenceService metadata sync                 │
│                                                                          │
│  [Predictor Service]                                                     │
│  - Namespace: {user-namespace}                                           │
│  - Port: 8080/TCP (HTTP) or 8443/TCP (HTTPS)                             │
│  - Purpose: Model inference endpoint                                     │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: odh-model-controller-role
  - ServiceAccount: odh-model-controller
  - Key permissions:
    - serving.kserve.io: inferenceservices, servingruntimes, inferencegraphs, llminferenceservices (full CRUD)
    - route.openshift.io: routes (full CRUD + custom-host)
    - kuadrant.io: authpolicies (full CRUD)
    - networking.istio.io: envoyfilters (full CRUD)
    - networking.k8s.io: networkpolicies (full CRUD)
    - monitoring.coreos.com: servicemonitors, podmonitors (full CRUD)
    - nim.opendatahub.io: accounts (get, list, patch, update, watch)
    - rbac.authorization.k8s.io: clusterrolebindings, rolebindings, roles (full CRUD)
    - keda.sh: triggerauthentications (full CRUD)
    - core: configmaps, secrets, serviceaccounts, services (full CRUD)
    - core: pods, endpoints, namespaces (get, list, watch, create, patch, update)
    - template.openshift.io: templates (full CRUD)
    - gateway.networking.k8s.io: gateways, httproutes (get, list, watch, patch, update)

- ClusterRole: kserve-prometheus-k8s
  - Resources: services, endpoints, pods (get, list, watch)

- Role: leader-election-role (namespace-scoped)
  - Resources: configmaps, leases, events

Service Mesh / Auth Configuration:
- LLMInferenceService (RHOAI 3.x):
  - Kuadrant AuthPolicy on Gateway: enforces user-defined authentication
  - Kuadrant AuthPolicy on HTTPRoute: enables anonymous access where configured
  - Istio EnvoyFilter: TLS bootstrap between gateway Envoy and Authorino
- InferenceService (RHOAI 2.x pattern):
  - OpenShift Route: TLS Edge or Reencrypt
  - Optional auth via security.opendatahub.io/enable-auth annotation

Secrets:
- odh-model-controller-webhook-cert (kubernetes.io/tls) - Webhook TLS cert, OpenShift service-ca, auto-rotate
- storage-config (Opaque, dynamic) - Aggregated S3 data connection credentials, per namespace, no auto-rotate
- ray-ca-tls (Opaque) - Self-signed CA for Ray inter-node TLS, controller namespace, no auto-rotate
- ray-tls (Opaque, dynamic) - Per-pod Ray TLS certificates, per serving namespace, no auto-rotate
- NIM pull secret (kubernetes.io/dockerconfigjson, dynamic) - NGC registry credentials, no auto-rotate
