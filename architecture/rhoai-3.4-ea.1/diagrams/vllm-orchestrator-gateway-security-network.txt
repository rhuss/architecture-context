Security Network Diagram: vllm-orchestrator-gateway
Generated by: Claude Opus 4.6
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Client]                                                                │
│  - Any HTTP client (curl, application, etc.)                            │
│         │                                                                │
│         │ Port: 8090/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plaintext)                                   │
│         │ Auth: Authorization header (pass-through to orchestrator)      │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  GATEWAY SERVICE                                                         │
│                                                                          │
│  [vllm-orchestrator-gateway]                                             │
│  - Namespace: {deployment-namespace}                                     │
│  - Binary: /app/bin/vllm-orchestrator-gateway                           │
│  - Base Image: registry.access.redhat.com/ubi9/ubi-minimal              │
│  - Listen: 0.0.0.0:8090 (configurable via HOST, HTTP_PORT)             │
│  - Runtime: Rust (tokio async, multi-threaded)                          │
│  - Config: /app/config/config.yaml (GATEWAY_CONFIG env)                 │
│                                                                          │
│  Endpoints:                                                              │
│  - POST /{route_name}/v1/chat/completions                               │
│    - Dynamic routes from YAML config (e.g., /pii/, /passthrough/)       │
│    - Supports streaming (SSE) and non-streaming                         │
│    - No local authentication enforcement                                 │
│    - Forwards: Authorization, X-Forwarded-* headers                     │
│                                                                          │
│  Processing:                                                             │
│  - Resolves route -> detector mapping from config                       │
│  - Injects detector specs into request payload                          │
│  - On detection + fallback_message: replaces response content           │
│         │                                                                │
│         │ Port: 8085/TCP (configurable, default also 8032/TCP)          │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: Optional mTLS (TLS 1.2+)                          │
│         │   - Client cert: /etc/tls/private/tls.crt (PEM)              │
│         │   - Client key: /etc/tls/private/tls.key (PEM)               │
│         │   - CA cert: /etc/tls/ca/service-ca.crt (PEM)                │
│         │   - Identity: PKCS#12 constructed from cert+key via OpenSSL   │
│         │ Auth: mTLS client certificate (if TLS files present)          │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  BACKEND SERVICES (Internal)                                             │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Endpoint: /api/v2/chat/completions-detection                         │
│  - Port: 8085/TCP (or 8032/TCP, configurable)                          │
│  - Purpose: Performs chat completions with detector-based filtering     │
│  - Receives: Injected detector config + original chat messages          │
│         │                                                                │
│         ├───────────────────────────────────┐                           │
│         ▼                                    ▼                           │
│  [Guardrails Detectors]                [vLLM / LLM Backend]             │
│  - e.g., regex-language                - LLM inference service          │
│  - Referenced by name in config        - Chat completion generation     │
│  - Input/output detection              - Accessed via orchestrator      │
│  - Indirect access (via orchestrator)  - Indirect access                │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined (stateless HTTP service, no K8s API interactions)
- No RoleBindings defined
- No ServiceAccount-specific permissions required

Security Model:
- Gateway does NOT enforce authentication
- Authorization is delegated to orchestrator/upstream services
- Authorization header is forwarded as-is (pass-through)
- X-Forwarded-* headers forwarded for proxy chain context

mTLS Configuration:
- TLS mode is auto-detected based on file presence:
  - cert + key present: HTTPS with client certificate (mTLS)
  - CA cert only: HTTPS with server cert verification (one-way TLS)
  - No TLS files: plaintext HTTP
- Certificate format: PEM
- Identity construction: OpenSSL PKCS#12 (cert + key combined)
- TLS backend: native-tls (via reqwest)

Secrets:
- /etc/tls/private/tls.crt (kubernetes.io/tls, mounted) - Client cert for mTLS
  - Provisioned by: Platform (service-ca operator or cert-manager)
  - Rotation: Depends on provisioner
- /etc/tls/private/tls.key (kubernetes.io/tls, mounted) - Private key for mTLS
  - Provisioned by: Platform (service-ca operator or cert-manager)
  - Rotation: Depends on provisioner
- /etc/tls/ca/service-ca.crt (ConfigMap/Secret, mounted) - CA cert for server verification
  - Provisioned by: Platform (service-ca operator)
  - Rotation: Depends on provisioner

Supported Architectures: amd64, arm64, ppc64le, s390x
Supported OCP Versions: v4.19, v4.20, v4.21
