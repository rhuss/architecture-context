Security Network Diagram: Guardrails Regex Detector
Generated by: Claude Opus 4.6
Source: architecture/rhoai-3.4-ea.1/guardrails-regex-detector.md
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CLUSTER (No External Ingress)                                 │
│                                                                          │
│  This service has NO external ingress. All access is cluster-internal.   │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  PLATFORM SECURITY LAYER (if configured)                                │
│                                                                          │
│  [Istio Service Mesh] (optional)                                         │
│  - PeerAuthentication: mTLS (if enabled at namespace/mesh level)        │
│  - Sidecar injection: Envoy proxy for transparent mTLS                   │
│                                                                          │
│  [NetworkPolicy] (optional)                                              │
│  - Not defined in component repo                                         │
│  - Expected to be managed by platform operator                           │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE (Internal Only)                                                 │
│                                                                          │
│  [Guardrails Regex Detector]                                             │
│  - Service: regex-detector (ClusterIP, inferred)                        │
│  - Port: 8080/TCP                                                        │
│  - Protocol: HTTP (plaintext at app level)                               │
│  - Encryption: None at application level                                 │
│  - Auth: None at application level                                       │
│  - Bind: 0.0.0.0:8080 (configurable via HTTP_PORT, HOST env vars)       │
│  - Base Image: UBI 9 Minimal                                            │
│  - Language: Rust 1.84.0                                                 │
│                                                                          │
│  Endpoints:                                                              │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ GET  /health                                                     │    │
│  │ - Purpose: Liveness/readiness probe                              │    │
│  │ - Auth: None                                                     │    │
│  │ - Encryption: None (plaintext HTTP)                              │    │
│  ├─────────────────────────────────────────────────────────────────┤    │
│  │ POST /api/v1/text/contents                                       │    │
│  │ - Purpose: PII & custom regex detection                          │    │
│  │ - Auth: None (relies on platform-level security)                 │    │
│  │ - Encryption: None (plaintext HTTP)                              │    │
│  │ - Input: JSON with contents[] and detector_params.regex[]        │    │
│  │ - Output: JSON with detection spans, types, scores               │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  INBOUND CONNECTIONS                                                     │
│                                                                          │
│  [FMS Guardrails Orchestrator] ──────────────────────────────────────►  │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: Platform-dependent (mesh mTLS if available)        │
│         │ Auth: None at app level                                        │
│         │ Method: POST /api/v1/text/contents                             │
│         ▼                                                                │
│  [Regex Detector]                                                        │
│                                                                          │
│  [Kubernetes Kubelet] ──────────────────────────────────────────────►   │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None                                               │
│         │ Auth: None                                                     │
│         │ Method: GET /health                                            │
│         ▼                                                                │
│  [Regex Detector]                                                        │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  OUTBOUND CONNECTIONS (EGRESS)                                           │
│                                                                          │
│  None. This is a pure computation service with no outbound               │
│  network dependencies. All processing is done in-memory                  │
│  using regex matching.                                                   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles, Roles, or RoleBindings defined
- No Kubernetes API interaction
- Component runs as a stateless HTTP service

Secrets:
- None defined in this repository

Authentication & Authorization:
- No application-level authentication on any endpoint
- Security enforcement delegated to platform infrastructure:
  - Istio service mesh (mTLS between services)
  - NetworkPolicy (restrict pod-to-pod communication)
  - kube-rbac-proxy sidecar (if injected by operator)

Network Policies:
- None defined in this repository
- Expected to be managed by guardrails-detectors operator or platform

Configuration:
- HTTP_PORT (default: 8080) - Server listen port
- HOST (default: 0.0.0.0) - Server bind address
