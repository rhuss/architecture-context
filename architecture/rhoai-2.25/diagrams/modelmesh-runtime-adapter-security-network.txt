Security Network Diagram: modelmesh-runtime-adapter
Generated by: Claude Opus 4.6
====================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  POD BOUNDARY (Single ModelMesh Serving Pod)                            │
│                                                                         │
│  All internal communication is via localhost (plaintext gRPC).          │
│  No Kubernetes Services are defined by this component.                  │
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  [ModelMesh Container]                                          │    │
│  │  - Main ModelMesh process                                       │    │
│  │         │                                                       │    │
│  │         │ Port: 8084/TCP (localhost)                             │    │
│  │         │ Protocol: gRPC                                        │    │
│  │         │ Encryption: plaintext                                 │    │
│  │         │ Auth: None (Pod-internal only)                        │    │
│  │         ▼                                                       │    │
│  │  [Model-Serving-Puller]                                         │    │
│  │  - Binary: /opt/app/puller                                      │    │
│  │  - Port: 8084/TCP (gRPC server)                                 │    │
│  │  - Credentials: /storage-config (mounted Secret)                │    │
│  │         │                                                       │    │
│  │         │ Port: 8085/TCP (localhost)                             │    │
│  │         │ Protocol: gRPC                                        │    │
│  │         │ Encryption: plaintext                                 │    │
│  │         │ Auth: None (Pod-internal only)                        │    │
│  │         ▼                                                       │    │
│  │  [Runtime Adapter] (one of: triton/ovms/mlserver/torchserve)    │    │
│  │  - Binary: /opt/app/{runtime}-adapter                           │    │
│  │  - Port: 8085/TCP (gRPC server)                                 │    │
│  │         │                                                       │    │
│  │         │ Port: 8001/TCP (Triton/OVMS/MLServer)                 │    │
│  │         │   or  7070-7071/TCP (TorchServe)                      │    │
│  │         │ Protocol: gRPC                                        │    │
│  │         │ Encryption: plaintext                                 │    │
│  │         │ Auth: None (Pod-internal only)                        │    │
│  │         ▼                                                       │    │
│  │  [Model Runtime Container]                                      │    │
│  │  - Triton: gRPC :8001                                           │    │
│  │  - OVMS: gRPC :8001 + config file                               │    │
│  │  - MLServer: gRPC :8001                                         │    │
│  │  - TorchServe: mgmt :7071, inference :7070                      │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                         │
│  [Puller] also makes EGRESS connections:                                │
│         │                                                               │
│         │                                                               │
├─────────┼───────────────────────────────────────────────────────────────┤
│  EGRESS │ (External Storage)                                            │
│         │                                                               │
│         ├──► [S3-compatible Storage (AWS S3 / IBM COS)]                 │
│         │    - Port: 443/TCP                                            │
│         │    - Protocol: HTTPS                                          │
│         │    - Encryption: TLS 1.2+                                     │
│         │    - Auth: AWS IAM / Access Key ID + Secret Access Key        │
│         │    - Purpose: Download model artifacts                        │
│         │                                                               │
│         ├──► [Google Cloud Storage]                                     │
│         │    - Port: 443/TCP                                            │
│         │    - Protocol: HTTPS                                          │
│         │    - Encryption: TLS 1.2+                                     │
│         │    - Auth: GCP Service Account JSON / OAuth2                  │
│         │    - Purpose: Download model artifacts                        │
│         │                                                               │
│         ├──► [Azure Blob Storage]                                       │
│         │    - Port: 443/TCP                                            │
│         │    - Protocol: HTTPS                                          │
│         │    - Encryption: TLS 1.2+                                     │
│         │    - Auth: Azure AD / SAS Token / Managed Identity            │
│         │    - Purpose: Download model artifacts                        │
│         │                                                               │
│         └──► [HTTP/HTTPS Endpoints]                                     │
│              - Port: 80/TCP or 443/TCP                                  │
│              - Protocol: HTTP or HTTPS                                  │
│              - Encryption: TLS 1.2+ (HTTPS) / plaintext (HTTP)         │
│              - Auth: None / Bearer Token                                │
│              - Purpose: Download model artifacts from URLs              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

INGRESS: None
  - This component has no external ingress
  - All traffic is Pod-internal via localhost

RBAC Summary: None
  - This component does not define RBAC resources
  - Runs as sidecar, does not interact with Kubernetes API

Secrets:
  - storage-config (Opaque) - Storage credentials (S3 keys, GCS SA, Azure creds)
    Provisioned by: User / ModelMesh Serving controller
    Mounted at: /storage-config
    Auto-Rotate: No

FIPS Compliance:
  - Built with GOEXPERIMENT=strictfipsruntime
  - Build flag: -tags strictfipsruntime
  - Ensures FIPS-compliant cryptographic operations

Security Notes:
  - All Pod-internal communication is plaintext gRPC on localhost
  - Security boundary is the Pod itself (no cross-Pod communication)
  - External storage access uses TLS 1.2+ with appropriate credentials
  - No network policies defined by this component
  - Network isolation relies on Pod boundary and Kubernetes NetworkPolicies
    set by the modelmesh-serving controller or platform
