┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL CONSUMERS (Trusted - Internal Platform)                            │
│                                                                              │
│  [FMS Guardrails Orchestrator]                                               │
│  - Namespace: guardrails / user-defined                                      │
│  - Role: Calls detectors for text content analysis                           │
│         │                          │                         │               │
│         │ Port: 8080/TCP           │ Port: 8000/TCP          │ Port: 8000/TCP│
│         │ Protocol: HTTP           │ Protocol: HTTP          │ Protocol: HTTP│
│         │ Encryption: None         │ Encryption: Istio mTLS  │ Encryption:   │
│         │ (plaintext, intra-mesh)  │ (ISTIO_MUTUAL)          │  Istio mTLS   │
│         │ Auth: None               │ Auth: detector_id hdr   │ Auth:         │
│         │                          │                         │  detector_id  │
│         ▼                          ▼                         ▼    header     │
├──────────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (Istio Sidecar Injection: sidecar.istio.io/inject='true')      │
│                                                                              │
│  ┌─────────────────────────┐  ┌──────────────────────────┐                   │
│  │ Built-In Detector       │  │ HuggingFace Detector     │                   │
│  │ Port: 8080/TCP          │  │ Port: 8000/TCP           │                   │
│  │ Protocol: HTTP          │  │ Protocol: HTTP           │                   │
│  │ Image: rhoai/odh-built  │  │ Image: rhoai/odh-        │                   │
│  │  -in-detector-rhel9     │  │  guardrails-detector-    │                   │
│  │ Auth: None (app-level)  │  │  huggingface-runtime-    │                   │
│  │ Endpoints:              │  │  rhel9                   │                   │
│  │  POST /api/v1/text/     │  │ Auth: detector_id hdr    │                   │
│  │       contents          │  │  (no verification)       │                   │
│  │  GET  /registry         │  │ Endpoints:               │                   │
│  │  GET  /health           │  │  POST /api/v1/text/      │                   │
│  │  GET  /metrics          │  │       contents           │                   │
│  │ No external deps        │  │  GET  /health            │                   │
│  └─────────────────────────┘  │  GET  /metrics           │                   │
│                               │ KServe: InferenceService │                   │
│  ┌─────────────────────────┐  │  + ServingRuntime        │                   │
│  │ LLM Judge Detector      │  │ Istio: mTLS ISTIO_MUTUAL│                   │
│  │ Port: 8000/TCP          │  └─────────┬────────────────┘                   │
│  │ Protocol: HTTP          │            │                                    │
│  │ Image: quay.io/trustyai │            │ Port: 9000/TCP                     │
│  │  /guardrails-detector-  │            │ Protocol: HTTP                     │
│  │  llm-judge              │            │ Encryption: None                   │
│  │ Auth: detector_id hdr   │            │ Auth: AWS IAM credentials          │
│  │  (no verification)      │            │       (from Secret)                │
│  │ Endpoints:              │            ▼                                    │
│  │  POST /api/v1/text/     │  ┌──────────────────────────┐                   │
│  │       contents          │  │ KServe Storage Init      │                   │
│  │  POST /api/v1/text/     │  │ (init container)         │                   │
│  │       generation        │  │ Downloads model to       │                   │
│  │  GET  /api/v1/metrics   │  │ /mnt/models              │                   │
│  │  GET  /health           │  └──────────────────────────┘                   │
│  │  GET  /metrics          │                                                 │
│  │ KServe: InferenceService│                                                 │
│  │  + ServingRuntime       │                                                 │
│  │ Istio: mTLS ISTIO_MUTUAL│                                                │
│  └────────┬────────────────┘                                                 │
│           │                                                                  │
│           │ Port: 8080/TCP (configurable)                                    │
│           │ Protocol: HTTP                                                   │
│           │ Encryption: None (plaintext, intra-mesh)                         │
│           │ Auth: None                                                       │
│           ▼                                                                  │
│  ┌──────────────────────────┐                                                │
│  │ vLLM Server             │                                                 │
│  │ (OpenAI-compatible API) │                                                 │
│  │ Port: 8080/TCP          │                                                 │
│  │ Protocol: HTTP          │                                                 │
│  │ Purpose: LLM evaluation │                                                 │
│  └──────────────────────────┘                                                │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  [S3/MinIO Model Storage]                                                    │
│  - Port: 9000/TCP                                                            │
│  - Protocol: HTTP                                                            │
│  - Encryption: None                                                          │
│  - Auth: AWS IAM credentials (aws-connection-minio-data-connection-          │
│          guardrails-guardian Secret)                                          │
│  - Purpose: Model artifact storage for HuggingFace detector                  │
│                                                                              │
│  [Prometheus]                                                                │
│  - Port: 8080/TCP                                                            │
│  - Protocol: HTTP                                                            │
│  - Encryption: None                                                          │
│  - Purpose: Scrapes /metrics from all detectors                              │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC roles defined. Detectors are application workloads without
  Kubernetes API access.
- Example binding only: user-one-view → view ClusterRole → user-one SA
  (from model_container.yaml, not production)

Authentication & Authorization:
- Built-in Detector: Unauthenticated (no auth at app level)
- HuggingFace Detector: detector_id HTTP header required but NOT verified
- LLM Judge Detector: detector_id HTTP header required but NOT verified
- Platform Auth: security.opendatahub.io/enable-auth='true' annotation
  delegates to KServe/Istio/RHOAI infrastructure layer
- Transport Security: Istio mTLS (ISTIO_MUTUAL) for KServe InferenceServices
  via sidecar.istio.io/inject='true'

Secrets:
- aws-connection-minio-data-connection-guardrails-guardian (Opaque/S3)
  Purpose: S3 credentials for MinIO model storage access
  Provisioned By: Manual (example manifest)
  Auto-Rotate: No

TLS Configuration:
- Application-level: Optional SSL support via ssl_ca_certs config key
  (Python ssl module → system OpenSSL from UBI 9)
- All detectors run plaintext HTTP, relying on Istio sidecar for mTLS
- No TLS termination at application layer

FIPS Compliance:
- Python services inherit FIPS from UBI 9 base image OpenSSL
- No explicit FIPS opt-in present
- No non-FIPS crypto detected (no MD5, RC4, etc.)

Build Hermeticity:
- hermetic: false (Tekton pipeline)
- Python deps pinned in requirements.txt but no pip lock file
- No RPM lock file or Hermeto prefetch
- Supply chain risk: transitive Python dependencies not locked
