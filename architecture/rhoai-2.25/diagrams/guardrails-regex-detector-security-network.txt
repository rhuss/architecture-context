┌─────────────────────────────────────────────────────────────────────────┐
│  CLUSTER INTERNAL (No External Ingress)                                  │
│                                                                          │
│  This service has NO external ingress. All access is pod-to-pod          │
│  within the Kubernetes cluster.                                          │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH / CLUSTER NETWORK                                          │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Namespace: {guardrails-namespace}                                     │
│  - Role: Upstream caller / content router                                │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP (plaintext)                                     │
│         │ Encryption: None                                               │
│         │ Auth: None                                                     │
│         │ Direction: Inbound to detector                                 │
│         ▼                                                                │
│  [Regex Detector Service]                                                │
│  - Service Type: ClusterIP (presumed)                                    │
│  - Namespace: {guardrails-namespace}                                     │
│  - Port: 8080/TCP                                                        │
│  - Protocol: HTTP (plaintext)                                            │
│  - Image: guardrails-regex-detector                                      │
│  - Runtime: Rust (Axum)                                                  │
│  - Base Image: ubi9/ubi-minimal                                          │
│                                                                          │
│  Endpoints:                                                              │
│  ┌──────────────────────────────────────────────────────────────────┐    │
│  │  GET  /health                                                    │    │
│  │  - Auth: None                                                    │    │
│  │  - Purpose: Health check                                         │    │
│  │  - Response: "healthy" (string)                                  │    │
│  ├──────────────────────────────────────────────────────────────────┤    │
│  │  POST /api/v1/text/contents                                      │    │
│  │  - Auth: None                                                    │    │
│  │  - Purpose: Text PII/pattern detection                           │    │
│  │  - Input: JSON (text content + regex params)                     │    │
│  │  - Output: JSON ([[detection results]])                          │    │
│  └──────────────────────────────────────────────────────────────────┘    │
│         │                                                                │
│         │ Response (same connection)                                     │
│         │ Protocol: HTTP (plaintext)                                     │
│         │ Encryption: None                                               │
│         ▼                                                                │
│  [FMS Guardrails Orchestrator]                                           │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  None — this service makes no outbound connections.                      │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No ServiceAccount-specific roles
- Service relies entirely on network-level isolation for security

Secrets:
- None — no secrets required or consumed

TLS / Encryption:
- No TLS configured on any endpoint
- No crypto libraries in dependencies (no openssl, rustls, or ring crates)
- Service performs zero cryptographic operations
- Security relies on Kubernetes network policies and namespace isolation

Authentication & Authorization:
- /health: No authentication (unauthenticated health check)
- /api/v1/text/contents: No authentication
- Authorization: None — relies on pod-to-pod network isolation within cluster
- Expected deployment: Same namespace as FMS Guardrails Orchestrator

FIPS Compliance:
- No cryptographic operations performed
- compat-openssl11 installed in runtime image (may satisfy check-payload)
- Binary does not link against OpenSSL
- FIPS not applicable at application level

Build Security:
- Builder: rust:1.84.0 (Docker Hub, pinned)
- Runtime: ubi9/ubi-minimal (tag defaults to latest — non-hermetic)
- No Konflux Dockerfile on this branch
- Cargo.lock present for dependency pinning
- No rpms.lock.yaml or Hermeto prefetch
