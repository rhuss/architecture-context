┌──────────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                            │
│                                                                                  │
│  [Client - gRPC]                          [Client - REST]                        │
│         │                                        │                               │
│         │ Port: 8033/TCP                         │ Port: 8443/TCP                 │
│         │ Protocol: gRPC                         │ Protocol: HTTPS                │
│         │ Encryption: Optional TLS               │ Encryption: TLS                │
│         │ Auth: Optional mTLS client cert        │ Auth: OpenShift OAuth           │
│         │ validated against ca.crt               │ SAR: services/get in namespace  │
│         ▼                                        ▼                               │
├──────────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER-INTERNAL SERVICES                                                       │
│                                                                                  │
│  [modelmesh-serving Service]                                                     │
│  - Type: ClusterIP / Headless                                                    │
│  - Ports:                                                                        │
│    - 8033/TCP (gRPC inference) → Optional TLS, Optional mTLS                     │
│    - 8008/TCP (REST plaintext) → HTTP, No auth                                   │
│    - 8443/TCP (REST HTTPS) → TLS (OpenShift serving cert), OAuth proxy           │
│    - 2112/TCP (metrics) → HTTP/HTTPS configurable, Prometheus scrape             │
│                                                                                  │
│  [modelmesh-webhook-server-service]                                              │
│  - Type: ClusterIP                                                               │
│  - Port: 9443/TCP → HTTPS, TLS (cert-manager or OpenShift serving cert)         │
│  - Auth: Kubernetes API server                                                   │
│                                                                                  │
│  [controller-manager-metrics-service]                                            │
│  - Type: ClusterIP                                                               │
│  - Port: 8443/TCP → HTTPS, TLS, kube-rbac-proxy                                 │
│                                                                                  │
│  [modelmesh-controller]                                                          │
│  - Type: ClusterIP (ODH overlay)                                                 │
│  - Port: 8080/TCP → HTTP, No auth                                                │
│         │                                                                        │
│         ▼                                                                        │
├──────────────────────────────────────────────────────────────────────────────────┤
│  CONTROLLER NAMESPACE (opendatahub / redhat-ods-applications)                    │
│                                                                                  │
│  [modelmesh-controller Pod]                                                      │
│  - ServiceAccount: modelmesh-controller                                          │
│  - Ports:                                                                        │
│    - 8081/TCP (health probes: /healthz, /readyz) HTTP, No auth                   │
│    - 8080/TCP (metrics) HTTP, No auth                                            │
│    - 9443/TCP (webhook) HTTPS, TLS                                               │
│    - 9999/TCP (pprof, optional) HTTP, Basic auth (etcd creds)                    │
│         │                                                                        │
│         │ Port: 8033/TCP                                                         │
│         │ Protocol: gRPC (client)                                                │
│         │ Encryption: Optional TLS                                               │
│         │ Auth: Optional mTLS                                                    │
│         ▼                                                                        │
├──────────────────────────────────────────────────────────────────────────────────┤
│  RUNTIME PODS (user namespaces with modelmesh-enabled label)                     │
│                                                                                  │
│  [Runtime Pod - Multi-Container]                                                 │
│  - ServiceAccount: modelmesh-serving-sa                                          │
│                                                                                  │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐    │
│  │  ModelMesh    │ │ REST Proxy   │ │ OAuth Proxy  │ │ Puller/Adapter       │    │
│  │  Container    │ │ (optional)   │ │ (optional)   │ │ Sidecar              │    │
│  │  :8033 gRPC   │ │ :8008 HTTP   │ │ :8443 HTTPS  │ │ :8085-8086 gRPC      │    │
│  └──────┬───────┘ └──────────────┘ └──────────────┘ └──────────┬───────────┘    │
│         │                                                       │                │
│  ┌──────┴───────────────────────────────────────────────────────┘                │
│  │                                                                               │
│  │  ┌──────────────────────────────────────────────────────────────┐             │
│  │  │ Runtime Container (one of):                                  │             │
│  │  │  - OpenVINO Model Server (OVMS) - openvino_ir, onnx         │             │
│  │  │  - Triton Inference Server - multi-framework                 │             │
│  │  │  - MLServer - sklearn, xgboost, lightgbm                    │             │
│  │  │  - TorchServe - pytorch-mar                                  │             │
│  │  │  Comm: gRPC via localhost or Unix socket                     │             │
│  │  └──────────────────────────────────────────────────────────────┘             │
│         │                                                                        │
│         │ Port: 2379/TCP                                                         │
│         │ Protocol: gRPC                                                         │
│         │ Encryption: Optional TLS/mTLS                                          │
│         │ Auth: Username/password + optional client certs                         │
│         ▼                                                                        │
├──────────────────────────────────────────────────────────────────────────────────┤
│  INFRASTRUCTURE SERVICES                                                         │
│                                                                                  │
│  [etcd Cluster]                                                                  │
│  - Port: 2379/TCP                                                                │
│  - Protocol: gRPC                                                                │
│  - Encryption: Optional TLS/mTLS                                                 │
│  - Auth: Username/password from model-serving-etcd Secret                        │
│  - Purpose: Model registry state, vmodel lifecycle, cross-pod coordination       │
│  - Isolation: Namespace-prefixed keys ({prefix}/mm_ns/{namespace})               │
│                                                                                  │
│  [Kubernetes API Server]                                                         │
│  - Port: 6443/TCP                                                                │
│  - Protocol: HTTPS                                                               │
│  - Encryption: TLS                                                               │
│  - Auth: ServiceAccount token                                                    │
│  - Purpose: CRD watch, resource CRUD, status updates                             │
│         │                                                                        │
│         ▼                                                                        │
├──────────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Egress)                                                      │
│                                                                                  │
│  [S3-Compatible Storage - s3.amazonaws.com / custom]                             │
│  - Port: 443/TCP                                                                 │
│  - Protocol: HTTPS                                                               │
│  - Encryption: TLS                                                               │
│  - Auth: AWS IAM credentials (from storage-config Secret)                        │
│  - Purpose: Model artifact download (via puller sidecar)                         │
│                                                                                  │
│  [GCS Storage - storage.googleapis.com]                                          │
│  - Port: 443/TCP                                                                 │
│  - Protocol: HTTPS                                                               │
│  - Encryption: TLS                                                               │
│  - Auth: GCP credentials (from storage-config Secret)                            │
│  - Purpose: Model artifact download (via puller sidecar)                         │
│                                                                                  │
│  [Azure Blob Storage - *.blob.core.windows.net]                                  │
│  - Port: 443/TCP                                                                 │
│  - Protocol: HTTPS                                                               │
│  - Encryption: TLS                                                               │
│  - Auth: Azure credentials (from storage-config Secret)                          │
│  - Purpose: Model artifact download (via puller sidecar)                         │
│                                                                                  │
└──────────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
┌────────────────────────────────────────────────────────────────────────────────┐
│ Service Account: modelmesh-controller                                          │
│                                                                                │
│ ClusterRole: modelmesh-controller-role                                         │
│   - "": configmaps, secrets, services, services/finalizers                     │
│     → create, delete, get, list, patch, update, watch                          │
│   - "": namespaces, namespaces/finalizers                                      │
│     → get, list, watch, update, patch                                          │
│   - "": endpoints                                                              │
│     → get, list, watch                                                         │
│   - apps: deployments, deployments/finalizers                                  │
│     → create, delete, get, list, patch, update, watch                          │
│   - serving.kserve.io: servingruntimes, clusterservingruntimes,                │
│     predictors, inferenceservices (+ /finalizers, /status)                      │
│     → create, delete, get, list, patch, update, watch                          │
│   - autoscaling: horizontalpodautoscalers                                      │
│     → create, delete, get, list, update, watch                                 │
│   - monitoring.coreos.com: servicemonitors                                     │
│     → create, delete, get, list, patch, update, watch                          │
│                                                                                │
│ ClusterRole: modelmesh-controller-leader-election-role                          │
│   - coordination.k8s.io: leases → create, get, list, update                   │
│   - "": configmaps → create, delete, get, list, patch, update, watch           │
│   - "": events → create                                                        │
│                                                                                │
│ ClusterRole: proxy-role                                                        │
│   - authentication.k8s.io: tokenreviews → create                               │
│   - authorization.k8s.io: subjectaccessreviews → create                        │
│                                                                                │
│ ClusterRole: restricted-scc                                                    │
│   - security.openshift.io: securitycontextconstraints (restricted) → use       │
│   - Bound to: modelmesh, modelmesh-serving-sa                                  │
└────────────────────────────────────────────────────────────────────────────────┘

Secrets:
┌────────────────────────────────────────────────────────────────────────────────┐
│ Secret                     │ Type             │ Provisioned By      │ Rotate  │
│────────────────────────────┼──────────────────┼─────────────────────┼─────────│
│ model-serving-etcd         │ Opaque (JSON)    │ Manual / Platform   │ No      │
│ storage-config             │ Opaque (JSON)    │ Manual / User       │ No      │
│ model-serving-proxy-tls    │ kubernetes.io/tls│ OpenShift serving-  │ Yes     │
│                            │                  │ cert-controller     │         │
│ <tls-secret-name>          │ kubernetes.io/tls│ Manual / cert-mgr   │ Varies  │
└────────────────────────────────────────────────────────────────────────────────┘

Authentication & Authorization Summary:
┌────────────────────────────────────────────────────────────────────────────────┐
│ Endpoint                │ Auth Mechanism           │ Enforcement Point         │
│─────────────────────────┼──────────────────────────┼───────────────────────────│
│ gRPC inference :8033    │ Optional mTLS client     │ ModelMesh TLS config      │
│                         │ certificates             │                           │
│ REST inference :8443    │ OpenShift OAuth proxy     │ oauth-proxy sidecar       │
│                         │ + SAR services/get       │                           │
│ Controller metrics :8443│ kube-rbac-proxy          │ kube-rbac-proxy sidecar   │
│                         │ TokenReview + SAR        │                           │
│ Webhook :9443           │ K8s API server TLS       │ Admission webhook infra   │
└────────────────────────────────────────────────────────────────────────────────┘

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, -tags strictfipsruntime
- Linking: Dynamic (CGO_ENABLED=1) for OpenSSL via UBI9 base image
- Crypto: stdlib crypto/tls → OpenSSL (via strictfipsruntime)
- Risk: InsecureSkipVerify=true on ServiceMonitor TLS config (metrics only)

Network Policies:
- networkpolicy-controller: Controls ingress to controller pod
- networkpolicy-runtimes: Controls ingress to runtime pods
- networkpolicy-webhook: Controls ingress to webhook service
- networkpolicy-etcd (ODH overlay): Controls egress to etcd cluster
