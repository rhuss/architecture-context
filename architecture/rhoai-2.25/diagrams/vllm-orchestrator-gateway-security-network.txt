================================================================================
  SECURITY NETWORK DIAGRAM: vllm-orchestrator-gateway
  Component: vllm-orchestrator-gateway (Rust / axum HTTP service)
  Distribution: RHOAI 2.25
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [Client - OpenAI-compatible]                                               │
│         │                                                                   │
│         │ Port: 8090/TCP                                                    │
│         │ Protocol: HTTP                                                    │
│         │ Encryption: None (plaintext)                                      │
│         │ Auth: Authorization header (passthrough — not enforced locally)   │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  GATEWAY SERVICE (Internal — no ingress defined in component)               │
│                                                                             │
│  [vllm-orchestrator-gateway]                                                │
│  - Listen: 0.0.0.0:8090/TCP                                                │
│  - Framework: axum 0.7.9 (Rust)                                            │
│  - Endpoint: /{route_name}/v1/chat/completions (POST)                      │
│  - No local authentication enforcement                                      │
│  - Header forwarding: Authorization, X-Forwarded-* only                    │
│  - Detector config injected per route from YAML config                     │
│  - Fallback message substitution on detection                              │
│                                                                             │
│  Config Sources:                                                            │
│  - GATEWAY_CONFIG env → YAML config file (ConfigMap/Volume)                │
│  - /etc/tls/private/tls.crt (client certificate for mTLS)                  │
│  - /etc/tls/private/tls.key (private key for mTLS)                         │
│  - /etc/tls/ca/service-ca.crt (CA cert for orchestrator validation)        │
│         │                                                                   │
│         │ Port: Configurable (default 8085/TCP)                             │
│         │ Protocol: HTTP or HTTPS                                           │
│         │ Encryption: Optional mTLS                                         │
│         │   - When TLS certs present at /etc/tls/private/:                  │
│         │     * Client identity: PKCS#12 (converted from PEM cert+key)     │
│         │     * Server validation: custom CA from /etc/tls/ca/             │
│         │     * TLS backend: native-tls (OpenSSL-backed)                   │
│         │     * Default TLS config (no custom cipher suites)               │
│         │   - When no TLS certs: plaintext HTTP                             │
│         │ Auth: Forwarded Authorization + X-Forwarded-* headers            │
│         │ Endpoint: POST /api/v2/chat/completions-detection                │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  BACKEND SERVICES (Internal Platform)                                       │
│                                                                             │
│  [FMS Guardrails Orchestrator]                                              │
│  - Port: Configurable (default 8085/TCP)                                   │
│  - Purpose: LLM inference with detector-based content filtering            │
│  - Receives: chat completion requests with detector configurations         │
│  - Returns: completions + detections field                                  │
│         │                         │                                         │
│         │                         │                                         │
│         ▼                         ▼                                         │
│  [Guardrails Detectors]    [vLLM / LLM Server]                             │
│  (e.g., regex-detector)    - Model serving backend                         │
│  - Content filtering       - Receives generation requests                  │
│  - Referenced by name        from orchestrator                             │
│    in detector config                                                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  No RBAC resources defined — stateless HTTP service with no Kubernetes API
  interactions. No ClusterRoles, Roles, or RoleBindings.

Service Mesh Configuration:
  No service mesh configuration defined in component. Service mesh integration
  (if any) is managed externally by platform operator.

Secrets:
  - TLS cert (/etc/tls/private/tls.crt)
    Type: x509 certificate
    Purpose: Client certificate for mTLS to orchestrator
    Provisioned by: External (mounted volume)
    Auto-rotate: Unknown

  - TLS key (/etc/tls/private/tls.key)
    Type: Private key
    Purpose: Private key for mTLS to orchestrator
    Provisioned by: External (mounted volume)
    Auto-rotate: Unknown

  - CA cert (/etc/tls/ca/service-ca.crt)
    Type: CA certificate
    Purpose: Custom CA for validating orchestrator's server certificate
    Provisioned by: External (mounted volume, likely OpenShift service-ca)
    Auto-rotate: Unknown

Authentication Flow:
  1. Client sends Authorization header with request
  2. Gateway does NOT validate or enforce authentication locally
  3. Gateway forwards Authorization header to FMS Guardrails Orchestrator
  4. Auth enforcement is delegated to orchestrator and/or external ingress

Crypto Notes:
  - TLS backend: native-tls (OpenSSL-backed via openssl 0.10.73)
  - Certificate handling: PEM → PKCS#12 conversion for reqwest Identity
  - Known risk: danger_accept_invalid_hostnames(true) when host is localhost
    (development convenience; not a production concern)
  - No hardcoded non-FIPS ciphers detected
  - Build links against system OpenSSL; compat-openssl11 in release image

Build Hermeticity:
  - hermetic: false (Tekton PipelineRun)
  - Cargo.lock present (reproducible Rust deps)
  - No rpms.lock.yaml
  - No Hermeto/cachi2 prefetch
