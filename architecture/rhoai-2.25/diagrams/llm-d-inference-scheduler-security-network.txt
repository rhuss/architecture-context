┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 80/TCP or 443/TCP                                            │
│         │ Protocol: HTTP / HTTPS                                             │
│         │ Encryption: TLS (if configured)                                    │
│         │ Auth: Application-level (model API key)                            │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Gateway API)                                                       │
│                                                                              │
│  [Envoy Gateway]                                                             │
│  - Type: Gateway API implementation                                          │
│  - Port: 80/TCP (dev) or 443/TCP (prod, TLS edge via OpenShift Route)       │
│  - HTTPRoute: routes / → InferencePool on port 8000                         │
│         │                                                                    │
│         │ Port: 9002/TCP                                                     │
│         │ Protocol: gRPC (HTTP/2)                                            │
│         │ Encryption: plaintext                                              │
│         │ Auth: None (network-level isolation, same namespace)               │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  SCHEDULER (EPP - llm-d-inference-scheduler)                                 │
│  Namespace: ${NAMESPACE} (hc4ai-operator)                                    │
│  Deployment: StatefulSet (OpenShift) / Deployment (dev)                      │
│  ServiceAccount: ${EPP_NAME}                                                 │
│                                                                              │
│  [EPP ext-proc gRPC Server]                                                  │
│  - Port: 9002/TCP (gRPC ext-proc) ← Envoy calls here                       │
│  - Port: 9003/TCP (gRPC health)   ← Kubelet probes                         │
│  - Port: 9090/TCP (HTTP metrics)  ← Prometheus scrapes                     │
│  - All ports: plaintext, unauthenticated                                     │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS                                                     │
│         │ Auth: ServiceAccount token (automounted)                           │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE                                                    │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Auth: ServiceAccount token                                                │
│  - EPP watches: InferencePool, InferenceModel, Pods, EndpointSlices         │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  MODEL-SERVING PODS (Internal)                                               │
│                                                                              │
│  [vLLM Model-Serving Pods]                                                   │
│  - Namespace: ${NAMESPACE}                                                   │
│  - Port: 8000/TCP (inference) ← Envoy forwards here (NOT EPP direct)       │
│  - Port: 5557/TCP (ZMQ PUB)  ← EPP subscribes (cache_tracking mode)        │
│  - Protocol: HTTP (8000), ZMQ (5557)                                         │
│  - Encryption: plaintext (both ports)                                        │
│  - Auth: None                                                                │
│         │                                                                    │
│         │ (Envoy routes to selected pod based on EPP decision)              │
│         │                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  [OpenTelemetry Collector]                                                   │
│  - Port: 4317/TCP                                                            │
│  - Protocol: gRPC (OTLP)                                                     │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: None                                                                │
│  - Purpose: Distributed tracing export (when configured)                     │
│                                                                              │
│  [HuggingFace Hub - huggingface.co]                                          │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS                                                           │
│  - Auth: HF_TOKEN (environment variable)                                     │
│  - Purpose: Download tokenizer models (cache_tracking mode only)             │
│                                                                              │
│  [Prometheus]                                                                │
│  - Port: 9090/TCP ← scrapes EPP metrics endpoint                           │
│  - Protocol: HTTP                                                            │
│  - Encryption: plaintext                                                     │
│  - Auth: None                                                                │
│  - Purpose: Scheduler telemetry (latency, cache hits, plugin scores)         │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- Role: ${EPP_NAME}
  Namespace: ${NAMESPACE}
  - API Group: inference.networking.x-k8s.io
    Resources: inferencepools, inferencemodels
    Verbs: get, watch, list
  - API Group: "" (core)
    Resources: pods
    Verbs: get, watch, list
  - API Group: discovery.k8s.io
    Resources: endpointslices
    Verbs: get, watch, list
  - API Group: authentication.k8s.io
    Resources: tokenreviews
    Verbs: create
  - API Group: authorization.k8s.io
    Resources: subjectaccessreviews
    Verbs: create

- Role: exec-role
  Namespace: ${NAMESPACE}
  - API Group: "" (core)
    Resources: pods/exec
    Verbs: create

- RoleBinding: ${EPP_NAME}-binding
  Binds: ${EPP_NAME} Role → ${EPP_NAME} ServiceAccount

- RoleBinding: exec-rolebinding
  Binds: exec-role → system:authenticated (Group)

Secrets:
- HF_TOKEN (env var) — HuggingFace token for tokenizer download
  Provisioned by: Manual
  Auto-rotate: No
  Used in: cache_tracking mode only

Network Exposure Summary:
- EPP gRPC ext-proc (9002/TCP): Internal only — Envoy-to-EPP
- EPP health (9003/TCP): Internal only — Kubelet probes
- EPP metrics (9090/TCP): Internal only — Prometheus scraping
- OpenShift Route (443/TCP): External — TLS edge termination
- Gateway (80/TCP): External — dev environments, plaintext

FIPS Compliance Notes:
- No GOEXPERIMENT=strictfipsruntime — Go binary does NOT use FIPS-validated crypto
- CGO_ENABLED=1 for ZMQ/tokenizer FFI, not FIPS
- gRPC communication is plaintext (relies on network-level encryption)
- K8s API TLS handled by controller-runtime default TLS config
- Potential FIPS gap if FIPS 140-2/3 compliance required
