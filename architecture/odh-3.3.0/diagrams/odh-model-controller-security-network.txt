┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client]                                                       │
│  [NVIDIA NGC API - nvcr.io]                                              │
│         │                         │                                      │
│         │ Port: 443/TCP           │ Port: 443/TCP                        │
│         │ Protocol: HTTPS         │ Protocol: HTTPS                      │
│         │ Encryption: TLS 1.2+    │ Encryption: TLS 1.2+                │
│         │ Auth: Bearer Token      │ Auth: API Key                        │
│         ▼                         ▼                                      │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (DMZ)                                                          │
│                                                                          │
│  [OpenShift Router]                                                      │
│  - Port: 443/TCP (external) → 8080/TCP (internal)                      │
│  - TLS Termination: TLS 1.2+ (Edge mode)                                │
│  - AuthN: Optional Bearer Token validation                              │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: mTLS (STRICT via Istio)                           │
│         │ Auth: Service Identity (Istio)                                 │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (Trusted - mTLS STRICT)                                   │
│                                                                          │
│  [Istio Gateway]                                                         │
│  - Namespace: istio-system                                               │
│  - ServiceAccount: istio-ingressgateway                                  │
│  - AuthorizationPolicy enforcement point                                 │
│         │                                                                │
│         │ Port: 8080/TCP (HTTP) or 9000/TCP (gRPC)                      │
│         │ Protocol: HTTP/gRPC                                            │
│         │ Encryption: mTLS (ServiceMesh sidecar)                         │
│         │ Auth: Service Identity + Bearer Token                          │
│         ▼                                                                │
│  [Predictor Pod]                                                         │
│  - Namespace: {user-namespace}                                           │
│  - ServiceAccount: {inferenceservice-name}-sa                           │
│  - Image: {serving-runtime-image}:tag                                    │
│  - Sidecar: Istio Envoy proxy                                            │
│         │                         │                                      │
│         │ S3 Access               │ NGC Access (NIM only)                │
│         │ Port: 443/TCP           │ Port: 443/TCP                        │
│         │ Protocol: HTTPS         │ Protocol: HTTPS                      │
│         │ Encryption: TLS 1.2+    │ Encryption: TLS 1.2+                │
│         │ Auth: AWS credentials   │ Auth: API Key                        │
│         ▼                         ▼                                      │
├─────────────────────────────────────────────────────────────────────────┤
│  CONTROLLER PLANE                                                        │
│                                                                          │
│  [ODH Model Controller]                                                  │
│  - Namespace: opendatahub                                                │
│  - ServiceAccount: odh-model-controller                                  │
│  - Deployment: odh-model-controller-manager                              │
│         │                                                                │
│         │ Kubernetes API Access                                          │
│         │ Port: 6443/TCP                                                 │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: ServiceAccount Token                                     │
│         ▼                                                                │
│  [Kubernetes API Server]                                                 │
│  - Watches: InferenceService, ServingRuntime, Account CRs               │
│  - Creates: Routes, HTTPRoutes, AuthorizationPolicies, ConfigMaps       │
│         │                                                                │
│         │ NGC API Access (NIM Account management)                        │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: API Key (from Secret)                                    │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [S3 Storage - s3.amazonaws.com or compatible]                           │
│  - Purpose: Model artifact storage                                       │
│  - Auth: AWS IAM credentials or access keys                              │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [NVIDIA NGC API - nvcr.io/api]                                          │
│  - Purpose: NIM model downloads and configurations                       │
│  - Auth: NGC API key                                                     │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [Model Registry API]                                                    │
│  - Port: 8080/TCP                                                        │
│  - Protocol: HTTP                                                        │
│  - Encryption: None (internal service mesh)                              │
│  - Auth: Bearer Token                                                    │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ClusterRole: odh-model-controller-role
├── API Group: "" (core)
│   ├── Resources: services, configmaps, secrets, serviceaccounts, pods,
│   │             namespaces, endpoints
│   └── Verbs: create, delete, get, list, patch, update, watch
│
├── API Group: serving.kserve.io
│   ├── Resources: inferenceservices, servingruntimes, inferencegraphs
│   └── Verbs: create, delete, get, list, patch, update, watch
│
├── API Group: route.openshift.io
│   ├── Resources: routes
│   └── Verbs: create, delete, get, list, patch, update, watch
│
├── API Group: gateway.networking.k8s.io
│   ├── Resources: gateways, httproutes
│   └── Verbs: create, delete, get, list, patch, update, watch
│
├── API Group: security.istio.io
│   ├── Resources: authorizationpolicies
│   └── Verbs: create, delete, get, list, patch, update, watch
│
├── API Group: nim.opendatahub.io
│   ├── Resources: accounts
│   └── Verbs: create, delete, get, list, patch, update, watch
│
├── API Group: networking.k8s.io
│   ├── Resources: ingresses
│   └── Verbs: get, list, watch
│
└── API Group: keda.sh
    ├── Resources: triggerauthentications, scaledobjects
    └── Verbs: create, delete, get, list, patch, update, watch

ClusterRoleBinding: odh-model-controller-rolebinding
├── Namespace: opendatahub
├── Role: odh-model-controller-role (ClusterRole)
└── ServiceAccount: odh-model-controller

Service Mesh Configuration:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PeerAuthentication: STRICT (namespace-scoped)
├── Mode: STRICT (requires mTLS for all service-to-service communication)
└── Scope: User namespaces with InferenceServices

AuthorizationPolicy: {inferenceservice}-authpolicy
├── Namespace: {user-namespace}
├── Selector: matchLabels for InferenceService predictor pods
├── Action: ALLOW
├── Rules:
│   ├── Source principals:
│   │   └── cluster.local/ns/istio-system/sa/istio-ingressgateway
│   ├── Request principals (JWT):
│   │   └── Bearer token validation (when configured)
│   └── Methods: GET, POST
└── Created by: ODH Model Controller (for LLM InferenceServices)

Secrets:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. {nim-account}-ngc-secret (Opaque)
   ├── Purpose: NVIDIA NGC API key for NIM model downloads
   ├── Keys: NGC_API_KEY
   ├── Provisioned by: User
   ├── Auto-rotate: No
   └── Mounted in: NIM predictor pods, ODH Model Controller

2. aws-s3-credentials (Opaque)
   ├── Purpose: S3 access credentials for model artifacts
   ├── Keys: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY
   ├── Provisioned by: User
   ├── Auto-rotate: No
   └── Mounted in: Predictor pods

3. {inferenceservice}-sa-token (kubernetes.io/service-account-token)
   ├── Purpose: Service account token for KServe predictor pods
   ├── Provisioned by: Kubernetes (automatic)
   ├── Auto-rotate: Yes (Kubernetes TokenRequest API)
   └── Used by: Predictor pods for K8s API access

4. istio-ca-cert (kubernetes.io/tls)
   ├── Purpose: Istio CA certificate for mTLS
   ├── Provisioned by: cert-manager / Istio
   ├── Auto-rotate: Yes (90-day rotation via cert-manager)
   └── Used by: Istio sidecars for mutual TLS authentication

Network Policies:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Egress from Predictor Pods:
├── Allow: S3 Storage (443/TCP HTTPS)
├── Allow: NVIDIA NGC API (443/TCP HTTPS) - NIM only
├── Allow: DNS (53/UDP)
└── Deny: All other external egress (default deny)

Ingress to Predictor Pods:
├── Allow: Istio Gateway (8080/TCP HTTP, 9000/TCP gRPC with mTLS)
├── Allow: Prometheus (metrics scraping on configured port)
└── Deny: Direct external access (must go through Router/Gateway)

Controller Egress:
├── Allow: Kubernetes API (6443/TCP HTTPS)
├── Allow: NVIDIA NGC API (443/TCP HTTPS) - NIM Account management
└── Allow: DNS (53/UDP)

Metrics and Monitoring:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Service: odh-model-controller-metrics
├── Type: ClusterIP
├── Port: 8080/TCP
├── Protocol: HTTP
├── Encryption: None
├── Auth: None (internal only)
├── Exposed to: Prometheus (ServiceMonitor)
└── Metrics: Controller reconciliation, InferenceService status, model serving metrics

Health Endpoints:
├── /healthz (8081/TCP) - Liveness probe
├── /readyz (8081/TCP) - Readiness probe
└── Access: Internal only (Kubernetes probes)
