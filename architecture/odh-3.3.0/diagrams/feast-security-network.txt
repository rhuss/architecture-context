┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [ML Application / Training Job / Stream Processing]                    │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Bearer Token (Optional) / OIDC (UI)                      │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (DMZ)                                                          │
│                                                                          │
│  [OpenShift Route / Kubernetes Ingress]                                 │
│  - Feast UI Route: *.cluster.local                                      │
│  - Port: 443/TCP (external) → 80/TCP (internal)                        │
│  - TLS Mode: Edge Termination                                           │
│  - TLS Version: TLS 1.2+                                                │
│  - Auth: OIDC (UI), Bearer Token (API)                                  │
│         │                                                                │
│         │ Port: 80/TCP                                                   │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (or optional mTLS via Istio)                  │
│         │ Auth: Bearer Token / Service Identity                          │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (Trusted - Optional mTLS)                                 │
│                                                                          │
│  [Feature Server - Online]                                              │
│  - Namespace: {user-namespace}                                           │
│  - ServiceAccount: {name}-feast-online                                  │
│  - Service: {name}-feast-online (ClusterIP)                             │
│  - Ports: 80/TCP → 6566/TCP (HTTP/gRPC)                                │
│  - Auth: Optional Bearer Token (JWT), Optional mTLS (Istio)             │
│  - Endpoints:                                                            │
│    - POST /get-online-features (real-time inference)                    │
│    - POST /push (feature push)                                          │
│    - POST /materialize (offline to online)                              │
│    - POST /materialize-incremental                                      │
│    - GET /health (health check)                                         │
│    - GET /metrics (8000/TCP, Prometheus)                                │
│         │                                                                │
│         │ Port: 6570/TCP                                                 │
│         │ Protocol: gRPC                                                 │
│         │ Encryption: None (or mTLS via Istio)                           │
│         │ Auth: None (internal)                                          │
│         ▼                                                                │
│  [Registry Service]                                                      │
│  - Namespace: {user-namespace}                                           │
│  - ServiceAccount: {name}-feast-registry                                │
│  - Service: {name}-feast-registry (ClusterIP)                           │
│  - Ports: 80/TCP → 6570/TCP (gRPC), 80/TCP → 6572/TCP (REST)           │
│  - Auth: Optional Bearer Token (REST)                                   │
│         │                                                                │
│         │ Port: 6569/TCP                                                 │
│         │ Protocol: gRPC                                                 │
│         │ Encryption: None (or mTLS via Istio)                           │
│         │ Auth: None (internal)                                          │
│         ▼                                                                │
│  [Transformation Service]                                                │
│  - Namespace: {user-namespace}                                           │
│  - ServiceAccount: {name}-feast-transformation                          │
│  - Service: {name}-feast-transformation (ClusterIP)                     │
│  - Port: 80/TCP → 6569/TCP (gRPC)                                       │
│  - Auth: None (internal)                                                 │
│         │                                                                │
│  [Feature Server - Offline]                                              │
│  - Namespace: {user-namespace}                                           │
│  - ServiceAccount: {name}-feast-offline                                 │
│  - Service: {name}-feast-offline (ClusterIP)                            │
│  - Port: 80/TCP → 8815/TCP (HTTP)                                       │
│  - Auth: Optional Bearer Token                                          │
│         │                                                                │
│  [UI Service]                                                            │
│  - Namespace: {user-namespace}                                           │
│  - ServiceAccount: {name}-feast-ui                                      │
│  - Service: {name}-feast-ui (ClusterIP)                                 │
│  - Port: 80/TCP → 8888/TCP (HTTP)                                       │
│  - Auth: Optional OIDC (OpenID Connect)                                 │
│         │                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  STORAGE BACKENDS (Internal/External)                                   │
│                                                                          │
│  [Online Store - Redis/PostgreSQL/DynamoDB]                              │
│  - Purpose: Low-latency online feature storage                          │
│  - Ports:                                                                │
│    - Redis: 6379/TCP                                                    │
│    - PostgreSQL: 5432/TCP                                               │
│  - Protocol: Redis/PostgreSQL                                            │
│  - Encryption: TLS (Optional for Redis/PostgreSQL)                      │
│  - Auth: Password (Redis), User/Password (PostgreSQL), AWS IAM (DynamoDB)│
│         │                                                                │
│  [Offline Store - BigQuery/Snowflake/S3]                                 │
│  - Purpose: Historical feature storage for training                     │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth:                                                                 │
│    - BigQuery: GCP Service Account                                      │
│    - Snowflake: User/Password/OAuth                                     │
│    - S3: AWS IAM credentials                                            │
│         │                                                                │
│  [Registry Backend - PostgreSQL/S3/GCS]                                  │
│  - Purpose: Feature definitions and metadata storage                    │
│  - Ports:                                                                │
│    - PostgreSQL: 5432/TCP                                               │
│    - S3/GCS: 443/TCP (HTTPS)                                            │
│  - Encryption: TLS 1.2+ (S3/GCS), TLS Optional (PostgreSQL)             │
│  - Auth:                                                                 │
│    - PostgreSQL: User/Password                                          │
│    - S3: AWS IAM                                                         │
│    - GCS: GCP Service Account                                           │
│         │                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  OPERATOR CONTROL PLANE                                                 │
│                                                                          │
│  [Feast Operator]                                                        │
│  - Namespace: feast-operator-system                                      │
│  - ServiceAccount: feast-operator-controller-manager                    │
│  - Service: feast-operator-metrics (ClusterIP)                          │
│  - Port: 8443/TCP (HTTPS)                                                │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Bearer Token (ServiceAccount) via kube-rbac-proxy              │
│         │                                                                │
│         │ Port: 6443/TCP                                                 │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: ServiceAccount Token                                     │
│         ▼                                                                │
│  [Kubernetes API Server]                                                 │
│  - Manages FeatureStore CRs and Feast deployments                       │
│         │                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  OBSERVABILITY & INTEGRATION                                            │
│                                                                          │
│  [Prometheus]                                                            │
│  - Scrapes /metrics endpoints                                           │
│  - Feature Server: 8000/TCP (HTTP)                                      │
│  - Operator: 8443/TCP (HTTPS, Bearer Token)                             │
│         │                                                                │
│  [Istio Service Mesh] (Optional)                                         │
│  - Provides mTLS between services                                       │
│  - PeerAuthentication: Optional STRICT mode                             │
│  - AuthorizationPolicy: Optional fine-grained access control            │
│         │                                                                │
│  [cert-manager] (Optional)                                               │
│  - Provisions TLS certificates                                          │
│  - Secrets: {name}-feast-tls, feast-operator-webhook-server-cert       │
│  - Rotation: 90 days                                                     │
│         │                                                                │
└─────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════
RBAC SUMMARY
═══════════════════════════════════════════════════════════════════════════

Operator ClusterRole: manager-role
├─ API Groups:
│  ├─ feast.dev: featurestores, featurestores/status, featurestores/finalizers
│  ├─ apps: deployments
│  ├─ "": services, configmaps, persistentvolumeclaims, serviceaccounts, secrets (get/list/watch), pods (get/list/watch), pods/exec (create), namespaces (get/list/watch)
│  ├─ rbac.authorization.k8s.io: roles, rolebindings, clusterroles, clusterrolebindings, subjectaccessreviews
│  ├─ authentication.k8s.io: tokenreviews
│  ├─ route.openshift.io: routes
│  ├─ batch: cronjobs
│  ├─ autoscaling: horizontalpodautoscalers
│  ├─ policy: poddisruptionbudgets
│  └─ apiextensions.k8s.io: customresourcedefinitions (get/list only)
└─ Verbs: get, list, watch, create, update, patch, delete

User ClusterRoles:
├─ featurestore-editor-role: create, update, patch, delete FeatureStore CRs
└─ featurestore-viewer-role: get, list, watch FeatureStore CRs

ClusterRole Bindings:
├─ manager-rolebinding: Binds manager-role to feast-operator-controller-manager SA
└─ leader-election-rolebinding: Binds leader-election-role to feast-operator-controller-manager SA

═══════════════════════════════════════════════════════════════════════════
SERVICE MESH CONFIGURATION
═══════════════════════════════════════════════════════════════════════════

Optional Istio Integration:
├─ PeerAuthentication: Optional (not enforced by default)
│  └─ Mode: PERMISSIVE or STRICT (configurable)
├─ AuthorizationPolicy: Optional (not configured by default)
│  └─ Allows: Service-to-service communication within namespace
└─ TLS Mode: Edge Termination at Ingress, Optional mTLS within mesh

═══════════════════════════════════════════════════════════════════════════
SECRETS
═══════════════════════════════════════════════════════════════════════════

TLS Certificates:
├─ {name}-feast-tls (kubernetes.io/tls)
│  ├─ Purpose: TLS certificates for HTTPS endpoints
│  ├─ Provisioned By: cert-manager
│  └─ Auto-Rotate: Yes (90 days)
├─ feast-operator-webhook-server-cert (kubernetes.io/tls)
│  ├─ Purpose: Webhook server certificate (future use)
│  ├─ Provisioned By: cert-manager
│  └─ Auto-Rotate: Yes (90 days)

Credentials:
├─ {name}-feast-secret (Opaque)
│  ├─ Purpose: Database credentials and API keys
│  ├─ Contents: DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, API_KEY
│  ├─ Provisioned By: User or External Secrets Operator
│  └─ Auto-Rotate: No (manual or ESO-managed)
├─ {name}-feast-registry-secret (Opaque)
│  ├─ Purpose: Registry backend credentials (S3, GCS)
│  ├─ Contents: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, GCP_SERVICE_ACCOUNT_KEY
│  ├─ Provisioned By: User or External Secrets Operator
│  └─ Auto-Rotate: No (manual or ESO-managed)
└─ {name}-feast-oidc-secret (Opaque)
   ├─ Purpose: OIDC client credentials for UI authentication
   ├─ Contents: CLIENT_ID, CLIENT_SECRET, ISSUER_URL
   ├─ Provisioned By: User
   └─ Auto-Rotate: No (manual)

═══════════════════════════════════════════════════════════════════════════
AUTHENTICATION & AUTHORIZATION
═══════════════════════════════════════════════════════════════════════════

API Endpoints:
├─ POST /get-online-features
│  ├─ Auth: Bearer Token (JWT) - Optional
│  ├─ Enforcement: FastAPI middleware
│  └─ Policy: Configured via authz.kubernetes or authz.oidc
├─ POST /push
│  ├─ Auth: Bearer Token (JWT) - Optional
│  ├─ Enforcement: FastAPI middleware
│  └─ Policy: Requires WRITE permission
├─ POST /materialize, /materialize-incremental
│  ├─ Auth: Bearer Token (JWT) - Optional
│  ├─ Enforcement: FastAPI middleware
│  └─ Policy: Requires WRITE permission
├─ gRPC ServingService
│  ├─ Auth: mTLS client certificates - Optional
│  ├─ Enforcement: Istio PeerAuthentication
│  └─ Policy: Istio-based service identity
├─ GET /ui
│  ├─ Auth: OIDC (OpenID Connect) - Optional
│  ├─ Enforcement: FastAPI OAuth2
│  └─ Policy: Configured via authz.oidc
├─ GET /metrics
│  ├─ Auth: None (Feature Server), Bearer Token (Operator)
│  ├─ Enforcement: kube-rbac-proxy (Operator only)
│  └─ Policy: Unauthenticated (Feature Server), Kubernetes RBAC (Operator)
└─ Operator Metrics (8443/TCP)
   ├─ Auth: Bearer Token (ServiceAccount)
   ├─ Enforcement: kube-rbac-proxy
   └─ Policy: Kubernetes RBAC

═══════════════════════════════════════════════════════════════════════════
SECURITY CONSIDERATIONS
═══════════════════════════════════════════════════════════════════════════

1. Authentication:
   ├─ OIDC for UI (optional)
   ├─ Kubernetes RBAC for feature-level authorization
   └─ Bearer tokens for API access (optional)

2. Authorization:
   └─ Fine-grained permissions via Kubernetes RBAC (read/write on feature views, services)

3. Encryption in Transit:
   ├─ Optional TLS for external databases
   ├─ Istio mTLS for service mesh integration (optional)
   └─ Edge TLS termination at Ingress (TLS 1.2+)

4. Encryption at Rest:
   └─ Depends on backend storage (Cloud KMS, database encryption)

5. Secret Management:
   ├─ Kubernetes Secrets for credentials
   └─ External Secrets Operator integration recommended

6. Network Policies:
   └─ Not included by default, can be added for namespace isolation

7. Pod Security:
   ├─ Runs as non-root user (configurable)
   └─ No default SecurityContext constraints

8. Audit Logging:
   ├─ Kubernetes audit logs
   └─ Application logs (structured JSON, configurable log level)
