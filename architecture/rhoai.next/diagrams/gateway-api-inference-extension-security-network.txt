┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [External Client]                                                          │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: Bearer Token / API Key                                      │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  GATEWAY LAYER (Envoy-compatible proxy)                                     │
│                                                                             │
│  [Envoy Gateway / kgateway / Istio / GKE Gateway]                          │
│  - Terminates external TLS                                                  │
│  - HTTPRoute references InferencePool as backendRef                        │
│  - ext-proc filter chain: BBR → EPP                                        │
│         │                              │                                    │
│         │ (Step 1 - optional)          │ (Step 2)                           │
│         │ Port: 9004/TCP               │ Port: 9002/TCP                    │
│         │ Protocol: gRPC/HTTP2         │ Protocol: gRPC/HTTP2              │
│         │ Encryption: TLS              │ Encryption: TLS                   │
│         │   (self-signed)              │   (self-signed or custom cert)    │
│         │ Auth: None (proxy-local)     │ Auth: None (proxy-local)          │
│         ▼                              ▼                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│  EXT-PROC SERVICES (Internal — ClusterIP only)                              │
│                                                                             │
│  [Body-Based Router (BBR)]                                                  │
│  - Namespace: {release-namespace}                                           │
│  - ServiceAccount: {bbr-name}-{release}                                    │
│  - Ports:                                                                   │
│    - 9004/TCP gRPC ext-proc (TLS, self-signed)                             │
│    - 9005/TCP gRPC health (plaintext, no auth)                             │
│    - 9090/TCP HTTP metrics (Bearer Token RBAC)                             │
│  - Purpose: Parse request body → extract model name → inject headers       │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS                                                   │
│         │ Auth: ServiceAccount Token                                        │
│         ▼                                                                   │
│  [Kubernetes API Server] ←── (ConfigMap watches, bbr-managed label)        │
│                                                                             │
│  [Endpoint Picker (EPP)]                                                    │
│  - Namespace: {release-namespace}                                           │
│  - ServiceAccount: {epp-name}                                              │
│  - Ports:                                                                   │
│    - 9002/TCP gRPC ext-proc (TLS, self-signed or custom cert)              │
│    - 9003/TCP gRPC health (plaintext, no auth)                             │
│    - 9090/TCP HTTP metrics (Bearer Token RBAC, configurable)               │
│    - 9090/TCP HTTP pprof /debug/pprof/* (no auth, when enabled)            │
│  - Purpose: Intelligent endpoint selection via scheduling pipeline         │
│         │                         │                                         │
│         │ (Metrics Scrape)        │ (CRD Watches)                          │
│         │ Port: varies/TCP        │ Port: 443/TCP                          │
│         │ Protocol: HTTP/HTTPS    │ Protocol: HTTPS                        │
│         │ Encryption: configurable│ Encryption: TLS                        │
│         │   (InsecureSkipVerify   │ Auth: ServiceAccount Token             │
│         │    default=true)        │                                         │
│         │ Auth: None              │                                         │
│         ▼                         ▼                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│  MODEL SERVER LAYER                                                         │
│                                                                             │
│  [Model Server Pods (vLLM / SGLang / Triton TensorRT-LLM)]                │
│  - Namespace: {pool-namespace}                                              │
│  - Selected by InferencePool label selector                                │
│  - Ports:                                                                   │
│    - varies/TCP (inference endpoint)                                        │
│    - varies/TCP (/metrics — Prometheus)                                     │
│  - Metrics exposed: KV cache utilization, queue depth, running requests,   │
│    LoRA adapter info, token counts                                          │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE                                                   │
│                                                                             │
│  [Kubernetes API Server]                                                    │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS                                                          │
│  - Auth: ServiceAccount Token (mounted)                                    │
│  - Resources watched by EPP:                                                │
│    - InferencePool (inference.networking.k8s.io/v1)                        │
│    - InferenceObjective (inference.networking.x-k8s.io/v1alpha2)           │
│    - InferenceModelRewrite (inference.networking.x-k8s.io/v1alpha2)        │
│    - Pods (core/v1, matching pool selector)                                │
│    - Leases (coordination.k8s.io/v1, leader election)                     │
│  - Resources watched by BBR:                                                │
│    - ConfigMaps (core/v1, label: bbr-managed=true)                         │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  OPTIONAL EXTERNAL SERVICES                                                 │
│                                                                             │
│  [Prometheus]                                                               │
│  - Scrapes: EPP 9090/TCP, BBR 9090/TCP                                    │
│  - Protocol: HTTP                                                           │
│  - Auth: Bearer Token (Kubernetes RBAC)                                    │
│                                                                             │
│  [OpenTelemetry Collector]                                                  │
│  - Port: 4317/TCP                                                           │
│  - Protocol: gRPC                                                           │
│  - Encryption: None (configurable)                                          │
│  - Auth: None                                                               │
│  - Purpose: Receive distributed traces from EPP/BBR                        │
│                                                                             │
│  [Latency Training Server] (sidecar in EPP pod)                            │
│  - Port: 8000/TCP                                                           │
│  - Protocol: HTTP                                                           │
│  - Encryption: None                                                         │
│  - Auth: None                                                               │
│                                                                             │
│  [Latency Prediction Server] (sidecar in EPP pod)                          │
│  - Port: 8001+/TCP                                                          │
│  - Protocol: HTTP                                                           │
│  - Encryption: None                                                         │
│  - Auth: None                                                               │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

ClusterRoles (conditional: monitoring enabled):
- {epp-cluster-rbac}
  - API Groups: authentication.k8s.io
  - Resources: tokenreviews
  - Verbs: create
  - API Groups: authorization.k8s.io
  - Resources: subjectaccessreviews
  - Verbs: create
  - Non-resource: /metrics
  - Verbs: get

- bbr-configmap-reader (conditional: multiNamespace)
  - API Groups: ""
  - Resources: configmaps
  - Verbs: get, list, watch

Roles (namespace-scoped):
- {epp-name}-sa
  - Namespace: {release-namespace}
  - API Groups: ""
  - Resources: pods
  - Verbs: get, watch, list

- {bbr-name}-{release}-configmap-reader
  - Namespace: {release-namespace}
  - API Groups: ""
  - Resources: configmaps
  - Verbs: get, list, watch

Service Accounts:
- {epp-name} → bound to {epp-name}-sa Role
- {bbr-name}-{release} → bound to {bbr-name}-{release}-configmap-reader Role

Secrets:
- inference-gateway-sa-metrics-reader-secret (kubernetes.io/service-account-token)
  Purpose: ServiceAccount token for Prometheus metrics auth
  Provisioned by: Kubernetes
  Auto-Rotate: Yes

- TLS certs (self-signed at runtime)
  Type: tls
  Purpose: gRPC TLS for ext-proc communication
  Provisioned by: Self-generated (4096-bit RSA, 10-year validity)
  Auto-Rotate: No

Security Observations:
- InsecureSkipVerify=true by default for model server metrics scraping (HTTP/HTTPS)
- No client certificate verification on ext-proc gRPC (proxy-local assumption)
- No NetworkPolicy or PeerAuthentication resources defined
- No cipher suite restrictions configured in TLS settings
- Self-signed TLS certs have 10-year validity (no rotation)
- pprof endpoint (/debug/pprof/*) exposed on metrics port without authentication
- Latency prediction sidecars communicate over plaintext HTTP within pod

FIPS Compliance Notes:
- Upstream: CGO_ENABLED=0 with distroless/static base (would NOT pass check-payload)
- RHOAI: Requires downstream Konflux Dockerfile with CGO_ENABLED=1,
  GOEXPERIMENT=strictfipsruntime, UBI base
- Uses stdlib crypto/tls, crypto/rsa, crypto/x509 (no external crypto libs)
