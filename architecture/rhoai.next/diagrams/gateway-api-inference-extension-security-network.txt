┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [External Client]                                                          │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: Bearer Token / API Key                                      │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  GATEWAY LAYER (Envoy-Compatible Proxy)                                     │
│                                                                             │
│  [Envoy Gateway / kgateway / Istio / GKE Gateway]                          │
│  - Port: 443/TCP (external) → internal routing                             │
│  - TLS Termination: TLS 1.2+                                               │
│  - AuthN: Per-gateway configuration                                        │
│  - ext-proc filter chain: BBR → EPP                                        │
│         │                          │                                        │
│         │ (1st ext-proc)           │ (2nd ext-proc)                         │
│         │ Port: 9004/TCP           │ Port: 9002/TCP                         │
│         │ Protocol: gRPC/HTTP2     │ Protocol: gRPC/HTTP2                   │
│         │ Encryption: TLS          │ Encryption: TLS                        │
│         │  (self-signed)           │  (self-signed or custom)               │
│         │ Auth: None               │ Auth: None                             │
│         │  (proxy-local)           │  (proxy-local)                         │
│         ▼                          ▼                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│  INFERENCE GATEWAY EXTENSION (Cluster Internal)                             │
│                                                                             │
│  [Body-Based Router (BBR)]                                                  │
│  - Namespace: {release-namespace}                                           │
│  - ServiceAccount: {bbr-name}-{release}                                    │
│  - Ports:                                                                   │
│    - 9004/TCP gRPC ext-proc (TLS, self-signed)                             │
│    - 9005/TCP gRPC health (plaintext, no auth)                             │
│    - 9090/TCP HTTP metrics (Bearer Token RBAC)                             │
│  - Image: gateway-api-inference-extension/bbr                              │
│                                                                             │
│  [Endpoint Picker (EPP)]                                                    │
│  - Namespace: {release-namespace}                                           │
│  - ServiceAccount: {epp-name}                                              │
│  - Ports:                                                                   │
│    - 9002/TCP gRPC ext-proc (TLS, self-signed or custom cert)              │
│    - 9003/TCP gRPC health (plaintext, no auth)                             │
│    - 9090/TCP HTTP metrics (Bearer Token RBAC, configurable)               │
│  - Image: gateway-api-inference-extension/epp                              │
│  - Optional sidecars:                                                       │
│    - Latency Training Server (8000/TCP HTTP, no auth)                      │
│    - Latency Prediction Server (8001+/TCP HTTP, no auth)                   │
│         │                                                                   │
│         │ Port: configurable/TCP                                            │
│         │ Protocol: HTTP/HTTPS                                              │
│         │ Encryption: configurable                                          │
│         │   ⚠ InsecureSkipVerify=true by DEFAULT                           │
│         │ Auth: None                                                        │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  MODEL SERVER PODS (Cluster Internal)                                       │
│                                                                             │
│  [vLLM / SGLang / Triton TensorRT-LLM Model Servers]                      │
│  - Namespace: {pool-namespace}                                              │
│  - Selected by: InferencePool label selector                                │
│  - Ports:                                                                   │
│    - {targetPort}/TCP (inference serving)                                   │
│    - /metrics endpoint (Prometheus, HTTP)                                   │
│  - Metrics exposed: KV cache utilization, queue depth,                     │
│    running requests, LoRA adapter info                                      │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE                                                   │
│                                                                             │
│  [Kubernetes API Server]                                                    │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS                                                          │
│  - Auth: ServiceAccount Token                                               │
│  - EPP watches: InferencePool, InferenceObjective,                         │
│    InferenceModelRewrite, Pods, Leases                                      │
│  - BBR watches: ConfigMaps (bbr-managed label)                             │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  OBSERVABILITY (Optional)                                                   │
│                                                                             │
│  [Prometheus]                                                               │
│  - Scrapes EPP/BBR: 9090/TCP HTTP                                          │
│  - Auth: Bearer Token (Kubernetes RBAC)                                    │
│                                                                             │
│  [OpenTelemetry Collector]                                                  │
│  - Receives from EPP/BBR: 4317/TCP gRPC                                   │
│  - Encryption: None (configurable)                                          │
│  - Auth: None                                                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

ClusterRoles (conditional: monitoring enabled):
- {epp-cluster-rbac}
  - authentication.k8s.io: tokenreviews → create
  - authorization.k8s.io: subjectaccessreviews → create
  - Non-resource /metrics → get
- bbr-configmap-reader (conditional: multiNamespace)
  - "": configmaps → get, list, watch

Roles (namespace-scoped):
- {epp-name}-sa ({release-namespace})
  - "": pods → get, watch, list
- {bbr-name}-{release}-configmap-reader ({release-namespace})
  - "": configmaps → get, list, watch

RoleBindings:
- {epp-name}-sa → ServiceAccount {epp-name}
- {bbr-name}-{release} → ServiceAccount {bbr-name}-{release}

TLS Configuration:
- EPP ext-proc (9002): Server TLS, self-signed (4096-bit RSA, 10-year validity)
  or custom cert with file-watcher reloading. No client cert verification.
  Cipher suites: Go defaults (no explicit restriction). HTTP/2 only.
- BBR ext-proc (9004): Server TLS, self-signed. No client cert verification.
- Health endpoints (9003, 9005): Plaintext gRPC, no auth.
- Metrics (9090): HTTP, Bearer Token via Kubernetes TokenReview + SubjectAccessReview.
- Model server scraping: ⚠ InsecureSkipVerify=true by default.

Secrets:
- inference-gateway-sa-metrics-reader-secret (kubernetes.io/service-account-token)
  Purpose: ServiceAccount token for Prometheus metrics auth
  Provisioned by: Kubernetes
  Auto-rotate: Yes

- TLS certs (self-signed at runtime)
  Purpose: gRPC TLS for ext-proc communication
  Provisioned by: Self-generated (crypto/rsa, 4096-bit, 10-year validity)
  Auto-rotate: No (custom certs support file-watcher reloading)

Security Observations:
- ⚠ Model server metrics scraping uses InsecureSkipVerify=true by default
- ⚠ Self-signed TLS certs have 10-year validity (no rotation)
- ⚠ No cipher suite restrictions on TLS configuration
- ⚠ No NetworkPolicy or PeerAuthentication resources defined
- ⚠ CGO_ENABLED=0 upstream — would not pass check-payload for RHOAI FIPS
- No client certificate verification on ext-proc endpoints (relies on proxy-local trust)
- Latency sidecar communication (8000, 8001+) is unencrypted HTTP with no auth
