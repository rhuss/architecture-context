┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client]                                                       │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Bearer Token / API Key                                   │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  GATEWAY LAYER (Envoy-Compatible Proxy)                                 │
│                                                                          │
│  [Envoy Gateway / kgateway / Istio Gateway / GKE Gateway]               │
│  - Port: 443/TCP (external) → internal routing                          │
│  - TLS Termination: TLS 1.2+                                            │
│  - Auth: Gateway-level (configurable per implementation)                │
│  - HTTPRoute → InferencePool backendRef resolution                      │
│         │                                                                │
│         ├── ext-proc filter chain (BBR first, then EPP)                  │
│         │                                                                │
│         │ Port: 9004/TCP                                                 │
│         │ Protocol: gRPC/HTTP2 (ext-proc)                                │
│         │ Encryption: TLS (self-signed)                                  │
│         │ Auth: None (proxy-local)                                       │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  IGW SERVICES (Cluster-Internal)                                        │
│                                                                          │
│  [Body-Based Router (BBR)]                                               │
│  - Namespace: {release-namespace}                                        │
│  - ServiceAccount: {bbr-name}-{release}                                 │
│  - Image: gateway-api-inference-extension/bbr                           │
│  - Port: 9004/TCP (gRPC ext-proc, TLS self-signed)                     │
│  - Port: 9005/TCP (gRPC health, plaintext)                              │
│  - Port: 9090/TCP (HTTP metrics, Bearer Token RBAC)                     │
│  - Purpose: Parse request body, extract model name,                     │
│             resolve LoRA adapter → base model, inject headers           │
│         │                                                                │
│         │ Returns ext-proc response with model-name headers             │
│         ▼                                                                │
│  [Gateway] ── ext-proc continues ──                                     │
│         │                                                                │
│         │ Port: 9002/TCP                                                 │
│         │ Protocol: gRPC/HTTP2 (ext-proc)                                │
│         │ Encryption: TLS (self-signed or custom cert)                   │
│         │ Auth: None (proxy-local)                                       │
│         ▼                                                                │
│  [Endpoint Picker (EPP)]                                                 │
│  - Namespace: {release-namespace}                                        │
│  - ServiceAccount: {epp-name}                                           │
│  - Image: gateway-api-inference-extension/epp                           │
│  - Port: 9002/TCP (gRPC ext-proc, TLS self-signed/custom)              │
│  - Port: 9003/TCP (gRPC health, plaintext)                              │
│  - Port: 9090/TCP (HTTP metrics, Bearer Token RBAC)                     │
│  - Purpose: Intelligent endpoint selection via scheduling pipeline      │
│             (filter → score → pick) with flow control                   │
│         │                                                                │
│         │ Returns ext-proc response with target-endpoint header         │
│         ▼                                                                │
│  [Gateway] ── forwards to selected model server ──                      │
│         │                                                                │
│         │ Port: configurable/TCP                                         │
│         │ Protocol: HTTP/gRPC                                            │
│         │ Encryption: per-gateway config                                 │
│         │ Auth: per-gateway config                                       │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  MODEL SERVING LAYER                                                     │
│                                                                          │
│  [Model Server Pods (vLLM / SGLang / Triton TensorRT-LLM)]             │
│  - Namespace: {user-namespace}                                           │
│  - Port: configurable/TCP (inference)                                    │
│  - Port: configurable/TCP (Prometheus /metrics)                         │
│         ▲                                                                │
│         │ Port: configurable/TCP                                         │
│         │ Protocol: HTTP/HTTPS                                           │
│         │ Encryption: configurable (InsecureSkipVerify=true default)     │
│         │ Auth: None                                                     │
│         │ Purpose: Metrics scraping (KV cache, queue, LoRA)             │
│         │ Interval: 50ms default                                         │
│         │                                                                │
│  [EPP Metrics Collector] ─────────────────────────────────┘             │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  OPTIONAL SIDECARS (EPP Pod)                                            │
│                                                                          │
│  [Latency Training Server]                                               │
│  - Port: 8000/TCP                                                        │
│  - Protocol: HTTP                                                        │
│  - Encryption: None                                                      │
│  - Auth: None                                                            │
│  - Purpose: Train XGBoost/LightGBM latency models                      │
│                                                                          │
│  [Latency Prediction Server]                                             │
│  - Port: 8001+/TCP                                                       │
│  - Protocol: HTTP                                                        │
│  - Encryption: None                                                      │
│  - Auth: None                                                            │
│  - Purpose: Serve TTFT/TPOT latency predictions                         │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  CLUSTER SERVICES                                                        │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS                                                       │
│  - Auth: ServiceAccount Token                                            │
│  - EPP watches: InferencePool, InferenceObjective,                      │
│    InferenceModelRewrite, Pods, Leases (leader election)                │
│  - BBR watches: ConfigMaps (bbr-managed=true)                           │
│                                                                          │
│  [Prometheus]                                                            │
│  - Port: 9090/TCP                                                        │
│  - Protocol: HTTP                                                        │
│  - Auth: Bearer Token (Kubernetes RBAC)                                  │
│  - Purpose: Scrape EPP/BBR metrics                                       │
│                                                                          │
│  [OpenTelemetry Collector] (optional)                                    │
│  - Port: 4317/TCP                                                        │
│  - Protocol: gRPC                                                        │
│  - Encryption: None (configurable)                                       │
│  - Auth: None                                                            │
│  - Purpose: Receive distributed traces from EPP/BBR                     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

ClusterRole: {epp-cluster-rbac} (conditional: monitoring enabled)
  - API Group: authentication.k8s.io
    Resources: tokenreviews
    Verbs: create
  - API Group: authorization.k8s.io
    Resources: subjectaccessreviews
    Verbs: create
  - Non-resource: /metrics
    Verbs: get

ClusterRole: bbr-configmap-reader (conditional: multiNamespace)
  - API Group: ""
    Resources: configmaps
    Verbs: get, list, watch

Role: {epp-name}-sa (Namespace: {release-namespace})
  - API Group: ""
    Resources: pods
    Verbs: get, watch, list

Role: {bbr-name}-{release}-configmap-reader (Namespace: {release-namespace})
  - API Group: ""
    Resources: configmaps
    Verbs: get, list, watch

RoleBindings:
  - {epp-name}-sa → {epp-name}-sa Role → SA: {epp-name}
  - {bbr-name}-{release} → {bbr-name}-{release}-configmap-reader Role → SA: {bbr-name}-{release}

Secrets:
  - inference-gateway-sa-metrics-reader-secret (kubernetes.io/service-account-token)
    Purpose: ServiceAccount token for Prometheus metrics auth
    Provisioned By: Kubernetes
    Auto-Rotate: Yes

  - TLS certs (self-signed at runtime)
    Type: tls
    Purpose: gRPC TLS for ext-proc communication
    Key: 4096-bit RSA, 10-year validity
    Provisioned By: Self-generated at EPP/BBR startup
    Auto-Rotate: No (unless enable-cert-reload with custom certs)

TLS Configuration:
  - EPP ext-proc (9002/TCP): Server TLS (self-signed or custom cert), HTTP/2 only
  - BBR ext-proc (9004/TCP): Server TLS (self-signed), HTTP/2 only
  - No client certificate verification (proxy-local assumption)
  - Model server metrics scraping: InsecureSkipVerify=true by default
  - No cipher suite restrictions configured

Security Observations:
  - InsecureSkipVerify=true default for model server HTTPS scraping
  - Self-signed TLS certs with 10-year validity (no rotation)
  - No NetworkPolicy definitions present
  - No PeerAuthentication or AuthorizationPolicy (Istio) present
  - Health endpoints (9003, 9005) are unauthenticated plaintext
  - pprof endpoints exposed on metrics port (configurable)
  - Upstream repo; FIPS compliance requires downstream Konflux Dockerfile
