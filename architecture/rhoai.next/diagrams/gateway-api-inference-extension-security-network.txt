┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: Gateway-defined                                              │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Gateway Layer)                                                     │
│                                                                              │
│  [Gateway / Envoy Proxy]                                                     │
│  - Port: 443/TCP (external) → internal routing                              │
│  - TLS Termination: TLS 1.2+                                                │
│  - ext-proc filter enabled                                                   │
│  - HTTPRoute: routes to InferencePool                                        │
│         │                              │                                     │
│         │ (1) Body parsing             │ (2) Endpoint selection               │
│         │ Port: 9004/TCP               │ Port: 9002/TCP                      │
│         │ Protocol: gRPC/HTTP2         │ Protocol: gRPC/HTTP2                │
│         │ Encryption: TLS              │ Encryption: TLS                     │
│         │   (self-signed or provided)  │   (self-signed or provided)         │
│         │ Auth: None                   │ Auth: None                          │
│         ▼                              ▼                                     │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (ext-proc Services)                                        │
│                                                                              │
│  [Body Based Router (BBR)]                                                   │
│  - Namespace: {release-ns}                                                   │
│  - ServiceAccount: body-based-router-{release}                               │
│  - Service: body-based-router (ClusterIP)                                    │
│  - Ports: 9004/TCP (ext-proc gRPC), 9005/TCP (health), 9090/TCP (metrics)   │
│  - Function: Extracts "model" from body → X-Gateway-Model-Name header       │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount Bearer Token                                  │
│         ▼                                                                    │
│  [Kubernetes API Server] ◄──────────────────────────────────────┐            │
│  - ConfigMap get/list/watch (BBR)                                │            │
│  - InferencePool, InferenceObjective, InferenceModelRewrite     │            │
│    get/list/watch (EPP)                                         │            │
│  - Pod get/list/watch (EPP)                                     │            │
│  - Lease CRUD (EPP leader election)                             │            │
│                                                                  │            │
│  [Endpoint Picker (EPP)]                                        │            │
│  - Namespace: {release-ns}                                      │            │
│  - ServiceAccount: {release}-inference-extension                │            │
│  - Service: {release}-inference-extension (ClusterIP)           │            │
│  - Ports: 9002/TCP (ext-proc gRPC), 9003/TCP (health),         │            │
│           9090/TCP (metrics)                                    │            │
│  - Deployment Strategy: Recreate (stateful scheduling context)  │            │
│  - TerminationGracePeriod: 130s                                 │            │
│  - Leader Election: via coordination.k8s.io/Lease (HA)         │            │
│         │                              │                        │            │
│         │ Port: 443/TCP                │ Port: configurable/TCP │            │
│         │ Protocol: HTTPS              │ Protocol: HTTP or HTTPS│            │
│         │ Encryption: TLS 1.2+         │ Encryption: Configurable            │
│         │ Auth: SA Bearer Token        │   (insecureSkipVerify  │            │
│         │                              │    default)            │            │
│         │                              │ Auth: None             │            │
│         ▼                              ▼                        │            │
│  [Kubernetes API Server]         [Model Server Pods]────────────┘            │
│                                  - vLLM / SGLang / Triton                    │
│                                  - Port: 8000/TCP (inference)                │
│                                  - /metrics endpoint scraped                 │
│                                  - Metrics: kv_cache_usage,                  │
│                                    queue_depth, running_requests,            │
│                                    lora_adapter_info                         │
│                                                                              │
│  [Latency Predictor (optional sidecars in EPP Pod)]                          │
│  - Training Server: 8000/TCP HTTP (trains XGBoost models)                    │
│  - Prediction Server: 8001/TCP HTTP (serves latency predictions)             │
│  - Encryption: None (localhost/pod-internal)                                 │
│  - Auth: None                                                                │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  OPTIONAL EXTERNAL SERVICES                                                  │
│                                                                              │
│  [OpenTelemetry Collector]                                                   │
│  - Port: 4317/TCP                                                            │
│  - Protocol: gRPC                                                            │
│  - Encryption: Plaintext (default)                                           │
│  - Auth: None                                                                │
│  - Purpose: Distributed tracing export                                       │
│                                                                              │
│  [Prometheus]                                                                │
│  - Port: 9090/TCP (scrapes EPP + BBR)                                        │
│  - Protocol: HTTP                                                            │
│  - Encryption: None                                                          │
│  - Auth: Optional (TokenReview + SubjectAccessReview for EPP)                │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

ClusterRole: {release}-epp-cluster-rbac
  - authentication.k8s.io / tokenreviews: create
  - authorization.k8s.io / subjectaccessreviews: create
  - (non-resource) /metrics: get

Role: {release}-sa (Namespace: {release-ns})
  - "" / pods: get, list, watch

Role: {release}-leader-election (Namespace: {release-ns})
  - coordination.k8s.io / leases: get, list, watch, create, update, patch, delete
  - "" / events: create, patch

Role: bbr-{release}-configmap-reader (Namespace: {release-ns})
  - "" / configmaps: get, list, watch

ClusterRoleBinding: {release}-epp-cluster-rbac
  - Binds: ClusterRole {release}-epp-cluster-rbac
  - To: ServiceAccount {release}-inference-extension

RoleBinding: {release}-sa
  - Binds: Role {release}-sa
  - To: ServiceAccount {release}-inference-extension

RoleBinding: {release}-leader-election-binding
  - Binds: Role {release}-leader-election
  - To: ServiceAccount {release}-inference-extension

RoleBinding: bbr-{release}-configmap-reader
  - Binds: Role bbr-{release}-configmap-reader
  - To: ServiceAccount body-based-router-{release}

Secrets:

- inference-gateway-sa-metrics-reader-secret (kubernetes.io/service-account-token)
  Purpose: ServiceAccount token for metrics endpoint authentication
  Provisioned: Helm chart
  Auto-Rotate: No

- Self-signed TLS certificates (in-memory generated, RSA 4096-bit)
  Purpose: TLS for ext-proc gRPC servers when no --cert-path provided
  Provisioned: EPP/BBR runtime
  Auto-Rotate: No (but --enable-cert-reload supports hot-reload via fsnotify)

TLS Configuration:

- ext-proc gRPC (9002, 9004): TLS enabled by default (--secure-serving=true)
  - Self-signed RSA 4096-bit when no --cert-path
  - Supports cert hot-reload via fsnotify (--enable-cert-reload)
  - Body chunking: max 62KB per chunk (below Envoy 64KB limit)

- Kubernetes API: TLS 1.2+ with ServiceAccount bearer tokens
- Model server scraping: Configurable (insecureSkipVerify default)
- Metrics endpoint (9090): HTTP plaintext, optional TokenReview auth
- OpenTelemetry: Plaintext gRPC (default)
