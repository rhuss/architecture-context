┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: Platform auth (gateway-level)                                │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Gateway API)                                                       │
│                                                                              │
│  [data-science-gateway] (HTTPRoute)                                          │
│  - Type: Gateway API HTTPRoute                                               │
│  - Port: 443/TCP (external) → 8443/TCP (backend)                            │
│  - TLS Mode: SIMPLE (gateway-terminated + backend TLS)                       │
│  - Path: /mlflow/*                                                           │
│         │                                                                    │
│         │ Port: 8443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (service-ca cert)                                  │
│         │ Auth: Forwarded Bearer Token                                       │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INTERNAL (Namespace: redhat-ods-applications)                               │
│                                                                              │
│  [mlflow Service] (ClusterIP)                                                │
│  - Port: 8443/TCP                                                            │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS (uvicorn SSL, OpenShift service-ca certs)                 │
│  - Auth: Kubernetes RBAC (SelfSubjectAccessReview)                           │
│  - NetworkPolicy Ingress: Port 8443 from ANY namespace                       │
│         │                                                                    │
│         ▼                                                                    │
│  [MLflow Server Pod]                                                         │
│  - ServiceAccount: mlflow-sa                                                 │
│  - Image: quay.io/opendatahub/mlflow:odh-stable                             │
│  - Containers: mlflow (main), ca-bundle-watcher (sidecar)                    │
│  - Probes: /mlflow/health (8443/TCP HTTPS)                                   │
│  - TLS Cert: mlflow-tls Secret (service-ca provisioned)                      │
│  - CA Bundle: combined from system + platform + custom CAs                   │
│  - Env vars: SSL_CERT_FILE, REQUESTS_CA_BUNDLE, CURL_CA_BUNDLE,             │
│              AWS_CA_BUNDLE, PGSSLROOTCERT, MLFLOW_MYSQL_CA                   │
│                                                                              │
│  [MLflow Operator Pod]                                                       │
│  - ServiceAccount: controller-manager                                        │
│  - Health: /healthz (8081/TCP HTTP), /readyz (8081/TCP HTTP)                 │
│  - Metrics: 8443/TCP HTTPS (Bearer Token + TokenReview/SAR)                  │
│                                                                              │
│  NetworkPolicy Egress Rules:                                                 │
│         │                                                                    │
│         ├──→ DNS (CoreDNS)                                                   │
│         │    Port: 53/UDP, 53/TCP, 5353/UDP, 5353/TCP                        │
│         │    Protocol: DNS                                                   │
│         │    Encryption: None                                                │
│         │    Auth: None                                                      │
│         │    Note: Port 5353 for OVN-Kubernetes post-DNAT                    │
│         │                                                                    │
│         ├──→ Kubernetes API Server                                           │
│         │    Port: 6443/TCP                                                  │
│         │    Protocol: HTTPS                                                 │
│         │    Encryption: TLS 1.2+                                            │
│         │    Auth: Bearer Token (ServiceAccount)                             │
│         │    Purpose: SelfSubjectAccessReview, namespace listing             │
│         │                                                                    │
│         ├──→ HTTPS Services (general)                                        │
│         │    Port: 443/TCP                                                   │
│         │    Protocol: HTTPS                                                 │
│         │    Encryption: TLS 1.2+                                            │
│         │    Auth: Varies                                                    │
│         │    Purpose: S3-compatible storage, external services               │
│         │                                                                    │
│         ├──→ PostgreSQL                                                      │
│         │    Port: 5432/TCP                                                  │
│         │    Protocol: PostgreSQL                                            │
│         │    Encryption: TLS (optional, via PGSSLMODE)                       │
│         │    Auth: Connection string credentials                             │
│         │    Purpose: Backend/registry store database                        │
│         │                                                                    │
│         ├──→ MySQL                                                           │
│         │    Port: 3306/TCP                                                  │
│         │    Protocol: MySQL                                                 │
│         │    Encryption: TLS (optional)                                      │
│         │    Auth: Connection string credentials                             │
│         │    Purpose: Backend/registry store database                        │
│         │                                                                    │
│         ├──→ MinIO                                                           │
│         │    Port: 9000/TCP                                                  │
│         │    Protocol: HTTP/HTTPS                                            │
│         │    Encryption: TLS (optional)                                      │
│         │    Auth: AWS IAM credentials (via envFrom Secret)                  │
│         │    Purpose: S3-compatible artifact storage                         │
│         │                                                                    │
│         └──→ SeaweedFS                                                       │
│              Port: 8333/TCP (HTTP), 8334/TCP (HTTPS)                         │
│              Protocol: HTTP/HTTPS                                            │
│              Encryption: TLS (port 8334)                                     │
│              Auth: AWS IAM credentials                                       │
│              Purpose: S3-compatible artifact storage                         │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  [PostgreSQL Database]                                                       │
│  - Purpose: Experiment metadata and model registry store                     │
│  - Port: 5432/TCP                                                            │
│  - Auth: Connection string credentials                                       │
│  - Encryption: TLS (optional, via CA bundle)                                 │
│                                                                              │
│  [MySQL Database]                                                            │
│  - Purpose: Alternative backend/registry store                               │
│  - Port: 3306/TCP                                                            │
│  - Auth: Connection string credentials                                       │
│  - Encryption: TLS (optional)                                                │
│                                                                              │
│  [S3-compatible Storage (MinIO / SeaweedFS / AWS S3)]                        │
│  - Purpose: Artifact storage (models, plots, files)                          │
│  - Port: 443/TCP, 9000/TCP, 8333/TCP, 8334/TCP                              │
│  - Auth: AWS IAM credentials (from mlflow-artifact-connection Secret)        │
│  - Encryption: TLS (optional, via combined CA bundle)                        │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

Operator Service Account: controller-manager (redhat-ods-applications)
  ClusterRole: manager-role
    - mlflow.opendatahub.io: mlflows (get, list, watch, create, update, patch, delete)
    - mlflow.opendatahub.io: mlflows/status (get, patch, update)
    - mlflow.opendatahub.io: mlflows/finalizers (update)
    - "": namespaces (get, list, watch)
    - "": secrets [mlflow-artifact-connection] (get, list, watch)
    - mlflow.kubeflow.org: mlflowconfigs (get, list, watch)
    - console.openshift.io: consolelinks (get, list, watch, create, update, patch, delete)
    - gateway.networking.k8s.io: httproutes (get, list, watch, create, update, patch, delete)
    - rbac.authorization.k8s.io: clusterroles, clusterrolebindings (full CRUD)
  Role (namespace-scoped):
    - "": configmaps, pvcs, secrets, serviceaccounts, services (full CRUD)
    - apps: deployments (full CRUD)
    - networking.k8s.io: networkpolicies (full CRUD)
    - monitoring.coreos.com: servicemonitors (full CRUD)

MLflow Server Service Account: mlflow-sa
  ClusterRole: mlflow
    - "": namespaces (get, list, watch)
    - "": secrets [mlflow-artifact-connection] (get, list, watch)
    - mlflow.kubeflow.org: mlflowconfigs (get, list, watch)

Aggregate Roles (integrate into standard K8s admin/edit/view):
  mlflow-view: read-only access to mlflows, mlflowconfigs, datasets, experiments, etc.
  mlflow-edit: full CRUD on mlflows, mlflowconfigs, datasets, experiments, etc.
  mlflow-integration: scoped access for other platform components

Secrets:
  - mlflow-tls (kubernetes.io/tls)
    Purpose: TLS certificate for MLflow server HTTPS
    Provisioned by: OpenShift service-ca (via service annotation)
    Auto-rotate: Yes (service-ca auto-rotation)

  - mlflow-artifact-connection (Opaque)
    Purpose: Per-namespace S3/artifact storage credentials
    Provisioned by: User/Admin
    Auto-rotate: No

Authentication & Authorization:
  - /mlflow/* (MLflow API):
    Auth: Kubernetes Bearer Token → SelfSubjectAccessReview
    Enforcement: MLflow kubernetes-auth app (built-in middleware)
    Policy: Maps API operations to RBAC verbs on pseudo-resources
    Note: MLFLOW_SERVER_DISABLE_SECURITY_MIDDLEWARE hardcoded to false

  - /metrics (operator):
    Auth: Bearer Token (ServiceAccount)
    Enforcement: controller-runtime metrics server (TokenReview + SubjectAccessReview)
    Policy: metrics-reader ClusterRole required

  - /metrics (MLflow):
    Auth: Bearer Token (ServiceAccount)
    Enforcement: ServiceMonitor with TLS
    Policy: service-ca TLS verification (OpenShift) or insecureSkipVerify (non-OpenShift)

NetworkPolicy Configuration:
  Ingress:
    - Port 8443/TCP from any namespace (required for gateway, dashboard, Prometheus)
  Egress:
    - DNS: 53/UDP, 53/TCP, 5353/UDP, 5353/TCP
    - HTTPS: 443/TCP
    - K8s API: 6443/TCP
    - PostgreSQL: 5432/TCP
    - MySQL: 3306/TCP
    - MinIO: 9000/TCP
    - SeaweedFS: 8333/TCP, 8334/TCP
    - Extensible: networkPolicyAdditionalEgressRules spec field for custom ports
