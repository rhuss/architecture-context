┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: Bearer Token (Kubernetes ServiceAccount/User token)          │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Gateway API)                                                       │
│                                                                              │
│  [data-science-gateway (Envoy)]                                              │
│  - Type: Gateway API Gateway CR                                             │
│  - Port: 443/TCP (external) → 8443/TCP (backend)                            │
│  - TLS: TLS 1.2+ termination at gateway                                     │
│  - Path rewrite: /mlflow/api → /api                                         │
│  - Routing: HTTPRoute parentRef                                              │
│         │                                                                    │
│         │ Port: 8443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (OpenShift service-ca cert)                        │
│         │ Auth: Passthrough (Bearer Token forwarded)                         │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER SERVICES (NetworkPolicy-controlled)                                 │
│                                                                              │
│  [MLflow Service]                                                            │
│  - Type: ClusterIP                                                           │
│  - Namespace: redhat-ods-applications (RHOAI) / opendatahub (ODH)           │
│  - Port: 8443/TCP                                                            │
│  - Encryption: TLS (service-ca cert at /etc/tls/private)                    │
│  - Auth: Kubernetes RBAC (SelfSubjectAccessReview)                           │
│  - NetworkPolicy Ingress: ANY namespace → port 8443                          │
│  - CORS: Configurable allowed origins                                        │
│  - Security middleware: ALWAYS enabled (CEL validation prevents override)    │
│         │                                                                    │
│         │                                                                    │
│  [MLflow Pod]                                                                │
│  - ServiceAccount: mlflow-sa                                                 │
│  - Init container: combine-ca-bundles (system + platform + custom CAs)      │
│  - Sidecar: ca-bundle-watcher (30s polling for CA updates)                  │
│  - Image: quay.io/opendatahub/mlflow:odh-stable                             │
│  - SSL: UVICORN_SSL_CIPHERS=PROFILE=SYSTEM (system crypto policy)           │
│  - FIPS: Delegates to system crypto policy                                   │
│         │                                                                    │
│         ├──── Port: 6443/TCP ────────────────────────────────────────────┐   │
│         │     Protocol: HTTPS                                            │   │
│         │     Encryption: TLS 1.2+                                       │   │
│         │     Auth: ServiceAccount Token                                  │   │
│         │     Purpose: SelfSubjectAccessReview, namespace listing         │   │
│         │                                                          ▼     │   │
│         │                                    [Kubernetes API Server]      │   │
│         │                                    Port: 6443/TCP              │   │
│         │                                                                │   │
│         ├──── Port: 53/UDP, 53/TCP, 5353/UDP, 5353/TCP ──────────────┐  │   │
│         │     Protocol: DNS                                           │  │   │
│         │     Encryption: None                                        │  │   │
│         │     Auth: None                                              │  │   │
│         │                                                       ▼    │  │   │
│         │                                              [CoreDNS]     │  │   │
│         │                                                            │  │   │
│         │                                                            │  │   │
├─────────┼────────────────────────────────────────────────────────────┼──┼───┤
│  OPERA  │  TOR NAMESPACE                                             │  │   │
│         │                                                            │  │   │
│  [mlflow-operator]                                                   │  │   │
│  - ServiceAccount: mlflow-operator-controller-manager                │  │   │
│  - Port: 8443/TCP (metrics, HTTPS)                                   │  │   │
│  - Port: 8081/TCP (health probes, HTTP)                              │  │   │
│  - Auth: Bearer Token (TokenReview/SubjectAccessReview) for metrics  │  │   │
│  - FIPS: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1              │  │   │
│  - TLS: crypto/tls, HTTP/2 disabled (NextProtos: http/1.1)          │  │   │
│         │                                                            │  │   │
│         │ Port: 6443/TCP                                             │  │   │
│         │ Protocol: HTTPS                                            │  │   │
│         │ Encryption: TLS 1.2+                                       │  │   │
│         │ Auth: ServiceAccount Token                                  │  │   │
│         │ Purpose: Watch CRs, server-side apply, discovery API       │  │   │
│         └────────────────────────────────────────────────────────────┘  │   │
│                                                                         │   │
├─────────────────────────────────────────────────────────────────────────┼───┤
│  EXTERNAL SERVICES                                                      │   │
│                                                                         │   │
│  [S3-compatible Storage]                                                │   │
│  - Providers: AWS S3, MinIO, SeaweedFS                                  │   │
│  - Ports: 443/TCP (AWS S3), 9000/TCP (MinIO),                           │   │
│           8333/TCP, 8334/TCP (SeaweedFS)                                │   │
│  - Protocol: HTTPS/HTTP                                                 │   │
│  - Encryption: TLS 1.2+ (when available)                               │   │
│  - Auth: S3 credentials from mlflow-artifact-connection secret          │   │
│  - Purpose: Artifact storage                                            │   │
│         ▲                                                               │   │
│         └───── Port: 443/9000/8333/8334 TCP ────────────────────────────┘   │
│                                                                              │
│  [PostgreSQL]                                                                │
│  - Port: 5432/TCP                                                            │
│  - Protocol: PostgreSQL wire protocol                                        │
│  - Encryption: TLS verify-full (when CA bundle configured)                  │
│  - Auth: Connection string credentials                                       │
│  - Purpose: Backend/registry metadata store                                  │
│                                                                              │
│  [MySQL]                                                                     │
│  - Port: 3306/TCP                                                            │
│  - Protocol: MySQL wire protocol                                             │
│  - Encryption: TLS (when CA bundle configured)                              │
│  - Auth: Connection string credentials                                       │
│  - Purpose: Backend/registry metadata store                                  │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  Operator ServiceAccount: mlflow-operator-controller-manager
  - ClusterRole: manager-role
    - mlflow.opendatahub.io: mlflows, mlflows/status, mlflows/finalizers → full CRUD
    - gateway.networking.k8s.io: httproutes → full CRUD
    - console.openshift.io: consolelinks → full CRUD
    - rbac.authorization.k8s.io: clusterroles, clusterrolebindings → full CRUD
    - "": namespaces → get, list, watch
    - "": secrets (resourceName: mlflow-artifact-connection) → get, list, watch
    - mlflow.kubeflow.org: mlflowconfigs → get, list, watch
  - Role: manager-role (namespace-scoped)
    - "": configmaps, pvcs, secrets, serviceaccounts, services → full CRUD
    - apps: deployments → full CRUD
    - networking.k8s.io: networkpolicies → full CRUD
    - monitoring.coreos.com: servicemonitors → full CRUD
  - ClusterRole: metrics-auth-role
    - authentication.k8s.io: tokenreviews → create
    - authorization.k8s.io: subjectaccessreviews → create

  MLflow Workload ServiceAccount: mlflow-sa
  - ClusterRole: mlflow
    - "": namespaces → get, list, watch
    - "": secrets (resourceName: mlflow-artifact-connection) → get, list, watch
    - mlflow.kubeflow.org: mlflowconfigs → get, list, watch

  Aggregate Roles (for users):
  - mlflow-view: read-only access to mlflows, mlflowconfigs, datasets, experiments, etc.
  - mlflow-edit: full CRUD on mlflows, mlflowconfigs, datasets, experiments, etc.
  - mlflow-integration: create/read on datasets, experiments, registeredmodels, gatewayendpoints

NetworkPolicy:
  Ingress: ANY namespace → MLflow pod port 8443/TCP (permissive)
  Egress (scoped):
    - DNS: 53/UDP, 53/TCP, 5353/UDP, 5353/TCP
    - HTTPS: 443/TCP
    - Kubernetes API: 6443/TCP
    - PostgreSQL: 5432/TCP
    - MySQL: 3306/TCP
    - S3 (MinIO): 9000/TCP
    - S3 (SeaweedFS): 8333/TCP, 8334/TCP
    - User-extensible via CR networkPolicyAdditionalEgressRules (CEL validated)

Secrets:
  - mlflow-tls (kubernetes.io/tls)
    Purpose: TLS cert/key for MLflow HTTPS
    Provisioned by: OpenShift service-ca (auto-rotated)
    Mounted at: /etc/tls/private
  - mlflow-artifact-connection (Opaque, user-provided, per-namespace)
    Purpose: S3 credentials (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_S3_BUCKET, AWS_S3_ENDPOINT)
    Provisioned by: Manual (user-created)
    Auto-rotate: No

CA Bundle Management:
  - Init container: combine-ca-bundles concatenates system CAs + odh-trusted-ca-bundle + custom CAs
  - Sidecar: ca-bundle-watcher polls ConfigMap changes every 30s, regenerates combined bundle
  - Env vars cover: Python requests, curl, boto3 (AWS), psycopg2 (PostgreSQL), mysql

FIPS Compliance:
  - Operator: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking against UBI9 OpenSSL
  - MLflow: UVICORN_SSL_CIPHERS=PROFILE=SYSTEM (delegates to system crypto policy)
  - No non-FIPS crypto detected (no MD5, RC4, DES usage)
