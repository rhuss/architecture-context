┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL SERVICES (Cluster Network)                                    │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Namespace: {guardrails-namespace}                                     │
│  - Role: Upstream caller, routes content inspection requests             │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plaintext)                                   │
│         │ Auth: None                                                     │
│         │ Method: POST /api/v1/text/contents                             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  TARGET SERVICE                                                          │
│                                                                          │
│  [Regex Detector]                                                        │
│  - Service Type: ClusterIP (internal only)                               │
│  - Namespace: {guardrails-namespace}                                     │
│  - Port: 8080/TCP                                                        │
│  - Protocol: HTTP                                                        │
│  - Encryption: None (plaintext)                                          │
│  - Auth: None                                                            │
│  - Image: UBI 9 Minimal + statically-linked Rust binary                 │
│  - Env: HTTP_PORT (default 8080), HOST (default 0.0.0.0)                │
│  - Endpoints:                                                            │
│    - POST /api/v1/text/contents (text detection)                        │
│    - GET /health (health check)                                          │
│                                                                          │
│  No outbound network connections                                         │
│  No persistent state / no database                                       │
│  No external service dependencies                                        │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

INGRESS: None (no external exposure)
EGRESS: None (no outbound connections)

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No ServiceAccount with elevated permissions

Secrets:
- None

Authentication & Authorization:
- Endpoint: /api/v1/text/contents (POST)
  - Auth: None
  - Enforcement: None
  - Policy: Relies on network-level isolation (Kubernetes NetworkPolicy or service mesh)
- Endpoint: /health (GET)
  - Auth: None
  - Enforcement: None
  - Policy: Health check, no authentication required

Service Mesh Configuration:
- Not explicitly configured in this repository
- Security relies on network-level isolation
- If deployed within a service mesh, mTLS may be applied at the mesh level

Risk Notes:
- No TLS termination at the application level
- No authentication mechanism — relies entirely on cluster network isolation
- Custom regex patterns compiled per-request: potential ReDoS vector
  (mitigated partially by the `regex` crate's built-in backtracking protection)
- No rate limiting at application level
