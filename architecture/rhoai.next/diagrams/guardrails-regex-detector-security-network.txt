┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CLUSTER NETWORK (Trusted - Network Isolation)                 │
│                                                                          │
│  ┌─────────────────────────────────────────────┐                        │
│  │  FMS Guardrails Orchestrator                 │                        │
│  │  - Routes text to detector backends          │                        │
│  │  - Merges detection results                  │                        │
│  └──────────────────┬──────────────────────────┘                        │
│                     │                                                    │
│                     │ Port: 8080/TCP                                     │
│                     │ Protocol: HTTP                                     │
│                     │ Encryption: None (plaintext)                       │
│                     │ Auth: None                                         │
│                     │ Method: POST /api/v1/text/contents                 │
│                     ▼                                                    │
│  ┌─────────────────────────────────────────────┐                        │
│  │  Regex Detector Service                      │                        │
│  │  - Type: ClusterIP (internal only)           │                        │
│  │  - Port: 8080/TCP                            │                        │
│  │  - Image: regex-detector (UBI9 minimal)      │                        │
│  │  - Runtime: Rust + Tokio async               │                        │
│  │  - Endpoints:                                │                        │
│  │    GET  /health               (health check) │                        │
│  │    POST /api/v1/text/contents (detection)    │                        │
│  └─────────────────────────────────────────────┘                        │
│                                                                          │
│  No egress connections - service is self-contained                       │
│  No ingress from external network                                        │
│  No TLS - relies on network-level isolation                              │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL NETWORK                                                        │
│                                                                          │
│  No external connections.                                                │
│  This service has no ingress from or egress to external networks.        │
│  All communication is internal cluster-only.                             │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No ServiceAccount RBAC configuration in this repository
  (Deployment manifests managed externally by rhods-operator or orchestrator)

Service Mesh Configuration:
- No PeerAuthentication defined in this repository
- No AuthorizationPolicy defined in this repository
- No Istio/OSSM sidecar configuration present
  Note: mTLS may be enforced at namespace level by platform operator

Secrets:
- None defined in this repository
- No TLS certificates (service uses plaintext HTTP)
- No API keys or credentials

Authentication & Authorization:
- /health (GET): Unauthenticated - open health check
- /api/v1/text/contents (POST): Unauthenticated
  Security relies entirely on network-level isolation
  (pod network policies, namespace boundaries, service mesh)

Container Security:
- Base image: ubi9/ubi-minimal (Red Hat Universal Base Image)
- OpenSSL: compat-openssl11 installed (system dep, not used by app)
- No crypto operations performed by application
- No privileged capabilities required
- Binary: regex-detector (Rust compiled, dynamically linked)

Network Policy Notes:
- No NetworkPolicy resources in this repository
- Expected to be deployed behind network policies managed by:
  - rhods-operator (RHOAI platform operator)
  - FMS Guardrails orchestrator deployment manifests
  - Namespace-level policies
