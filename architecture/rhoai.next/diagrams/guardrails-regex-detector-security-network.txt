┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CLUSTER NETWORK (Network-Level Isolation)                     │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │  UPSTREAM CALLER                                                   │  │
│  │                                                                    │  │
│  │  [FMS Guardrails Orchestrator]                                     │  │
│  │  - Namespace: {guardrails-namespace}                               │  │
│  │  - Sends text content inspection requests                         │  │
│  │         │                                                          │  │
│  │         │ Port: 8080/TCP                                           │  │
│  │         │ Protocol: HTTP                                           │  │
│  │         │ Encryption: None (plaintext)                             │  │
│  │         │ Auth: None                                               │  │
│  │         │ Method: POST /api/v1/text/contents                       │  │
│  │         ▼                                                          │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │  GUARDRAILS REGEX DETECTOR                                         │  │
│  │                                                                    │  │
│  │  [regex-detector]                                                  │  │
│  │  - Service Type: ClusterIP (internal only)                        │  │
│  │  - Namespace: {guardrails-namespace}                               │  │
│  │  - Image: UBI 9 minimal + statically-linked Rust binary           │  │
│  │  - Listen: 0.0.0.0:8080                                           │  │
│  │  - Env: HTTP_PORT (default 8080), HOST (default 0.0.0.0)         │  │
│  │                                                                    │  │
│  │  Endpoints Exposed:                                                │  │
│  │  ┌──────────────────────────────────────────────────────────────┐  │  │
│  │  │ POST /api/v1/text/contents  │ 8080/TCP │ HTTP │ No TLS     │  │  │
│  │  │ GET  /health                │ 8080/TCP │ HTTP │ No TLS     │  │  │
│  │  └──────────────────────────────────────────────────────────────┘  │  │
│  │                                                                    │  │
│  │  No outbound network connections (pure computation service)       │  │
│  │                                                                    │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │  HEALTH PROBES                                                     │  │
│  │                                                                    │  │
│  │  [Kubernetes Kubelet / Probe]                                      │  │
│  │         │                                                          │  │
│  │         │ Port: 8080/TCP                                           │  │
│  │         │ Protocol: HTTP                                           │  │
│  │         │ Method: GET /health                                      │  │
│  │         │ Auth: None                                               │  │
│  │         ▼                                                          │  │
│  │  [regex-detector] → responds "healthy"                            │  │
│  │                                                                    │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  None — this service makes no outbound network calls.                   │
│  All computation is performed locally via regex matching.                │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

INGRESS SUMMARY:
- No external ingress (ClusterIP service, internal-only)
- Inbound traffic from FMS Guardrails Orchestrator on 8080/TCP HTTP

EGRESS SUMMARY:
- No egress connections
- Pure computation service with no external dependencies at runtime

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No ServiceAccount-specific permissions documented

Secrets:
- None

Authentication & Authorization:
- No authentication on any endpoint
- Security relies entirely on network-level isolation:
  - Kubernetes NetworkPolicy (recommended)
  - Service mesh mTLS (if deployed with Istio/OSSM sidecar)
- WARNING: Without NetworkPolicy or service mesh, any pod in the cluster
  can reach this service on 8080/TCP

Security Considerations:
- No TLS termination at application level (plaintext HTTP)
- Potential ReDoS risk: Custom regex patterns compiled on every request
  with no complexity limit or timeout (mitigated partially by regex crate defaults)
- No rate limiting at application level
- No input size validation at application level
- Base image: UBI 9 minimal (Red Hat supported, minimal attack surface)
