┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL (Cluster Network — No External Ingress)                       │
│                                                                          │
│  This component has NO external ingress. All traffic is internal         │
│  cluster traffic only.                                                   │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  CLUSTER NETWORK (Internal Only)                                         │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Namespace: (platform namespace)                                       │
│  - Role: Upstream caller, routes text to detectors                       │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plaintext)                                   │
│         │ Auth: None                                                     │
│         │ Method: POST /api/v1/text/contents                             │
│         ▼                                                                │
│  [Guardrails Regex Detector]                                             │
│  - Service: regex-detector (ClusterIP)                                   │
│  - Namespace: (platform namespace)                                       │
│  - Container Port: 8080/TCP                                              │
│  - Image: UBI9 minimal + Rust binary                                     │
│  - Endpoints:                                                            │
│    • GET  /health                  (health check, unauthenticated)       │
│    • POST /api/v1/text/contents    (detection, unauthenticated)          │
│  - Env Vars:                                                             │
│    • HTTP_PORT (default: 8080)                                           │
│         │                                                                │
│         │ (HTTP response — same connection)                              │
│         │ DetectionResponse JSON                                         │
│         ▼                                                                │
│  [FMS Guardrails Orchestrator]                                           │
│  - Receives detection results                                            │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  None — this component has zero external egress dependencies.            │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No ServiceAccount requirements documented
  (This is a stateless HTTP service with no Kubernetes API interaction)

Service Mesh Configuration:
- No PeerAuthentication defined in this repository
- No AuthorizationPolicy defined in this repository
- No sidecar injection annotations documented
  NOTE: Network-level isolation (pod network policies, service mesh mTLS)
  is expected to be configured by the platform operator (rhods-operator)
  or the FMS Guardrails Orchestrator deployment manifests.

Secrets:
- None — this component uses no secrets, certificates, or credentials.

TLS / Encryption:
- Application-level TLS: None (HTTP only, plaintext)
- No crypto libraries in dependencies (no ring, rustls, openssl crates)
- Transport security relies entirely on network-level isolation
- Container image includes compat-openssl11 (system dependency, not used by app)

FIPS Compliance:
- Build: Rust binary, dynamic linking on glibc (Debian builder → UBI9 runtime)
- OpenSSL: compat-openssl11 installed in image (system-level, not app-level)
- Crypto: No cryptographic operations performed by this service
- Risk: None — pure text processing (regex matching only)

Authentication:
- All endpoints are unauthenticated
- Security relies on network-level isolation (ClusterIP service, not exposed externally)
- No ingress controllers or routes configured
