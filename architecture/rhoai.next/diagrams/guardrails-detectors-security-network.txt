┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [FMS Guardrails Orchestrator]                                               │
│  - Purpose: Calls detector endpoints for content analysis                    │
│         │                                                                    │
│         │ Port: 8080/TCP (built-in), 8000/TCP (HF, LLM Judge)               │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None (application-level); Istio mTLS when in mesh      │
│         │ Auth: None (application-level); platform-managed via KServe        │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Istio Service Mesh - mTLS PASSTHROUGH)                             │
│                                                                              │
│  [KServe InferenceService - HuggingFace Detector]                            │
│  - Ingress Type: KServe RawDeployment                                        │
│  - Istio sidecar: inject=true (PASSTHROUGH mode)                             │
│  - Port: 8000/TCP (external) → 8000/TCP (container)                         │
│  - Auth annotation: security.opendatahub.io/enable-auth                     │
│                                                                              │
│  [KServe InferenceService - LLM Judge Detector]                              │
│  - Ingress Type: KServe RawDeployment                                        │
│  - Istio sidecar: inject=true (PASSTHROUGH mode)                             │
│  - Port: 8000/TCP (external) → 8000/TCP (container)                         │
│  - Auth annotation: security.opendatahub.io/enable-auth                     │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (Internal - Istio mTLS when configured)                        │
│                                                                              │
│  [Built-in Detector]                                                         │
│  - Service: ClusterIP                                                        │
│  - Namespace: (deployment ns)                                                │
│  - Port: 8080/TCP                                                            │
│  - Protocol: HTTP                                                            │
│  - Encryption: None (configurable via ssl_ca_certs in config)                │
│  - Auth: None                                                                │
│  - Endpoints:                                                                │
│    POST /api/v1/text/contents (content analysis)                             │
│    GET  /health (liveness/readiness)                                         │
│    GET  /metrics (Prometheus)                                                │
│    GET  /registry (list detectors)                                           │
│                                                                              │
│  [HuggingFace Detector]                                                      │
│  - Service: ClusterIP (via KServe InferenceService)                          │
│  - Namespace: (deployment ns)                                                │
│  - Port: 8000/TCP                                                            │
│  - Protocol: HTTP                                                            │
│  - Encryption: None (Istio sidecar mTLS)                                     │
│  - Auth: KServe auth (security.opendatahub.io/enable-auth)                  │
│  - Image: rhoai/odh-guardrails-detector-huggingface-runtime-rhel9           │
│  - Endpoints:                                                                │
│    POST /api/v1/text/contents (ML content analysis)                          │
│    GET  /health (liveness/readiness)                                         │
│    GET  /metrics (Prometheus)                                                │
│         │                                                                    │
│         │ (in-process PyTorch inference - no network call)                    │
│         │                                                                    │
│  [LLM Judge Detector] (upstream only - no Konflux build)                     │
│  - Service: ClusterIP (via KServe InferenceService)                          │
│  - Namespace: (deployment ns)                                                │
│  - Port: 8000/TCP                                                            │
│  - Protocol: HTTP                                                            │
│  - Encryption: None (Istio sidecar mTLS)                                     │
│  - Auth: KServe auth (security.opendatahub.io/enable-auth)                  │
│  - Image: quay.io/trustyai/guardrails-detector-llm-judge                    │
│  - Endpoints:                                                                │
│    POST /api/v1/text/contents (LLM judge analysis)                           │
│    POST /api/v1/text/generation (prompt+response evaluation)                 │
│    GET  /api/v1/metrics (list evaluation metrics)                            │
│    GET  /health (liveness/readiness)                                         │
│    GET  /metrics (Prometheus)                                                │
│         │                                                                    │
│         │ Port: configurable                                                 │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None                                                   │
│         │ Auth: None                                                         │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  [vLLM-compatible LLM Server]                                                │
│  - Purpose: LLM-as-a-judge evaluation (OpenAI-compatible API)                │
│  - Port: configurable                                                        │
│  - Protocol: HTTP                                                            │
│  - Encryption: None                                                          │
│  - Auth: None                                                                │
│                                                                              │
│  [S3/Minio Storage - Model Artifacts]                                        │
│  - Purpose: HuggingFace model file storage                                   │
│  - Port: 9000/TCP                                                            │
│  - Protocol: HTTP                                                            │
│  - Encryption: None                                                          │
│  - Auth: AWS-style credentials (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)    │
│                                                                              │
│  [HuggingFace Hub - huggingface.co]                                          │
│  - Purpose: Model download via huggingface-cli (build-time/init)             │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS                                                           │
│  - Auth: HuggingFace token (optional)                                        │
│                                                                              │
│  [Prometheus]                                                                │
│  - Purpose: Metrics collection                                               │
│  - Scrape ports: 8080/TCP (built-in), 8000/TCP (HF, LLM Judge)              │
│  - Protocol: HTTP                                                            │
│  - Encryption: None                                                          │
│  - Auth: None                                                                │
│  - Annotation: prometheus.io/port                                            │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined (application-level pods, no Kubernetes API interaction)
- RoleBinding (example): user-one-view → ClusterRole:view → SA:user-one

Service Mesh Configuration:
- Istio sidecar injection: sidecar.istio.io/inject=true
- TLS mode: PASSTHROUGH (Istio sidecar handles mTLS)
- Auth: security.opendatahub.io/enable-auth annotation (KServe-managed)

Secrets:
- aws-connection-minio-data-connection-guardrails-guardian (Opaque)
  - Keys: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_S3_BUCKET, AWS_S3_ENDPOINT
  - Purpose: S3 credentials for model storage access
  - Provisioned by: Manual
  - Auto-rotate: No

TLS Configuration:
- Application-level: Optional SSL via uvicorn (ssl_ca_certs config key)
- No hardcoded cipher suites
- System trust store (UBI9 OpenSSL)
- OpenSSL pinned in HF Konflux Dockerfile: openssl-libs-3.5.1-5.el9_7

FIPS Compliance:
- Python services inherit FIPS from UBI9 base image OpenSSL
- No application-level crypto code detected
- No Go binaries (N/A for GOEXPERIMENT/strictfipsruntime)

Custom Detector Security:
- AST-based static code analysis sandboxing
- Blocked imports: os, subprocess, sys, shutil
- Blocked functions: eval, exec, open, compile, input
- Allowed exception: os.getenv, os.environ (read-only env access)
