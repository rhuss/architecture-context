┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL / PLATFORM (Untrusted → Orchestrated)                            │
│                                                                             │
│  [FMS Guardrails Orchestrator]                                              │
│  - Purpose: Content analysis orchestration                                  │
│  - Calls detector endpoints for guardrailing                                │
│         │                                                                   │
│         │ Port: 8080/TCP (built-in), 8000/TCP (HF, Judge)                  │
│         │ Protocol: HTTP                                                    │
│         │ Encryption: mTLS (Istio sidecar)                                 │
│         │ Auth: Platform auth (KServe annotation)                          │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (Trusted - mTLS via Istio sidecar injection)                 │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────┐        │
│  │  [Built-in Detector]                                            │        │
│  │  - Namespace: (deployment namespace)                            │        │
│  │  - Port: 8080/TCP                                               │        │
│  │  - Protocol: HTTP                                               │        │
│  │  - Image: odh-built-in-detector-rhel9                           │        │
│  │  - Endpoints:                                                   │        │
│  │    POST /api/v1/text/contents (content analysis)                │        │
│  │    GET  /health (liveness/readiness)                            │        │
│  │    GET  /metrics (Prometheus)                                   │        │
│  │    GET  /registry (registered detectors)                        │        │
│  │  - No external dependencies (self-contained)                    │        │
│  │  - Istio sidecar: injected (mTLS)                               │        │
│  └─────────────────────────────────────────────────────────────────┘        │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────┐        │
│  │  [HuggingFace Detector] (KServe InferenceService)               │        │
│  │  - Namespace: (user namespace)                                  │        │
│  │  - Port: 8000/TCP                                               │        │
│  │  - Protocol: HTTP                                               │        │
│  │  - Image: odh-guardrails-detector-huggingface-runtime-rhel9     │        │
│  │  - Endpoints:                                                   │        │
│  │    POST /api/v1/text/contents (ML model inference)              │        │
│  │    GET  /health (liveness/readiness)                            │        │
│  │    GET  /metrics (Prometheus)                                   │        │
│  │  - Istio sidecar: injected (mTLS)                               │        │
│  │  - KServe annotations:                                          │        │
│  │    serving.knative.openshift.io/enablePassthrough: "true"       │        │
│  │    sidecar.istio.io/inject: "true"                              │        │
│  │    security.opendatahub.io/enable-auth: "true"                  │        │
│  │         │                                                       │        │
│  │         │ Port: 9000/TCP                                        │        │
│  │         │ Protocol: HTTP                                        │        │
│  │         │ Encryption: None                                      │        │
│  │         │ Auth: AWS IAM credentials                             │        │
│  │         ▼                                                       │        │
│  └─────────────────────────────────────────────────────────────────┘        │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────┐        │
│  │  [LLM Judge Detector] (KServe InferenceService)                 │        │
│  │  - Namespace: (user namespace)                                  │        │
│  │  - Port: 8000/TCP                                               │        │
│  │  - Protocol: HTTP                                               │        │
│  │  - Image: guardrails-detector-llm-judge                         │        │
│  │  - Endpoints:                                                   │        │
│  │    POST /api/v1/text/contents (LLM judge content analysis)      │        │
│  │    POST /api/v1/text/generation (generation evaluation)         │        │
│  │    GET  /api/v1/metrics (available eval metrics)                 │        │
│  │    GET  /health (liveness/readiness)                            │        │
│  │    GET  /metrics (Prometheus)                                   │        │
│  │  - Istio sidecar: injected (mTLS)                               │        │
│  │  - KServe annotations:                                          │        │
│  │    serving.knative.openshift.io/enablePassthrough: "true"       │        │
│  │    sidecar.istio.io/inject: "true"                              │        │
│  │    security.opendatahub.io/enable-auth: "true"                  │        │
│  │         │                                                       │        │
│  │         │ Port: 8080/TCP                                        │        │
│  │         │ Protocol: HTTP                                        │        │
│  │         │ Encryption: None                                      │        │
│  │         │ Auth: None                                            │        │
│  │         ▼                                                       │        │
│  └─────────────────────────────────────────────────────────────────┘        │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                          │
│                                                                             │
│  [S3/MinIO Storage - model storage]                                         │
│  - Port: 9000/TCP                                                           │
│  - Protocol: HTTP                                                           │
│  - Encryption: None                                                         │
│  - Auth: AWS IAM credentials (ACCESS_KEY, SECRET_KEY)                       │
│  - Purpose: Model file storage for HuggingFace detector                    │
│  - Access: KServe Storage Initializer (init container)                     │
│                                                                             │
│  [vLLM Server - LLM inference backend]                                      │
│  - Port: 8080/TCP                                                           │
│  - Protocol: HTTP (OpenAI-compatible API)                                   │
│  - Encryption: None                                                         │
│  - Auth: None                                                               │
│  - Purpose: LLM inference for Judge evaluations                            │
│                                                                             │
│  [HuggingFace Hub - model download]                                         │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: None                                                               │
│  - Purpose: Model download in example init container (not production)      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No custom RBAC roles defined
- Detectors run as application workloads
- Relies on KServe and platform-level RBAC
- Example: ClusterRole/view binding for user-one (demo only)

Service Mesh Configuration:
- Istio sidecar injection: Enabled via annotation sidecar.istio.io/inject: "true"
- mTLS: Enforced at platform level via PeerAuthentication
- KServe auth: Enabled via annotation security.opendatahub.io/enable-auth: "true"
- Knative passthrough: Enabled via annotation serving.knative.openshift.io/enablePassthrough: "true"
- No application-level authentication (delegated to Istio + KServe)

Secrets:
- aws-connection-minio-data-connection-guardrails-guardian (Opaque)
  - Contents: ACCESS_KEY, SECRET_KEY, ENDPOINT, BUCKET, REGION
  - Purpose: S3 credentials for HuggingFace model storage
  - Provisioned by: Manual (example) / Platform operator (production)
  - Auto-rotate: No

FIPS Compliance:
- Base image: UBI9 (registry.access.redhat.com/ubi9/ubi-minimal)
- OpenSSL: System-provided via UBI9 (pinned 3.5.1-5.el9_7 in Konflux HF image)
- Application-level crypto: None (no direct crypto imports)
- TLS: Optional via uvicorn ssl_ca_certs config
- Certificate handling: System trust store (UBI9 default)

Prometheus Metrics:
- Endpoint: GET /metrics on each detector (8080/TCP, 8000/TCP)
- Metric prefix: trustyai_guardrails_*
- Dimensions: detections, requests, errors, runtime
- Labels: detector_kind, detector_name
- Multi-process compatible: Yes (PROMETHEUS_MULTIPROC_DIR)
