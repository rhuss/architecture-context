┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [User / kubectl]                                                            │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: Bearer Token (kubeconfig)                                    │
│         ▼                                                                    │
│  [RHOAI Dashboard]                                                           │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+ (FIPS ciphers)                               │
│         │ Auth: Bearer Token (user identity)                                 │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (OpenShift Routes)                                                  │
│                                                                              │
│  [Per-ISVC OpenShift Route]                                                  │
│  - Host: {isvc-name}.{namespace}.{cluster-domain}                           │
│  - Port: 443/TCP (external)                                                 │
│  - TLS Mode: Edge or Reencrypt                                              │
│  - Auth: None or Kuadrant AuthPolicy (per annotation)                       │
│         │                                                                    │
│         │ Port: varies                                                       │
│         │ Protocol: HTTP/HTTPS                                               │
│         │ Encryption: TLS (reencrypt) or plaintext (edge)                   │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INTERNAL SERVICES (Cluster Network)                                         │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  Namespace: {operator-namespace} (e.g., redhat-ods-applications)    │    │
│  │                                                                     │    │
│  │  [odh-model-controller]                                             │    │
│  │  - Deployment: odh-model-controller (1 replica, leader election)    │    │
│  │  - ServiceAccount: odh-model-controller                             │    │
│  │  - Image: odh-model-controller-container                            │    │
│  │  - Ports:                                                           │    │
│  │    - 8080/TCP  HTTP   Prometheus metrics (optional TLS)             │    │
│  │    - 8081/TCP  HTTP   Health probes (/healthz, /readyz)             │    │
│  │    - 9443/TCP  HTTPS  Admission webhooks (TLS, cert injection)      │    │
│  │                                                                     │    │
│  │  [model-serving-api]                                                │    │
│  │  - Deployment: model-serving-api (separate binary)                  │    │
│  │  - ServiceAccount: model-serving-api                                │    │
│  │  - Image: odh-model-serving-api-container                           │    │
│  │  - Ports:                                                           │    │
│  │    - 8443/TCP  HTTPS  Gateway API (/api/v1/gateways)               │    │
│  │                       TLS 1.2+ FIPS ciphers                         │    │
│  │                       ECDHE+AES-GCM only (P-256, P-384)            │    │
│  │                       Auth: Bearer Token + SSAR                     │    │
│  │    - 9090/TCP  HTTPS  Prometheus metrics                            │    │
│  │                       TLS 1.2+                                      │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
│  Services:                                                                   │
│  ┌────────────────────────────────────────────────────────────────────┐      │
│  │ model-serving-api          ClusterIP  443→8443   HTTPS TLS 1.2+   │      │
│  │                                       (OpenShift serving cert)     │      │
│  │ model-serving-api (metrics) ClusterIP 9090→9090  HTTPS TLS 1.2+   │      │
│  │ odh-model-controller-      ClusterIP  8443→8443  HTTPS TLS        │      │
│  │   metrics-service                     (authn/authz filter)        │      │
│  │ webhook-service            ClusterIP  443→9443   HTTPS TLS        │      │
│  │                                       (cert injection)            │      │
│  └────────────────────────────────────────────────────────────────────┘      │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS (Outbound Connections)                                               │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │ Destination                Port      Protocol   Auth               │    │
│  │ ─────────────────────────  ────────  ─────────  ──────────────────│    │
│  │ Kubernetes API server      443/TCP   HTTPS      ServiceAccount     │    │
│  │                                      TLS 1.2+   token (projected)  │    │
│  │                                                                     │    │
│  │ api.ngc.nvidia.com         443/TCP   HTTPS      NGC API Key        │    │
│  │ (NIM API key validation)             TLS 1.2+   (Bearer)          │    │
│  │                                                                     │    │
│  │ nvcr.io (proxy_auth)       443/TCP   HTTPS      NGC API Key        │    │
│  │ (NIM pull token)                     TLS 1.2+   (Basic auth)      │    │
│  │                                                                     │    │
│  │ authn.nvidia.com           443/TCP   HTTPS      None               │    │
│  │ (NGC token exchange)                 TLS 1.2+                      │    │
│  │                                                                     │    │
│  │ Model Registry API         Config    HTTP/HTTPS  Bearer Token      │    │
│  │ (ISVC metadata sync)                TLS optional                   │    │
│  │                            (MR_SKIP_TLS_VERIFY=false)              │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
─────────────
ClusterRole: odh-model-controller-role
  ServiceAccount: odh-model-controller
  Key Permissions:
  - "" (core): configmaps, secrets, serviceaccounts, services, endpoints, namespaces, pods, events
  - config.openshift.io: authentications (get, list, watch)
  - datasciencecluster.opendatahub.io: datascienceclusters (get, list, watch)
  - dscinitialization.opendatahub.io: dscinitializations (get, list, watch)
  - gateway.networking.k8s.io: gateways, httproutes (get, list, watch, patch, update)
  - keda.sh: triggerauthentications (full CRUD)
  - kuadrant.io: authpolicies (full CRUD), kuadrants (get, list, watch)
  - monitoring.coreos.com: podmonitors, servicemonitors (full CRUD)
  - networking.istio.io: envoyfilters (full CRUD)
  - networking.k8s.io: networkpolicies (full CRUD)
  - nim.opendatahub.io: accounts, accounts/finalizers, accounts/status
  - operator.authorino.kuadrant.io: authorinos (get, list, watch)
  - rbac.authorization.k8s.io: clusterrolebindings, rolebindings, roles (full CRUD)
  - route.openshift.io: routes, routes/custom-host (full CRUD)
  - serving.kserve.io: inferenceservices, llminferenceservices, servingruntimes, inferencegraphs, etc.
  - template.openshift.io: templates (full CRUD)

ClusterRole: model-serving-api
  ServiceAccount: model-serving-api
  - gateway.networking.k8s.io: gateways (list)
  - "" (core): namespaces (get)
  (+ impersonated SelfSubjectAccessReview via user Bearer token)

Dynamic RBAC (per InferenceService):
  - ClusterRoleBinding: {ns}-{sa}-auth-delegator → system:auth-delegator
  - Role/RoleBinding: inference-prometheus-auth (KEDA Prometheus auth)

Admission Webhooks:
───────────────────
Mutating:
  - /mutate--v1-pod: Injects Ray TLS init containers (RAY_USE_TLS)
  - /mutate-serving-kserve-io-v1beta1-inferenceservice: Name length + namespace protection
  - /mutate-serving-kserve-io-v1alpha1-inferencegraph: Cluster-internal URL validation

Validating:
  - /validate-nim-opendatahub-io-v1-account: Singleton Account per namespace
  - /validate-serving-kserve-io-v1beta1-inferenceservice: Name length validation
  - /validate-serving-kserve-io-v1alpha1-inferencegraph: Cluster-internal URL validation

Secrets:
────────
- model-serving-api-tls (kubernetes.io/tls) — OpenShift serving-cert, auto-rotate: Yes
- ray-ca-tls (kubernetes.io/tls) — Self-signed CA, 5yr validity, auto-rotate: No
- ray-tls (kubernetes.io/tls) — Self-signed per-pod, 3yr validity, auto-rotate: No
- storage-config (Opaque) — Aggregated S3/storage creds, recreated on change
- ngc-secret (kubernetes.io/dockerconfigjson) — nvcr.io Docker pull secret
- nvidia-nim-secrets (Opaque) — NGC API key, user-provisioned
- inference-prometheus-auth (kubernetes.io/service-account-token) — KEDA auth, auto-rotate: Yes (K8s)

FIPS Compliance:
────────────────
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking
- Base image: ubi9/ubi-minimal (includes OpenSSL)
- model-serving-api TLS: FIPS-compliant cipher suites only
  Ciphers: ECDHE+AES-GCM (P-256, P-384 curves)
  Min version: TLS 1.2
- NIM HTTP client: TLS 1.2+ minimum
- No non-FIPS crypto detected (InsecureSkipVerify only in test code)
