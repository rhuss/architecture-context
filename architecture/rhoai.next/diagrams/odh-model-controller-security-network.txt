┌─────────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                           │
│                                                                                  │
│  [User / kubectl]                        [RHOAI Dashboard]                       │
│         │                                       │                                │
│         │ Port: 443/TCP                         │ Port: 443/TCP                  │
│         │ Protocol: HTTPS                       │ Protocol: HTTPS                │
│         │ Encryption: TLS 1.2+                  │ Encryption: TLS 1.2+ (FIPS)    │
│         │ Auth: Bearer Token                    │ Auth: Bearer Token             │
│         ▼                                       ▼                                │
├─────────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (External Model Access)                                                │
│                                                                                  │
│  [Per-ISVC OpenShift Route]                                                      │
│  - Host: {isvc-name}.{namespace}.{cluster-domain}                               │
│  - Port: 443/TCP (external) → backend service (internal)                        │
│  - TLS Mode: Edge or Reencrypt                                                  │
│  - Auth: None or Kuadrant AuthPolicy (per annotation)                           │
│                                                                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (Trusted - Kubernetes Network)                                │
│                                                                                  │
│  ┌─────────────────────────────────────────────────────────────────────────┐    │
│  │  Namespace: opendatahub (RHOAI application namespace)                   │    │
│  │                                                                          │    │
│  │  [odh-model-controller]                                                  │    │
│  │  - Deployment: single replica, leader election                           │    │
│  │  - ServiceAccount: odh-model-controller                                  │    │
│  │  - Image: odh-model-controller-container                                 │    │
│  │  - Ports:                                                                │    │
│  │    - 8080/TCP (metrics, HTTP/HTTPS, optional TLS, authn/authz filter)   │    │
│  │    - 8081/TCP (health probes, HTTP, no auth)                            │    │
│  │    - 9443/TCP (webhooks, HTTPS, TLS cert injection)                     │    │
│  │                                                                          │    │
│  │  [model-serving-api]                                                     │    │
│  │  - Deployment: separate binary, own ServiceAccount                       │    │
│  │  - ServiceAccount: model-serving-api                                     │    │
│  │  - Image: odh-model-serving-api-container                                │    │
│  │  - Ports:                                                                │    │
│  │    - 8443/TCP (HTTPS, TLS 1.2+ FIPS ciphers, Bearer Token)             │    │
│  │    - 9090/TCP (metrics, HTTPS, TLS 1.2+, no auth)                      │    │
│  │  - TLS: OpenShift serving-cert (auto-rotate), FIPS cipher suite only    │    │
│  │    - Ciphers: ECDHE+AES-GCM (P-256, P-384 curves)                      │    │
│  │                                                                          │    │
│  └─────────────────────────────────────────────────────────────────────────┘    │
│                                                                                  │
│  ┌────────────────── Services ──────────────────┐                               │
│  │                                               │                               │
│  │  model-serving-api (ClusterIP)                │                               │
│  │  - 443/TCP → 8443 (HTTPS, TLS 1.2+, Bearer)  │                               │
│  │  - 9090/TCP → 9090 (HTTPS, TLS 1.2+)         │                               │
│  │                                               │                               │
│  │  odh-model-controller-metrics-service (CIP)   │                               │
│  │  - 8443/TCP → 8443 (HTTPS, TLS, authn/authz) │                               │
│  │                                               │                               │
│  │  webhook-service (ClusterIP)                  │                               │
│  │  - 443/TCP → 9443 (HTTPS, TLS cert inject)   │                               │
│  └───────────────────────────────────────────────┘                               │
│                                                                                  │
│  Internal Interactions:                                                          │
│         │                                                                        │
│         │ Port: 443/TCP                                                          │
│         │ Protocol: HTTPS                                                        │
│         │ Encryption: TLS 1.2+                                                   │
│         │ Auth: ServiceAccount Token                                             │
│         ▼                                                                        │
│  [Kubernetes API Server]                                                         │
│  - All controller CRUD operations                                                │
│  - SelfSubjectAccessReview (model-serving-api, user token forwarding)           │
│  - Admission webhook calls to 9443/TCP                                           │
│                                                                                  │
├─────────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Egress)                                                      │
│                                                                                  │
│  [api.ngc.nvidia.com]                                                            │
│  - Purpose: NIM API key validation, runtime discovery, model metadata            │
│  - Port: 443/TCP                                                                 │
│  - Protocol: HTTPS                                                               │
│  - Encryption: TLS 1.2+                                                          │
│  - Auth: NGC API Key (Bearer)                                                    │
│                                                                                  │
│  [nvcr.io]                                                                       │
│  - Purpose: NIM pull token acquisition for Docker auth                           │
│  - Port: 443/TCP                                                                 │
│  - Protocol: HTTPS                                                               │
│  - Encryption: TLS 1.2+                                                          │
│  - Auth: NGC API Key (Basic auth / proxy_auth)                                   │
│                                                                                  │
│  [authn.nvidia.com]                                                              │
│  - Purpose: NGC token exchange                                                   │
│  - Port: 443/TCP                                                                 │
│  - Protocol: HTTPS                                                               │
│  - Encryption: TLS 1.2+                                                          │
│  - Auth: None (token exchange flow)                                              │
│                                                                                  │
│  [Model Registry API]                                                            │
│  - Purpose: Sync InferenceService metadata (labels, IDs)                         │
│  - Port: Configurable                                                            │
│  - Protocol: HTTPS or HTTP                                                       │
│  - Encryption: TLS 1.2+ (optional, MR_SKIP_TLS_VERIFY flag)                    │
│  - Auth: Bearer Token                                                            │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  ClusterRole: odh-model-controller-role
  ServiceAccount: odh-model-controller
  Bound via: odh-model-controller-rolebinding (Cluster)
  Key permissions:
  - "" (core): configmaps, secrets, serviceaccounts, services, endpoints,
    namespaces, pods, events — create, delete, get, list, patch, update, watch
  - config.openshift.io: authentications — get, list, watch
  - datasciencecluster.opendatahub.io: datascienceclusters — get, list, watch
  - gateway.networking.k8s.io: gateways, httproutes — get, list, patch, update, watch
  - keda.sh: triggerauthentications — full CRUD
  - kuadrant.io: authpolicies — full CRUD
  - monitoring.coreos.com: podmonitors, servicemonitors — full CRUD
  - networking.istio.io: envoyfilters — full CRUD
  - nim.opendatahub.io: accounts — get, list, patch, update, watch
  - rbac.authorization.k8s.io: clusterrolebindings, rolebindings, roles — full CRUD
  - route.openshift.io: routes — full CRUD
  - serving.kserve.io: inferenceservices, llminferenceservices, servingruntimes — watch + manage
  - template.openshift.io: templates — full CRUD

  ClusterRole: model-serving-api
  ServiceAccount: model-serving-api
  Bound via: model-serving-api-binding (Cluster)
  Permissions:
  - gateway.networking.k8s.io: gateways — list
  - "" (core): namespaces — get

  Dynamic Bindings (per-ISVC):
  - {ns}-{sa}-auth-delegator → ClusterRole/system:auth-delegator
  - inference-prometheus-auth → Role/inference-prometheus-auth (user namespace)

Webhook Configuration:
  MutatingWebhookConfiguration:
  - /mutate--v1-pod: Injects Ray TLS init containers (CREATE)
  - /mutate-serving-kserve-io-v1alpha1-inferencegraph: URL validation (CREATE)
  - /mutate-serving-kserve-io-v1beta1-inferenceservice: Name validation (CREATE)

  ValidatingWebhookConfiguration:
  - /validate-nim-opendatahub-io-v1-account: Singleton enforcement (CREATE, UPDATE)
  - /validate-serving-kserve-io-v1alpha1-inferencegraph: URL validation (CREATE, UPDATE)
  - /validate-serving-kserve-io-v1beta1-inferenceservice: Name validation (CREATE)

Secrets:
  - model-serving-api-tls (kubernetes.io/tls) — API server TLS, OpenShift serving-cert, auto-rotate
  - ray-ca-tls (kubernetes.io/tls) — Ray CA cert, self-signed, 5yr validity, NO auto-rotate
  - ray-tls (kubernetes.io/tls) — Ray node certs per-pod IP, self-signed, 3yr validity, NO auto-rotate
  - storage-config (Opaque) — Aggregated S3/storage credentials, recreated on change
  - ngc-secret (kubernetes.io/dockerconfigjson) — Docker pull secret for nvcr.io, NIM AccountReconciler
  - nvidia-nim-secrets (Opaque) — NGC API key, user-provisioned
  - inference-prometheus-auth (kubernetes.io/service-account-token) — KEDA Prometheus auth, K8s auto-rotate

FIPS Compliance:
  - Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking
  - Runtime (model-serving-api): TLS 1.2+ with ECDHE+AES-GCM only (P-256, P-384 curves)
  - Runtime (NIM client): TLS 1.2+ minimum
  - Base image: UBI9/ubi-minimal (OpenSSL FIPS provider)

Authentication Summary:
  - /api/v1/gateways: Bearer Token + SelfSubjectAccessReview (user must have 'create' on llminferenceservices)
  - Per-ISVC Routes: Kuadrant AuthPolicy (Anonymous or UserDefined per annotation)
  - Gateway-level auth: Kuadrant AuthPolicy + EnvoyFilter (Authorino TLS bootstrap)
  - Webhook endpoints: Kubernetes API server cert-based TLS
  - Metrics (operator): authn/authz filter (ServiceAccount token)
