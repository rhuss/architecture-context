┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Client / Data Scientist]                                          │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (passthrough/reencrypt via OpenShift Route)         │
│         │ Auth: Varies per InferenceService (Kuadrant AuthPolicy)            │
│         ▼                                                                    │
│  [OpenShift Route] (dynamically created per InferenceService)                │
│  - TLS Mode: passthrough or reencrypt                                        │
│  - Created by: odh-model-controller Route Reconciler                         │
│                                                                              │
│  [Dashboard / CLI Client]                                                    │
│         │                                                                    │
│         │ Port: 443/TCP → target 8443/TCP                                    │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+ (FIPS: ECDHE+AES-GCM, P-256/P-384)          │
│         │ Auth: Bearer Token (user's Kubernetes token)                       │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER SERVICES (Internal)                                                 │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │  model-serving-api (Deployment)                                     │     │
│  │  - Namespace: {applications-namespace}                              │     │
│  │  - ServiceAccount: model-serving-api                                │     │
│  │  - Image: odh-model-serving-api                                     │     │
│  │                                                                     │     │
│  │  Service: model-serving-api                                         │     │
│  │    - 443/TCP → 8443 (HTTPS, TLS 1.2+ FIPS ciphers, Bearer Token)  │     │
│  │    - 8080/TCP → 8080 (HTTPS, TLS 1.2+, metrics, no auth)          │     │
│  │                                                                     │     │
│  │  TLS Certificate: model-serving-api-tls                             │     │
│  │    - Provisioned by: OpenShift service-ca (annotation-based)        │     │
│  │    - Auto-rotate: Yes (service-ca managed)                          │     │
│  │    - Cert reload: fsnotify-based (zero-downtime)                    │     │
│  │                                                                     │     │
│  │  FIPS TLS Config:                                                   │     │
│  │    - MinVersion: TLS 1.2                                            │     │
│  │    - CipherSuites: TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256,         │     │
│  │                    TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384,          │     │
│  │                    TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256,        │     │
│  │                    TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384         │     │
│  │    - Curves: P-256, P-384                                           │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│         │                                                                    │
│         │ Port: 6443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: User's Bearer Token (SelfSubjectAccessReview)                │
│         │       + ServiceAccount token (list Gateways, get Namespaces)       │
│         ▼                                                                    │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │  odh-model-controller (Deployment)                                  │     │
│  │  - Namespace: {applications-namespace}                              │     │
│  │  - ServiceAccount: odh-model-controller                             │     │
│  │  - Image: odh-model-controller                                      │     │
│  │  - Leader election: configurable                                    │     │
│  │                                                                     │     │
│  │  Service: odh-model-controller-metrics-service                      │     │
│  │    - 8080/TCP → 8080 (HTTP, no encryption, metrics, no auth)       │     │
│  │                                                                     │     │
│  │  Service: odh-model-controller-webhook-service                      │     │
│  │    - 443/TCP → 9443 (HTTPS, TLS, webhook serving cert)             │     │
│  │                                                                     │     │
│  │  Health/Ready: 8081/TCP (HTTP, no encryption, no auth)              │     │
│  │                                                                     │     │
│  │  Cache Filtering:                                                   │     │
│  │    - Secrets: only opendatahub.io/managed=true                      │     │
│  │    - Pods: only component=predictor                                 │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│         │                                                                    │
│         │ Port: 6443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount token                                         │
│         │ Operations: Watch CRDs, Create Routes, NetworkPolicies,            │
│         │            AuthPolicies, EnvoyFilters, ServiceMonitors,            │
│         │            Templates, Secrets, ClusterRoleBindings                 │
│         ▼                                                                    │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Port: 6443/TCP                                                            │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: ServiceAccount token, User Bearer token                             │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Egress)                                                  │
│                                                                              │
│  [api.ngc.nvidia.com]                                                        │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: Bearer Token (NGC API key from NIM Account Secret)                  │
│  - Purpose: NIM model catalog queries, API key validation                    │
│  - Conditional: Only when NIM_STATE=managed                                  │
│  - Note: TLS config uses MinVersion TLS 1.2 without explicit                │
│           cipher suite restriction (non-FIPS risk)                            │
│                                                                              │
│  [nvcr.io]                                                                   │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS (OCI registry)                                            │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: Bearer Token (registry token from NGC token exchange)               │
│  - Purpose: Container image manifest validation, pull secret creation        │
│  - Conditional: Only when NIM_STATE=managed                                  │
│                                                                              │
│  [Model Registry]                                                            │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (configurable: MR_SKIP_TLS_VERIFY)                  │
│  - Auth: Bearer Token                                                        │
│  - Purpose: InferenceService metadata sync                                   │
│  - Conditional: Only when MODELREGISTRY_STATE=managed                        │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
┌──────────────────────────────────────────────────────────────────────────────┐
│  ClusterRole: odh-model-controller-role                                      │
│  Bound to SA: odh-model-controller                                           │
│                                                                              │
│  Key Permissions:                                                            │
│  - serving.kserve.io: inferenceservices, llminferenceservices,               │
│    inferencegraphs, servingruntimes (full CRUD + watch)                      │
│  - nim.opendatahub.io: accounts (get, list, patch, update, watch)           │
│  - gateway.networking.k8s.io: gateways, httproutes (watch + patch)          │
│  - kuadrant.io: authpolicies (full CRUD + watch)                            │
│  - networking.istio.io: envoyfilters (full CRUD + watch)                    │
│  - route.openshift.io: routes (full CRUD + watch)                           │
│  - networking.k8s.io: networkpolicies (full CRUD + watch)                   │
│  - monitoring.coreos.com: servicemonitors, podmonitors (full CRUD)          │
│  - rbac.authorization.k8s.io: clusterrolebindings, roles, rolebindings      │
│  - template.openshift.io: templates (full CRUD + watch)                     │
│  - keda.sh: triggerauthentications (full CRUD + watch)                      │
│  - core: configmaps, secrets, serviceaccounts, services (full CRUD)         │
│  - core: endpoints, namespaces, pods (read + create + patch)                │
│  - config.openshift.io: authentications (read only)                         │
│  - datasciencecluster.opendatahub.io: datascienceclusters (read only)      │
│  - dscinitialization.opendatahub.io: dscinitializations (read only)        │
│                                                                              │
│  ClusterRole: model-serving-api                                              │
│  Bound to SA: model-serving-api                                              │
│  - gateway.networking.k8s.io: gateways (list)                               │
│  - core: namespaces (get)                                                    │
└──────────────────────────────────────────────────────────────────────────────┘

Webhook Configuration:
┌──────────────────────────────────────────────────────────────────────────────┐
│  Webhook Server Port: 9443/TCP (HTTPS, TLS cert)                             │
│                                                                              │
│  Mutating Webhooks:                                                          │
│  - Pod webhook: Injects Ray TLS volumes/init-containers                      │
│  - InferenceService webhook: Name/namespace validation (no-op defaulter)     │
│  - InferenceGraph webhook: Cross-namespace service URL check                 │
│                                                                              │
│  Validating Webhooks:                                                        │
│  - InferenceService: Name length + protected namespace check                 │
│  - NIM Account: Singleton per-namespace constraint                           │
│  - InferenceGraph: Cross-namespace service URL validation                    │
│                                                                              │
│  Auth: TLS client certificate (Kubernetes API server validates)              │
└──────────────────────────────────────────────────────────────────────────────┘

Secrets:
┌──────────────────────────────────────────────────────────────────────────────┐
│  Secret                    │ Type              │ Provisioner       │ Rotate │
│  ─────────────────────────┼───────────────────┼───────────────────┼────────│
│  model-serving-api-tls     │ kubernetes.io/tls │ OpenShift svc-ca  │ Yes    │
│  {account-name}-pull       │ dockercfg         │ NIM Account ctrl  │ No*    │
│  ray-ca-tls                │ kubernetes.io/tls │ odh-model-ctrl    │ Yes**  │
│  ray-tls                   │ kubernetes.io/tls │ Pod webhook init  │ Yes*** │
│                                                                              │
│  * Recreated when NGC API key changes                                        │
│  ** 4096-bit RSA, 5yr CA validity, checked every 2yr                         │
│  *** 3yr validity, regenerated on pod creation                               │
└──────────────────────────────────────────────────────────────────────────────┘

FIPS Compliance:
┌──────────────────────────────────────────────────────────────────────────────┐
│  Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, UBI9 base image       │
│  Runtime: stdlib crypto/tls via OpenSSL (strictfipsruntime)                  │
│  model-serving-api: Explicit FIPS cipher suites (ECDHE+AES-GCM only)        │
│  Risk: NIM outbound TLS (api.ngc.nvidia.com) uses MinVersion TLS 1.2        │
│         without explicit FIPS cipher suite restriction                        │
│  HTTP/2: Explicitly disabled to mitigate CVEs                                │
└──────────────────────────────────────────────────────────────────────────────┘
