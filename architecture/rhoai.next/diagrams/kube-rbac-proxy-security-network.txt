┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CLIENTS (Cluster Services)                                    │
│                                                                          │
│  [In-Cluster Client / Component Pod]                                     │
│         │                                                                │
│         │ Port: 8443/TCP (configurable via --secure-listen-address)      │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+ (configurable min version & ciphers)      │
│         │ Auth: Bearer Token (TokenReview) / Client Cert (x509) /       │
│         │       OIDC JWT (--oidc-issuer)                                 │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SIDECAR PROXY (kube-rbac-proxy)                                        │
│                                                                          │
│  [kube-rbac-proxy Container]                                             │
│  - Namespace: {component-namespace}                                      │
│  - ServiceAccount: kube-rbac-proxy                                       │
│  - Image: registry.redhat.io/.../kube-rbac-proxy:v0.21.1                │
│  - Deployment: Sidecar (injected by rhods-operator)                      │
│  - FIPS: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1                   │
│                                                                          │
│  Secure Listener:                                                        │
│  - Port: 8443/TCP (configurable)                                         │
│  - TLS Mode: SIMPLE (--tls-cert-file) or MUTUAL (--client-ca-file)       │
│  - TLS Min Version: 1.2 (configurable)                                   │
│  - Certificate: Hot-reloaded via CertReloader (default 1min interval)    │
│                                                                          │
│  Health Endpoint:                                                        │
│  - Port: configurable (--proxy-endpoints-port)                           │
│  - Path: /healthz                                                        │
│  - Auth: None (unauthenticated)                                          │
│                                                                          │
│  Deprecated Insecure Listener:                                           │
│  - Port: configurable (--insecure-listen-address)                        │
│  - Protocol: HTTP (no TLS)                                               │
│  - Status: DEPRECATED                                                    │
│         │                                                                │
│         │                                                                │
│  ┌──────┼──── Authentication ─────────────────────────────────────┐      │
│  │      │                                                         │      │
│  │  [Delegating Authenticator]                                    │      │
│  │  - TokenReview API call to K8s API server                      │      │
│  │  - 2-minute cache TTL                                          │      │
│  │  - Retry backoff                                               │      │
│  │                                                                │      │
│  │  [OIDC Authenticator] (when --oidc-issuer set)                 │      │
│  │  - JWT validation via cached JWKS                              │      │
│  │  - Configurable username/groups claims                         │      │
│  │                                                                │      │
│  │  [x509 Authenticator] (when --client-ca-file set)              │      │
│  │  - Client certificate validation                               │      │
│  │  - Dynamic CA reload via DynamicFileCAContent                  │      │
│  └────────────────────────────────────────────────────────────────┘      │
│                                                                          │
│  ┌──────── Authorization (Union, priority order) ─────────────────┐      │
│  │                                                                │      │
│  │  1. [Hardcoded Authorizer]                                     │      │
│  │     - Allow: system:serviceaccount:openshift-monitoring:        │      │
│  │             prometheus-k8s                                      │      │
│  │     - Resource: GET /metrics                                    │      │
│  │     - Purpose: Unconditional Prometheus scraping                │      │
│  │                                                                │      │
│  │  2. [Static Authorizer] (--config-file)                         │      │
│  │     - Resource attributes from config file                      │      │
│  │                                                                │      │
│  │  3. [SubjectAccessReview Authorizer]                            │      │
│  │     - Delegates to K8s API server                               │      │
│  └────────────────────────────────────────────────────────────────┘      │
│         │                                                                │
│         │ Port: configurable (--upstream)                                 │
│         │ Protocol: HTTP / HTTPS / h2c (--upstream-force-h2c)            │
│         │ Encryption: None (localhost) or TLS (--upstream-ca-file)        │
│         │ Auth: None or mTLS (--upstream-client-cert-file)               │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  UPSTREAM APPLICATION (Pod-Local)                                        │
│                                                                          │
│  [Application Container]                                                 │
│  - Port: configurable (localhost only)                                   │
│  - Protocol: HTTP (typical) / HTTPS / h2c                                │
│  - Purpose: Protected application endpoint                               │
│  - Auth: None (trusted pod-local traffic via loopback)                   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘


EGRESS CONNECTIONS:

┌─────────────────────────────────────────────────────────────────────────┐
│  KUBERNETES API SERVER                                                   │
│                                                                          │
│  [kube-apiserver]                                                        │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: ServiceAccount Token (in-cluster config)                        │
│  - APIs Used:                                                            │
│    - POST authentication.k8s.io/v1/tokenreviews (authn)                  │
│    - POST authorization.k8s.io/v1/subjectaccessreviews (authz)           │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  OIDC IDENTITY PROVIDER (Optional)                                       │
│                                                                          │
│  [OIDC Issuer - e.g., Keycloak, Dex]                                    │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+ (--oidc-ca-file for custom CA)                   │
│  - Auth: None                                                            │
│  - Purpose: OIDC discovery document + JWT signing key (JWKS) fetching    │
│  - Activated: Only when --oidc-issuer is configured                      │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  PROMETHEUS METRICS SCRAPING (Ingress)                                   │
│                                                                          │
│  [Prometheus - openshift-monitoring namespace]                           │
│  - ServiceAccount: system:serviceaccount:openshift-monitoring:           │
│                    prometheus-k8s                                         │
│  - Port: 8443/TCP → kube-rbac-proxy                                     │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Bearer Token (SA token)                                         │
│  - Path: GET /metrics                                                    │
│  - Authorization: Hardcoded allow (no SAR check required)                │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘


RBAC Summary:
  ClusterRole: kube-rbac-proxy
    - API Group: authentication.k8s.io
      Resources: tokenreviews
      Verbs: create
    - API Group: authorization.k8s.io
      Resources: subjectaccessreviews
      Verbs: create

  ClusterRoleBinding: kube-rbac-proxy
    - Scope: cluster-wide
    - Role: ClusterRole: kube-rbac-proxy
    - ServiceAccount: kube-rbac-proxy (in deploying namespace)

Secrets / Certificates:
  1. TLS Server Cert (--tls-cert-file, --tls-private-key-file)
     - Type: kubernetes.io/tls
     - Provisioned by: cert-manager / platform operator / self-signed fallback
     - Auto-rotate: Yes (CertReloader, configurable interval, default 1min)

  2. Client CA (--client-ca-file)
     - Type: CA bundle
     - Provisioned by: Platform operator
     - Auto-rotate: Yes (DynamicFileCAContent)

  3. Upstream Client Cert (--upstream-client-cert-file, --upstream-client-key-file)
     - Type: x509 keypair
     - Provisioned by: Platform operator
     - Auto-rotate: No

  4. OIDC CA (--oidc-ca-file)
     - Type: CA bundle
     - Provisioned by: Platform operator
     - Auto-rotate: Yes (DynamicFileCAContent)

FIPS Compliance:
  - Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1
  - TLS: stdlib crypto/tls via OpenSSL (strictfipsruntime)
  - Min TLS Version: 1.2 (default)
  - Cipher Suites: FIPS-compatible defaults via k8s component-base
  - No non-FIPS crypto detected (no MD5, RC4, DES)

Security Controls:
  - SanitizingFilter: Masks bearer tokens in klog output
  - Path allow-list: --ignore-paths bypasses auth for configured paths
  - Health endpoint: Separate port (--proxy-endpoints-port), unauthenticated
  - Self-signed cert fallback: Deprecated, triggers warning
