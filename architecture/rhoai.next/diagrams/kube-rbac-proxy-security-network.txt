┌─────────────────────────────────────────────────────────────────────────────┐
│  IN-CLUSTER CLIENTS (Trusted - within cluster network)                      │
│                                                                             │
│  [In-Cluster Service / User]                                                │
│         │                                                                   │
│         │ Port: 8443/TCP                                                    │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+ (configurable min version + cipher suites)   │
│         │ Auth: Bearer Token (TokenReview) / Client TLS Cert / OIDC JWT     │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  SIDECAR PROXY (kube-rbac-proxy container - same pod)                       │
│                                                                             │
│  [kube-rbac-proxy v0.21.1]                                                  │
│  - Listen: 0.0.0.0:8443/TCP (--secure-listen-address)                      │
│  - TLS: Server cert (--tls-cert-file, --tls-private-key-file)              │
│  - TLS Mode: SIMPLE or MUTUAL (with --client-ca-file)                      │
│  - TLS Min Version: 1.2 (configurable via k8s component-base)              │
│  - Certificate Hot-Reload: Yes (CertReloader, 1 min interval)             │
│  - FIPS: strictfipsruntime (CGO_ENABLED=1, OpenSSL via UBI 9)              │
│  - Token Sanitization: Bearer tokens masked in log output                  │
│  - ServiceAccount: kube-rbac-proxy (in deploying namespace)                │
│                                                                             │
│  Authentication Chain (mutually exclusive):                                 │
│    1. Delegating (TokenReview) - default                                    │
│    2. OIDC JWT (when --oidc-issuer set)                                    │
│    3. x509 Client Certificate (when --client-ca-file set)                  │
│                                                                             │
│  Authorization Chain (union, priority order):                               │
│    1. Hardcoded: ALLOW prometheus-k8s SA on GET /metrics                   │
│    2. Static: config-file based resource attribute matching                │
│    3. SAR: SubjectAccessReview against Kubernetes API                      │
│         │                                                                   │
│         │ EGRESS TO KUBERNETES API                                          │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: ServiceAccount Token (in-cluster config)                    │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES API SERVER                                                      │
│                                                                             │
│  [kube-apiserver]                                                           │
│  - TokenReview API: authentication.k8s.io/v1                              │
│    - Cache TTL: 2 minutes                                                  │
│    - Retry: backoff on failure                                             │
│  - SubjectAccessReview API: authorization.k8s.io/v1                        │
│    - Evaluates: Kubernetes RBAC policies                                   │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  POD-LOCAL (localhost - trusted, same network namespace)                    │
│                                                                             │
│  [kube-rbac-proxy] ──────────────────► [Upstream Application Container]    │
│         │                                                                   │
│         │ Port: configurable (localhost)                                    │
│         │ Protocol: HTTP / HTTPS / h2c (--upstream-force-h2c)              │
│         │ Encryption: None (HTTP) or TLS (--upstream-ca-file)              │
│         │ Auth: None or mTLS (--upstream-client-cert-file)                 │
│         │ Trust: Localhost (same pod network namespace)                     │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Optional)                                               │
│                                                                             │
│  [OIDC Identity Provider]                                                   │
│  - Purpose: OIDC discovery + JWT signing key fetching                      │
│  - Port: 443/TCP                                                           │
│  - Protocol: HTTPS                                                         │
│  - Encryption: TLS 1.2+ (--oidc-ca-file for custom CA)                    │
│  - Auth: None (public endpoints)                                           │
│  - Enabled: Only when --oidc-issuer is configured                          │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  MONITORING                                                                 │
│                                                                             │
│  [Prometheus - openshift-monitoring/prometheus-k8s]                         │
│  - Port: 8443/TCP (same as secure-listen-address)                          │
│  - Protocol: HTTPS                                                         │
│  - Encryption: TLS 1.2+                                                    │
│  - Auth: Bearer Token (prometheus-k8s ServiceAccount)                      │
│  - Authorization: Hardcoded ALLOW (no SAR needed)                          │
│  - Endpoint: GET /metrics                                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  ClusterRole: kube-rbac-proxy
    - API Group: authentication.k8s.io
      Resources: tokenreviews
      Verbs: create
    - API Group: authorization.k8s.io
      Resources: subjectaccessreviews
      Verbs: create

  ClusterRoleBinding: kube-rbac-proxy
    Role: ClusterRole: kube-rbac-proxy
    ServiceAccount: kube-rbac-proxy (in deploying namespace)
    Scope: cluster-wide

Secrets:
  - TLS cert/key (--tls-cert-file, --tls-private-key-file)
    Type: kubernetes.io/tls
    Provisioned By: cert-manager / platform operator / self-signed fallback
    Auto-Rotate: Yes (CertReloader, configurable interval, default 1 min)

  - Client CA (--client-ca-file)
    Type: CA bundle
    Provisioned By: Platform operator
    Auto-Rotate: Yes (DynamicFileCAContent)

  - Upstream client cert (--upstream-client-cert-file, --upstream-client-key-file)
    Type: x509 keypair
    Provisioned By: Platform operator
    Auto-Rotate: No

  - OIDC CA (--oidc-ca-file)
    Type: CA bundle
    Provisioned By: Platform operator
    Auto-Rotate: Yes (DynamicFileCAContent)

FIPS Compliance:
  - Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1
  - Runtime: OpenSSL via UBI 9 minimal base image
  - TLS: Configurable min version (default 1.2), FIPS-safe cipher suites via k8s component-base
  - No non-FIPS crypto risks detected

Health Check:
  - Endpoint: /healthz
  - Port: configurable (--proxy-endpoints-port, separate from main listener)
  - Auth: None (unauthenticated)
  - Purpose: Liveness/readiness probes
