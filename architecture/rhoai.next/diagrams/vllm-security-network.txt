┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client]                                                       │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Bearer Token / OAuth (enforced by KServe)                │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (KServe Gateway / DMZ)                                         │
│                                                                          │
│  [KServe Gateway (Istio Ingress)]                                        │
│  - Port: 443/TCP (external) → 8000/TCP or 8033/TCP (internal)          │
│  - TLS Termination: TLS 1.2+                                            │
│  - AuthN: Bearer Token / OAuth validation                                │
│         │                                                                │
│         │ Port: 8000/TCP (HTTP) or 8033/TCP (gRPC)                      │
│         │ Protocol: HTTP or gRPC                                         │
│         │ Encryption: mTLS (STRICT - Istio)                              │
│         │ Auth: Service Identity (Istio mTLS)                            │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (Trusted - mTLS STRICT)                                   │
│                                                                          │
│  [Istio Sidecar]                                                         │
│  - Namespace: {user-namespace}                                           │
│  - Mode: mTLS STRICT                                                     │
│  - Purpose: TLS termination, traffic management                          │
│         │                                                                │
│         │ Port: 8000/TCP (HTTP) or 8033/TCP (gRPC)                      │
│         │ Protocol: HTTP or gRPC                                         │
│         │ Encryption: None (localhost / loopback)                         │
│         │ Auth: None (pod-internal)                                       │
│         ▼                                                                │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  vLLM Inference Pod (User: 2000, non-root)                       │   │
│  │                                                                   │   │
│  │  [vllm_tgis_adapter]                                              │   │
│  │  - Port: 8033/TCP (gRPC)                                          │   │
│  │  - Protocol: gRPC (TGIS GenerationService)                        │   │
│  │  - Image: odh-vllm-cuda-rhel9                                     │   │
│  │  - Entrypoint: python -m vllm_tgis_adapter                       │   │
│  │         │                                                          │   │
│  │         │ Port: 8000/TCP                                           │   │
│  │         │ Protocol: HTTP (internal bridge)                         │   │
│  │         │ Encryption: None (localhost)                              │   │
│  │         │ Auth: None                                                │   │
│  │         ▼                                                          │   │
│  │  [vLLM HTTP Server]                                                │   │
│  │  - Port: 8000/TCP (HTTP)                                           │   │
│  │  - Protocol: HTTP (OpenAI-compatible API)                          │   │
│  │  - Endpoints:                                                      │   │
│  │    - POST /v1/completions                                          │   │
│  │    - POST /v1/chat/completions                                     │   │
│  │    - GET  /v1/models                                               │   │
│  │    - GET  /health                                                  │   │
│  │  - Base: registry.redhat.io/rhaiis/vllm-cuda-rhel9:3.2.2         │   │
│  │         │                                                          │   │
│  │         │ CUDA API                                                  │   │
│  │         │ Protocol: CUDA (device-local)                             │   │
│  │         │ Auth: N/A                                                 │   │
│  │         ▼                                                          │   │
│  │  [NVIDIA GPU]                                                      │   │
│  │  - Resource: nvidia.com/gpu                                        │   │
│  │  - Purpose: LLM tensor computation                                 │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS (External Services)                                              │
│                                                                          │
│  [S3 Storage / PVC]                                                      │
│  - Port: 443/TCP (S3) or N/A (PVC)                                     │
│  - Protocol: HTTPS (S3) or filesystem (PVC)                             │
│  - Encryption: TLS 1.2+ (S3) or N/A (PVC)                              │
│  - Auth: AWS IAM / service account (S3) or None (PVC)                   │
│  - Purpose: LLM model weight storage and download at startup             │
│                                                                          │
│  [Hugging Face Hub - huggingface.co]                                     │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Bearer Token (optional)                                         │
│  - Purpose: Download model weights and tokenizer configurations          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  No RBAC resources defined by this component.
  This is a container image, not an operator.
  RBAC is managed externally by KServe.

Authentication & Authorization:
  - All HTTP/gRPC endpoints: Auth enforced by Istio mTLS + KServe Gateway
  - Enforcement Point: Istio sidecar / KServe gateway (not the vLLM process)
  - Policy: Platform-level (not application-enforced)
  - Container runs as non-root USER 2000 with no privilege escalation

Service Mesh Configuration:
  - Istio sidecar injection: enabled (required)
  - mTLS Mode: STRICT (namespace-scoped)
  - Traffic management: Handled by KServe/Istio

Secrets:
  - No secrets defined by this repository
  - Model access credentials are injected by KServe at runtime
  - S3 credentials (if used): Provided via KServe storage-config secret

FIPS Compliance:
  - No crypto code in this repository
  - FIPS compliance inherited from RHAIIS base image (vllm-cuda-rhel9:3.2.2)
  - TLS handling delegated to Istio sidecar
  - Must be assessed at RHAIIS base image level

Build Security:
  - Build system: Konflux (Tekton pipelines)
  - Hermetic build: false (no package installation in Dockerfile)
  - Vulnerability scanning: Clair scan + ecosystem-cert-preflight-checks
  - Multi-arch: amd64, arm64
