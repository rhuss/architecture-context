┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client]                                                       │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Bearer Token / OAuth (application-level)                 │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (DMZ) — Managed by KServe                                      │
│                                                                          │
│  [KServe Gateway / Istio Ingress]                                        │
│  - Port: 443/TCP (external) → 8000/TCP or 8033/TCP (internal)          │
│  - TLS Termination: TLS 1.2+                                            │
│  - AuthN: KServe auth (Bearer Token / OAuth validation)                 │
│         │                                                                │
│         │ Port: 8000/TCP (HTTP) or 8033/TCP (gRPC)                      │
│         │ Protocol: HTTP or gRPC                                         │
│         │ Encryption: mTLS (STRICT — Istio Service Mesh)                │
│         │ Auth: Service Identity (Istio mTLS)                            │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (Trusted — mTLS STRICT)                                   │
│                                                                          │
│  [Istio Sidecar Proxy]                                                   │
│  - Namespace: {user-namespace}                                           │
│  - Injected: Automatic sidecar injection                                 │
│  - mTLS: STRICT mode                                                     │
│         │                                                                │
│         │ Port: 8000/TCP (HTTP) or 8033/TCP (gRPC)                      │
│         │ Protocol: HTTP or gRPC                                         │
│         │ Encryption: None (localhost — same pod)                         │
│         │ Auth: None (pod-internal traffic)                               │
│         ▼                                                                │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  [vLLM CUDA Container — odh-vllm-cuda-rhel9]                   │    │
│  │  - Image: registry.redhat.io/rhaiis/vllm-cuda-rhel9:3.2.2     │    │
│  │  - User: 2000 (non-root)                                       │    │
│  │  - No privilege escalation                                      │    │
│  │                                                                  │    │
│  │  ┌──────────────────────────────────────────────────────────┐   │    │
│  │  │  vllm_tgis_adapter (Entrypoint)                          │   │    │
│  │  │  - Port: 8033/TCP (gRPC)                                 │   │    │
│  │  │  - Protocol: gRPC (TGIS GenerationService)               │   │    │
│  │  │  - Bridges TGIS gRPC → vLLM HTTP                         │   │    │
│  │  └────────────────────────┬─────────────────────────────────┘   │    │
│  │                           │ HTTP localhost:8000                  │    │
│  │  ┌────────────────────────▼─────────────────────────────────┐   │    │
│  │  │  vLLM HTTP Server                                        │   │    │
│  │  │  - Port: 8000/TCP (HTTP)                                 │   │    │
│  │  │  - Endpoints: /v1/completions, /v1/chat/completions,     │   │    │
│  │  │    /v1/models, /health                                   │   │    │
│  │  │  - OpenAI-compatible API                                 │   │    │
│  │  └────────────────────────┬─────────────────────────────────┘   │    │
│  │                           │ CUDA                                │    │
│  │  ┌────────────────────────▼─────────────────────────────────┐   │    │
│  │  │  NVIDIA GPU (CUDA)                                       │   │    │
│  │  │  - Resource: nvidia.com/gpu                              │   │    │
│  │  │  - Model weights loaded into VRAM                        │   │    │
│  │  └──────────────────────────────────────────────────────────┘   │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│         │                                                                │
│         │ Port: 443/TCP or N/A (PVC)                                    │
│         │ Protocol: HTTPS or filesystem                                  │
│         │ Encryption: TLS 1.2+ or N/A                                   │
│         │ Auth: AWS IAM / service account / Bearer Token                 │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [Model Storage — S3 / PVC]                                              │
│  - Purpose: LLM model weights and tokenizer configs                     │
│  - Port: 443/TCP (S3) or N/A (PVC)                                     │
│  - Auth: AWS IAM credentials / service account                           │
│  - Encryption: TLS 1.2+ (S3)                                            │
│                                                                          │
│  [Hugging Face Hub — huggingface.co]                                     │
│  - Purpose: Download model weights and tokenizer configs                │
│  - Port: 443/TCP                                                         │
│  - Auth: Bearer Token (optional)                                         │
│  - Encryption: TLS 1.2+                                                  │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC resources defined by this component
- RBAC is fully managed by KServe at the platform level
- Container runs as non-root USER 2000 with no privilege escalation

Service Mesh Configuration:
- Istio sidecar: Automatic injection
- PeerAuthentication: STRICT (mTLS enforced)
- Authentication & Authorization enforced externally by KServe/Istio
- vLLM process has no application-level auth

Secrets:
- No secrets defined by this repository
- Model access credentials injected by KServe at runtime
- S3/storage credentials managed by KServe storage-config

FIPS Compliance:
- Inherited from RHAIIS base image (registry.redhat.io/rhaiis/vllm-cuda-rhel9:3.2.2)
- No compiled code or crypto libraries added by this repository
- OpenSSL provided via UBI base in RHAIIS image
- TLS configuration delegated to KServe/Istio sidecar

Container Security:
- User: 2000 (non-root)
- No privilege escalation
- Base image: registry.redhat.io/rhaiis/vllm-cuda-rhel9:3.2.2
- Vulnerability scanning: Clair scan + ecosystem-cert-preflight-checks (Konflux pipeline)
- Build: Non-hermetic (hermetic: false) — acceptable as no packages installed
