┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [Browser User]                      [Service Account Client]               │
│         │                                    │                              │
│         │ Port: 4180/TCP (auth mode)         │ Port: 8443/TCP (rbac mode)   │
│         │ Protocol: HTTP                     │ Protocol: HTTPS              │
│         │ Encryption: None                   │ Encryption: TLS 1.2+         │
│         │ (expects TLS at ingress)           │ Auth: Bearer token /         │
│         │ Auth: None (initiates              │   client cert / OIDC JWT     │
│         │   OAuth2 flow)                     │                              │
│         ▼                                    ▼                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  POD NETWORK (Sidecar Proxy)                                                │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  Container Image: quay.io/rhoai/odh-kube-auth-proxy               │    │
│  │  FIPS: CGO_ENABLED=1, GOEXPERIMENT=strictfipsruntime              │    │
│  │                                                                    │    │
│  │  ┌──────────────────────────┐  ┌──────────────────────────────┐   │    │
│  │  │  kube-auth-proxy         │  │  kube-rbac-proxy             │   │    │
│  │  │  (auth mode)             │  │  (rbac mode, default)        │   │    │
│  │  │                          │  │                              │   │    │
│  │  │  Listen: 4180/TCP HTTP   │  │  Listen: 8443/TCP HTTPS     │   │    │
│  │  │  (opt HTTPS configurable)│  │  TLS 1.2+ (auto/configured) │   │    │
│  │  │                          │  │                              │   │    │
│  │  │  AuthN Chain:            │  │  AuthN:                      │   │    │
│  │  │  1. K8s SA TokenReview   │  │  - Delegated TokenReview     │   │    │
│  │  │  2. JWT Bearer / OShift  │  │  - OIDC JWT verification     │   │    │
│  │  │  3. Basic Auth (htpasswd)│  │  - X.509 client certificate  │   │    │
│  │  │  4. Session cookie       │  │                              │   │    │
│  │  │                          │  │  AuthZ:                      │   │    │
│  │  │  Session: AES-CFB        │  │  - SubjectAccessReview       │   │    │
│  │  │  encrypted cookie        │  │  - Static rules (config)     │   │    │
│  │  │  or Redis backend        │  │  - Hardcoded metrics perms   │   │    │
│  │  └──────────┬───────────────┘  └──────────────┬───────────────┘   │    │
│  └─────────────┼──────────────────────────────────┼──────────────────┘    │
│                │                                  │                       │
│                ▼                                  ▼                       │
│  ┌─────────────────────────────────────────────────────────────────┐      │
│  │  [Upstream Application]                                         │      │
│  │  - Receives proxied requests                                    │      │
│  │  - Auth headers: GAP-Auth / x-remote-user / x-remote-groups    │      │
│  │  - Port: configurable                                           │      │
│  │  - Protocol: HTTP/HTTPS (configurable)                          │      │
│  │  - No additional auth required (proxy handles it)               │      │
│  └─────────────────────────────────────────────────────────────────┘      │
│                                                                           │
├─────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER SERVICES (Trusted - In-Cluster)                                    │
│                                                                             │
│  [Kubernetes API Server]                                                    │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS 1.2+ (in-cluster CA)                                     │
│  - Auth: Service account token (mounted)                                    │
│  - APIs Used:                                                               │
│    - POST /apis/authentication.k8s.io/v1/tokenreviews (authn)              │
│    - POST /apis/authorization.k8s.io/v1/subjectaccessreviews (authz)       │
│    - DELETE /apis/oauth.openshift.io/v1/oauthaccesstokens/{name} (logout)  │
│                                                                             │
│  [OpenShift OAuth Server]                                                   │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS 1.2+ (service CA)                                        │
│  - Auth: OAuth2 client credentials (client_id + client_secret)              │
│  - APIs Used:                                                               │
│    - GET /.well-known/oauth-authorization-server (discovery)                │
│    - POST /oauth/token (token exchange)                                     │
│    - GET /oauth/authorize (authorization code flow)                         │
│                                                                             │
│  [OpenShift User API]                                                       │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS 1.2+ (service CA)                                        │
│  - Auth: Bearer access token                                                │
│  - APIs Used:                                                               │
│    - GET /apis/user.openshift.io/v1/users/~ (user info)                    │
│                                                                             │
│  [Redis] (Optional)                                                         │
│  - Port: 6379/TCP                                                           │
│  - Protocol: TCP                                                            │
│  - Encryption: TLS (optional)                                               │
│  - Auth: Password (optional)                                                │
│  - Purpose: Session storage backend                                         │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                          │
│                                                                             │
│  [OIDC Provider (Keycloak, DEX, etc.)]                                      │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: OAuth2 client credentials                                          │
│  - APIs Used:                                                               │
│    - GET /.well-known/openid-configuration (OIDC discovery)                │
│    - POST /token (token exchange)                                           │
│    - GET /userinfo (user info)                                              │
│    - GET /end_session (RP-initiated logout, browser redirect)              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC roles defined in this repository
- Required permissions (provisioned by rhods-operator):
  - API Group: authentication.k8s.io
    - Resources: tokenreviews
    - Verbs: create
  - API Group: authorization.k8s.io
    - Resources: subjectaccessreviews
    - Verbs: create
  - API Group: oauth.openshift.io (OpenShift provider only)
    - Resources: oauthaccesstokens
    - Verbs: delete (for sign-out)

Secrets:
- cookie-secret (Opaque) - AES-CFB session cookie encryption, operator/manual, no auto-rotate
- client-secret (Opaque) - OAuth2 client secret for IdP, operator/manual, no auto-rotate
- tls-cert + tls-key (kubernetes.io/tls) - HTTPS listener cert (kube-rbac-proxy), cert-manager/operator
- jwt_signing_key.pem (File) - Empty placeholder (GCP compat), build process

FIPS Compliance:
- CGO_ENABLED=1 with GOEXPERIMENT=strictfipsruntime
- Build tags: -tags strictfipsruntime
- TLS minimum: TLS 1.2 enforced across both proxy modes
- Architectures: amd64, arm64, ppc64le, s390x

Ext_Authz Compatibility:
- /oauth2/auth endpoint designed for Envoy ext_authz / nginx subrequest
- Returns 302 (not 403) on CSRF failure for proxy-behind-proxy UX
- Compatible with RHOAI 3.x Gateway API + Envoy ingress stack
