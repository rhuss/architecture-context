┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client]                                                       │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.3                                            │
│         │ Auth: Bearer Token (platform-managed)                          │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Platform-Managed)                                             │
│                                                                          │
│  [RHOAI Gateway / KServe Ingress]                                       │
│  - Port: 443/TCP (external) → 8000/TCP or 8033/TCP (internal)          │
│  - TLS Termination: TLS 1.3                                             │
│  - AuthN: Platform-managed (kube-rbac-proxy or Gateway API auth)        │
│  - AuthZ: Delegated to platform                                         │
│         │                                          │                     │
│         │ Port: 8000/TCP                           │ Port: 8033/TCP      │
│         │ Protocol: HTTP                           │ Protocol: gRPC      │
│         │ Encryption: None (pod-internal)          │ Encryption: None    │
│         │ Auth: None (platform-managed)            │ Auth: None          │
│         ▼                                          ▼                     │
├─────────────────────────────────────────────────────────────────────────┤
│  WORKLOAD (Container - Non-Root UID 2000)                               │
│                                                                          │
│  [vllm-spyre Container: odh-vllm-spyre-rhel9]                          │
│  - Base Image: registry.redhat.io/rhaiis/vllm-spyre-rhel9:3.2.2        │
│  - Entrypoint: vllm_tgis_adapter                                        │
│  - Runtime User: UID 2000 (non-root)                                    │
│  - Architecture: x86_64 only (IBM Spyre requirement)                    │
│                                                                          │
│  ┌──────────────────────────┐  ┌───────────────────────────┐            │
│  │ OpenAI HTTP Server       │  │ TGIS gRPC Server          │            │
│  │ Port: 8000/TCP           │  │ Port: 8033/TCP            │            │
│  │ Endpoints:               │  │ Service:                  │            │
│  │  POST /v1/completions    │  │  GenerationService        │            │
│  │  POST /v1/chat/completions│ │                           │            │
│  │  GET  /v1/models         │  │                           │            │
│  │  GET  /health            │  │                           │            │
│  │  GET  /metrics           │  │                           │            │
│  └───────────┬──────────────┘  └─────────────┬─────────────┘            │
│              │                                │                          │
│              └───────────┬────────────────────┘                          │
│                          ▼                                               │
│  ┌──────────────────────────────────────────┐                           │
│  │ vLLM Inference Engine                     │                           │
│  │ - Model loading from S3/PVC/HF Hub       │                           │
│  │ - Speculative decoding (logprobs enabled) │                           │
│  └───────────────────┬──────────────────────┘                           │
│                      │                                                   │
│                      │ PCIe / Device Driver                              │
│                      │ Encryption: N/A (hardware bus)                    │
│                      │ Auth: N/A                                         │
│                      ▼                                                   │
│  ┌──────────────────────────────────────────┐                           │
│  │ IBM Spyre Accelerator Card                │                           │
│  │ - Hardware accelerated LLM inference      │                           │
│  └──────────────────────────────────────────┘                           │
│         │                               │                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS (External Services)                                             │
│         │                               │                                │
│         │ Port: 443/TCP                 │ Port: 443/TCP                  │
│         │ Protocol: HTTPS               │ Protocol: HTTPS                │
│         │ Encryption: TLS 1.2+          │ Encryption: TLS 1.2+           │
│         │ Auth: AWS IAM / SA (S3)       │ Auth: Bearer Token (optional)  │
│         ▼                               ▼                                │
│                                                                          │
│  [Model Storage - S3/PVC]          [Hugging Face Hub]                    │
│  - Purpose: Model weight storage    - Purpose: Model/tokenizer download  │
│  - Auth: AWS IAM credentials        - Auth: Bearer Token (optional)      │
│    or service account (S3)          - URL: huggingface.co                │
│    or none (PVC volume mount)                                            │
│                                                                          │
│  [Prometheus]                                                            │
│  - Port: 8000/TCP (inbound scrape)                                      │
│  - Protocol: HTTP                                                        │
│  - Path: /metrics                                                        │
│  - Encryption: None (cluster-internal)                                   │
│  - Auth: None                                                            │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC resources defined in this repository
- RBAC is managed by KServe and the RHOAI platform operator
- Container runs as non-root UID 2000

Authentication & Authorization:
- /v1/* (HTTP API): Platform-managed (kube-rbac-proxy or Gateway API auth)
  - Enforcement Point: KServe / RHOAI Gateway
  - Policy: Delegated to platform
- TGIS gRPC: Platform-managed
  - Enforcement Point: KServe
  - Policy: Delegated to platform

Container Security:
- Runtime user: UID 2000 (non-root)
- Base image: registry.redhat.io/rhaiis/vllm-spyre-rhel9 (Red Hat certified)
- Package sources: Inherited from RHAIIS base image (Red Hat repos)
- No secrets managed by this component

Secrets:
- No secrets defined in this repository
- Model storage credentials managed by KServe and the platform
- TLS certificates managed by KServe and the platform

Network Notes:
- HTTP/gRPC ports (8000, 8033) are NOT encrypted at the container level
- TLS termination occurs at the platform Gateway/Ingress layer
- Pod-internal communication is plaintext by design (sidecar proxy handles mTLS if enabled)
- Ingress is fully managed by KServe and RHOAI platform Gateway/HTTPRoute infrastructure
