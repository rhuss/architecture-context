┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [User / API Client]                                                        │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: Bearer Token / Client Certificate                           │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE                                                   │
│                                                                             │
│  [Kubernetes API Server]                                                    │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: ServiceAccount Bearer Token                                        │
│         │                                                                   │
│         │ Port: 9443/TCP                                                    │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS (self-managed certs)                              │
│         │ Auth: API Server TLS client certificate                           │
│         │ Policy: ValidatingWebhookConfiguration failurePolicy=Fail         │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (opendatahub)                                           │
│                                                                             │
│  [trainer-controller-manager]                                               │
│  - ServiceAccount: kubeflow-trainer-controller-manager                      │
│  - Image: rhoai/odh-trainer-rhel9                                           │
│  - Ports:                                                                   │
│    - 9443/TCP: Webhook Server (HTTPS, TLS, API Server client cert)          │
│    - 8443/TCP: Metrics (HTTPS, TLS, no auth)                               │
│    - 8081/TCP: Health Probes (HTTP, no auth)                                │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: ServiceAccount Bearer Token                                 │
│         │ Purpose: CRD watches, resource CRUD, status updates               │
│         ▼                                                                   │
│  [Kubernetes API Server] (bidirectional)                                     │
│         │                                                                   │
│         │ Port: 28080/TCP (configurable)                                    │
│         │ Protocol: HTTP                                                    │
│         │ Encryption: plaintext                                             │
│         │ Auth: None (NetworkPolicy enforced)                               │
│         │ Purpose: RHAI progression metrics polling                         │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  TRAINING NAMESPACE ({user-namespace})                                      │
│                                                                             │
│  [Training Pod (rank-0)]                                                    │
│  - Namespace: {user-namespace}                                              │
│  - Labels: jobset.sigs.k8s.io/jobset-name: {trainjob-name}                │
│  - Ports:                                                                   │
│    - 28080/TCP: Metrics endpoint (HTTP, plaintext)                          │
│    - 29500/TCP: PyTorch master (TCP, plaintext)                             │
│    - 2222/TCP: SSH (MPI mode, ECDSA P521 key auth)                         │
│         │                                                                   │
│         │ Dynamic ports                                                     │
│         │ Protocol: TCP (NCCL/MPI)                                          │
│         │ Encryption: plaintext                                             │
│         │ Auth: None (NetworkPolicy: same-job pods only)                    │
│         ▼                                                                   │
│  [Training Worker Pods]                                                     │
│  - Same labels, same NetworkPolicy scope                                    │
│  - Ports:                                                                   │
│    - 29500/TCP: PyTorch rendezvous (TCP, plaintext)                         │
│    - 2222/TCP: SSH (MPI mode, ECDSA P521 key auth)                         │
│    - Dynamic: NCCL all-reduce (TCP, plaintext)                              │
│                                                                             │
│  ┌─ NetworkPolicy (per TrainJob) ──────────────────────────────────────┐    │
│  │ Ingress Rule 1: Same-job pod communication                          │    │
│  │   Source: Pods with jobset.sigs.k8s.io/jobset-name: {trainjob-name}│    │
│  │   Ports: All                                                        │    │
│  │                                                                     │    │
│  │ Ingress Rule 2: Controller metrics access (if progression enabled)  │    │
│  │   Source: Controller namespace, labels app.kubernetes.io/name:      │    │
│  │           trainer, app.kubernetes.io/component: controller          │    │
│  │   Port: 28080/TCP                                                   │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  MONITORING                                                                 │
│                                                                             │
│  [Prometheus]                                                               │
│  - Scrapes via PodMonitor                                                   │
│  - Port: 8443/TCP                                                           │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS                                                          │
│  - Auth: None                                                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

SERVICES:
┌────────────────────────────────────────────┬───────────┬──────────┬─────────────┬──────────────────────────┐
│ Service Name                               │ Type      │ Port     │ Target Port │ Purpose                  │
├────────────────────────────────────────────┼───────────┼──────────┼─────────────┼──────────────────────────┤
│ kubeflow-trainer-controller-manager        │ ClusterIP │ 443/TCP  │ 9443        │ Webhook (internal)       │
│ kubeflow-trainer-controller-manager        │ ClusterIP │ 8443/TCP │ 8443        │ Metrics (internal, RHOAI)│
└────────────────────────────────────────────┴───────────┴──────────┴─────────────┴──────────────────────────┘

RBAC Summary:
- ClusterRole: kubeflow-trainer-controller-manager
  - API Groups: "" (core), admissionregistration.k8s.io, coordination.k8s.io, jobset.x-k8s.io,
                networking.k8s.io, node.k8s.io, scheduling.volcano.sh, scheduling.x-k8s.io,
                trainer.kubeflow.org
  - Key Resources: configmaps, secrets, events, pods (RHOAI), jobsets, networkpolicies (RHOAI),
                   podgroups, trainjobs, trainingruntimes, clustertrainingruntimes
  - Verbs: create, get, list, watch, patch, update, delete (varies by resource)

- ClusterRole: training-admin (aggregate)
  - Full CRUD on trainjobs, trainingruntimes, clustertrainingruntimes

- ClusterRole: training-edit (aggregate)
  - CRUD on trainjobs, read-only on runtimes

- ClusterRole: training-view (aggregate)
  - Read-only on all trainer.kubeflow.org resources

- ClusterRoleBinding: kubeflow-trainer-controller-manager
  - ServiceAccount: kubeflow-trainer-controller-manager (cluster-scoped)

Secrets:
- kubeflow-trainer-webhook-cert (Opaque) - Webhook TLS cert + CA, cert-controller managed, auto-rotate
- {trainjob-name}-mpi-ssh-auth (kubernetes.io/ssh-auth) - ECDSA P521 SSH key pair per TrainJob,
  immutable, recreated on TrainJob recreation

Authentication & Authorization:
- Webhook endpoints (/validate-*): POST, K8s API Server TLS client cert, failurePolicy=Fail
- Metrics (/metrics): GET, TLS only (no auth), HTTPS on 8443
- Training pod metrics: GET, None (NetworkPolicy restricts to controller pod only)

Certificate Management:
- cert-controller (open-policy-agent) manages webhook certificates
- CertManagement.Enable: true
- WebhookServiceName: kubeflow-trainer-controller-manager
- WebhookSecretName: kubeflow-trainer-webhook-cert
