┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client]                                                       │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Bearer Token / OAuth (enforced at gateway)               │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Platform-Managed)                                              │
│                                                                          │
│  [KServe Gateway / Gateway API]                                          │
│  - Managed by: KServe + rhods-operator                                   │
│  - TLS Termination: TLS 1.2+ at gateway                                 │
│  - External Port: 443/TCP                                                │
│         │                                                                │
│         │ Port: varies (internal service port)                           │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: Platform-managed (mTLS if service mesh enabled)    │
│         │ Auth: Passed through from gateway                              │
│         ▼                                                                │
│  [Auth Proxy (kube-rbac-proxy / oauth-proxy)]                            │
│  - RHOAI 3.x: kube-rbac-proxy sidecar                                   │
│  - RHOAI 2.x: oauth-proxy sidecar                                       │
│  - Purpose: Enforce authentication before reaching inference container   │
│         │                                                                │
│         │ Port: 8000/TCP (HTTP) or 8033/TCP (gRPC)                      │
│         │ Protocol: HTTP or gRPC                                         │
│         │ Encryption: None (pod-internal, localhost)                      │
│         │ Auth: None (already enforced by proxy)                         │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  APPLICATION (Container)                                                 │
│                                                                          │
│  [vllm-rocm Container (odh-vllm-rocm-rhel9)]                            │
│  - User: 2000 (non-root)                                                │
│  - Base Image: rhaiis/vllm-rocm-rhel9:3.2.1                             │
│  - Entrypoint: python -m vllm_tgis_adapter                              │
│  - Namespace: {user-namespace} (KServe InferenceService namespace)       │
│  - ServiceAccount: Managed by KServe                                     │
│                                                                          │
│  Listening Ports:                                                        │
│    - 8000/TCP  HTTP   OpenAI-compatible API                              │
│      - POST /v1/completions                                              │
│      - POST /v1/chat/completions                                         │
│      - GET  /v1/models                                                   │
│      - GET  /health                                                      │
│    - 8033/TCP  gRPC   TGIS Generation Service                            │
│                                                                          │
│  No TLS configured at application level                                  │
│  No authentication configured at application level                       │
│         │                                                                │
│         │ (GPU access)                                                   │
│         │ Interface: PCIe / HIP Runtime                                   │
│         │ Protocol: Device API                                           │
│         │ Encryption: N/A (hardware bus)                                  │
│         │ Auth: N/A                                                      │
│         ▼                                                                │
│  [AMD ROCm GPU]                                                          │
│  - Purpose: Tensor operations for LLM inference                          │
│  - Interface: PCIe / HIP device API                                      │
│  - Driver: Host-provided ROCm driver                                     │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS (Outbound from Container)                                        │
│                                                                          │
│  [Model Storage (S3/PVC)]                                                │
│  - Port: 443/TCP (S3) or N/A (PVC filesystem)                           │
│  - Protocol: HTTPS or local filesystem                                   │
│  - Encryption: TLS 1.2+ (S3) or N/A (PVC)                               │
│  - Auth: AWS IAM / Service Account credentials / None                    │
│  - Purpose: Download model weights at container startup                  │
│                                                                          │
│  [Hugging Face Hub - huggingface.co]                                     │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: HF Token (optional)                                             │
│  - Purpose: Download model weights/tokenizers (optional)                 │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC resources defined in this repository
- This is a container image, not an operator or controller
- RBAC for the pod is managed by KServe InferenceService configuration

Service Mesh Configuration:
- No service mesh configuration defined in this repository
- Service mesh integration (mTLS, PeerAuthentication) managed by platform operator
- Auth proxy sidecar injected by KServe (kube-rbac-proxy in RHOAI 3.x)

Secrets:
- No secrets defined in this repository
- Model storage credentials: Managed by KServe InferenceService storage config
- HuggingFace tokens: Managed by KServe InferenceService environment configuration
- TLS certificates: Not applicable (no TLS at application level)

Container Security:
- Runs as non-root user (UID 2000)
- Base image: UBI9 (rhaiis/vllm-rocm-rhel9)
- FIPS: Delegated to base image system OpenSSL (FIPS mode when host node FIPS-enabled)
- Build hermeticity: Non-hermetic (hermetic: false); deps inherited from RHAIIS base image
- No lock files present on main branch

FIPS Compliance:
- Python runtime (not Go) — Go FIPS build flags not applicable
- System OpenSSL from UBI9 base operates in FIPS mode when host has FIPS enabled
- No application-level TLS configuration
- Crypto library usage inherited from base image (cannot assess from this repo)
