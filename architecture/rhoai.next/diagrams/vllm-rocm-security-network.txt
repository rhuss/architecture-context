┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client]                                                       │
│         │                                                                │
│         │ Auth & encryption handled by KServe platform layer             │
│         │ (kube-rbac-proxy sidecar / Route / HTTPRoute)                  │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  PLATFORM LAYER (KServe-Managed)                                        │
│                                                                          │
│  [KServe Router / Ingress]                                               │
│  - TLS termination at Route/HTTPRoute level                              │
│  - Auth: kube-rbac-proxy sidecar or platform-managed auth               │
│  - Manages ServingRuntime lifecycle                                      │
│         │                                                                │
│         │ Port: 8000/TCP (HTTP) and 8033/TCP (gRPC)                     │
│         │ Protocol: HTTP / gRPC                                          │
│         │ Encryption: None (pod-level, plaintext)                        │
│         │ Auth: None (pod-level)                                         │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  POD BOUNDARY (vllm-rocm Container)                                     │
│                                                                          │
│  [vllm-rocm Pod]                                                         │
│  - Namespace: {user-namespace}                                           │
│  - ServiceAccount: KServe-managed                                        │
│  - Image: vllm-rocm (based on rhaiis/vllm-rocm-rhel9:3.2.1)            │
│  - UID: 2000 (non-root)                                                  │
│  - Entrypoint: python3 -m vllm_tgis_adapter                             │
│                                                                          │
│  Listening Ports:                                                        │
│  ┌──────────────────────────────────────────────────────────────┐        │
│  │  8000/TCP  │  HTTP   │  OpenAI-compatible API               │        │
│  │            │         │  /v1/completions (POST)               │        │
│  │            │         │  /v1/chat/completions (POST)          │        │
│  │            │         │  /v1/models (GET)                     │        │
│  │            │         │  /health (GET)                        │        │
│  ├────────────┼─────────┼──────────────────────────────────────┤        │
│  │  8033/TCP  │  gRPC   │  TGIS GenerationService              │        │
│  └──────────────────────────────────────────────────────────────┘        │
│  Encryption: None (plaintext within pod)                                 │
│  Auth: None (delegated to platform layer)                                │
│         │                                                                │
│         │ PCIe/ROCm (device passthrough)                                 │
│         ▼                                                                │
│  [AMD ROCm GPU]                                                          │
│  - Device plugin: amd.com/gpu                                            │
│  - Interface: PCIe/ROCm                                                  │
│  - Encryption: N/A (hardware bus)                                        │
│  - Auth: N/A (device plugin)                                             │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS (Outbound from Pod)                                              │
│                                                                          │
│  [Model Storage - S3/PVC]                                                │
│  ┌──────────────────────────────────────────────────────────────┐        │
│  │  PVC (Filesystem)                                            │        │
│  │  - Port: N/A (volume mount)                                  │        │
│  │  - Protocol: Filesystem                                      │        │
│  │  - Encryption: N/A                                           │        │
│  │  - Auth: ServiceAccount (volume mount permissions)           │        │
│  ├──────────────────────────────────────────────────────────────┤        │
│  │  S3-Compatible Object Storage                                │        │
│  │  - Port: 443/TCP                                             │        │
│  │  - Protocol: HTTPS                                           │        │
│  │  - Encryption: TLS 1.2+                                      │        │
│  │  - Auth: AWS IAM credentials / Service Account               │        │
│  └──────────────────────────────────────────────────────────────┘        │
│                                                                          │
│  [Hugging Face Hub - huggingface.co] (Optional)                          │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Bearer Token (optional)                                         │
│  - Purpose: Download model weights/tokenizer if not pre-cached           │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles or RoleBindings defined by this component
- RBAC is fully managed by KServe (ServingRuntime/InferenceService)
- Pod runs as non-root UID 2000

Secrets:
- (model storage credentials) (Opaque) - S3/storage credentials for model download
  - Provisioned by: Platform admin / KServe
  - Auto-Rotate: No

Security Notes:
- Container exposes plaintext HTTP/gRPC — TLS and auth are platform responsibilities
- Non-root execution (UID 2000)
- DISABLE_LOGPROBS_DURING_SPEC_DECODING=false (overrides upstream default)
- No application-level TLS configuration
- FIPS compliance inherited from RHAIIS base image (UBI + OpenSSL)
- Build is non-hermetic (hermetic: false) — acceptable for image repackaging
- Base image version pinned via RHAIIS_VERSION=3.2.1 ARG

Trust Boundaries:
1. External → Platform Layer: TLS + Auth managed by KServe/Route
2. Platform Layer → Pod: Plaintext (no pod-level security)
3. Pod → GPU: Hardware bus (no software security boundary)
4. Pod → External Storage: TLS 1.2+ with credential-based auth
