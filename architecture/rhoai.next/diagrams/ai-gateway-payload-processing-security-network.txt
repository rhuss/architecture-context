┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: Client-provided (passthrough)                                │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Gateway)                                                           │
│                                                                              │
│  [Istio Gateway (Envoy)]                                                     │
│  - Port: 443/TCP (external) → routes via HTTPRoute                          │
│  - TLS Termination: TLS 1.2+ (SIMPLE mode)                                  │
│  - Routing: HTTPRoute per ExternalModel                                      │
│    - Path match: /{namespace}/{model-name}                                   │
│    - Header match: X-Gateway-Model-Name                                      │
│         │                                                                    │
│         │ Port: 9004/TCP                                                     │
│         │ Protocol: gRPC (ext_proc)                                          │
│         │ Encryption: plaintext (mesh-internal mTLS via Istio sidecar)       │
│         │ Auth: None (Envoy-to-sidecar localhost)                            │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (Trusted - Istio mTLS)                                         │
│                                                                              │
│  [AI Gateway Payload Processing (BBR Plugin Host)]                           │
│  - Namespace: same as Gateway (required for EnvoyFilter targeting)           │
│  - ServiceAccount: payload-processing-{release}                              │
│  - Image: odh-ai-gateway-payload-processing-rhel9                            │
│  - Ports:                                                                    │
│    - 9004/TCP gRPC (ext_proc listener)                                       │
│    - 9005/TCP HTTP (health check)                                            │
│  - FIPS: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, UBI9 OpenSSL         │
│                                                                              │
│    Plugin Pipeline (Request Phase):                                          │
│    ┌─────────────────────────────────────────────────────────────────┐       │
│    │ 1. model-provider-resolver → in-memory cache (ExternalModel)    │       │
│    │ 2. api-translation → in-process body mutation                   │       │
│    │ 3. apikey-injection → in-memory cache (Secrets)                 │       │
│    │ 4. nemo-request-guard → HTTP POST to NeMo (optional)            │       │
│    └─────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│    Plugin Pipeline (Response Phase):                                         │
│    ┌─────────────────────────────────────────────────────────────────┐       │
│    │ 1. api-translation → in-process body reverse-translation        │       │
│    │ 2. nemo-response-guard → HTTP POST to NeMo (optional)           │       │
│    └─────────────────────────────────────────────────────────────────┘       │
│         │                                                                    │
│         │ (Controller watches)                                               │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount token                                         │
│         ▼                                                                    │
│  [Kubernetes API Server]                                                     │
│  - Watches: ExternalModel, ExternalProvider, Secret CRs                      │
│  - Creates: HTTPRoute, Service, ServiceEntry, DestinationRule                │
│                                                                              │
│  [NeMo Guardrails (optional)]                                                │
│  - Port: configurable                                                        │
│  - Protocol: HTTP/HTTPS                                                      │
│  - Encryption: configurable                                                  │
│  - Auth: None                                                                │
│  - Endpoint: /v1/guardrail/checks                                            │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS (External LLM Providers via Istio Mesh)                              │
│                                                                              │
│  Traffic routed via: ExternalName Service → ServiceEntry → DestinationRule   │
│  TLS: Istio DestinationRule SIMPLE mode (TLS origination by mesh)            │
│                                                                              │
│  [api.openai.com]                                                            │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (DestinationRule SIMPLE)                             │
│  - Auth: Bearer token (Authorization header)                                 │
│  - Path: /v1/chat/completions                                                │
│                                                                              │
│  [api.anthropic.com]                                                         │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (DestinationRule SIMPLE)                             │
│  - Auth: API key (x-api-key header)                                          │
│  - Path: /v1/messages                                                        │
│                                                                              │
│  [{azure-endpoint}.openai.azure.com]                                         │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (DestinationRule SIMPLE)                             │
│  - Auth: API key (api-key header)                                            │
│  - Path: /openai/v1/chat/completions                                         │
│                                                                              │
│  [bedrock.amazonaws.com]                                                     │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (DestinationRule SIMPLE)                             │
│  - Auth: Bearer token (Authorization header)                                 │
│  - Path: /v1/chat/completions                                                │
│                                                                              │
│  [{location}-aiplatform.googleapis.com]                                      │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+ (DestinationRule SIMPLE)                             │
│  - Auth: Bearer token (Authorization header)                                 │
│  - Path: /v1/projects/{p}/locations/{l}/endpoints/{e}/chat/completions       │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: {release}-externalmodels-reader
  - API Groups: "" (core), maas.opendatahub.io
  - Resources: secrets (get, list, watch); externalmodels (get, list, watch)
  - Bound to: SA payload-processing-{release}

- Controller RBAC (kubebuilder markers):
  - API Groups: inference.opendatahub.io
    - Resources: externalmodels (get, list, watch)
    - Resources: externalmodels/status (get, update, patch)
    - Resources: externalmodels/finalizers (update)
    - Resources: externalproviders (get, list, watch)
    - Resources: externalproviders/status (get, update, patch)
    - Resources: externalproviders/finalizers (update)
  - API Groups: "" (core)
    - Resources: secrets (get, list, watch)
    - Resources: services (get, list, watch, create, update, delete)
  - API Groups: networking.istio.io
    - Resources: serviceentries (get, list, watch, create, update, delete)
    - Resources: destinationrules (get, list, watch, create, update, delete)
  - API Groups: gateway.networking.k8s.io
    - Resources: httproutes (get, list, watch, create, update, delete)

Service Mesh Configuration:
- EnvoyFilter: Attached to Gateway (INSERT_AFTER Kuadrant WASM plugin anchor)
- ServiceEntry: Per provider, MESH_EXTERNAL with DNS resolution
- DestinationRule: Per provider, TLS origination SIMPLE mode
- mTLS: Istio mesh-internal mTLS for ext_proc communication

Secrets:
- {provider}-credentials (Opaque) - Provider API key (data key: "api-key")
  - Provisioned by: Manual (user-created)
  - Label selector: inference.networking.k8s.io/bbr-managed=true
  - Auto-rotate: No
  - Cached by: apikey-injection plugin in-memory store

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1
- Base image: registry.access.redhat.com/ubi9/ubi-minimal (OpenSSL)
- TLS: No custom TLS config; relies on Istio mTLS (intra-cluster)
        and DestinationRule SIMPLE (egress TLS origination)
- Crypto: stdlib crypto/tls (FIPS via OpenSSL at runtime)
- No non-FIPS crypto risks detected
