┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Client (OpenAI-compatible)]                                            │
│         │                                                                │
│         │ Port: 8090/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plaintext)                                   │
│         │ Auth: Authorization header (pass-through, not enforced)        │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  APPLICATION LAYER                                                       │
│                                                                          │
│  [vLLM Orchestrator Gateway]                                             │
│  - Language: Rust (axum 0.7.9 framework)                                │
│  - Container Port: 8090/TCP                                              │
│  - No Kubernetes Service manifest (deployed by external operator)        │
│  - No local authentication enforcement                                   │
│  - Routes: /{route_name}/v1/chat/completions (POST)                     │
│  - Config: YAML from GATEWAY_CONFIG env var (default: config/config.yaml)│
│         │                                                                │
│         │ Port: Configurable (default 8085/TCP)                          │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: Optional mTLS                                      │
│         │   - When TLS certs present at /etc/tls/private/:               │
│         │     HTTPS with mTLS (client cert + custom CA)                  │
│         │   - When TLS certs absent:                                     │
│         │     HTTP plaintext                                             │
│         │ Auth: Authorization header forwarded + mTLS client certificate │
│         │ Note: danger_accept_invalid_hostnames(true) for localhost only  │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  BACKEND SERVICES (Internal)                                             │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Endpoint: /api/v2/chat/completions-detection                         │
│  - Port: Configurable (default 8085/TCP)                                │
│  - Purpose: Chat completions with detector-based content analysis        │
│  - Auth: Receives forwarded Authorization header + validates mTLS cert   │
│         │                                                                │
│         │ (Internal orchestrator communication)                          │
│         ▼                                                                │
│  [Guardrails Detectors]                                                  │
│  - e.g., regex-detector, PII detectors                                  │
│  - Invoked by orchestrator, NOT by gateway directly                     │
│  - Purpose: Content safety analysis (input/output)                       │
│                                                                          │
│  [vLLM / LLM Inference Server]                                          │
│  - Invoked by orchestrator, NOT by gateway directly                     │
│  - Purpose: Text generation (model inference)                            │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No Kubernetes RBAC definitions exist for this component
- No ClusterRoles, Roles, ClusterRoleBindings, or RoleBindings
- Component is a stateless HTTP service, not a Kubernetes operator

Authentication & Authorization:
- Gateway Level: No authentication enforcement
  - Authorization header passed through transparently to orchestrator
  - x-forwarded-* headers forwarded to orchestrator
- Orchestrator Level: Auth enforcement delegated to orchestrator or upstream proxy
- mTLS: Client certificate-based mutual authentication (when TLS certs present)

Secrets:
- /etc/tls/private/tls.crt (X.509 PEM)
  - Purpose: Client certificate for mTLS to orchestrator
  - Provisioned by: Platform (cert-manager or manual volume mount)
  - Auto-Rotate: Unknown
- /etc/tls/private/tls.key (PEM private key)
  - Purpose: Private key for mTLS client identity
  - Provisioned by: Platform (cert-manager or manual volume mount)
  - Auto-Rotate: Unknown
- /etc/tls/ca/service-ca.crt (X.509 PEM CA)
  - Purpose: Custom CA for validating orchestrator server certificate
  - Provisioned by: Platform (service-ca-operator or manual mount)
  - Auto-Rotate: Unknown

TLS Implementation Details:
- Library: openssl 0.10.73 (Rust bindings to system OpenSSL) + native-tls 0.2.12
- Certificate path: PEM → X509/PKey → PKCS#12 → native-tls Identity
  (workaround: reqwest's native-tls backend requires PKCS#12 format)
- Scheme selection: Automatic based on cert file presence at /etc/tls/private/
- Dev convenience: danger_accept_invalid_hostnames(true) when host=localhost
- Base image: UBI 9 minimal with compat-openssl11

FIPS Compliance:
- Build: Dynamic linking against system OpenSSL (openssl-devel at build, compat-openssl11 at runtime)
- Crypto: Uses system OpenSSL via openssl + native-tls crates (FIPS when system OpenSSL is FIPS-validated)
- Risk: No explicit cipher suite restrictions; relies on OpenSSL defaults
- Note: Not a Go binary — GOEXPERIMENT/strictfipsruntime not applicable
