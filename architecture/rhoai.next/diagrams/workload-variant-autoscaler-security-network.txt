┌─────────────────────────────────────────────────────────────────────────────┐
│  CLUSTER INTERNAL (Trusted - Kubernetes Network)                            │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │  WVA Controller Namespace (system namespace)                          │  │
│  │                                                                       │  │
│  │  [WVA Controller Manager]                                             │  │
│  │  - ServiceAccount: controller-manager                                 │  │
│  │  - Image: ghcr.io/llm-d/llm-d-workload-variant-autoscaler:v0.6.0    │  │
│  │                                                                       │  │
│  │     Exposed Endpoints:                                                │  │
│  │     ┌──────────────────────────────────────────────────────────────┐  │  │
│  │     │ /metrics    Port: 8443/TCP  HTTPS  TLS (self-signed/cert-mgr) │  │  │
│  │     │             Auth: Bearer Token (SA) + TokenReview/SAR filter   │  │  │
│  │     │ /healthz    Port: 8081/TCP  HTTP   No Encryption  No Auth     │  │  │
│  │     │ /readyz     Port: 8081/TCP  HTTP   No Encryption  No Auth     │  │  │
│  │     └──────────────────────────────────────────────────────────────┘  │  │
│  │                                                                       │  │
│  │     Services:                                                         │  │
│  │     ┌──────────────────────────────────────────────────────────────┐  │  │
│  │     │ controller-manager-metrics-service  ClusterIP  8443/TCP      │  │  │
│  │     │ Protocol: HTTPS  TLS (self-signed/cert-manager)              │  │  │
│  │     │ Auth: Bearer Token (SA) + authn/authz filter                 │  │  │
│  │     │ Exposure: Internal only                                      │  │  │
│  │     └──────────────────────────────────────────────────────────────┘  │  │
│  │                                                                       │  │
│  │     Secrets:                                                          │  │
│  │     - prometheus-client-cert (kubernetes.io/tls) - TLS client certs  │  │
│  │       for Prometheus HTTPS (ca.crt, tls.crt, tls.key)               │  │
│  │       Provisioned: Manual/cert-manager | Auto-Rotate: No            │  │
│  │     - epp-metrics-token (Opaque) - Bearer token for EPP pod scraping│  │
│  │       Provisioned: Manual/SA token | Auto-Rotate: No                │  │
│  │                                                                       │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│         │                    │                         │                     │
│         │                    │                         │                     │
│  ═══════╪════════════════════╪═════════════════════════╪═════════════════   │
│  EGRESS │FLOWS               │                         │                     │
│  ═══════╪════════════════════╪═════════════════════════╪═════════════════   │
│         │                    │                         │                     │
│         │ Port: 9091/TCP     │ Port: configurable/TCP  │ Port: 443/TCP      │
│         │ Protocol: HTTPS    │ Protocol: HTTPS         │ Protocol: HTTPS    │
│         │ Encryption:        │ Encryption: TLS         │ Encryption:        │
│         │   TLS 1.2+         │ Auth: Bearer Token      │   TLS 1.2+         │
│         │ Auth: Bearer Token │   (EPP metrics token    │ Auth: SA token     │
│         │   (SA token)       │    from mounted secret) │                     │
│         ▼                    ▼                         ▼                     │
│                                                                             │
│  ┌────────────────────┐ ┌───────────────────┐ ┌──────────────────────────┐ │
│  │ Prometheus /       │ │ EPP Pods          │ │ Kubernetes API Server    │ │
│  │ Thanos Querier     │ │ (Endpoint Picker) │ │                          │ │
│  │                    │ │                   │ │ Port: 443/TCP            │ │
│  │ Namespace:         │ │ Namespace:        │ │ Protocol: HTTPS          │ │
│  │  openshift-        │ │  (user namespaces)│ │ Encryption: TLS 1.2+     │ │
│  │  monitoring        │ │                   │ │ Auth: SA token           │ │
│  │                    │ │ Purpose:          │ │                          │ │
│  │ Purpose:           │ │  Scrape flow      │ │ Purpose:                 │ │
│  │  Query vLLM metrics│ │  control queue    │ │  CRUD VA status,         │ │
│  │  (KV cache, queue  │ │  size metrics for │ │  read Deployments/LWS/   │ │
│  │  depth, tokens,    │ │  scale-from-zero  │ │  ConfigMaps/Pods/        │ │
│  │  TTFT, ITL) and    │ │  detection        │ │  Services/Nodes,         │ │
│  │  scheduler flow    │ │                   │ │  leader election leases, │ │
│  │  control metrics   │ │                   │ │  scale subresource       │ │
│  │                    │ │                   │ │                          │ │
│  └────────────────────┘ └───────────────────┘ └──────────────────────────┘ │
│                                                                             │
│  ═══════════════════════════════════════════════════════════════════════    │
│  INGRESS FLOWS (to /metrics)                                                │
│  ═══════════════════════════════════════════════════════════════════════    │
│                                                                             │
│  ┌────────────────────┐                                                    │
│  │ Prometheus         │                                                    │
│  │ (ServiceMonitor    │──── Port: 8443/TCP ────► WVA /metrics endpoint    │
│  │  scrape)           │     Protocol: HTTPS                                │
│  │                    │     Encryption: TLS                                 │
│  │                    │     Auth: Bearer Token (SA) + TokenReview/SAR      │
│  └────────────────────┘                                                    │
│                                                                             │
│  NetworkPolicy: allow-metrics-traffic                                       │
│  - Restricts /metrics access to namespaces with specific labels            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

No external ingress (no Routes, Ingresses, or external-facing Services)

RBAC Summary:
┌──────────────────────────────────────────────────────────────────────────────┐
│ ClusterRole: manager-role                                                    │
│ ├─ "" : configmaps (get, list, update, watch)                               │
│ ├─ "" : configmaps/status (get)                                             │
│ ├─ "" : events (create, patch)                                              │
│ ├─ "" : namespaces, pods, secrets, services (get, list, watch)              │
│ ├─ "" : nodes, nodes/status (get, list, patch, update, watch)               │
│ ├─ apps : deployments (get, list, patch, update, watch)                     │
│ ├─ apps : deployments/scale (get, update)                                   │
│ ├─ apps : replicasets, statefulsets (get, list, watch)                      │
│ ├─ inference.networking.k8s.io : inferencepools (get, list, watch)          │
│ ├─ inference.networking.x-k8s.io : inferencepools (get, list, watch)        │
│ ├─ leaderworkerset.x-k8s.io : leaderworkersets (get, list, patch, update, watch) │
│ ├─ leaderworkerset.x-k8s.io : leaderworkersets/scale (get, update)          │
│ ├─ llmd.ai : variantautoscalings (create, delete, get, list, patch, update, watch) │
│ ├─ llmd.ai : variantautoscalings/finalizers (update)                        │
│ ├─ llmd.ai : variantautoscalings/status (get, patch, update)                │
│ └─ monitoring.coreos.com : servicemonitors (get, list, watch)               │
│                                                                              │
│ Role: leader-election-role (namespace-scoped)                                │
│ ├─ "" : configmaps (get, list, watch, create, update, patch, delete)        │
│ ├─ coordination.k8s.io : leases (get, list, watch, create, update, patch, delete) │
│ └─ "" : events (create, patch)                                              │
│                                                                              │
│ ClusterRole: metrics-auth-role                                               │
│ ├─ authentication.k8s.io : tokenreviews (create)                            │
│ └─ authorization.k8s.io : subjectaccessreviews (create)                     │
│                                                                              │
│ ClusterRole: metrics-reader-role                                             │
│ └─ (non-resource: /metrics) (get)                                           │
└──────────────────────────────────────────────────────────────────────────────┘

Role Bindings:
┌──────────────────────────────────────────────────────────────────────────────┐
│ manager-rolebinding (cluster) ─► manager-role → SA: controller-manager      │
│ leader-election-rolebinding (ns) ─► leader-election-role → SA: controller-manager │
│ metrics-auth-rolebinding (cluster) ─► metrics-auth-role → SA: controller-manager │
│ metrics-reader-rolebinding (cluster) ─► metrics-reader-role → SA: prometheus │
│ epp-metrics-reader-rolebinding (cluster) ─► epp-metrics-reader-role → SA: epp-metrics-reader │
└──────────────────────────────────────────────────────────────────────────────┘

Authentication Endpoints:
┌──────────────────────────────────────────────────────────────────────────────┐
│ /metrics (8443) │ GET │ Bearer Token + TokenReview/SAR │ metrics-reader role │
│ Prometheus API  │ GET │ Bearer Token (SA token)        │ cluster-monitoring-view │
│ EPP pod metrics │ GET │ Bearer Token (EPP token)       │ EPP-side enforcement │
└──────────────────────────────────────────────────────────────────────────────┘

FIPS Compliance:
  Status: EXPLICITLY DISABLED
  Reason: CGO_ENABLED=0 (static linking) incompatible with GOEXPERIMENT=strictfipsruntime
  TLS: TLS 1.2+ minimum configured, stdlib crypto/tls only
