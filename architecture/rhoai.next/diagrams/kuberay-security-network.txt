┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External User / Data Scientist]                                            │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: Session cookie (OpenShift OAuth)                             │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Platform Namespace: redhat-ods-applications)                       │
│                                                                              │
│  [data-science-gateway (Envoy/Gateway API)]                                  │
│  - HTTPRoute: /ray/{namespace}/{cluster}/*                                   │
│  - TLS Termination: TLS 1.2+ (Gateway-terminated)                           │
│  - URL Rewrite: strips /ray/{ns}/{cluster}/ prefix                          │
│  - ReferenceGrant: cross-namespace Service reference authorized              │
│         │                                                                    │
│         │ Port: 8443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (self-signed RSA 2048)                             │
│         │ Auth: SubjectAccessReview (kube-rbac-proxy)                        │
│         │       OR OAuth delegate-urls RBAC (oauth-proxy)                    │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  AUTHENTICATION LAYER (User Namespace)                                       │
│                                                                              │
│  [kube-rbac-proxy / oauth-proxy sidecar]                                     │
│  - Container in Ray Head Pod                                                 │
│  - Port: 8443/TCP (external-facing)                                          │
│  - TLS: Self-signed cert ({cluster}-oauth-tls or {cluster}-oidc-tls)        │
│  - AuthZ: SubjectAccessReview on service/{cluster}-head-svc (GET)           │
│         │                                                                    │
│         │ Port: 8265/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None (localhost loopback)                              │
│         │ Auth: None (trusted local proxy)                                   │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  RAY CLUSTER (User Namespace - NetworkPolicy Protected)                      │
│                                                                              │
│  ┌──────────────────────────────────────────────────────┐                    │
│  │ [Ray Head Pod]                                        │                   │
│  │ - ServiceAccount: {cluster}-autoscaler-sa             │                   │
│  │ - Ports:                                              │                   │
│  │   - 8265/TCP (Dashboard API)                          │                   │
│  │   - 10001/TCP (Client port)                           │                   │
│  │   - 6379/TCP (GCS Server)                             │                   │
│  │   - 8080/TCP (Metrics)                                │                   │
│  │   - 8000/TCP (Serve)                                  │                   │
│  │   - 8443/TCP (Auth proxy - external)                  │                   │
│  └───────────────┬──────────────────────────────────────┘                    │
│                   │                                                           │
│                   │ Ports: various/TCP                                        │
│                   │ Protocol: gRPC + TCP                                      │
│                   │ Encryption: mTLS (cert-manager, 90-day certs)            │
│                   │ Auth: Mutual TLS with per-cluster CA                     │
│                   │ CA: {cluster}-ca-secret-{uid} (RSA 4096, 10yr)          │
│                   │ Head cert: ray-head-secret-{cluster} (90d, renew 15d)   │
│                   │ Worker cert: ray-worker-secret-{cluster} (90d, renew 15d)│
│                   ▼                                                           │
│  ┌──────────────────────────────────────────────────────┐                    │
│  │ [Ray Worker Pods]                                     │                   │
│  │ - Ports:                                              │                   │
│  │   - 10001/TCP (Client port)                           │                   │
│  │   - 8080/TCP (Metrics)                                │                   │
│  │   - 8000/TCP (Serve)                                  │                   │
│  └──────────────────────────────────────────────────────┘                    │
│                                                                              │
│  NetworkPolicy Rules:                                                        │
│  - Head pod: Allow intra-cluster traffic (all ports)                        │
│  - Head pod: Allow namespace-local on ports 10001, 8265                     │
│  - Head pod: Allow operator namespace for health checks                     │
│  - Head pod: Allow gateway namespace on port 8443 (auth proxy)             │
│  - Head pod: Deny all other ingress                                         │
│  - Worker pods: Allow intra-cluster traffic only                            │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (e.g., opendatahub)                                      │
│                                                                              │
│  [KubeRay Operator]                                                          │
│  - ServiceAccount: kuberay-operator                                          │
│  - Ports:                                                                    │
│    - 8080/TCP (Prometheus metrics, HTTP, no auth)                            │
│    - 8082/TCP (Health probes: /healthz, /readyz, HTTP, no auth)             │
│    - 9443/TCP (Webhooks, HTTPS, OpenShift serving-cert)                     │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount Token                                         │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  PLATFORM SERVICES                                                           │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Port: 443/TCP                                                             │
│  - Purpose: CRD watches, pod/service/secret CRUD, RBAC operations           │
│  - Auth: ServiceAccount Token                                                │
│  - Encryption: TLS 1.2+                                                      │
│                                                                              │
│  [cert-manager]                                                              │
│  - Interaction: CRD (Issuer, Certificate)                                    │
│  - Purpose: Issues and rotates mTLS certificates for Ray clusters           │
│  - Auth: ServiceAccount Token (Kubernetes API)                               │
│  - Encryption: TLS 1.2+                                                      │
│                                                                              │
│  [OpenShift OAuth / Authentication API]                                      │
│  - Port: 443/TCP                                                             │
│  - Purpose: Read OAuth configuration for proxy setup                        │
│  - API: config.openshift.io/authentications, oauths                         │
│  - Auth: ServiceAccount Token                                                │
│  - Encryption: TLS 1.2+                                                      │
│                                                                              │
│  [Prometheus]                                                                │
│  - Scrapes: 8080/TCP (operator), 8080/TCP (Ray cluster metrics)             │
│  - Protocol: HTTP                                                            │
│  - Encryption: None                                                          │
│  - Auth: None                                                                │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: kuberay-operator
  - API Groups: "" (core), apps, batch, ray.io, networking.k8s.io, extensions,
                 cert-manager.io, rbac.authorization.k8s.io, route.openshift.io,
                 config.openshift.io, operator.openshift.io, authentication.k8s.io,
                 authorization.k8s.io, coordination.k8s.io
  - Key Resources: pods (full CRUD + deletecollection), services, configmaps,
                    rayclusters/rayjobs/rayservices (full CRUD), secrets (read + delete),
                    networkpolicies, ingresses, certificates, issuers,
                    roles, rolebindings, routes, leases
  - Verbs: create, delete, deletecollection, get, list, patch, update, watch

- ClusterRole: kuberay-operator-gateway-api
  - API Groups: gateway.networking.k8s.io
  - Resources: gateways (read), httproutes (full CRUD), referencegrants (full CRUD)

- ClusterRole: kuberay-operator-configmap
  - API Groups: "" (core)
  - Resources: configmaps (full CRUD)

- Per-Cluster Role: {cluster} (autoscaler)
  - Resources: pods (get/list/watch), rayclusters (get/patch)
  - Bound to: {cluster}-autoscaler-sa

Secrets:
- kuberay-webhook-server-cert (kubernetes.io/tls) - Webhook TLS, OpenShift serving-cert, auto-rotate
- {cluster}-oauth-tls (Opaque) - OAuth proxy TLS (RSA 2048, 365d), self-signed, NO auto-rotate
- {cluster}-oidc-tls (Opaque) - OIDC proxy TLS, self-signed, NO auto-rotate
- {cluster}-oauth-config (Opaque) - OAuth proxy config (cookie secret), NO auto-rotate
- {cluster}-oidc-config (Opaque) - OIDC proxy config, NO auto-rotate
- {cluster}-ca-secret-{uid} (kubernetes.io/tls) - mTLS CA (RSA 4096, 10yr), cert-manager, auto-rotate (30d before expiry)
- ray-head-secret-{cluster} (kubernetes.io/tls) - Head mTLS cert (90d), cert-manager, auto-rotate (15d before expiry)
- ray-worker-secret-{cluster} (kubernetes.io/tls) - Worker mTLS cert (90d), cert-manager, auto-rotate (15d before expiry)

SecurityContextConstraints:
- SCC: run-as-ray-user
  - RunAsUser: MustRunAsRange
  - Purpose: Ray pods run as dedicated user (not root)

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, -tags strictfipsruntime
- Base Image: UBI9 ubi-minimal (OpenSSL included)
- TLS: stdlib crypto/tls (FIPS via OpenSSL at runtime)
- No non-FIPS crypto detected in production code
