┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External User / Data Scientist]                                            │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: Session cookie                                               │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Platform Namespace - e.g., redhat-ods-applications)                │
│                                                                              │
│  [data-science-gateway (Envoy)]                                              │
│  - Type: Gateway (gateway.networking.k8s.io)                                 │
│  - Port: 443/TCP (external) → routes via HTTPRoute                          │
│  - TLS Termination: TLS 1.2+ (SIMPLE mode)                                  │
│  - Path: /ray/{namespace}/{cluster}/*                                        │
│  - URL Rewrite: strips /ray/{ns}/{cluster} prefix                           │
│                                                                              │
│  [HTTPRoute: {ns}-{cluster}]                                                 │
│  - Namespace: platform namespace (cross-namespace via ReferenceGrant)        │
│  - Parent: data-science-gateway                                              │
│  - Backend: {cluster}-head-svc in user namespace                             │
│         │                                                                    │
│         │ Port: 8443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (self-signed RSA 2048, 365 days)                   │
│         │ Auth: SubjectAccessReview (get service/{cluster}-head-svc)          │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  AUTHENTICATION BOUNDARY (User Namespace)                                    │
│                                                                              │
│  [kube-rbac-proxy sidecar] OR [OAuth proxy sidecar]                         │
│  - Port: 8443/TCP (HTTPS, self-signed TLS)                                   │
│  - Auth (kube-rbac-proxy): SubjectAccessReview on service resource           │
│  - Auth (OAuth proxy): OpenShift OAuth with delegate-urls RBAC              │
│  - Secret: {cluster}-oidc-tls or {cluster}-oauth-tls (self-signed)           │
│         │                                                                    │
│         │ Port: 8265/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None (localhost loopback)                               │
│         │ Auth: None                                                         │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  RAY CLUSTER (User Namespace - NetworkPolicy enforced)                       │
│                                                                              │
│  [Ray Head Pod]                                                              │
│  - Namespace: {user-namespace}                                               │
│  - ServiceAccount: default (user-provided)                                   │
│  - Ports:                                                                    │
│    - 6379/TCP  (GCS server - Redis protocol)                                │
│    - 8265/TCP  (Ray Dashboard API - HTTP)                                    │
│    - 10001/TCP (Ray Client server)                                           │
│    - 8080/TCP  (Metrics - Prometheus)                                        │
│    - 8000/TCP  (Ray Serve HTTP proxy)                                        │
│    - 8443/TCP  (Auth proxy - HTTPS, exposed externally)                      │
│  - mTLS: cert-manager issued (90-day cert, 15-day auto-renewal)             │
│  - Secret: ray-head-secret-{cluster} (kubernetes.io/tls)                     │
│         │                                                                    │
│         │ Ports: various/TCP (task distribution)                             │
│         │ Protocol: gRPC + custom Ray protocol                               │
│         │ Encryption: mTLS (per-cluster CA, cert-manager)                    │
│         │ Auth: Mutual TLS certificates                                      │
│         ▼                                                                    │
│  [Ray Worker Pods]                                                           │
│  - Namespace: {user-namespace}                                               │
│  - ServiceAccount: default (user-provided)                                   │
│  - DNS: {cluster}-headless (headless service for multi-host)                 │
│  - mTLS: cert-manager issued (90-day cert, 15-day auto-renewal)             │
│  - Secret: ray-worker-secret-{cluster} (kubernetes.io/tls)                   │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (e.g., opendatahub)                                      │
│                                                                              │
│  [KubeRay Operator - ray-operator]                                           │
│  - ServiceAccount: kuberay-operator                                          │
│  - Ports:                                                                    │
│    - 8080/TCP (Metrics - HTTP, no auth)                                      │
│    - 8082/TCP (Health/Ready probes - HTTP, no auth)                          │
│    - 9443/TCP (Webhooks - HTTPS, OpenShift serving-cert)                     │
│  - Secret: kuberay-webhook-server-cert (kubernetes.io/tls, auto-rotated)     │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount Token                                         │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  PLATFORM SERVICES (Internal)                                                │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Port: 443/TCP, HTTPS, TLS 1.2+                                           │
│  - Purpose: CRD watches, pod/service/secret CRUD, RBAC, admission webhooks  │
│  - Auth: ServiceAccount Token (projected volume)                             │
│                                                                              │
│  [cert-manager]                                                              │
│  - Port: N/A (Kubernetes API - CRD-based interaction)                        │
│  - Purpose: Issues per-cluster CA (RSA 4096, 10yr), head/worker TLS certs   │
│  - Cert hierarchy: SelfSigned Issuer → CA Certificate → CA Issuer → Certs   │
│  - Rotation: CA 30 days before expiry, node certs 15 days before expiry     │
│                                                                              │
│  [OpenShift OAuth / config.openshift.io]                                     │
│  - Port: 443/TCP, HTTPS, TLS 1.2+                                           │
│  - Purpose: OAuth configuration for proxy setup                              │
│                                                                              │
│  [Prometheus]                                                                │
│  - Port: 8080/TCP (scrape), HTTP                                             │
│  - Purpose: Operator and Ray cluster metrics collection                      │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

NETWORK POLICIES (per-cluster, created by NetworkPolicy Controller):

  Head Pod NetworkPolicy:
  ┌─────────────────────────────────────────────────────────────────────┐
  │ Ingress Rules:                                                      │
  │  - Intra-cluster (same cluster label): ALL ports                   │
  │  - Same namespace: ports 10001, 8265 only                          │
  │  - Operator namespace: ALL ports (health checks)                   │
  │  - Gateway namespace: port 8443 only (auth proxy)                  │
  │                                                                     │
  │ Effective result: Only port 8443 reachable from outside cluster     │
  │ (via Gateway → auth proxy → localhost dashboard)                   │
  └─────────────────────────────────────────────────────────────────────┘

  Worker Pod NetworkPolicy:
  ┌─────────────────────────────────────────────────────────────────────┐
  │ Ingress Rules:                                                      │
  │  - Intra-cluster (same cluster label): ALL ports                   │
  │  - Same namespace: port 10001 only                                 │
  │                                                                     │
  │ Effective result: Workers only reachable by cluster peers and       │
  │ namespace-local clients on Ray client port                         │
  └─────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  ClusterRole: kuberay-operator
  - API Groups: core, apps, batch, ray.io, networking.k8s.io, extensions,
                cert-manager.io, rbac.authorization.k8s.io, route.openshift.io,
                config.openshift.io, operator.openshift.io, authentication.k8s.io,
                authorization.k8s.io, coordination.k8s.io
  - Resources: pods, services, configmaps, events, secrets, deployments, jobs,
               rayclusters, rayjobs, rayservices (+ finalizers, status),
               ingresses, networkpolicies, certificates, issuers, roles,
               rolebindings, routes, tokenreviews, subjectaccessreviews, leases
  - Verbs: Full CRUD + deletecollection (pods), watch

  ClusterRole: kuberay-operator-gateway-api
  - API Groups: gateway.networking.k8s.io
  - Resources: gateways (get/list/watch), httproutes + referencegrants (full CRUD)

  ClusterRole: kuberay-operator-configmap
  - API Groups: core
  - Resources: configmaps (full CRUD)

  Per-cluster Role: {cluster} (autoscaler)
  - Resources: pods (get/list/watch), rayclusters (get/patch)
  - Bound to: {cluster}-autoscaler-sa

SECRETS:
  ┌───────────────────────────────────┬──────────────────┬───────────────────────┬──────────────┐
  │ Secret                            │ Type             │ Provisioned By        │ Auto-Rotate  │
  ├───────────────────────────────────┼──────────────────┼───────────────────────┼──────────────┤
  │ kuberay-webhook-server-cert       │ kubernetes.io/tls│ OpenShift serving-cert│ Yes          │
  │ {cluster}-oauth-tls               │ Opaque           │ Operator (self-signed)│ No           │
  │ {cluster}-oidc-tls                │ Opaque           │ Operator (self-signed)│ No           │
  │ {cluster}-oauth-config            │ Opaque           │ Operator              │ No           │
  │ {cluster}-oidc-config             │ Opaque           │ Operator              │ No           │
  │ {cluster}-ca-secret-{uid}         │ kubernetes.io/tls│ cert-manager          │ Yes (30d)    │
  │ ray-head-secret-{cluster}         │ kubernetes.io/tls│ cert-manager          │ Yes (15d)    │
  │ ray-worker-secret-{cluster}       │ kubernetes.io/tls│ cert-manager          │ Yes (15d)    │
  └───────────────────────────────────┴──────────────────┴───────────────────────┴──────────────┘

ADMISSION WEBHOOKS:
  ┌────────────────────────────────────────────────────────────────────────────┐
  │ Internal (KubeRay):                                                       │
  │  - mraycluster.kb.io (mutating): Enforces Gateway-only, secure-network   │
  │  - vraycluster.kb.io (validating): Name format + worker uniqueness       │
  │                                                                           │
  │ External (peer components):                                               │
  │  - mraycluster.ray.openshift.ai / vraycluster.ray.openshift.ai           │
  │    (codeflare-operator)                                                   │
  │  - mraycluster.kb.io / vraycluster.kb.io (kueue)                         │
  └────────────────────────────────────────────────────────────────────────────┘

DEFENSE-IN-DEPTH LAYERS (controlled by odh.ray.io/secure-trusted-network):
  Layer 1: NetworkPolicy - Per-cluster network isolation
  Layer 2: Authentication Proxy - OAuth/kube-rbac-proxy sidecar (TLS on 8443)
  Layer 3: mTLS - cert-manager per-cluster CA with 90-day node certificates
  Layer 4: RBAC - SubjectAccessReview for dashboard access authorization
