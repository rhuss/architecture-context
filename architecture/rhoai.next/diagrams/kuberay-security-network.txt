┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [User / Data Scientist]                                                     │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: Session cookie / Bearer Token                                │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (DMZ) — data-science-gateway                                        │
│                                                                              │
│  [data-science-gateway (Envoy)]                                              │
│  - Namespace: redhat-ods-applications (platform namespace)                   │
│  - Port: 443/TCP (external) → 8443/TCP (internal)                           │
│  - TLS Termination: SIMPLE (Gateway-terminated)                              │
│  - Routing: HTTPRoute path /ray/{namespace}/{cluster}/*                      │
│  - URL Rewrite: strips /ray/{ns}/{cluster} prefix                            │
│  - Cross-namespace: ReferenceGrant in cluster namespace authorizes           │
│         │                                                                    │
│         │ Port: 8443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (self-signed RSA 2048)                             │
│         │ Auth: SubjectAccessReview (kube-rbac-proxy)                         │
│         │       or OAuth cookie (oauth-proxy)                                │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL — Per-RayCluster Network Zone                              │
│                                                                              │
│  NetworkPolicy: Per-cluster policies enforced                                │
│  - Head pod: intra-cluster traffic on all ports                              │
│  - Head pod: namespace-local access on 10001, 8265                           │
│  - Head pod: operator access for health checks                               │
│  - Head pod: gateway namespace access for ingress                            │
│  - External exposure: only 8443 (auth proxy)                                 │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │  Head Pod                                                           │     │
│  │                                                                     │     │
│  │  [Auth Proxy Sidecar]                                               │     │
│  │  - kube-rbac-proxy or oauth-proxy                                   │     │
│  │  - Port: 8443/TCP (HTTPS, self-signed TLS)                          │     │
│  │  - Auth: SubjectAccessReview or OAuth                                │     │
│  │  - Secret: {cluster}-oauth-tls / {cluster}-oidc-tls (RSA 2048)      │     │
│  │         │                                                           │     │
│  │         │ Port: 8265/TCP                                            │     │
│  │         │ Protocol: HTTP                                            │     │
│  │         │ Encryption: None (loopback)                               │     │
│  │         │ Auth: None                                                 │     │
│  │         ▼                                                           │     │
│  │  [Ray Dashboard]                                                    │     │
│  │  - Port: 8265/TCP (HTTP) — dashboard UI + job API                   │     │
│  │  - Port: 8000/TCP (HTTP) — Ray Serve proxy                          │     │
│  │  - Port: 6379/TCP (TCP) — GCS server                                │     │
│  │  - Port: 10001/TCP (TCP) — Ray client                               │     │
│  │  - Port: 8080/TCP (HTTP) — Ray metrics                              │     │
│  │         │                                                           │     │
│  │         │ Ports: various/TCP                                        │     │
│  │         │ Protocol: gRPC/TCP                                        │     │
│  │         │ Encryption: mTLS (cert-manager, RSA 4096 CA, 90-day certs)│     │
│  │         │ Auth: Mutual TLS (per-cluster CA)                          │     │
│  │         │ Secrets: ray-head-secret-{cluster} (TLS, 90d, 15d renew)  │     │
│  │         ▼                                                           │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │  Worker Pods                                                        │     │
│  │  - Ports: various/TCP (gRPC, TCP)                                   │     │
│  │  - Encryption: mTLS (cert-manager)                                  │     │
│  │  - Secret: ray-worker-secret-{cluster} (TLS, 90d, 15d renew)        │     │
│  │  - CA: {cluster}-ca-secret-{uid} (RSA 4096, 10yr, 30d renew)        │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE                                                          │
│                                                                              │
│  [KubeRay Operator]                                                          │
│  - Namespace: opendatahub (default)                                          │
│  - ServiceAccount: kuberay-operator                                          │
│  - Metrics: 8080/TCP (HTTP, no auth) — scraped by Prometheus                 │
│  - Health: 8082/TCP (HTTP, no auth) — liveness/readiness probes              │
│  - Webhook: 9443/TCP (HTTPS, serving-cert) — K8s API admission              │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount Token                                         │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  PLATFORM SERVICES                                                           │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Auth: ServiceAccount Token                                                │
│  - Purpose: CRD watches, pod/service/secret CRUD, RBAC                       │
│                                                                              │
│  [cert-manager]                                                              │
│  - Port: 443/TCP (via Kubernetes API)                                        │
│  - Protocol: HTTPS                                                           │
│  - Auth: ServiceAccount Token                                                │
│  - Purpose: Issuer and Certificate lifecycle for mTLS                        │
│                                                                              │
│  [OpenShift OAuth / KubeAPIServer]                                           │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Auth: ServiceAccount Token                                                │
│  - Purpose: Read OAuth/authentication config for proxy setup                 │
│                                                                              │
│  [Prometheus]                                                                │
│  - Port: 8080/TCP (scrape target)                                            │
│  - Protocol: HTTP                                                            │
│  - Auth: None                                                                │
│  - Purpose: Operator and Ray cluster metrics collection                      │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: kuberay-operator
  - API Groups: "" (core), apps, batch, ray.io, networking.k8s.io, extensions,
                cert-manager.io, rbac.authorization.k8s.io, route.openshift.io,
                config.openshift.io, operator.openshift.io, authentication.k8s.io,
                authorization.k8s.io, coordination.k8s.io
  - Key Resources: pods, services, secrets, configmaps, rayclusters, rayjobs,
                   rayservices, ingresses, networkpolicies, certificates, issuers,
                   roles, rolebindings, routes, leases
  - Verbs: get, list, watch, create, update, patch, delete, deletecollection

- ClusterRole: kuberay-operator-gateway-api
  - API Groups: gateway.networking.k8s.io
  - Resources: gateways (read), httproutes (full CRUD), referencegrants (full CRUD)

- ClusterRole: kuberay-operator-configmap
  - API Groups: "" (core)
  - Resources: configmaps (full CRUD)

Role Bindings:
- kuberay-operator → ClusterRole: kuberay-operator → SA: kuberay-operator (cluster-scoped)
- kuberay-operator-gateway-api → ClusterRole → SA: kuberay-operator (cluster-scoped)
- kuberay-operator-configmap → ClusterRole → SA: kuberay-operator (cluster-scoped)
- kuberay-operator-leader-election → Role → SA: kuberay-operator (operator namespace)
- {cluster}-autoscaler → Role → SA: {cluster}-autoscaler-sa (per-cluster namespace)

Secrets:
- kuberay-webhook-server-cert (kubernetes.io/tls)
  - Purpose: Webhook server TLS
  - Provisioned by: OpenShift serving-cert-secret
  - Auto-rotate: Yes

- {cluster}-oauth-tls / {cluster}-oidc-tls (Opaque, self-signed)
  - Purpose: Auth proxy TLS (RSA 2048, 365 days)
  - Provisioned by: KubeRay operator
  - Auto-rotate: No

- {cluster}-ca-secret-{uid} (kubernetes.io/tls)
  - Purpose: CA certificate for mTLS (RSA 4096, 10 years)
  - Provisioned by: cert-manager
  - Auto-rotate: Yes (30 days before expiry)

- ray-head-secret-{cluster} / ray-worker-secret-{cluster} (kubernetes.io/tls)
  - Purpose: Node TLS certs for mTLS (90 days)
  - Provisioned by: cert-manager
  - Auto-rotate: Yes (15 days before expiry)

SecurityContextConstraints:
- SCC: run-as-ray-user (OpenShift-specific)
  - Applied to Ray pods for dedicated user execution

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1
- Runtime: stdlib crypto/tls via OpenSSL (UBI9 base)
- No non-FIPS crypto detected in production code
