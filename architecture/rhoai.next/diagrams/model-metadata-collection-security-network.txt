┌─────────────────────────────────────────────────────────────────────────────┐
│  CI/CD BUILD ENVIRONMENT (Build-Time Only)                                  │
│                                                                             │
│  [model-extractor] (Go CLI binary — NOT shipped in final image)             │
│  - Runs: CI/CD pipeline (Konflux or GitHub Actions)                         │
│  - User: Build process                                                      │
│  - Binary: CGO_ENABLED=0 (static, pure-Go crypto)                           │
│         │                    │                    │                          │
│         │                    │                    │                          │
│         │ Port: 443/TCP      │ Port: 443/TCP      │ Port: 443/TCP           │
│         │ Proto: HTTPS       │ Proto: HTTPS       │ Proto: HTTPS            │
│         │ Enc: TLS 1.2+      │ Enc: TLS 1.2+      │ Enc: TLS 1.2+          │
│         │ Auth: Bearer Token │ Auth: None          │ Auth: None              │
│         │ (HF_TOKEN, opt.)   │ (public registry)   │ (public registry)      │
│         ▼                    ▼                    ▼                          │
├─────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Outbound Only — Build-Time)                             │
│                                                                             │
│  [HuggingFace API — huggingface.co]                                         │
│  - Purpose: Fetch model details, README, collections, validated lists       │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS (REST API)                                               │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: Bearer Token (HF_TOKEN) — optional for public data                 │
│                                                                             │
│  [OCI Registry — registry.redhat.io]                                        │
│  - Purpose: Fetch model container image manifests, archs, timestamps        │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS (OCI Registry API v2)                                    │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: None (public read access)                                          │
│                                                                             │
│  [OCI Registry — registry.access.redhat.com]                                │
│  - Purpose: Fetch MCP server container image metadata                       │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS (OCI Registry API v2)                                    │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: None (public read access)                                          │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  CONTAINER BUILD & PUSH (Build-Time)                                        │
│                                                                             │
│  [Konflux Pipeline]                                                         │
│  - Purpose: Build hermetic multi-arch container image                       │
│  - Hermetic: true (hermetic build with gomod prefetch)                      │
│  - Architectures: x86_64, ppc64le, s390x, arm64                            │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: Quay credentials (QUAY_USERNAME/QUAY_PASSWORD)              │
│         ▼                                                                   │
│  [quay.io/rhoai/odh-model-metadata-collection]                              │
│  - Container registry for RHOAI distribution                                │
│                                                                             │
│  [GitHub Actions] (ODH upstream)                                            │
│  - Purpose: Build and push upstream container image                         │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Auth: Quay credentials                                            │
│         ▼                                                                   │
│  [quay.io/opendatahub/odh-model-metadata-collection]                        │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  RUNTIME (On-Cluster — Zero Network Surface)                                │
│                                                                             │
│  [odh-model-metadata-collection Container]                                  │
│  - Base Image: UBI9-minimal                                                 │
│  - User: UID 1001 (non-root)                                               │
│  - Process: sleep infinity (data-only, no binary)                           │
│  - Ingress: NONE                                                            │
│  - Egress: NONE                                                             │
│  - Network Listeners: NONE                                                  │
│  - Kubernetes API Access: NONE                                              │
│  - Volumes Exposed: /app/data (YAML catalog files)                         │
│  -                  /app/benchmarks                                         │
│         │                                                                   │
│         │ Interface: Shared volume mount                                     │
│         │ Protocol: Filesystem (no network)                                  │
│         │ Auth: Pod-level RBAC                                              │
│         ▼                                                                   │
│  [RHOAI Dashboard Pod]                                                      │
│  - Purpose: Reads catalog YAML files to populate model catalog UI           │
│  - Access: Volume mount to /app/data                                        │
│                                                                             │
│  [rhods-operator]                                                           │
│  - Purpose: Deploys data container alongside consumers                      │
│  - Access: Kubernetes API (deployment lifecycle)                            │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- Container runs as UID 1001 (non-root)
- No Kubernetes API access from data container
- No ServiceAccount privileges required

Secrets:
- HF_TOKEN (env var) — HuggingFace API auth, manual provisioning, no auto-rotate
  * BUILD-TIME ONLY — not present in shipped container
- git_auth_secret (Opaque/Tekton) — Git checkout in Konflux pipeline, auto-rotate via PaC
  * BUILD-TIME ONLY
- QUAY_USERNAME/QUAY_PASSWORD (Opaque/GitHub) — Registry push creds, manual, no auto-rotate
  * BUILD-TIME ONLY

Security Notes:
- ZERO runtime attack surface — shipped container has no binary, no network listeners,
  no egress, no ingress, no Kubernetes API access
- All external network communication occurs ONLY during CI/CD build time
- Go binary uses pure-Go crypto (CGO_ENABLED=0) — not FIPS-compliant, but binary is
  NOT shipped in the final image
- Data container is UBI9-minimal base with OpenSSL available (inherited from base)
- Hermetic build (Konflux) ensures reproducible, isolated builds
