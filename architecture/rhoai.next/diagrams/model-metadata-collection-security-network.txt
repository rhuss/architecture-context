┌─────────────────────────────────────────────────────────────────────────┐
│  BUILD-TIME ENVIRONMENT (CI/CD Pipeline - Trusted)                      │
│                                                                          │
│  [model-extractor CLI]                                                   │
│  - Language: Go 1.24                                                     │
│  - Runs: Build-time only (CI/CD, developer workstation)                  │
│  - User: Build pipeline user                                             │
│  - Not shipped in final container image                                  │
│         │                          │                                     │
│         │                          │                                     │
│         │ Port: 443/TCP            │ Port: 443/TCP                       │
│         │ Protocol: HTTPS          │ Protocol: HTTPS                     │
│         │ Encryption: TLS 1.2+     │ Encryption: TLS 1.2+               │
│         │ Auth: Bearer Token       │ Auth: None (public registry)        │
│         │   (HF_TOKEN, optional)   │                                     │
│         ▼                          ▼                                     │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Untrusted)                                           │
│                                                                          │
│  [HuggingFace API - huggingface.co]                                      │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS (REST API)                                            │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Bearer Token (optional, enables gated model access)             │
│  - Endpoints:                                                            │
│    - GET /api/collections/RedHatAI (collection discovery)                │
│    - GET /api/models/{id} (model metadata)                               │
│    - GET /{id}/raw/main/README.md (YAML frontmatter)                     │
│                                                                          │
│  [OCI Container Registry - registry.redhat.io]                           │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS (OCI Distribution Spec v2)                            │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: None (public registry)                                          │
│  - Operations:                                                           │
│    - GET /v2/{repo}/manifests/{ref} (image manifests)                    │
│    - GET /v2/{repo}/blobs/{digest} (layer blobs, config blobs)           │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  BUILD OUTPUT (Artifacts)                                                │
│                                                                          │
│  [Local Filesystem]                                                      │
│  - output/{model-name}/ → Extracted metadata per model                   │
│  - data/models-catalog.yaml → Full models catalog                        │
│  - data/validated-models-catalog.yaml → Validated models subset          │
│  - data/redhat-mcp-servers-catalog.yaml → MCP servers catalog            │
│  - data/redhat-mcp-servers-index.yaml → MCP index with path validation   │
│    (filepath.Clean, rejects absolute/.. paths)                           │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  CONTAINER BUILD (Konflux / GitHub Actions)                              │
│                                                                          │
│  [Dockerfile.konflux Build]                                              │
│  - Base: registry.access.redhat.com/ubi9-minimal                         │
│  - Individual file COPY (no globs - precise control)                     │
│  - Non-root user: UID 1001                                               │
│  - CMD: ["sleep", "infinity"]                                            │
│  - No Go binaries shipped (data-only container)                          │
│         │                                                                │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  RUNTIME ENVIRONMENT (Kubernetes Cluster)                                │
│                                                                          │
│  [odh-model-metadata-collection Container]                               │
│  - Image: managed-open-data-hub/odh-model-metadata-collection-rhel9      │
│  - User: 1001 (non-root)                                                 │
│  - Network: No listeners, no egress                                      │
│  - Volumes exposed:                                                      │
│    - /app/data/ → Catalog YAML files                                     │
│    - /app/benchmarks/ → Sample benchmark data                            │
│  - CMD: sleep infinity (keeps container alive for volume mounts)          │
│         │                                                                │
│         │ Access: Volume Mount                                           │
│         │ Protocol: Filesystem (in-pod or init container)                │
│         │ Encryption: N/A (local volume)                                 │
│         │ Auth: N/A (Kubernetes pod-level access)                        │
│         ▼                                                                │
│  [RHOAI Dashboard]                                                       │
│  - Reads: models-catalog.yaml, validated-models-catalog.yaml             │
│  - Reads: redhat-mcp-servers-catalog.yaml                                │
│  - Access pattern: Volume mount of /app/data/                            │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined
- No ClusterRoleBindings defined
- No Roles or RoleBindings defined
- Component operates as a passive data container with no Kubernetes API access

Security Controls:
- Container runs as non-root (UID 1001)
- No network listeners (no exposed ports)
- No runtime egress (all external API calls are build-time only)
- Dockerfile.konflux uses individual COPY statements (no globs)
- MCP index path validation: filepath.Clean + rejection of absolute/.. paths
- HuggingFace cross-family matching prevention (isCompatibleModelFamily)
- Legacy format migration handles schema evolution safely

Secrets:
- HF_TOKEN (Environment variable) - HuggingFace API authentication
  - Type: Bearer Token
  - Provisioned by: Manual (developer .env file)
  - Auto-rotate: No
  - Required: No (optional, enables gated model access)
  - Build-time only (never present in runtime container)

Service Mesh Configuration:
- N/A - Data container has no network services

Network Summary:
- Runtime ingress: None
- Runtime egress: None
- Build-time egress only:
  - huggingface.co:443/TCP (HTTPS, TLS 1.2+, Bearer Token optional)
  - registry.redhat.io:443/TCP (HTTPS, TLS 1.2+, no auth)
