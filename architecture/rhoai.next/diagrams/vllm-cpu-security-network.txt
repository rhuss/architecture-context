┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client]                                                       │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Bearer Token (optional)                                  │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (DMZ) - Managed by KServe                                      │
│                                                                          │
│  [KServe Ingress / HTTPRoute]                                            │
│  - Port: 443/TCP (external) → 8000/TCP (internal)                      │
│  - TLS Termination: TLS 1.2+ (SIMPLE mode)                             │
│  - Configured by: KServe InferenceService CRD                           │
│         │                                                                │
│         │ Port: 8000/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (TLS optional via --ssl-certfile)             │
│         │ Auth: Forwarded Bearer Token                                   │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  APPLICATION (Internal - ClusterIP Services)                             │
│                                                                          │
│  [vLLM-CPU API Server - vllm-openai Service]                             │
│  - Namespace: {user-namespace}                                           │
│  - Container: odh-vllm-cpu-rhel9                                         │
│  - Port: 8000/TCP (HTTP)                                                 │
│  - UID: 2000, GID: 0 (non-root, OpenShift compatible)                  │
│  - Auth: AuthenticationMiddleware                                        │
│    - API Key validation (VLLM_API_KEY / --api-key)                      │
│    - Skipped for: /health, /metrics, /ping, /version                    │
│  - TLS: Optional (--ssl-certfile / --ssl-keyfile)                       │
│    - Certificate hot-reload via SSLCertRefresher (watchfiles)            │
│  - CORS: Configurable (--allowed-origins, --allowed-methods)            │
│                                                                          │
│  API Endpoints (8000/TCP):                                               │
│    POST /v1/chat/completions    - Chat completion (streaming)            │
│    POST /v1/completions         - Text completion (streaming)            │
│    POST /v1/responses           - OpenAI Responses API (SSE)            │
│    POST /v1/messages            - Anthropic Messages API                 │
│    POST /v1/embeddings          - Text embedding generation              │
│    POST /v1/score               - Similarity scoring                     │
│    POST /v1/classify            - Text classification                    │
│    POST /v1/audio/transcriptions- Speech-to-text                         │
│    GET  /v1/models              - List models                            │
│    POST /tokenize               - Tokenization                           │
│    POST /detokenize             - Detokenization                         │
│    GET  /health                 - Health check (no auth)                 │
│    GET  /metrics                - Prometheus metrics (no auth)            │
│    GET  /version                - Version info (no auth)                  │
│    GET  /ping                   - Liveness probe (no auth)               │
│                                                                          │
│  [vLLM-CPU gRPC Server - vllm-grpc Service]                              │
│  - Port: 50051/TCP (gRPC)                                                │
│  - Encryption: plaintext (insecure - TLS NOT supported)                 │
│  - Auth: None                                                            │
│  - Service: VllmEngine (reflection enabled)                              │
│         │                                                                │
│         │ (in-process)                                                    │
│         ▼                                                                │
│  [AsyncLLM Engine (V1)]                                                  │
│  - PagedAttention, Continuous Batching, KV Cache                         │
│  - Multiprocessing: fork method                                          │
│  - VLLM_USE_V1=1 (always V1 engine)                                     │
│         │                                                                │
│         │ (in-process, Python + C/C++ native kernels)                    │
│         ▼                                                                │
│  [Model Executor]                                                        │
│  - CPU-optimized via OpenBLAS, numactl, intel-openmp                    │
│  - Architecture targets: ppc64le, s390x, x86_64                         │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS - External Services                                              │
│                                                                          │
│  [Hugging Face Hub - huggingface.co]                                     │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Bearer Token (HF_TOKEN env var)                                │
│  - Purpose: Download model weights and tokenizer files                   │
│                                                                          │
│  [S3-Compatible Storage]                                                 │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: AWS IAM credentials (S3_ACCESS_KEY_ID / S3_SECRET_ACCESS_KEY)  │
│  - Purpose: Load models via tensorizer from S3                           │
│                                                                          │
│  [vLLM Usage Stats - stats.vllm.ai]                                     │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: None                                                            │
│  - Purpose: Anonymous usage statistics                                   │
│  - Status: DISABLED (VLLM_NO_USAGE_STATS=1)                             │
│                                                                          │
│  [OpenTelemetry Collector]                                               │
│  - Port: configurable                                                    │
│  - Protocol: gRPC/HTTP                                                   │
│  - Encryption: TLS (configurable)                                        │
│  - Auth: None                                                            │
│  - Purpose: Distributed tracing export (optional, not on s390x)          │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

Secrets:
- HF_TOKEN (Opaque env var) - Hugging Face Hub auth, Manual provisioning, No auto-rotate
- S3_ACCESS_KEY_ID / S3_SECRET_ACCESS_KEY (Opaque env var) - S3 auth, Manual provisioning, No auto-rotate
- VLLM_API_KEY (Opaque env var) - Client API key auth, Manual provisioning, No auto-rotate
- ssl_keyfile / ssl_certfile (kubernetes.io/tls) - HTTPS TLS cert/key, cert-manager/Manual, Auto-rotate via --enable-ssl-refresh

RBAC Summary:
- No ClusterRoles defined (vLLM-CPU is a workload, not an operator)
- No RoleBindings defined
- Runs as UID 2000 / GID 0 (non-root, OpenShift compatible)
- Deployed via KServe ServingRuntime CRD (KServe manages RBAC)

Container Security:
- Base Image: UBI9-minimal (Red Hat Universal Base Image)
- User: 2000 (non-root)
- Group: 0 (root group for OpenShift compatibility)
- Usage Stats: Disabled (VLLM_NO_USAGE_STATS=1)
- Engine: V1 only (VLLM_USE_V1=1)
- Multiprocessing: fork method (VLLM_WORKER_MULTIPROC_METHOD=fork)

Network Exposure:
- Internal: ClusterIP services (8000/TCP HTTP, 50051/TCP gRPC)
- External: Via KServe Ingress only (443/TCP HTTPS, TLS 1.2+)
- gRPC: Internal only, NO TLS (insecure port)
