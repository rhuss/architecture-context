┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Client / Data Scientist / Application]                                 │
│         │                                                                │
│         │ Port: 8080/TCP (HTTP) or 8085/TCP (gRPC)                      │
│         │ Protocol: HTTP or gRPC                                         │
│         │ Encryption: None (plaintext in-cluster)                        │
│         │ Auth: Delegated to platform (kube-rbac-proxy / KServe /       │
│         │       Gateway API)                                             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  PLATFORM INGRESS (Managed by KServe / Gateway API)                     │
│                                                                          │
│  [KServe InferenceService / Gateway API / kube-rbac-proxy]              │
│  - TLS Termination: Platform-managed                                     │
│  - AuthN/AuthZ: Platform-level (Bearer token, mTLS, etc.)              │
│         │                                                                │
│         │ Port: 8080/TCP (HTTP) or 8085/TCP (gRPC)                      │
│         │ Protocol: HTTP or gRPC                                         │
│         │ Encryption: None (plaintext in-cluster)                        │
│         │ Auth: None (already validated at ingress)                      │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INTERNAL (Cluster Network - Trusted)                                    │
│                                                                          │
│  [caikit Runtime Container]                                              │
│  - Namespace: (user namespace, operator-configured)                      │
│  - ServiceAccount: (platform-managed)                                    │
│  - Image: ubi9/ubi-minimal based                                         │
│  - User: caikit (UID 1001, non-root)                                    │
│  - Env: RUNTIME_LIBRARY=caikit_nlp                                      │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │  caikit-nlp Library (in-process)                                │    │
│  │                                                                  │    │
│  │  [TextGeneration]     - Local HuggingFace inference              │    │
│  │  [TextGenerationTGIS] - Remote TGIS inference                    │    │
│  │  [PeftPromptTuning]   - Local training                           │    │
│  │  [PeftPromptTuningTGIS] - Remote TGIS inference                  │    │
│  │  [EmbeddingModule]    - Sentence embeddings                      │    │
│  │  [CrossEncoderModule] - Cross-encoder reranking                  │    │
│  │  [SequenceClassification] - Text classification                  │    │
│  │  [FilteredSpanClassification] - Span classification              │    │
│  │  [RegexSentenceSplitter] - Tokenization                          │    │
│  │  [TGISAutoFinder]     - Model discovery                          │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  Services Exposed:                                                       │
│  - ClusterIP: 8080/TCP (HTTP, plaintext, no auth)                       │
│  - ClusterIP: 8085/TCP (gRPC, plaintext, no auth)                       │
│         │                                                                │
│         │ EGRESS CONNECTIONS                                             │
│         │                                                                │
│         ├──── Port: configurable (default 8033/TCP)                      │
│         │     Protocol: gRPC (generation_pb2)                            │
│         │     Encryption: TLS (optional, configurable)                   │
│         │     Auth: mTLS (optional, configurable via cert files)         │
│         │     ▼                                                          │
│         │                                                                │
│         │  [TGIS Server]                                                 │
│         │  - Purpose: Remote text generation inference                   │
│         │  - TLS Config: ca_cert_file, client_cert_file, client_key_file│
│         │                                                                │
│         │                                                                │
│         └──── Port: 443/TCP                                              │
│               Protocol: HTTPS                                            │
│               Encryption: TLS 1.2+                                       │
│               Auth: Token (optional, HuggingFace API token)             │
│               ▼                                                          │
│                                                                          │
│            [HuggingFace Hub - huggingface.co]                            │
│            - Purpose: Model and tokenizer downloads                      │
│            - Condition: Only when allow_downloads=true                    │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  INTERNAL TRAINING TRAFFIC (Multi-GPU)                                   │
│                                                                          │
│  [caikit Runtime Pod] ←──── Port: 29550/TCP (configurable)              │
│                              Protocol: TCP (PyTorch Distributed)         │
│                              Encryption: None                            │
│                              Auth: None                                  │
│                              Purpose: Multi-GPU coordination (torchrun)  │
│                     ────→ [caikit Runtime Pod (other rank)]              │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No Kubernetes RBAC resources defined (caikit-nlp is a Python library)
- Auth/AuthZ delegated entirely to platform layer (KServe, Gateway API, kube-rbac-proxy)

Secrets:
- TGIS CA cert (File: ca_cert_file) - TLS verification for TGIS connections
  Provisioned by: Manual / platform operator | Auto-Rotate: No
- TGIS client cert (File: client_cert_file) - mTLS client authentication to TGIS
  Provisioned by: Manual / platform operator | Auto-Rotate: No
- TGIS client key (File: client_key_file) - mTLS client key for TGIS connections
  Provisioned by: Manual / platform operator | Auto-Rotate: No

Authentication & Authorization:
- HTTP/gRPC endpoints (All methods): Auth delegated to platform
  Enforcement: kube-rbac-proxy / KServe / Gateway API
- TGIS backend connection (gRPC): mTLS optional, configurable
  Enforcement: TGIS server, certificate-based

FIPS Compliance:
- Python library on UBI9 base image - inherits FIPS from base image OpenSSL
- No custom crypto operations detected
- TLS delegated to grpcio (system OpenSSL)

Container Security:
- Base image: ubi9/ubi-minimal
- Non-root user: caikit (UID 1001)
- No CMD set (designed as base image for ServingRuntime)
