┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Client Application / Data Scientist]                                   │
│         │                                                                │
│         │ Port: 8080/TCP (HTTP) or 8085/TCP (gRPC)                      │
│         │ Protocol: HTTP or gRPC                                         │
│         │ Encryption: None (plaintext, in-cluster only)                  │
│         │ Auth: Delegated to platform (kube-rbac-proxy / KServe /        │
│         │       Gateway API)                                             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  PLATFORM INGRESS (Managed by KServe / Gateway API)                     │
│                                                                          │
│  [kube-rbac-proxy / KServe InferenceService / Gateway API]              │
│  - TLS termination and authentication handled at platform level          │
│  - Not defined by caikit-nlp                                             │
│         │                                                                │
│         │ Port: 8080/TCP (HTTP) or 8085/TCP (gRPC)                      │
│         │ Protocol: HTTP or gRPC                                         │
│         │ Encryption: None (plaintext within pod/cluster)                │
│         │ Auth: Platform-verified identity                               │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  CAIKIT RUNTIME (In-Cluster - ClusterIP Service)                        │
│                                                                          │
│  [caikit Runtime Container]                                              │
│  - Base Image: ubi9/ubi-minimal                                          │
│  - User: caikit (UID 1001, non-root)                                    │
│  - Env: RUNTIME_LIBRARY=caikit_nlp                                      │
│  - Namespace: {user-namespace} (managed by platform operator)            │
│  - ServiceAccount: (platform-assigned)                                   │
│                                                                          │
│  Listening Ports:                                                        │
│    ├── 8080/TCP - HTTP API (auto-generated task endpoints)               │
│    │   ├── POST /api/v1/task/text-generation                             │
│    │   ├── POST /api/v1/task/server-streaming-text-generation            │
│    │   ├── POST /api/v1/task/embedding                                   │
│    │   ├── POST /api/v1/task/embeddings                                  │
│    │   ├── POST /api/v1/task/sentence-similarity                         │
│    │   ├── POST /api/v1/task/sentence-similarities                       │
│    │   ├── POST /api/v1/task/rerank                                      │
│    │   ├── POST /api/v1/task/reranks                                     │
│    │   ├── POST /api/v1/task/text-classification                         │
│    │   └── POST /api/v1/task/tokenization                                │
│    └── 8085/TCP - gRPC API                                               │
│        ├── caikit.runtime.Nlp.NlpService (all NLP tasks)                 │
│        ├── grpc.health.v1.Health (health checking)                       │
│        └── grpc.reflection.v1alpha.ServerReflection (discovery)          │
│                                                                          │
│  [caikit-nlp Library Modules - In-Process]                               │
│  - TextGeneration (local HuggingFace inference)                          │
│  - TextGenerationTGIS (remote TGIS delegation)                           │
│  - PeftPromptTuning (local training)                                     │
│  - PeftPromptTuningTGIS (remote TGIS inference)                          │
│  - EmbeddingModule (sentence-transformers)                               │
│  - CrossEncoderModule (cross-encoder reranking)                          │
│  - SequenceClassification, FilteredSpanClassification                    │
│  - RegexSentenceSplitter, TGISAutoFinder                                 │
│         │                                                                │
│         │ EGRESS CONNECTIONS:                                            │
│         │                                                                │
│         ├──── Port: configurable (default 8033/TCP)                      │
│         │     Protocol: gRPC (generation_pb2)                            │
│         │     Encryption: TLS (configurable via ca_cert_file)            │
│         │     Auth: mTLS (optional, via client_cert_file, client_key_file)│
│         │     Destination: TGIS Server                                   │
│         │                                                                │
│         └──── Port: 443/TCP                                              │
│               Protocol: HTTPS                                            │
│               Encryption: TLS 1.2+                                       │
│               Auth: Token (optional, HF_TOKEN)                           │
│               Destination: HuggingFace Hub                               │
│               Condition: Only when allow_downloads=true                   │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INTERNAL SERVICES                                                       │
│                                                                          │
│  [TGIS - Text Generation Inference Server]                               │
│  - Port: configurable (default 8033/TCP)                                 │
│  - Protocol: gRPC                                                        │
│  - Encryption: TLS (configurable)                                        │
│  - Auth: mTLS (optional)                                                 │
│  - Purpose: Remote model inference for text generation, prompt tuning    │
│  - Connection: caikit-tgis-backend manages gRPC channel                  │
│                                                                          │
│  [PyTorch Distributed - torchrun]                                        │
│  - Port: 29550/TCP (configurable)                                        │
│  - Protocol: TCP                                                         │
│  - Encryption: None                                                      │
│  - Auth: None                                                            │
│  - Purpose: Multi-GPU coordination for fine-tuning via elastic_launch    │
│  - Scope: Inter-process within same pod / training job                   │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [HuggingFace Hub - huggingface.co]                                      │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: HuggingFace Token (optional)                                    │
│  - Purpose: Model and tokenizer downloads                                │
│  - Condition: Only when allow_downloads=true in config                   │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- None — caikit-nlp is a Python library and does not define Kubernetes RBAC resources
- RBAC for the runtime pod is managed by the platform operator (rhods-operator)

Service Mesh Configuration:
- Not directly configured by caikit-nlp
- Service mesh (Istio/OSSM) configuration is managed at platform level
- mTLS enforcement depends on platform PeerAuthentication policies

Secrets:
- TGIS CA cert (File: ca_cert_file) - TLS verification for TGIS connections
  - Provisioned by: Manual / platform operator
  - Auto-Rotate: No
- TGIS client cert (File: client_cert_file) - mTLS client authentication to TGIS
  - Provisioned by: Manual / platform operator
  - Auto-Rotate: No
- TGIS client key (File: client_key_file) - mTLS client key for TGIS connections
  - Provisioned by: Manual / platform operator
  - Auto-Rotate: No

Container Security:
- Base Image: ubi9/ubi-minimal (FIPS-compliant OpenSSL)
- User: caikit (UID 1001, non-root)
- No direct crypto operations — TLS delegated to grpcio (system OpenSSL)
- No custom cipher suites specified
- FIPS compliance inherited from base image

Build Hermeticity:
- ⚠ No lock files present on main branch (rpms.lock.yaml, pip lock, artifacts.lock.yaml)
- Expected: Downstream release branches (rhoai-X.Y) add lock files during Konflux hardening
- Supply chain gap at RPM, Python deps, and artifacts layers on upstream branch
