┌──────────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                            │
│                                                                                  │
│  [External Client]                                                               │
│         │                                                                        │
│         │ Port: 8321/TCP                                                         │
│         │ Protocol: HTTP                                                         │
│         │ Encryption: None (application-level; relies on pod/mesh TLS)           │
│         │ Auth: None (delegated to deploying operator / kube-rbac-proxy sidecar) │
│         ▼                                                                        │
├──────────────────────────────────────────────────────────────────────────────────┤
│  APPLICATION LAYER (Llama Stack Distribution)                                    │
│                                                                                  │
│  [Llama Stack Server]                                                            │
│  - Namespace: {deployment-namespace}                                             │
│  - Service: llama-stack (ClusterIP)                                              │
│  - Port: 8321/TCP (HTTP)                                                         │
│  - Image: quay.io/redhat-ai-dev/llama-stack (Konflux)                           │
│  -        quay.io/opendatahub/llama-stack (Community)                            │
│  - User: Non-root (CNB_USER_ID)                                                 │
│  - No Kubernetes API access (except LM Eval with use_k8s=true)                  │
│  - OpenTelemetry instrumented (optional)                                        │
│         │                                                                        │
│         ├──────────────────────────────────────────────────────                   │
│         │  INFERENCE BACKENDS (Egress)                                           │
│         │                                                                        │
│         ├─── vLLM Inference Server                                               │
│         │    Port: configurable/TCP                                              │
│         │    Protocol: HTTP/HTTPS                                                │
│         │    Encryption: Configurable (VLLM_TLS_VERIFY)                          │
│         │    Auth: Bearer Token (VLLM_API_TOKEN)                                 │
│         │                                                                        │
│         ├─── vLLM Embedding Server                                               │
│         │    Port: configurable/TCP                                              │
│         │    Protocol: HTTP/HTTPS                                                │
│         │    Encryption: Configurable (VLLM_EMBEDDING_TLS_VERIFY)               │
│         │    Auth: Bearer Token (VLLM_EMBEDDING_API_TOKEN)                       │
│         │                                                                        │
│         ├─── AWS Bedrock                                                         │
│         │    Port: 443/TCP                                                       │
│         │    Protocol: HTTPS                                                     │
│         │    Encryption: TLS 1.2+                                                │
│         │    Auth: Bearer Token (AWS_BEARER_TOKEN_BEDROCK, 12h auto-rotate)      │
│         │                                                                        │
│         ├─── IBM watsonx                                                         │
│         │    Port: 443/TCP                                                       │
│         │    Protocol: HTTPS                                                     │
│         │    Encryption: TLS 1.2+                                                │
│         │    Auth: API Key (WATSONX_API_KEY)                                     │
│         │                                                                        │
│         ├─── Azure OpenAI                                                        │
│         │    Port: 443/TCP                                                       │
│         │    Protocol: HTTPS                                                     │
│         │    Encryption: TLS 1.2+                                                │
│         │    Auth: API Key (AZURE_API_KEY)                                       │
│         │                                                                        │
│         ├─── Google Vertex AI                                                    │
│         │    Port: 443/TCP                                                       │
│         │    Protocol: HTTPS                                                     │
│         │    Encryption: TLS 1.2+                                                │
│         │    Auth: Google ADC / Service Account                                  │
│         │                                                                        │
│         ├─── OpenAI API                                                          │
│         │    Port: 443/TCP                                                       │
│         │    Protocol: HTTPS                                                     │
│         │    Encryption: TLS 1.2+                                                │
│         │    Auth: API Key (OPENAI_API_KEY)                                      │
│         │                                                                        │
│         ├──────────────────────────────────────────────────────                   │
│         │  VECTOR DATABASES (Egress)                                             │
│         │                                                                        │
│         ├─── Milvus (remote)                                                     │
│         │    Port: configurable/TCP                                              │
│         │    Protocol: HTTP/HTTPS                                                │
│         │    Encryption: Configurable (MILVUS_SECURE, mTLS client certs)         │
│         │    Auth: Token (MILVUS_TOKEN)                                          │
│         │    Cert Paths: MILVUS_CA_PEM_PATH, MILVUS_CLIENT_PEM_PATH             │
│         │                                                                        │
│         ├─── pgvector                                                            │
│         │    Port: configurable/TCP                                              │
│         │    Protocol: PostgreSQL wire protocol                                  │
│         │    Encryption: None (default)                                          │
│         │    Auth: Username/Password                                             │
│         │                                                                        │
│         ├─── Qdrant                                                              │
│         │    Port: 6333/TCP (HTTP), 6334/TCP (gRPC)                             │
│         │    Protocol: HTTP or gRPC                                              │
│         │    Encryption: Configurable (QDRANT_HTTPS)                             │
│         │    Auth: API Key (QDRANT_API_KEY)                                      │
│         │                                                                        │
│         ├──────────────────────────────────────────────────────                   │
│         │  SAFETY & EVALUATION (Egress)                                          │
│         │                                                                        │
│         ├─── TrustyAI FMS Orchestrator                                           │
│         │    Port: configurable/TCP                                              │
│         │    Protocol: HTTPS                                                     │
│         │    Encryption: TLS (custom cert via FMS_SSL_CERT_PATH)                 │
│         │    Auth: None                                                          │
│         │                                                                        │
│         ├─── Kubeflow Pipelines (RAGAS, Garak, LM Eval)                          │
│         │    Port: configurable/TCP                                              │
│         │    Protocol: HTTP/HTTPS                                                │
│         │    Encryption: TLS 1.2+                                                │
│         │    Auth: Bearer Token (KUBEFLOW_PIPELINES_TOKEN)                       │
│         │                                                                        │
│         ├──────────────────────────────────────────────────────                   │
│         │  TOOL RUNTIMES & EXTERNAL SERVICES (Egress)                            │
│         │                                                                        │
│         ├─── Brave Search API                                                    │
│         │    Port: 443/TCP                                                       │
│         │    Protocol: HTTPS                                                     │
│         │    Encryption: TLS 1.2+                                                │
│         │    Auth: API Key (BRAVE_SEARCH_API_KEY)                                │
│         │                                                                        │
│         ├─── Tavily Search API                                                   │
│         │    Port: 443/TCP                                                       │
│         │    Protocol: HTTPS                                                     │
│         │    Encryption: TLS 1.2+                                                │
│         │    Auth: API Key (TAVILY_SEARCH_API_KEY)                               │
│         │                                                                        │
│         ├─── HuggingFace Hub                                                     │
│         │    Port: 443/TCP                                                       │
│         │    Protocol: HTTPS                                                     │
│         │    Encryption: TLS 1.2+                                                │
│         │    Auth: None / Token                                                  │
│         │                                                                        │
│         ├─── S3-compatible Storage                                               │
│         │    Port: 443/TCP                                                       │
│         │    Protocol: HTTPS                                                     │
│         │    Encryption: TLS 1.2+                                                │
│         │    Auth: AWS credentials (AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY)    │
│         │                                                                        │
│         ├─── MCP Servers                                                         │
│         │    Port: configurable/TCP                                              │
│         │    Protocol: HTTP/HTTPS (REST/SSE)                                     │
│         │    Encryption: Configurable                                            │
│         │    Auth: Configurable                                                  │
│         │                                                                        │
│         ├──────────────────────────────────────────────────────                   │
│         │  PERSISTENCE & OBSERVABILITY (Egress)                                  │
│         │                                                                        │
│         ├─── PostgreSQL                                                          │
│         │    Port: 5432/TCP                                                      │
│         │    Protocol: PostgreSQL wire protocol                                  │
│         │    Encryption: None (default)                                          │
│         │    Auth: Username/Password (POSTGRES_PASSWORD)                         │
│         │    Purpose: KV store, inference logs, agent state, file metadata       │
│         │    Write Queue: max_write_queue_size=10000, num_writers=4              │
│         │                                                                        │
│         └─── OTLP Collector                                                      │
│              Port: configurable/TCP                                              │
│              Protocol: HTTP                                                      │
│              Encryption: None (default)                                          │
│              Auth: None                                                          │
│              Purpose: OpenTelemetry traces and metrics export                    │
│                                                                                  │
└──────────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC resources defined in this repository
- Runs as non-root user (CNB_USER_ID in Konflux build)
- No Kubernetes API access required (except LM Eval with use_k8s=true)
- Auth enforcement is delegated to deploying operator (kube-rbac-proxy sidecar or network policy)

Ingress Configuration:
- No ingress resources defined in this repository
- Ingress managed by deploying operator (RHOAI platform operator creates HTTPRoute/Route)

Authentication Model:
- No built-in authentication on any /v1/* endpoints
- Security boundary enforcement is fully delegated to:
  1. kube-rbac-proxy sidecar container (if deployed)
  2. NetworkPolicy restricting access to llama-stack Service
  3. Service mesh mTLS (if deployed within service mesh)
  4. OpenShift Route with OAuth proxy (if exposed externally)

Secrets (Environment Variables):
- VLLM_API_TOKEN ................. vLLM inference auth (Bearer Token)
- VLLM_EMBEDDING_API_TOKEN ....... vLLM embedding auth (Bearer Token)
- AWS_BEARER_TOKEN_BEDROCK ....... AWS Bedrock auth (12h auto-rotate via IAM)
- WATSONX_API_KEY ................ IBM watsonx API key
- AZURE_API_KEY .................. Azure OpenAI API key
- OPENAI_API_KEY ................. OpenAI API key
- MILVUS_TOKEN ................... Milvus auth token
- QDRANT_API_KEY ................. Qdrant API key
- POSTGRES_PASSWORD .............. PostgreSQL database password
- BRAVE_SEARCH_API_KEY ........... Brave Search API key
- TAVILY_SEARCH_API_KEY .......... Tavily Search API key
- AWS_ACCESS_KEY_ID .............. S3 storage access key
- AWS_SECRET_ACCESS_KEY .......... S3 storage secret key
- KUBEFLOW_PIPELINES_TOKEN ....... Kubeflow Pipelines bearer token
- BOT_PAT ....................... GitLab wheel registry auth (build-time only)

FIPS Compliance:
- Python application on UBI9 base image
- FIPS mode inherited from OS-level OpenSSL configuration
- No explicit FIPS opt-in needed at application level
- When RHEL FIPS mode enabled, Python ssl module auto-operates in FIPS mode
- No non-FIPS crypto risks detected

Build Supply Chain:
- Konflux (RHOAI): Pre-compiled wheels from AIPCC base, supply chain hardened
- Community (ODH): uv pip install from PyPI + git sources, pinned versions, no lock file
- No rpms.lock.yaml, uv.lock, or hermeto prefetch on main branch
