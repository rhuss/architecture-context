┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 80/TCP                                                       │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None (dev config)                                      │
│         │ Auth: None                                                         │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Gateway API)                                                       │
│                                                                              │
│  [inference-gateway]                                                         │
│  - Type: gateway.networking.k8s.io/v1 Gateway                               │
│  - Port: 80/TCP (external) → 8000/TCP (backendRef via HTTPRoute)            │
│  - TLS: None (dev config; production should use TLS)                        │
│  - Auth: None                                                               │
│                                                                              │
│  [${POOL_NAME}-inference-route]                                              │
│  - Type: HTTPRoute                                                           │
│  - BackendRef: InferencePool → Envoy routes to model servers via EPP        │
│         │                                                                    │
│         │ Port: 9002/TCP                                                     │
│         │ Protocol: gRPC (HTTP/2)                                            │
│         │ Encryption: Optional TLS (--secure-serving)                        │
│         │ Auth: None (Envoy-internal)                                        │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  EPP SERVICE (Cluster-Internal)                                              │
│                                                                              │
│  [EPP - Endpoint Picker]                                                     │
│  - Namespace: ${NAMESPACE}                                                   │
│  - ServiceAccount: ${EPP_NAME}                                               │
│  - Image: odh-llm-d-inference-scheduler                                     │
│  - Ports exposed:                                                            │
│    ├─ 9002/TCP  gRPC ext_proc     Optional TLS   No auth                    │
│    ├─ 9003/TCP  gRPC health       None           No auth                    │
│    ├─ 9090/TCP  HTTP metrics      None           Optional (TokenReview+SAR) │
│    └─ 5557/TCP  ZMQ (TCP)         None           No auth                    │
│                                                                              │
│  [UDS Tokenizer Sidecar]                                                     │
│  - Container: sidecar in EPP pod                                             │
│  - Communication: Unix Domain Socket                                         │
│    /tmp/tokenizer/tokenizer-uds.socket                                       │
│  - Protocol: gRPC over UDS                                                   │
│  - Auth: None (pod-local)                                                    │
│         │                                                                    │
│         │ (EPP egress connections)                                            │
│         │                                                                    │
│         ├──── Port: 443/TCP ──────────────────────────────────────────┐      │
│         │     Protocol: HTTPS                                         │      │
│         │     Encryption: TLS 1.2+                                    │      │
│         │     Auth: ServiceAccount token (in-cluster)                 │      │
│         │                                                             ▼      │
│         │     [Kubernetes API Server]                                        │
│         │     - Purpose: Watch InferencePool, Pod,                           │
│         │       InferenceObjective, InferenceModelRewrite CRs                │
│         │                                                                    │
│         ├──── Port: configurable/TCP ─────────────────────────────┐  │      │
│         │     Protocol: HTTP/HTTPS                                 │  │      │
│         │     Encryption: Optional TLS                             │  │      │
│         │     Auth: None                                           │  │      │
│         │     ⚠ InsecureSkipVerify=true by default                │  │      │
│         │                                                          ▼  │      │
│         │     [vLLM Model Server Pods - Metrics]                          │
│         │     - Purpose: Scrape Prometheus metrics                           │
│         │       (queue depth, KV cache %, LoRA info)                        │
│         │                                                                    │
│         └──── Port: 5556/TCP ─────────────────────────────────────┐  │      │
│               Protocol: ZMQ PUB/SUB                                │  │      │
│               Encryption: None (plaintext)                         │  │      │
│               Auth: None                                           │  │      │
│                                                                    ▼  │      │
│               [vLLM Model Server Pods - KV Events]                       │
│               - Purpose: Real-time KV cache block events                    │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  P/D SIDECAR (Pod-Local, on Model Server Pods)                               │
│                                                                              │
│  [P/D Routing Sidecar]                                                       │
│  - Image: odh-llm-d-routing-sidecar                                        │
│  - Port: 8000/TCP                                                            │
│  - Protocol: HTTP/HTTPS                                                      │
│  - Encryption: TLS 1.2+ (self-signed or provided)                           │
│    - Cipher suites: ECDHE+AES-GCM, CHACHA20-POLY1305                       │
│  - Auth: None (SSRF protection via InferencePool allowlist)                 │
│         │                                                                    │
│         ├──── Port: configurable/TCP ──── [Prefiller Pod]                   │
│         │     Protocol: HTTP/HTTPS                                           │
│         │     Encryption: Optional TLS (--enable-tls prefiller)             │
│         │     Auth: None (SSRF validated against pod allowlist)              │
│         │                                                                    │
│         ├──── Port: 8001/TCP ──── [Local Decoder (vLLM)]                    │
│         │     Protocol: HTTP/HTTPS                                           │
│         │     Encryption: Optional TLS (--enable-tls decoder)               │
│         │     Auth: None                                                     │
│         │                                                                    │
│         ├──── Port: configurable/TCP ──── [Encoder Pod] (E/P/D only)        │
│         │     Protocol: HTTP/HTTPS                                           │
│         │     Encryption: Optional TLS (--enable-tls encoder)               │
│         │     Auth: None                                                     │
│         │                                                                    │
│         ├──── Port: 443/TCP ──── [Kubernetes API Server]                    │
│         │     Protocol: HTTPS                                                │
│         │     Encryption: TLS 1.2+                                           │
│         │     Auth: ServiceAccount token                                     │
│         │     Purpose: Watch InferencePool for SSRF allowlist               │
│         │                                                                    │
│         └──── Port: 8998/TCP ──── [SGLang Bootstrap] (SGLang only)          │
│               Protocol: TCP                                                  │
│               Auth: None                                                     │
│               Purpose: Room-based P/D coordination                           │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  MONITORING (Cluster-Internal)                                               │
│                                                                              │
│  [Prometheus / ServiceMonitor]                                               │
│  - Scrapes EPP metrics: 9090/TCP HTTP (15s interval)                        │
│  - No encryption, optional auth                                             │
│                                                                              │
│  [OpenTelemetry Collector] (Optional)                                        │
│  - Receives traces: 4317/TCP gRPC OTLP                                      │
│  - Optional TLS                                                              │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- Role: ${EPP_NAME}
  Namespace: ${NAMESPACE}
  ServiceAccount: ${EPP_NAME}

  API Groups & Resources:
  ├── inference.networking.x-k8s.io
  │   ├── inferencepools: get, watch, list
  │   ├── inferenceobjectives: get, watch, list
  │   └── inferencemodelrewrites: get, watch, list
  ├── inference.networking.k8s.io
  │   └── inferencepools: get, watch, list
  ├── "" (core)
  │   └── pods: get, watch, list
  ├── authentication.k8s.io
  │   └── tokenreviews: create
  └── authorization.k8s.io
      └── subjectaccessreviews: create

Secrets:
- TLS certs (--cert-path)
  Type: kubernetes.io/tls
  Purpose: TLS for ext_proc gRPC server (EPP)
  Provisioned By: Manual or cert-manager
  Auto-Rotate: Yes (fsnotify reload with 250ms debounce)

- Sidecar TLS certs
  Type: kubernetes.io/tls
  Purpose: TLS for sidecar HTTP server
  Provisioned By: Self-signed (auto-generated) or manual
  Auto-Rotate: Yes (fsnotify reload)

Authentication & Authorization:
- /metrics (EPP): Optional TokenReview + SubjectAccessReview (--metrics-endpoint-auth flag)
- ext_proc gRPC (EPP): None - Envoy-internal, relies on network isolation
- /v1/* (Sidecar): None - SSRF protection via InferencePool-based pod allowlist

Security Warnings:
⚠ InsecureSkipVerify=true by default for model server metrics scraping
⚠ InsecureSkipVerify=true by default for HTTP data source connections
⚠ EPP Dockerfile omits -tags strictfipsruntime (sidecar includes it)
  → Asymmetry in FIPS posture between EPP and sidecar
⚠ ZMQ connections (5556/TCP, 5557/TCP) are unencrypted plaintext
⚠ Gateway dev config uses HTTP/80 without TLS

FIPS Compliance:
- EPP: CGO_ENABLED=1, dynamic linking, NO strictfipsruntime tag
- Sidecar: CGO_ENABLED=1, dynamic linking, strictfipsruntime tag enabled
- Base image: UBI 9 Minimal (OpenSSL available)
- Sidecar TLS: ECDHE+AES-GCM, CHACHA20-POLY1305, TLS 1.2 minimum
