┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [User / DSP Operator]                                                   │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS (Kubernetes API)                               │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: ServiceAccount Bearer Token                              │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE                                                │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS (REST)                                                │
│  - Encryption: TLS 1.2+                                                  │
│  - AuthN: ServiceAccount tokens (Bearer)                                 │
│  - AuthZ: RBAC (argo-cluster-role)                                       │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS (client-go informers)                          │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: ServiceAccount Bearer Token                              │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  ARGO WORKFLOWS NAMESPACE (Controller Plane)                            │
│                                                                          │
│  [workflow-controller]                                                   │
│  - Namespace: {controller-namespace}                                     │
│  - ServiceAccount: argo                                                  │
│  - Image: odh-data-science-pipelines-argo-workflowcontroller             │
│  - FIPS: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1                   │
│  - Replicas: 1 (leader election via Lease API)                           │
│  - Port 9090/TCP: Prometheus metrics (HTTP, no encryption, no auth)      │
│  - Port 6060/TCP: Healthz probe (HTTP, no encryption, no auth)           │
│         │                                                                │
│         │ (creates workflow pods in user namespaces)                      │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  USER NAMESPACE (Workflow Execution Plane)                               │
│                                                                          │
│  [Workflow Pod]                                                          │
│  - Namespace: {user-namespace}                                           │
│  - ServiceAccount: {workflow-sa}                                         │
│                                                                          │
│    ┌──────────────────────────┐   ┌──────────────────────────┐          │
│    │ argoexec (init/sidecar)  │   │ Main Container           │          │
│    │ Image: odh-...-argoexec  │   │ (user workload)          │          │
│    │ FIPS: see note below     │   │                          │          │
│    │                          │◄─►│ Shared: /var/run/argo/   │          │
│    │ Emissary pattern:        │   │ (emptyDir volume)        │          │
│    │ - stdout/stderr capture  │   └──────────────────────────┘          │
│    │ - artifact I/O           │                                          │
│    │ - param extraction       │                                          │
│    └──────────┬───────────────┘                                          │
│               │                                                          │
│               │ Port: 443/TCP                                            │
│               │ Protocol: S3 API (HTTPS)                                 │
│               │ Encryption: TLS 1.2+                                     │
│               │ Auth: AccessKey/SecretKey from K8s Secret                 │
│               ▼                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [S3 / Minio - Artifact Storage]                                         │
│  - Port: 443/TCP                                                         │
│  - Protocol: S3 API (HTTPS)                                              │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: AccessKey/SecretKey or IAM Role                                 │
│  - Purpose: Pipeline artifact upload/download                            │
│                                                                          │
│  [PostgreSQL - Workflow Archive] (Optional)                              │
│  - Port: 5432/TCP                                                        │
│  - Protocol: PostgreSQL wire protocol                                    │
│  - Encryption: TLS (configurable: disable to verify-full)                │
│  - Auth: Secret-based username/password                                  │
│  - Purpose: Workflow archival and node status offloading                  │
│                                                                          │
│  [MySQL - Workflow Archive] (Optional, alternative)                      │
│  - Port: 3306/TCP                                                        │
│  - Protocol: MySQL wire protocol                                         │
│  - Encryption: TLS (configurable)                                        │
│  - Auth: Secret-based username/password                                  │
│  - Purpose: Alternative to PostgreSQL for archival                       │
│                                                                          │
│  [GCS - Google Cloud Storage] (Optional)                                 │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: ServiceAccountKey / Workload Identity                           │
│  - Purpose: Alternative artifact storage backend                         │
│                                                                          │
│  [Azure Blob Storage] (Optional)                                         │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: SharedKey / SAS / Managed Identity                              │
│  - Purpose: Alternative artifact storage backend                         │
│                                                                          │
│  [Git Repositories] (Optional)                                           │
│  - Port: 22/TCP (SSH) or 443/TCP (HTTPS)                                 │
│  - Protocol: SSH or HTTPS                                                │
│  - Encryption: TLS/SSH                                                   │
│  - Auth: SSH Key or BasicAuth from K8s Secret                            │
│  - Purpose: Git-based artifact cloning                                   │
│                                                                          │
│  [HDFS NameNodes] (Optional)                                             │
│  - Port: 9000/TCP                                                        │
│  - Protocol: Hadoop RPC                                                  │
│  - Encryption: Configurable                                              │
│  - Auth: Kerberos or Basic                                               │
│  - Purpose: HDFS artifact operations                                     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: argo-cluster-role
  - API Groups: "", argoproj.io, policy
  - Resources: pods, pods/exec, configmaps, persistentvolumeclaims,
               workflows, workflowtasksets, workflowartifactgctasks,
               workflowtemplates, clusterworkflowtemplates,
               workflowtaskresults, cronworkflows, serviceaccounts,
               events, poddisruptionbudgets, secrets (restricted)
  - Verbs: get, list, watch, create, update, patch, delete,
           deletecollection (taskresults only)
  - Secret access restricted to: argo-workflows-agent-ca-certificates

- ClusterRole: argo-server-cluster-role (upstream only, not in RHOAI)
  - API Groups: "", argoproj.io
  - Resources: configmaps, secrets, pods, pods/exec, pods/log, events,
               serviceaccounts, eventsources, sensors, workflows,
               workfloweventbindings, workflowtemplates, cronworkflows,
               clusterworkflowtemplates
  - Verbs: create, get, list, watch, update, patch, delete

- ClusterRoleBinding: argo-binding
  - Role: argo-cluster-role
  - ServiceAccount: argo (namespace: argo)

FIPS Compliance:
- workflow-controller: Single FIPS binary
  - GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1
  - Dynamic linking with OpenSSL via UBI9
- argoexec: DUAL BINARY BUILD (FIPS gap)
  - argoexec-fips: CGO_ENABLED=1, strictfipsruntime
  - argoexec: CGO_ENABLED=0 (static, NON-FIPS)
  - WARNING: Entrypoint defaults to non-FIPS binary
- Base image: UBI9 ubi-minimal (OpenSSL included)

Secrets:
- argo-workflows-agent-ca-certificates (Opaque)
  - CA certs for agent executor HTTPS, Manual provisioning, No auto-rotate
- TLS certificate secret (kubernetes.io/tls)
  - TLS certs for argo-server (upstream only), Manual or cert-manager, No auto-rotate
- DB credentials (Opaque)
  - PostgreSQL/MySQL username/password, Manual provisioning, No auto-rotate
- Artifact credentials (Opaque)
  - S3/GCS/Azure/Git credentials, Manual provisioning, No auto-rotate

Metrics Exposure:
- Port 9090/TCP: HTTP, no encryption, no authentication
  - Intended for Prometheus scraping within cluster
- Port 6060/TCP: HTTP, no encryption, no authentication
  - Liveness probe only
