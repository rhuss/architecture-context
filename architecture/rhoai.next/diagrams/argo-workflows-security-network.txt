┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [User / DSP Operator]                                                      │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS (Kubernetes API)                                  │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: ServiceAccount Bearer Token                                 │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE                                                   │
│                                                                             │
│  [Kubernetes API Server]                                                    │
│  - Port: 443/TCP (HTTPS)                                                   │
│  - Encryption: TLS 1.2+                                                    │
│  - AuthN: ServiceAccount tokens (Bearer)                                   │
│  - AuthZ: RBAC (argo-cluster-role ClusterRole)                             │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS (client-go informers)                             │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: ServiceAccount Bearer Token                                 │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  CONTROLLER NAMESPACE (e.g., dspa namespace)                                │
│                                                                             │
│  [workflow-controller]                                                      │
│  - Deployment: single replica, leader election via Lease API                │
│  - ServiceAccount: argo                                                     │
│  - Image: odh-data-science-pipelines-argo-workflowcontroller               │
│  - FIPS: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1                     │
│  - Ports exposed:                                                           │
│    ┌──────────────────────────────────────────────────────────────┐         │
│    │ 9090/TCP  HTTP  No encryption  No auth  Prometheus metrics  │         │
│    │ 6060/TCP  HTTP  No encryption  No auth  Liveness probe      │         │
│    └──────────────────────────────────────────────────────────────┘         │
│         │                                                                   │
│         │ (Creates workflow pods with argoexec sidecar)                     │
│         ▼                                                                   │
│                                                                             │
│  [Workflow Pod] (per workflow step)                                         │
│  - Namespace: user/pipeline namespace                                       │
│  - ServiceAccount: workflow-specific SA                                     │
│  - Contains: main container + argoexec sidecar                             │
│  - Shared volume: /var/run/argo/ (emptyDir, emissary pattern)              │
│                                                                             │
│    ┌─────────────────────────────────────┐                                  │
│    │ [argoexec]                          │                                  │
│    │ - Image: odh-data-science-pipelines-│                                  │
│    │   argo-argoexec                     │                                  │
│    │ - FIPS: Two binaries built:         │                                  │
│    │   * argoexec-fips (CGO_ENABLED=1)   │                                  │
│    │   * argoexec (CGO_ENABLED=0, static)│                                  │
│    │ - ⚠ Default entrypoint: non-FIPS   │                                  │
│    │ - No network listener ports         │                                  │
│    └─────────────────────────────────────┘                                  │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS (S3 API)                                          │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: AccessKey/SecretKey from K8s Secret                         │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  ARTIFACT STORAGE                                                           │
│                                                                             │
│  [S3 / Minio - Primary for RHOAI]                                          │
│  - Port: 443/TCP                                                            │
│  - Protocol: HTTPS (S3 API)                                                 │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: AccessKey/SecretKey                                                │
│  - Purpose: Pipeline artifact upload/download                               │
│                                                                             │
│  [GCS - google cloud storage] (alternative)                                 │
│  - Port: 443/TCP  HTTPS  TLS 1.2+  ServiceAccountKey/Workload Identity    │
│                                                                             │
│  [Azure Blob Storage] (alternative)                                         │
│  - Port: 443/TCP  HTTPS  TLS 1.2+  SharedKey/SAS/Managed Identity         │
│                                                                             │
│  [HDFS] (alternative)                                                       │
│  - Port: 9000/TCP  Hadoop RPC  Configurable  Kerberos/Basic               │
│                                                                             │
│  [Git Repositories] (alternative)                                           │
│  - Port: 22/TCP (SSH) or 443/TCP (HTTPS)  SSH Key/BasicAuth               │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  DATABASE (Optional)                                                        │
│                                                                             │
│  [PostgreSQL]                                                               │
│  - Port: 5432/TCP                                                           │
│  - Protocol: PostgreSQL wire protocol                                       │
│  - Encryption: TLS (configurable: disable to verify-full)                  │
│  - Auth: Secret-based username/password                                     │
│  - Purpose: Workflow archive and node status offload                        │
│                                                                             │
│  [MySQL] (alternative)                                                      │
│  - Port: 3306/TCP                                                           │
│  - Protocol: MySQL wire protocol                                            │
│  - Encryption: TLS (configurable)                                           │
│  - Auth: Secret-based username/password                                     │
│  - Purpose: Workflow archive and node status offload                        │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  MONITORING                                                                 │
│                                                                             │
│  [Prometheus]                                                               │
│  - Scrapes: 9090/TCP (workflow-controller metrics)                         │
│  - Protocol: HTTP                                                           │
│  - Encryption: None                                                         │
│  - Auth: None                                                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: argo-cluster-role
  - API Groups: "", argoproj.io, policy
  - Resources: pods, pods/exec, configmaps, PVCs, workflows, workflows/finalizers,
    workflowtasksets, workflowtasksets/finalizers, workflowartifactgctasks,
    workflowtemplates, clusterworkflowtemplates, workflowtaskresults,
    cronworkflows, cronworkflows/finalizers, serviceaccounts, events, poddisruptionbudgets
  - Verbs: get, list, watch, create, update, patch, delete, deletecollection
  - Special: secrets GET restricted to resourceName "argo-workflows-agent-ca-certificates"
  - ServiceAccount: argo (ClusterRoleBinding: argo-binding)

- ClusterRole: argo-server-cluster-role (upstream only, not in RHOAI)
  - Resources: configmaps, secrets, pods, pods/exec, pods/log, events, serviceaccounts,
    eventsources, sensors, workflows, workfloweventbindings, workflowtemplates,
    cronworkflows, clusterworkflowtemplates
  - ServiceAccount: argo-server (ClusterRoleBinding: argo-server-binding)

Secrets:
- argo-workflows-agent-ca-certificates (Opaque) - CA certs for agent HTTPS, Manual, No rotation
- TLS certificate secret (kubernetes.io/tls) - argo-server HTTPS certs, Manual/cert-manager, No
- DB credentials (Opaque) - PostgreSQL/MySQL username/password, Manual, No rotation
- Artifact credentials (Opaque) - S3/GCS/Azure/Git credentials, Manual, No rotation

FIPS Compliance:
- workflow-controller: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1 (single FIPS binary)
- argoexec: Builds BOTH FIPS (argoexec-fips) and non-FIPS (argoexec) binaries
  ⚠ WARNING: Default entrypoint uses non-FIPS binary
- TLS: crypto/tls with configurable MinVersion (default TLS 1.2), TLS_MIN_VERSION env
- ⚠ InsecureSkipVerify used in some artifact HTTP clients

Build Hermeticity:
- OS packages: rpms.lock.yaml (present)
- Go deps: go.sum (present)
- UI deps: yarn.lock (present)
- Hermeto/cachi2 prefetch: NOT present (deps fetched during build)
