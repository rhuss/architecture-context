Security Network Diagram: modelmesh-runtime-adapter
Generated by: Claude Opus 4.6
Source: architecture/rhoai-2.19/modelmesh-runtime-adapter.md

┌─────────────────────────────────────────────────────────────────────────┐
│  MODELMESH SERVING POD (All internal comms are localhost/plaintext)      │
│                                                                          │
│  [ModelMesh Container]                                                   │
│  - Component: kserve/modelmesh                                           │
│         │                                                                │
│         │ Port: 8084/TCP                                                 │
│         │ Protocol: gRPC                                                 │
│         │ Encryption: plaintext (localhost)                               │
│         │ Auth: None                                                     │
│         ▼                                                                │
│  ┌─────────────────────────────────────────────────────────────────┐     │
│  │  SIDECAR: model-serving-puller                                  │     │
│  │  - Listen: 8084/TCP gRPC (ModelRuntime service)                │     │
│  │  - Image: odh-modelmesh-runtime-adapter-rhel8                  │     │
│  │  - Storage Config: /storage-config (mounted secret)            │     │
│  │  - Model Dir: /models                                          │     │
│  │                                                                 │     │
│  │  [pullman - Storage Provider Framework]                         │     │
│  │  - S3/IBM COS provider                                          │     │
│  │  - GCS provider                                                 │     │
│  │  - Azure Blob provider                                          │     │
│  │  - HTTP/HTTPS provider                                          │     │
│  │  - PVC provider                                                 │     │
│  └─────────────────────────────────────────────────────────────────┘     │
│         │                                                                │
│         │ Port: 8085/TCP                                                 │
│         │ Protocol: gRPC                                                 │
│         │ Encryption: plaintext (localhost)                               │
│         │ Auth: None                                                     │
│         ▼                                                                │
│  ┌─────────────────────────────────────────────────────────────────┐     │
│  │  SIDECAR: Runtime Adapter (one of the following)                │     │
│  │  - Listen: 8085/TCP gRPC (ModelRuntime service)                │     │
│  │                                                                 │     │
│  │  [model-mesh-triton-adapter]    → gRPC 8001/TCP to Triton      │     │
│  │  [model-mesh-mlserver-adapter]  → gRPC 8001/TCP to MLServer    │     │
│  │  [model-mesh-ovms-adapter]      → HTTP 8001/TCP to OVMS        │     │
│  │  [model-mesh-torchserve-adapter]→ gRPC 7071/TCP mgmt           │     │
│  │                                   gRPC 7070/TCP inference       │     │
│  └─────────────────────────────────────────────────────────────────┘     │
│         │                                                                │
│         │ Port: 8001/TCP (Triton/MLServer/OVMS) or                      │
│         │       7071/TCP mgmt, 7070/TCP infer (TorchServe)              │
│         │ Protocol: gRPC or HTTP (OVMS)                                  │
│         │ Encryption: plaintext (localhost)                               │
│         │ Auth: None                                                     │
│         ▼                                                                │
│  ┌─────────────────────────────────────────────────────────────────┐     │
│  │  RUNTIME CONTAINER (one of the following)                       │     │
│  │                                                                 │     │
│  │  [Triton Inference Server]  - 8001/TCP gRPC                    │     │
│  │  [Seldon MLServer]          - 8001/TCP gRPC                    │     │
│  │  [OpenVINO Model Server]    - 8001/TCP HTTP REST               │     │
│  │  [TorchServe]               - 7071/TCP gRPC mgmt              │     │
│  │                               7070/TCP gRPC inference           │     │
│  └─────────────────────────────────────────────────────────────────┘     │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS (Outbound from Puller to External Storage)                       │
│                                                                          │
│  [S3 / IBM Cloud Object Storage]                                         │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Access Key ID + Secret Access Key                               │
│  - Purpose: Download model artifacts                                     │
│                                                                          │
│  [Google Cloud Storage]                                                  │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: GCP Service Account (private_key + client_email)               │
│  - Purpose: Download model artifacts                                     │
│                                                                          │
│  [Azure Blob Storage]                                                    │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Service Principal (client_id/secret/tenant_id)                 │
│         or Connection String                                             │
│  - Purpose: Download model artifacts                                     │
│                                                                          │
│  [HTTP/HTTPS Endpoints]                                                  │
│  - Port: 80/TCP or 443/TCP                                               │
│  - Protocol: HTTP or HTTPS                                               │
│  - Encryption: Optional TLS (supports custom CA, client certs/mTLS)     │
│  - Auth: Custom HTTP headers                                             │
│  - Purpose: Download model artifacts                                     │
│                                                                          │
│  [PVC Mount - /pvc_mounts/<name>]                                        │
│  - Protocol: Local filesystem (symlink)                                  │
│  - Auth: Kubernetes PVC access modes                                     │
│  - Purpose: Model artifact access via mounted PVC                        │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  This component does not define any RBAC resources.
  It runs as a sidecar container and does not interact with the Kubernetes API server.

Service Mesh Configuration:
  N/A - All communication is pod-internal (localhost). No Service Mesh sidecar
  injection is required for this component's own communications.
  The parent ModelMesh Serving pod may have Service Mesh sidecar for
  external-facing ports.

Secrets:
  - storage-config (Opaque) - Mounted at /storage-config
    Contains storage credentials for all supported backends:
    * S3: access_key_id, secret_access_key, endpoint_url, region, bucket, certificate
    * GCS: private_key, client_email, token_uri
    * Azure: account_name, container, client_id, client_secret, tenant_id (or connection_string)
    * HTTP: url, headers, certificate, client_certificate, client_key
    * PVC: name (PVC name)
    Provisioned By: User/Admin
    Auto-Rotate: No

Network Notes:
  - This component has NO Kubernetes Services (sidecar pattern)
  - This component has NO Ingress resources
  - All internal communication is localhost-scoped within the pod
  - All internal communication is plaintext (no encryption needed for localhost)
  - External storage communication uses TLS 1.2+ with storage-specific credentials
  - No RBAC needed (no Kubernetes API interaction)
