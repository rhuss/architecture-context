Security Network Diagram: FMS Guardrails Orchestrator
Generated by: Claude Opus 4.6
Source: architecture/rhoai-2.19/fms-guardrails-orchestrator.md

┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CLIENTS (Platform Services)                                   │
│                                                                          │
│  [Client Application]                                                    │
│         │                                                                │
│         │ Port: 8033/TCP                                                 │
│         │ Protocol: HTTP/HTTPS                                           │
│         │ Encryption: TLS 1.2+ (rustls, optional)                        │
│         │ Auth: mTLS client certs (optional)                             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  ORCHESTRATOR SERVICE                                                    │
│                                                                          │
│  [FMS Guardrails Orchestrator - fms-guardrails-orchestr8]                │
│  - Language: Rust 1.84.0 (axum 0.8.1)                                   │
│  - TLS: rustls 0.23.21 (no OpenSSL dependency)                          │
│  - Container: UBI 9 Minimal, UID 1001, GID 0 (non-root)                 │
│  - No RBAC / No CRDs (standalone service)                                │
│                                                                          │
│  ┌─────────────────────────┐  ┌─────────────────────────┐              │
│  │ Main API Server         │  │ Health Server            │              │
│  │ Port: 8033/TCP          │  │ Port: 8034/TCP           │              │
│  │ Protocol: HTTP/HTTPS    │  │ Protocol: HTTP           │              │
│  │ Encryption: TLS 1.2+    │  │ Encryption: plaintext    │              │
│  │   (rustls, optional)    │  │ Auth: None               │              │
│  │ Auth: mTLS (optional)   │  │ Endpoints:               │              │
│  │ Endpoints:              │  │   GET /health            │              │
│  │   POST /api/v1/*        │  │   GET /info              │              │
│  │   POST /api/v2/*        │  └─────────────────────────┘              │
│  └─────────────────────────┘                                            │
│         │                                                                │
│         │ Outgoing connections to backend services                       │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  BACKEND SERVICES (Internal)                                             │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ gRPC Backend Services                                            │   │
│  │                                                                   │   │
│  │  [TGIS - Text Generation Inference Server]                        │   │
│  │  - Port: Configurable                                             │   │
│  │  - Protocol: gRPC (fmaas.GenerationService)                       │   │
│  │  - Encryption: TLS/mTLS (configurable)                            │   │
│  │  - Auth: Client certs (optional)                                  │   │
│  │  - Purpose: Text generation (Generate, GenerateStream, Tokenize)  │   │
│  │                                                                   │   │
│  │  [caikit-nlp]                                                     │   │
│  │  - Port: Configurable                                             │   │
│  │  - Protocol: gRPC (caikit.runtime.Nlp.NlpService)                │   │
│  │  - Encryption: TLS/mTLS (configurable)                            │   │
│  │  - Auth: Client certs (optional)                                  │   │
│  │  - Purpose: Alternative text generation provider                  │   │
│  │                                                                   │   │
│  │  [Chunker Server (caikit)]                                        │   │
│  │  - Port: Configurable                                             │   │
│  │  - Protocol: gRPC (caikit.runtime.Chunkers.ChunkersService)       │   │
│  │  - Encryption: TLS/mTLS (configurable)                            │   │
│  │  - Auth: Client certs (optional)                                  │   │
│  │  - Purpose: Text chunking/tokenization for detectors              │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ HTTP Backend Services                                            │   │
│  │                                                                   │   │
│  │  [Detector Servers (HAP, PII, etc.)]                              │   │
│  │  - Port: Configurable (default 8080)                              │   │
│  │  - Protocol: HTTP/HTTPS                                           │   │
│  │  - Encryption: TLS/mTLS (configurable)                            │   │
│  │  - Auth: Client certs (optional)                                  │   │
│  │  - Purpose: Content safety detection                              │   │
│  │  - Types: text_contents, text_chat, text_context_doc,             │   │
│  │           text_generation                                         │   │
│  │                                                                   │   │
│  │  [OpenAI-compatible Server (vLLM)]                                │   │
│  │  - Port: Configurable (default 8080)                              │   │
│  │  - Protocol: HTTP/HTTPS (/v1/chat/completions)                    │   │
│  │  - Encryption: TLS/mTLS (configurable)                            │   │
│  │  - Auth: Client certs (optional)                                  │   │
│  │  - Purpose: Chat completions generation                           │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  OBSERVABILITY                                                           │
│                                                                          │
│  [OpenTelemetry Collector]                                               │
│  - Port: 4317/TCP (gRPC OTLP) or 4318/TCP (HTTP OTLP)                  │
│  - Protocol: gRPC or HTTP                                                │
│  - Encryption: None (configurable)                                       │
│  - Auth: None                                                            │
│  - Purpose: Distributed traces and metrics export                        │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC resources defined (standalone service, not a Kubernetes operator)
- Runs as non-root user: UID 1001, GID 0

Secrets:
- TLS server cert/key (kubernetes.io/tls) - Server TLS for incoming connections, provisioned by Platform/cert-manager
- TLS client CA cert (Opaque) - mTLS client verification for incoming connections, provisioned by Platform/cert-manager
- Client TLS certs per-service (Opaque) - TLS/mTLS for outgoing connections to backends, provisioned by Platform/cert-manager

TLS Configuration:
- Server TLS: Configured via TLS_CERT_PATH, TLS_KEY_PATH, TLS_CLIENT_CA_CERT_PATH env vars
- Client TLS: Per-service named TLS configs in config.yaml with cert_path, key_path, client_ca_cert_path
- TLS Implementation: rustls 0.23.21 (pure Rust, no OpenSSL dependency)
- Health server (8034/TCP) always runs plaintext (no TLS)

Authentication & Authorization:
- /api/v1/*, /api/v2/* endpoints: TLS/mTLS client certs (optional), enforced by orchestrator (rustls)
- /health, /info endpoints: No auth, plaintext HTTP on separate port 8034
- Backend services: Per-service TLS/mTLS client certs, configured in config.yaml

Service Exposure:
- guardrails-orchestrator (main): ClusterIP, 8033/TCP, Internal only
- guardrails-orchestrator (health): ClusterIP, 8034/TCP, Internal only
- No Ingress resources defined (internal service only)
