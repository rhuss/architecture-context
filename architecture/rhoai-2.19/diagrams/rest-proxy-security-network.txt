REST Proxy - Security Network Diagram
Generated by: Claude Opus 4.6
Source: architecture/rhoai-2.19/rest-proxy.md
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [KServe V2 REST Client]                                                │
│         │                                                                │
│         │ Port: 8008/TCP                                                 │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: Optional TLS (configurable via                     │
│         │             MM_TLS_KEY_CERT_PATH / MM_TLS_PRIVATE_KEY_PATH)    │
│         │ Auth: None (delegated to platform/ModelMesh infrastructure)    │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  POD NETWORK (ModelMesh Serving Pod - Sidecar)                          │
│                                                                          │
│  [REST Proxy Server]                                                     │
│  - Container: odh-mm-rest-proxy                                          │
│  - Image: quay.io/rhoai/odh-mm-rest-proxy-rhel8                        │
│  - Listen Port: 8008/TCP (REST_PROXY_LISTEN_PORT)                       │
│  - Max gRPC Message Size: 16MB (REST_PROXY_GRPC_MAX_MSG_SIZE_BYTES)     │
│  - TLS Config: Optional (MM_TLS_KEY_CERT_PATH, MM_TLS_PRIVATE_KEY_PATH) │
│  - No Kubernetes API access                                              │
│  - No RBAC roles required                                                │
│         │                                                                │
│         │ Port: 8033/TCP                                                 │
│         │ Protocol: gRPC                                                 │
│         │ Encryption: Optional TLS (configurable via REST_PROXY_USE_TLS) │
│         │ Auth: None                                                     │
│         │ Network: localhost (sidecar in same pod)                        │
│         ▼                                                                │
│  [ModelMesh Serving - gRPC Inference Service]                            │
│  - Port: 8033/TCP                                                        │
│  - Protocol: gRPC (KServe V2 Predict Protocol)                          │
│  - Services: ModelInfer, ModelMetadata, ModelReady,                      │
│              ServerLive, ServerReady, ServerMetadata                      │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

HTTP Endpoints Exposed:
  Path                                              Method  Port      Protocol   Encryption      Auth
  ──────────────────────────────────────────────────────────────────────────────────────────────────────
  /v2/models/{name}                                 GET     8008/TCP  HTTP/HTTPS Optional TLS    None (delegated)
  /v2/models/{name}/versions/{version}              GET     8008/TCP  HTTP/HTTPS Optional TLS    None (delegated)
  /v2/models/{model_name}/infer                     POST    8008/TCP  HTTP/HTTPS Optional TLS    None (delegated)
  /v2/models/{model_name}/versions/{version}/infer  POST    8008/TCP  HTTP/HTTPS Optional TLS    None (delegated)

gRPC Services Proxied (Backend on localhost:8033):
  Service                                  Port      Protocol  Encryption      Auth
  ──────────────────────────────────────────────────────────────────────────────────
  GRPCInferenceService.ServerLive          8033/TCP  gRPC      Optional TLS    None
  GRPCInferenceService.ServerReady         8033/TCP  gRPC      Optional TLS    None
  GRPCInferenceService.ModelReady          8033/TCP  gRPC      Optional TLS    None
  GRPCInferenceService.ServerMetadata      8033/TCP  gRPC      Optional TLS    None
  GRPCInferenceService.ModelMetadata       8033/TCP  gRPC      Optional TLS    None
  GRPCInferenceService.ModelInfer          8033/TCP  gRPC      Optional TLS    None

RBAC Summary:
  - No Kubernetes RBAC roles required
  - This component does not interact with the Kubernetes API

Authentication & Authorization:
  - REST Proxy does NOT perform authentication or authorization
  - All auth is delegated to the ModelMesh Serving infrastructure
  - Platform-level controls (e.g., NetworkPolicy, Service Mesh) protect access

Secrets:
  - TLS certificate (configurable) - TLS key/cert pair
    Purpose: HTTPS server and gRPC TLS
    Provisioned By: Platform / cert-manager
    Auto-Rotate: Depends on provisioner
    Paths: MM_TLS_KEY_CERT_PATH, MM_TLS_PRIVATE_KEY_PATH

Egress:
  - Destination: localhost (ModelMesh gRPC)
  - Port: 8033/TCP
  - Protocol: gRPC
  - Encryption: Optional TLS (REST_PROXY_USE_TLS)
  - Auth: None
  - No external network egress required

Configuration Environment Variables:
  Variable                              Default      Purpose
  ──────────────────────────────────────────────────────────────────────
  REST_PROXY_LISTEN_PORT                8008         HTTP/HTTPS listen port
  REST_PROXY_GRPC_PORT                  8033         Backend gRPC port
  REST_PROXY_GRPC_MAX_MSG_SIZE_BYTES    16777216     Max gRPC message size (16MB)
  REST_PROXY_USE_TLS                    false        Enable TLS for gRPC backend
  MM_TLS_KEY_CERT_PATH                  (unset)      TLS certificate path
  MM_TLS_PRIVATE_KEY_PATH               (unset)      TLS private key path
