┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL CALLERS (Trusted - within cluster)                            │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Namespace: (orchestrator namespace)                                   │
│  - Calls all three detector services                                     │
│         │                                                                │
│         │ Port: 8080/TCP (built-in) or 8000/TCP (hf, judge)             │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: Istio mTLS (STRICT)                                │
│         │ Auth: KServe auth (security.opendatahub.io/enable-auth)        │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  ISTIO SERVICE MESH (mTLS STRICT)                                       │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────┐             │
│  │ [Istio Sidecar]                                         │             │
│  │ - Injected via: sidecar.istio.io/inject: 'true'         │             │
│  │ - TLS Mode: ISTIO_MUTUAL                                │             │
│  │ - Auth: Service Identity                                │             │
│  └──────────┬──────────────┬──────────────┬────────────────┘             │
│             │              │              │                              │
│             ▼              ▼              ▼                              │
│  ┌──────────────┐ ┌───────────────┐ ┌──────────────┐                    │
│  │ Built-in     │ │ HuggingFace   │ │ LLM Judge    │                    │
│  │ Detector     │ │ Runtime       │ │ Detector     │                    │
│  │              │ │ Detector      │ │              │                    │
│  │ Port: 8080   │ │ Port: 8000    │ │ Port: 8000   │                    │
│  │ Protocol:    │ │ Protocol:     │ │ Protocol:    │                    │
│  │  HTTP        │ │  HTTP         │ │  HTTP        │                    │
│  │ Encryption:  │ │ Encryption:   │ │ Encryption:  │                    │
│  │  None (plain │ │  None (plain  │ │  None (plain │                    │
│  │  text; Istio │ │  text; Istio  │ │  text; Istio │                    │
│  │  sidecar     │ │  sidecar      │ │  sidecar     │                    │
│  │  provides    │ │  provides     │ │  provides    │                    │
│  │  mTLS)       │ │  mTLS)        │ │  mTLS)       │                    │
│  │ Exposure:    │ │ Exposure:     │ │ Exposure:    │                    │
│  │  Internal    │ │  Internal     │ │  Internal    │                    │
│  │              │ │              │  │              │                    │
│  │ Endpoints:   │ │ Endpoints:    │ │ Endpoints:   │                    │
│  │ POST /api/   │ │ POST /api/    │ │ POST /api/   │                    │
│  │  v1/text/    │ │  v1/text/     │ │  v1/text/    │                    │
│  │  contents    │ │  contents     │ │  contents    │                    │
│  │ GET /health  │ │ GET /health   │ │ POST /api/   │                    │
│  │ GET /metrics │ │ GET /metrics  │ │  v1/text/    │                    │
│  │ GET /registry│ │               │ │  generation  │                    │
│  │              │ │               │ │ GET /api/v1/ │                    │
│  │              │ │               │ │  metrics     │                    │
│  │ Local only:  │ │ Local only:   │ │ GET /health  │                    │
│  │ Regex PII    │ │ PyTorch       │ │ GET /metrics │                    │
│  │ File valid.  │ │ inference     │ │              │                    │
│  │ Custom Python│ │ (CPU/GPU)     │ │ Delegates to │                    │
│  │              │ │               │ │ vLLM server  │                    │
│  └──────────────┘ └───────┬───────┘ └──────┬───────┘                    │
│                           │                │                             │
│                           │                │ Port: 8080/TCP              │
│                           │                │ Protocol: HTTP              │
│                           │                │ Encryption: Istio mTLS      │
│                           │                │ Auth: None                  │
│                           │                ▼                             │
│                           │       ┌────────────────┐                    │
│                           │       │ vLLM Inference  │                    │
│                           │       │ Server          │                    │
│                           │       │ (OpenAI API     │                    │
│                           │       │  compatible)    │                    │
│                           │       └────────────────┘                    │
│                           │                                              │
│                           │ Port: 9000/TCP                               │
│                           │ Protocol: HTTP                               │
│                           │ Encryption: None (in-cluster)                │
│                           │ Auth: AWS IAM credentials                    │
│                           │       (access key/secret)                    │
│                           ▼                                              │
├─────────────────────────────────────────────────────────────────────────┤
│  STORAGE (Internal)                                                      │
│                                                                          │
│  [S3/MinIO Object Storage]                                               │
│  - Purpose: Model weight storage for HuggingFace detector               │
│  - Port: 9000/TCP                                                        │
│  - Protocol: HTTP                                                        │
│  - Encryption: None (in-cluster)                                         │
│  - Auth: AWS IAM credentials (access key + secret key)                   │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  MONITORING (Internal)                                                   │
│                                                                          │
│  [Prometheus]                                                            │
│  - Scrapes /metrics on 8080/TCP (built-in) and 8000/TCP (hf, judge)     │
│  - Protocol: HTTP                                                        │
│  - Encryption: None                                                      │
│  - Auth: None (unauthenticated)                                          │
│  - Metric prefix: trustyai_guardrails_*                                  │
│  - Multiprocess-safe collectors (PROMETHEUS_MULTIPROC_DIR)              │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC roles defined. These are application-level services that do not
  interact with the Kubernetes API directly.
- KServe manages deployment lifecycle via InferenceService CRs.
- Example role binding: user-one-view (ClusterRole/view) in deploy namespace.

Service Mesh Configuration:
- Istio sidecar injection: sidecar.istio.io/inject: 'true'
- TLS Mode: ISTIO_MUTUAL (mTLS between all services)
- KServe auth annotation: security.opendatahub.io/enable-auth: 'true'
- Deployment mode: RawDeployment (not serverless/Knative)

Secrets:
- aws-connection-minio-data-connection-guardrails-guardian (Opaque)
  - Purpose: S3 credentials for model storage access
  - Contents: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY
  - Provisioned by: Manual (example manifest)
  - Auto-rotate: No

Authentication & Authorization:
- /api/v1/text/contents: Istio mTLS + KServe auth annotation
- /health: Unauthenticated
- /metrics: Unauthenticated (Prometheus scrape)

FIPS Compliance:
- Build: Python application, no Go binaries; UBI9 base image
- OpenSSL: Pinned openssl-libs-3.5.1-5.el9_7 in HuggingFace Dockerfile
- TLS: Optional SSL via ssl_ca_certs config; Python ssl module → system OpenSSL
- Crypto: No non-FIPS crypto detected (no MD5, RC4, custom RNG)
