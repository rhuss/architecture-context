================================================================================
PLATFORM NETWORK TOPOLOGY — RHOAI 3.5-ea.1
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  Data Scientists, ML Engineers, External Applications, CI/CD Systems        │
│         │                                                                   │
├─────────┼───────────────────────────────────────────────────────────────────┤
│  INGRESS│(Gateway API — DMZ)                                               │
│         │                                                                   │
│  Gateway: data-science-gateway (Istio Gateway API implementation)           │
│  Modes:  LoadBalancer (xKS) | OcpRoute (OpenShift)                         │
│         │                                                                   │
│         ├─→ ODH Dashboard                                                   │
│         │   External: 443/TCP HTTPS, TLS 1.2+                               │
│         │   Internal: 8443/TCP (kube-rbac-proxy) → 8080/TCP (dashboard)     │
│         │   Auth: IntegratedOAuth (OpenShift) or OIDC (external provider)   │
│         │   Flow: EnvoyFilter → kube-auth-proxy → kube-rbac-proxy → app    │
│         │                                                                   │
│         ├─→ KServe Inference Endpoints                                      │
│         │   External: 443/TCP HTTPS, TLS 1.2+                               │
│         │   Internal: HTTPRoute → InferenceService pod                      │
│         │   Auth: Bearer Token (optional), API Key (MaaS)                   │
│         │   Flow: Gateway → Envoy ext-proc (EPP) → model pod               │
│         │                                                                   │
│         ├─→ MaaS API Gateway                                                │
│         │   External: 443/TCP HTTPS                                         │
│         │   Auth: Authorino-based API Key + Rate Limiting                   │
│         │   Flow: Gateway → Authorino ext-authz → payload-processing →     │
│         │          model pod                                                │
│         │                                                                   │
│         └─→ Notebook Access                                                 │
│             External: 443/TCP HTTPS                                         │
│             Auth: IntegratedOAuth / OIDC                                    │
│             Flow: Gateway → kube-auth-proxy → notebook StatefulSet          │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE: redhat-ods-operator                                    │
│                                                                             │
│  ┌─→ rhods-operator                                                         │
│  │   ServiceAccount: rhods-operator                                         │
│  │   Managed by: OLM (Subscription + ClusterServiceVersion)                │
│  │   Ports: 9443/TCP (webhooks), 8443/TCP (metrics via kube-rbac-proxy)    │
│  │   ├─→ Kubernetes API (HTTPS/443, SA token)                              │
│  │   │   Resources: DSC, DSCI CRDs; manages 16 component controllers      │
│  │   ├─→ Istio Control Plane (Gateway API resources)                       │
│  │   └─→ redhat-ods-applications (deploys all controllers)                 │
│  │                                                                          │
│  └─→ cloudmanager (xKS platforms only)                                      │
│      Manages: cert-manager, Sail, LeaderWorkerSet via Helm charts          │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  PLATFORM NAMESPACE: redhat-ods-applications                                │
│  mTLS: PERMISSIVE (default) / STRICT (configurable via Istio)              │
│                                                                             │
│  Authentication Layer:                                                      │
│  ┌─→ kube-auth-proxy (HPA 2+ replicas)                                     │
│  │   Handles: IntegratedOAuth (OAuthClient CR) + OIDC (external issuer)    │
│  │   Port: 8443/TCP (TLS terminated)                                       │
│  │   Integrates via: EnvoyFilter CRs on data-science-gateway               │
│  │                                                                          │
│  Platform Controllers (all use leader election):                            │
│  ┌─→ KServe controller-manager                                              │
│  │   SA: kserve-controller-manager                                          │
│  │   Ports: 9443/TCP (webhooks), 8443/TCP (metrics)                        │
│  │   ├─→ Kubernetes API (HTTPS/443, SA token)                              │
│  │   │   Creates: Deployments, Services, HTTPRoutes, HPA, KEDA ScaledObj  │
│  │   ├─→ storage-initializer (init container, model download)              │
│  │   └─→ User NS: InferenceService pod lifecycle                           │
│  │                                                                          │
│  ├─→ KServe llmisvc-controller-manager                                      │
│  │   SA: llmisvc-controller-manager                                         │
│  │   ├─→ Kubernetes API (HTTPS/443, SA token)                              │
│  │   │   Creates: LeaderWorkerSets, InferencePools, VariantAutoscalings   │
│  │   └─→ User NS: LLMInferenceService pod lifecycle                        │
│  │                                                                          │
│  ├─→ odh-model-controller                                                   │
│  │   SA: odh-model-controller                                               │
│  │   ├─→ Kubernetes API (HTTPS/443, SA token)                              │
│  │   │   Creates: NetworkPolicy, ServiceMonitor, Gateway discovery         │
│  │   └─→ Prometheus (monitoring resources)                                  │
│  │                                                                          │
│  ├─→ Data Science Pipelines Operator                                         │
│  │   SA: data-science-pipelines-operator                                    │
│  │   Ports: 8443/TCP (metrics)                                              │
│  │   └─→ User NS: Creates DSPA (API Server + Argo Controller)              │
│  │                                                                          │
│  ├─→ Model Registry Operator                                                │
│  │   kube-rbac-proxy sidecar: 8443/TCP (TokenReview + SAR)                 │
│  │   └─→ User NS: Creates Model Registry instances                         │
│  │                                                                          │
│  ├─→ TrustyAI Service Operator (5 CRD controllers)                          │
│  │   kube-rbac-proxy sidecar: 8443/TCP (TokenReview + SAR)                 │
│  │   └─→ User NS: TrustyAI services, EvalHub instances, NeMo instances     │
│  │                                                                          │
│  ├─→ Feast Operator                                                          │
│  │   kube-rbac-proxy sidecar: 8443/TCP                                     │
│  │   └─→ User NS: Feature server pods                                       │
│  │                                                                          │
│  ├─→ KubeRay Operator                                                       │
│  │   └─→ User NS: RayCluster, RayJob, RayService CRs                       │
│  │                                                                          │
│  ├─→ Spark Operator                                                          │
│  │   └─→ User NS: SparkApplication CRs                                      │
│  │                                                                          │
│  ├─→ Kubeflow Trainer (v2)                                                   │
│  │   └─→ User NS: TrainJob → JobSet → training pods                         │
│  │                                                                          │
│  ├─→ Training Operator (v1)                                                  │
│  │   └─→ User NS: PyTorchJob, MPIJob, TFJob, XGBoostJob                    │
│  │                                                                          │
│  ├─→ MLflow Operator                                                         │
│  │   Helm chart rendering (not kustomize)                                   │
│  │   └─→ User NS: MLflow server instances                                   │
│  │                                                                          │
│  ├─→ OGX K8s Operator                                                       │
│  │   kube-rbac-proxy sidecar: 8443/TCP                                     │
│  │   └─→ User NS: OGXServer instances                                       │
│  │                                                                          │
│  └─→ WVA Controller                                                         │
│      └─→ User NS: HPA/KEDA scaling for LLMInferenceService pods            │
│                                                                             │
│  Dashboard:                                                                 │
│  ┌─→ odh-dashboard (single pod, 9+ containers)                              │
│  │   kube-rbac-proxy sidecar: 8443/TCP                                     │
│  │   Main: 8080/TCP (dashboard backend)                                     │
│  │   BFF plugins (7): gen-ai, maas, model-registry, mlflow,                │
│  │                     eval-hub, automl, autorag                            │
│  │   ├─→ Kubernetes API (HTTPS/443, bearer token forwarding)               │
│  │   ├─→ Model Registry (gRPC, K8s API proxy)                              │
│  │   ├─→ KServe (InferenceService CRUD)                                    │
│  │   ├─→ DSP API Server (pipeline management)                              │
│  │   ├─→ MLflow (experiment tracking)                                      │
│  │   └─→ EvalHub API (evaluation orchestration)                            │
│  │                                                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│  MONITORING NAMESPACE: redhat-ods-monitoring                                │
│                                                                             │
│  ┌─→ Prometheus (dedicated RHOAI instance)                                   │
│  │   Scrapes: 50 metrics endpoints across all namespaces                    │
│  │   Auth: kube-rbac-proxy bearer token for protected endpoints            │
│  │   └─→ AlertManager                                                       │
│  │                                                                          │
│  └─→ AlertManager                                                            │
│      Routes: Platform health alerts, SLO violations                         │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH: istio-system / redhat-ods-istio                              │
│                                                                             │
│  ┌─→ Istio Control Plane (OpenShift Service Mesh / Sail)                     │
│  │   Gateway API implementation for RHOAI                                   │
│  │   mTLS: PERMISSIVE default, STRICT configurable                         │
│  │   PeerAuthentication: namespace-scoped                                  │
│  │   ├─→ EnvoyFilter CRs (auth integration)                               │
│  │   ├─→ DestinationRule CRs (TLS for cross-service calls)                │
│  │   └─→ Envoy Proxies (data plane)                                        │
│  │                                                                          │
│  └─→ Envoy Proxies                                                          │
│      ext-proc filters for:                                                  │
│      - ai-gateway-payload-processing (model resolution, API translation)    │
│      - MaaS payload-processing (credential injection)                       │
│      - EPP / llm-d-inference-scheduler (KV-cache routing)                  │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  USER DATA SCIENCE NAMESPACES (per-project, cross-namespace managed)        │
│                                                                             │
│  ┌─→ Notebooks (StatefulSets)                                                │
│  │   kube-rbac-proxy sidecar injected                                       │
│  │   Feast config auto-injected by operator                                │
│  │   ├─→ S3 (training data download, HTTPS/443)                            │
│  │   ├─→ HuggingFace Hub (model download, HTTPS/443)                      │
│  │   └─→ Kubernetes API (submit training jobs, SA token)                   │
│  │                                                                          │
│  ├─→ InferenceServices (classical: Deployment-based)                         │
│  │   Runtimes: vLLM, OpenVINO Model Server, MLServer                       │
│  │   storage-initializer init container for model download                 │
│  │   ├─→ S3/GCS/HuggingFace (model artifacts, HTTPS/443)                  │
│  │   └─→ Container Registry (runtime images, HTTPS/443)                    │
│  │                                                                          │
│  ├─→ LLMInferenceServices (llm-d stack)                                      │
│  │   LeaderWorkerSets, InferencePools, disaggregated P/D                   │
│  │   EPP sidecar for KV-cache-aware routing                                │
│  │   WVA-managed autoscaling (KV-cache utilization, queue depth)           │
│  │   ├─→ S3 (model artifacts, HTTPS/443)                                   │
│  │   ├─→ Container Registry (vLLM images, HTTPS/443)                       │
│  │   └─→ FMS Guardrails Orchestrator (content safety pipeline)             │
│  │       └─→ NeMo Guardrails (content safety detectors)                    │
│  │                                                                          │
│  ├─→ DSPA Instance (per-namespace)                                           │
│  │   DSP API Server + Argo Workflows Controller                             │
│  │   ├─→ S3/SeaweedFS/MinIO (pipeline artifacts, HTTPS/443)               │
│  │   ├─→ ML Metadata DB (pipeline metadata)                               │
│  │   └─→ KServe (auto-deploy models from pipeline)                        │
│  │                                                                          │
│  ├─→ Training Jobs                                                           │
│  │   PyTorchJob, TrainJob, MPIJob, TFJob, XGBoostJob                      │
│  │   Gang-scheduling via Kueue/Volcano PodGroups                           │
│  │   ├─→ S3 (training data, checkpoints, HTTPS/443)                       │
│  │   └─→ Kubernetes API (status updates, SA token)                         │
│  │                                                                          │
│  ├─→ Ray Clusters (CodeFlare SDK managed)                                    │
│  │   mTLS for client-to-cluster communication                              │
│  │   Kueue quota management                                                │
│  │                                                                          │
│  ├─→ MLflow Instance                                                         │
│  │   Experiment tracking with Kubernetes auth provider                     │
│  │   Workspace-based multi-tenancy                                         │
│  │                                                                          │
│  ├─→ Model Registry Instance                                                 │
│  │   gRPC/9090 (metadata API)                                               │
│  │   Leader election for singleton catalog                                  │
│  │   ├─→ S3 (artifact storage refs, HTTPS/443)                             │
│  │   └─→ KServe (InferenceService status sync)                             │
│  │                                                                          │
│  ├─→ EvalHub Instance                                                        │
│  │   Evaluation Jobs (lm-eval, RAGAS, Garak, GuideLLM, LightEval)         │
│  │   kube-rbac-proxy sidecar: 8443/TCP                                     │
│  │   ├─→ Model endpoints (evaluation target, HTTPS)                        │
│  │   ├─→ MLflow (experiment tracking)                                      │
│  │   ├─→ OCI Registry (eval results as OCI artifacts)                      │
│  │   └─→ Kueue (job scheduling, resource quotas)                           │
│  │                                                                          │
│  ├─→ TrustyAI Service                                                        │
│  │   PVC-backed state, single replica                                       │
│  │   kube-rbac-proxy sidecar: 8443/TCP                                     │
│  │                                                                          │
│  └─→ Feast Feature Server                                                    │
│      HPA + PodDisruptionBudget                                              │
│      kube-rbac-proxy sidecar: 8443/TCP                                     │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  OPTIONAL: knative-serving                                                  │
│                                                                             │
│  Knative Serving components (when KServe serverless mode is enabled)        │
│  Used for scale-to-zero and request-based autoscaling                      │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  EGRESS (External Services)                                                 │
│                                                                             │
│  S3-Compatible Storage (s3.amazonaws.com / MinIO / SeaweedFS)               │
│  - Purpose: Model artifacts, pipeline artifacts, training data,            │
│             evaluation results, checkpoints                                 │
│  - Auth: AWS IAM (IRSA or static credentials in Secrets)                   │
│  - Encryption: TLS 1.2+                                                     │
│  - Components: KServe, DSP, Notebooks, Training, Model Registry, EvalHub  │
│                                                                             │
│  Container Registry (quay.io, registry.redhat.io)                           │
│  - Purpose: Pull runtime images (vLLM, OVMS, Jupyter, operators)           │
│  - Auth: Pull secrets (imagePullSecrets)                                   │
│  - Encryption: TLS 1.2+                                                     │
│  - Components: All (126 production images)                                  │
│  - Disconnected: oc-mirror + SHA256 digest pinning                          │
│                                                                             │
│  HuggingFace Hub (huggingface.co)                                           │
│  - Purpose: Download pre-trained models and tokenizers                     │
│  - Auth: API Token (optional, for gated models)                            │
│  - Encryption: TLS 1.2+                                                     │
│  - Components: KServe storage-initializer, Notebooks                       │
│                                                                             │
│  OCI Artifact Registry                                                      │
│  - Purpose: Store evaluation results as OCI artifacts                      │
│  - Auth: Registry credentials                                              │
│  - Components: EvalHub                                                      │
│                                                                             │
│  Kubernetes API Server (cluster-internal)                                   │
│  - Purpose: CR management, RBAC checks, status updates                     │
│  - Auth: ServiceAccount tokens (bound tokens, projected volumes)           │
│  - Encryption: HTTPS/443                                                    │
│  - Components: All operators and controllers                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

================================================================================
NAMESPACE SUMMARY
================================================================================

Namespace                      Purpose                         Security
─────────────────────────────  ──────────────────────────────  ────────────────
redhat-ods-operator            OLM-managed operator            SA RBAC
redhat-ods-applications        Platform controllers + UI       kube-rbac-proxy
redhat-ods-monitoring          Prometheus + AlertManager       Dedicated instance
istio-system/redhat-ods-istio  Service mesh control plane      mTLS enforcement
knative-serving (optional)     Serverless runtime              Istio mTLS
User DS namespaces (N)         Per-project workloads           NetworkPolicy

================================================================================
SERVICE MESH CONFIGURATION
================================================================================

- Implementation: Istio via OpenShift Service Mesh / Sail
- Gateway API: Full coverage — all external ingress via HTTPRoutes
- mTLS Mode: PERMISSIVE (default), STRICT (configurable)
- PeerAuthentication: Namespace-scoped enforcement
- DestinationRule: TLS for cross-service calls
- EnvoyFilter: Authentication integration (IntegratedOAuth, OIDC)
- ext-proc Filters: ai-gateway-payload-processing, MaaS, EPP
- Certificate Management: cert-manager (xKS), OpenShift service-ca (OCP)

================================================================================
AUTHENTICATION SUMMARY
================================================================================

Mechanism                       Components Using              Enforcement Point
──────────────────────────────  ────────────────────────────  ────────────────────
kube-rbac-proxy (TokenReview    odh-dashboard, Model Reg Op,  Sidecar :8443, FIPS
+ SubjectAccessReview)          TrustyAI Op, OGX, EvalHub,    TLS cipher suites
                                Feast, NeMo Guardrails

IntegratedOAuth (OCP OAuth)     Gateway → Dashboard, Notebooks EnvoyFilter +
                                                              kube-auth-proxy

OIDC (external provider)        Gateway (configurable)         EnvoyFilter +
                                                              kube-auth-proxy

mTLS (client certificates)      Ray clusters, Istio mesh       Generated TLS certs

Bearer token forwarding         Dashboard → K8s API, DSP       User token from
                                                              Gateway/OAuth header

API key (Authorino)             MaaS, ai-gateway-payload-proc  ext-authz filter

ServiceAccount RBAC             All operators, training jobs,  K8s API server
                                pipeline steps

================================================================================
RBAC SUMMARY (Most Privileged Roles)
================================================================================

- rhods-operator: Full DSC/DSCI lifecycle, all 16 component CRDs, OLM resources
- kserve-controller-manager: serving.kserve.io/*, core/services, apps/deployments,
  networking.istio.io/*, autoscaling.keda.sh/*
- odh-model-controller: InferenceService status, NetworkPolicy, ServiceMonitor
- odh-dashboard: opendatahub.io/*, monitoring.coreos.com/servicemonitors,
  bearer token forwarding (impersonation-like)
- data-science-pipelines-operator: datasciencepipelinesapplications.opendatahub.io/*
- training-operator: kubeflow.org/* (PyTorchJob, MPIJob, TFJob, XGBoostJob)
- kubeflow-trainer: trainer.kubeflow.org/* (TrainJob, TrainingRuntime)

================================================================================
CONTAINER SECURITY POSTURE
================================================================================

Pattern                         Coverage                     Details
──────────────────────────────  ───────────────────────────  ───────────────────
Non-root (USER 1001)            82 Dockerfiles               OpenShift restricted
                                                             SCC compatible
Non-root (USER 65532:65532)     66 Dockerfiles               distroless convention
                                                             (Go operators)
UBI 9 base images               87 production Dockerfiles    RHEL 9 userspace
FIPS 140-2 Go builds            All Go operators             strictfipsruntime +
                                                             CGO_ENABLED=1
Capability dropping             All operators                drop: ALL, seccomp:
                                                             RuntimeDefault
Multi-stage builds              372 Dockerfiles              avg 2+ stages
FIPS TLS cipher suites          kube-rbac-proxy instances    6 approved ciphers,
                                                             TLS 1.2 minimum

Known FIPS Gaps:
- KServe localmodel components: CGO_ENABLED=0 (no FIPS crypto)
- KServe PodMonitor: InsecureSkipVerify in TLS config
- odh-dashboard Go BFFs: INSECURE_SKIP_VERIFY flags
