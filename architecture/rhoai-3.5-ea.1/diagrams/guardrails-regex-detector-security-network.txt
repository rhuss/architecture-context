┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CALLERS (Platform Services)                                   │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Purpose: Screens LLM inputs/outputs via guardrails pipeline          │
│  - Namespace: (platform-managed)                                         │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plaintext)                                   │
│         │ Auth: None                                                     │
│         │ Endpoint: POST /api/v1/text/contents                           │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE (No TLS, No Auth)                                              │
│                                                                          │
│  [guardrails-regex-detector]                                             │
│  - Type: Rust HTTP Service (axum 0.7.9)                                 │
│  - Namespace: (deployed by platform/orchestrator)                        │
│  - ServiceAccount: (none specified in repo)                              │
│  - Image: UBI 9 minimal base                                            │
│  - Container User: root (no USER directive in Dockerfile)               │
│  - Listen: 0.0.0.0:${HTTP_PORT:-8080}                                   │
│  - Endpoints:                                                            │
│    - GET /health (health check)                                          │
│    - POST /api/v1/text/contents (PII/regex detection)                   │
│                                                                          │
│  ⚠  No TLS termination at application level                             │
│  ⚠  No authentication/authorization enforced                            │
│  ⚠  Container runs as root by default                                   │
│  ⚠  Accepts arbitrary regex from clients (mitigated by Rust regex       │
│     crate's linear-time Thompson NFA — no ReDoS risk)                   │
│                                                                          │
│  No outbound connections (zero egress)                                  │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  (None — fully stateless, no egress)                                    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC resources defined
- No Kubernetes API interactions
- No ServiceAccount specified in repository

Secrets:
- No secrets consumed by this service

Network Policies:
- No NetworkPolicy resources defined in repository
- Security boundary enforcement expected from deploying infrastructure:
  - Network policies (restrict who can reach port 8080)
  - Service mesh (mTLS sidecar injection)
  - kube-rbac-proxy sidecar (authentication proxy)

Authentication & Authorization:
- No auth at application level
- Endpoint /api/v1/text/contents accepts unauthenticated POST requests
- Security enforcement delegated to infrastructure layer

FIPS Compliance:
- No cryptographic operations in application code
- No TLS — plain HTTP only
- Rust regex crate: pure Rust, no OpenSSL linkage
- compat-openssl11 installed in image (for platform compatibility)
- No Konflux Dockerfile — may not pass check-payload validation

Build Security:
- Cargo.lock present (reproducible Rust deps)
- No rpms.lock.yaml (OS packages not pinned)
- No Hermeto prefetch integration
- microdnf packages installed without version pinning
