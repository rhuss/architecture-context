┌─────────────────────────────────────────────────────────────────────────┐
│  BUILD-TIME ENVIRONMENT (CI/CD Pipeline - Konflux/Tekton)               │
│                                                                          │
│  [model-extractor CLI]                                                   │
│  - Language: Go 1.24                                                     │
│  - Build: CGO_ENABLED=0 (static linking)                                │
│  - User: Build pipeline service account                                  │
│         │                                                                │
│         ├── Port: 443/TCP                                                │
│         │   Destination: registry.redhat.io                              │
│         │   Protocol: HTTPS (OCI Registry v2 API)                       │
│         │   Encryption: TLS 1.2+                                         │
│         │   Auth: Registry credentials (Docker config / Tekton secret)  │
│         │   Purpose: Fetch OCI model container manifests, layers,       │
│         │            config blobs for metadata extraction                │
│         │                                                                │
│         ├── Port: 443/TCP                                                │
│         │   Destination: registry.access.redhat.com                     │
│         │   Protocol: HTTPS (OCI Registry v2 API)                       │
│         │   Encryption: TLS 1.2+                                         │
│         │   Auth: None (public access)                                   │
│         │   Purpose: Fetch MCP server container image metadata          │
│         │                                                                │
│         ├── Port: 443/TCP                                                │
│         │   Destination: huggingface.co                                  │
│         │   Protocol: HTTPS (REST API)                                   │
│         │   Encryption: TLS 1.2+                                         │
│         │   Auth: Bearer Token (HF_TOKEN, optional)                     │
│         │   Purpose: Fetch model details, collections, README content   │
│         │                                                                │
│         └── Port: 443/TCP                                                │
│             Destination: quay.io                                         │
│             Protocol: HTTPS (OCI Registry v2 API)                       │
│             Encryption: TLS 1.2+                                         │
│             Auth: Registry credentials                                   │
│             Purpose: Fetch OCI artifact metadata for architecture       │
│                      detection (MCP server enrichment)                  │
│                                                                          │
│  [metadata-report CLI]                                                   │
│  - Language: Go 1.24                                                     │
│  - Network: None (file I/O only)                                        │
│  - Purpose: Generate metadata completeness reports from catalogs        │
│                                                                          │
│         │ (File I/O only)                                                │
│         ▼                                                                │
│  [Generated Catalog Files]                                               │
│  - models-catalog.yaml                                                   │
│  - redhat-mcp-servers-catalog.yaml                                      │
│  - partner-mcp-servers-catalog.yaml                                     │
│  - community-mcp-servers-catalog.yaml                                   │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  CONTAINER BUILD (Konflux / Tekton Pipeline)                             │
│                                                                          │
│  [Dockerfile.konflux]                                                    │
│  - Base: registry.access.redhat.com/ubi9/ubi-minimal:latest             │
│  - Action: COPY pre-generated YAML files into /app/data/               │
│  - Action: COPY benchmark data into /app/benchmarks/                   │
│  - No Go compilation (data-only container)                              │
│  - USER: 1001 (non-root)                                                │
│  - CMD: sleep infinity                                                   │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  RUNTIME ENVIRONMENT (Kubernetes Cluster)                                │
│                                                                          │
│  [Data Container - odh-model-metadata-collection]                       │
│  - Image: odh-model-metadata-collection                                 │
│  - Deployment: Init container or sidecar (volume mount)                 │
│  - Network: NONE (no ports, no services, no ingress, no egress)        │
│  - CMD: sleep infinity                                                   │
│  - USER: 1001 (non-root)                                                │
│  - Provides: /app/data/*.yaml, /app/benchmarks/*                       │
│         │                                                                │
│         │ Volume Mount (Filesystem)                                      │
│         │ Protocol: N/A (local filesystem)                               │
│         │ Encryption: N/A                                                │
│         │ Auth: N/A (pod-level access)                                   │
│         ▼                                                                │
│  [RHOAI Dashboard]                                                       │
│  - Reads: /app/data/*.yaml                                              │
│  - Purpose: Display model catalog to end users                          │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

EXTERNAL SERVICES (Egress - Build-Time Only):

  ┌──────────────────────────────────┐
  │ registry.redhat.io               │
  │ - Port: 443/TCP                  │
  │ - Protocol: HTTPS                │
  │ - Encryption: TLS 1.2+           │
  │ - Auth: Registry credentials     │
  │ - Purpose: OCI model containers  │
  └──────────────────────────────────┘

  ┌──────────────────────────────────┐
  │ registry.access.redhat.com       │
  │ - Port: 443/TCP                  │
  │ - Protocol: HTTPS                │
  │ - Encryption: TLS 1.2+           │
  │ - Auth: None (public)            │
  │ - Purpose: MCP server images     │
  └──────────────────────────────────┘

  ┌──────────────────────────────────┐
  │ huggingface.co                   │
  │ - Port: 443/TCP                  │
  │ - Protocol: HTTPS (REST API)     │
  │ - Encryption: TLS 1.2+           │
  │ - Auth: Bearer Token (optional)  │
  │ - Purpose: Model metadata        │
  │            enrichment             │
  └──────────────────────────────────┘

  ┌──────────────────────────────────┐
  │ quay.io                          │
  │ - Port: 443/TCP                  │
  │ - Protocol: HTTPS                │
  │ - Encryption: TLS 1.2+           │
  │ - Auth: Registry credentials     │
  │ - Purpose: OCI architecture      │
  │            detection              │
  └──────────────────────────────────┘

RBAC Summary:
- No ClusterRoles or RoleBindings required
- Component is a CLI tool (build-time) and data container (runtime)
- No Kubernetes API access needed at runtime

Secrets:
- HF_TOKEN (env var) - HuggingFace API auth, optional, manual provisioning, no auto-rotation
- Docker/registry credentials (container registry auth) - Docker config / Tekton secret, no auto-rotation

FIPS Compliance:
- Go binaries built with CGO_ENABLED=0 (static linking, no FIPS-validated crypto)
- Go binaries are build-time tools only — NOT shipped in final container image
- Final container (Dockerfile.konflux) contains only static YAML data files on UBI9-minimal base
- TLS: Uses Go stdlib net/http defaults via containers/image library
- No custom TLS configuration

Build Hermeticity:
- go.sum: Present (Go module dependencies)
- rpms.lock.yaml: Not present (UBI9-minimal base provides OS packages)
- artifacts.lock.yaml: Not present
- Hermeto/cachi2 prefetch: Not configured
- Note: Konflux Dockerfile does not compile Go; only copies static YAML files
