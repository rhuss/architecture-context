// C4 Context Diagram: Guardrails Regex Detector
// Generated by: Claude Opus 4.6
// Source: architecture/rhoai-3.3/guardrails-regex-detector.md

workspace {
    model {
        orchestrator = person "FMS Guardrails Orchestrator" "Routes text content to detectors for PII scanning before/after LLM inference"

        regexDetector = softwareSystem "Guardrails Regex Detector" "Lightweight Rust-based HTTP microservice that detects PII in text using regex patterns" {
            httpService = container "regex-detector" "Axum HTTP server listening on port 8080, processes regex detection requests" "Rust 1.84.0 / Axum 0.7.9"
            detectorsModule = container "detectors module" "Built-in PII detectors (email, SSN, credit card) and custom regex matching" "Rust Module"
        }

        kubelet = softwareSystem "Kubernetes Kubelet" "Performs liveness/readiness health checks" "Infrastructure"

        // Relationships
        orchestrator -> regexDetector "Sends text for PII/regex detection" "HTTP/8080 plaintext"
        kubelet -> regexDetector "Health check probes" "HTTP GET /health"

        // Internal relationships
        httpService -> detectorsModule "Invokes for pattern matching" "In-process"
    }

    views {
        systemContext regexDetector "SystemContext" {
            include *
            autoLayout
        }

        container regexDetector "Containers" {
            include *
            autoLayout
        }

        styles {
            element "Software System" {
                background #4a90e2
                color #ffffff
            }
            element "Infrastructure" {
                background #999999
                color #ffffff
            }
            element "Person" {
                background #7ed321
                color #ffffff
                shape person
            }
            element "Container" {
                background #438dd5
                color #ffffff
            }
        }
    }
}
