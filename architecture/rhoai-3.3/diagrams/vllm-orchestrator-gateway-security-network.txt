Security Network Diagram: vllm-orchestrator-gateway
Generated by: Claude Opus 4.6
Source: architecture/rhoai-3.3/vllm-orchestrator-gateway.md

┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CLIENTS (Cluster-Internal)                                    │
│                                                                          │
│  [Client Service / Pod]                                                  │
│         │                                                                │
│         │ Port: 8090/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: plaintext                                          │
│         │ Auth: Authorization header (optional, forwarded)               │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  GATEWAY SERVICE                                                        │
│                                                                          │
│  [vllm-orchestrator-gateway]                                             │
│  - Type: Rust HTTP Service (axum)                                        │
│  - Service: ClusterIP (inferred)                                         │
│  - Port: 8090/TCP (configurable via HTTP_PORT)                          │
│  - Host: 0.0.0.0 (configurable via HOST)                                │
│  - Endpoints: /{route_name}/v1/chat/completions                         │
│    - /pii/v1/chat/completions (PII detection)                           │
│    - /passthrough/v1/chat/completions (no filtering)                    │
│  - Image: quay.io/rhoai/odh-trustyai-vllm-orchestrator-gateway-rhel9   │
│  - Base: registry.access.redhat.com/ubi9/ubi-minimal:latest             │
│  - Platforms: amd64, arm64, ppc64le, s390x                              │
│         │                                                                │
│         │ Port: 8085/TCP (configurable)                                  │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: Optional mTLS (TLS 1.2+ with client cert)         │
│         │ Auth: mTLS client certificate (if TLS configured)             │
│         │       + forwarded Authorization header                         │
│         │       + forwarded X-Forwarded-* headers                        │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  BACKEND SERVICES (Cluster-Internal)                                    │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Endpoint: /api/v2/chat/completions-detection                          │
│  - Port: 8085/TCP (configurable)                                         │
│  - Protocol: HTTP or HTTPS                                               │
│  - Purpose: Content detection + LLM inference orchestration              │
│         │                                                                │
│         ├──── [Guardrails Detectors]                                     │
│         │     - e.g., regex-language (PII detection)                     │
│         │     - Referenced by name in gateway config                     │
│         │     - Accessed indirectly via Orchestrator                     │
│         │                                                                │
│         └──── [vLLM Inference Server]                                    │
│               - LLM backend (e.g., Qwen/Qwen2.5-1.5B-Instruct)         │
│               - Accessed indirectly via Orchestrator                     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles or RoleBindings defined
- Stateless HTTP service without Kubernetes API interactions
- No ServiceAccount with special permissions required

Authentication & Authorization:
- Gateway does NOT enforce authentication directly
- Authorization header is passed through to FMS Guardrails Orchestrator
- Authentication/authorization is delegated to orchestrator backend
- All routes: POST /{route}/v1/chat/completions → pass-through auth

TLS Configuration:
- Client cert:  /etc/tls/private/tls.crt (PEM)
- Client key:   /etc/tls/private/tls.key (PEM)
- CA cert:      /etc/tls/ca/service-ca.crt (PEM)
- If TLS files present → HTTPS with mTLS to orchestrator
- If TLS files absent  → HTTP plaintext to orchestrator
- TLS identity: PKCS#12 constructed from cert+key at startup

Secrets:
- /etc/tls/private/tls.crt  (TLS certificate, PEM)
  - Purpose: Client certificate for mTLS to orchestrator
  - Provisioner: OpenShift service-serving-cert-signer (inferred)
  - Auto-Rotate: Yes

- /etc/tls/private/tls.key  (TLS private key, PEM)
  - Purpose: Client private key for mTLS to orchestrator
  - Provisioner: OpenShift service-serving-cert-signer (inferred)
  - Auto-Rotate: Yes

- /etc/tls/ca/service-ca.crt (CA certificate, PEM)
  - Purpose: Custom CA for validating orchestrator's server certificate
  - Provisioner: OpenShift service-ca (inferred)
  - Auto-Rotate: Yes

Network Policy Summary:
- Ingress: 8090/TCP from cluster-internal clients (HTTP, plaintext)
- Egress: 8085/TCP to FMS Guardrails Orchestrator (HTTP or HTTPS, optional mTLS)
- No external ingress (no Ingress/Route manifests)
- No direct external egress
