FMS Guardrails Orchestrator - Security Network Diagram
Generated by: Claude Opus 4.6
======================================================================

┌──────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                    │
│                                                                          │
│  [API Client / Application]                                              │
│         │                                                                │
│         │ Port: 8033/TCP                                                 │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: Optional TLS 1.2+ (rustls/ring)                    │
│         │ Auth: Header passthrough (configurable headers)                │
│         │       Optional X-Forwarded-Access-Token rewrite to Bearer      │
│         ▼                                                                │
├──────────────────────────────────────────────────────────────────────────┤
│  ORCHESTRATOR SERVICE                                                    │
│                                                                          │
│  [FMS Guardrails Orchestrator - fms-guardrails-orchestr8]                │
│  - Language: Rust 1.89.0 (Axum 0.8.4 / Tokio 1.47.1)                   │
│  - Image: UBI 9 Minimal base                                            │
│  - Architectures: amd64, arm64, ppc64le, s390x                          │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────┐         │
│  │  Guardrails Server                                          │         │
│  │  - Port: 8033/TCP                                           │         │
│  │  - Protocol: HTTP/HTTPS                                     │         │
│  │  - TLS Modes: None | TLS | mTLS                             │         │
│  │  - TLS Implementation: rustls 0.23.31 (ring backend)        │         │
│  │  - Endpoints:                                               │         │
│  │    POST /api/v1/task/classification-with-text-generation    │         │
│  │    POST /api/v1/task/server-streaming-classification-*      │         │
│  │    POST /api/v2/text/detection/content                      │         │
│  │    POST /api/v2/text/detection/chat                         │         │
│  │    POST /api/v2/text/detection/context                      │         │
│  │    POST /api/v2/text/detection/generated                    │         │
│  │    POST /api/v2/text/generation-detection                   │         │
│  │    POST /api/v2/text/detection/stream-content               │         │
│  │    POST /api/v2/chat/completions-detection                  │         │
│  │    POST /api/v2/text/completions-detection                  │         │
│  └─────────────────────────────────────────────────────────────┘         │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────┐         │
│  │  Health Server                                              │         │
│  │  - Port: 8034/TCP                                           │         │
│  │  - Protocol: HTTP (plaintext)                               │         │
│  │  - Encryption: None                                         │         │
│  │  - Auth: None                                               │         │
│  │  - Endpoints:                                               │         │
│  │    GET /health                                              │         │
│  │    GET /info                                                │         │
│  └─────────────────────────────────────────────────────────────┘         │
│         │                                                                │
│         │ Egress connections to backend services                         │
│         ▼                                                                │
├──────────────────────────────────────────────────────────────────────────┤
│  BACKEND SERVICES (Internal)                                             │
│                                                                          │
│  [TGIS Generation Server]                                                │
│  - Port: 8033/TCP (configurable)                                         │
│  - Protocol: gRPC/HTTP2                                                  │
│  - Encryption: Optional TLS/mTLS                                         │
│  - Auth: Certificate-based                                               │
│  - Service: fmaas.GenerationService                                      │
│  - Methods: Generate, GenerateStream, Tokenize, ModelInfo                │
│                                                                          │
│  [caikit-nlp Server]                                                     │
│  - Port: configurable                                                    │
│  - Protocol: gRPC/HTTP2                                                  │
│  - Encryption: Optional TLS/mTLS                                         │
│  - Auth: Certificate-based                                               │
│  - Service: caikit.runtime.Nlp.NlpService                               │
│                                                                          │
│  [Content Safety Detectors]                                              │
│  - Port: 8080/TCP (default)                                              │
│  - Health Port: 8081/TCP (configurable)                                  │
│  - Protocol: HTTP/HTTPS                                                  │
│  - Encryption: Optional TLS/mTLS                                         │
│  - Auth: Header passthrough                                              │
│  - Endpoints: /api/v1/text/contents, /chat, /context/doc, /generation   │
│                                                                          │
│  [Chunker Services]                                                      │
│  - Port: 8085/TCP (configurable)                                         │
│  - Protocol: gRPC/HTTP2                                                  │
│  - Encryption: Optional TLS/mTLS                                         │
│  - Auth: Certificate-based                                               │
│  - Service: caikit.runtime.Chunkers.ChunkersService                     │
│                                                                          │
│  [vLLM / OpenAI-compatible Server]                                       │
│  - Port: 8080/TCP (default)                                              │
│  - Protocol: HTTP/HTTPS                                                  │
│  - Encryption: Optional TLS/mTLS                                         │
│  - Auth: Header passthrough                                              │
│  - Endpoints: /v1/chat/completions, /v1/completions, /tokenize          │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│  OBSERVABILITY                                                           │
│                                                                          │
│  [OTLP Collector]                                                        │
│  - Port: 4317/TCP (gRPC) or 4318/TCP (HTTP)                             │
│  - Protocol: gRPC or HTTP                                                │
│  - Encryption: Configurable                                              │
│  - Auth: None                                                            │
│  - Purpose: Distributed tracing and metrics export                       │
└──────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC resources defined. This component is a standalone service
  that does not interact with the Kubernetes API.

TLS Configuration:
- Server TLS Modes:
  1. No TLS: Default, plaintext HTTP on port 8033
  2. TLS: Server-side TLS with cert/key (TLS_CERT_PATH, TLS_KEY_PATH)
  3. mTLS: Mutual TLS with client CA cert (TLS_CLIENT_CA_CERT_PATH)
- TLS Implementation: rustls 0.23.31 with ring crypto backend
- Client TLS: Per-service configuration in YAML config file
  - Named TLS configurations for reuse across services
  - Per-service cert, key, and CA cert paths
  - Insecure mode option for development

Secrets:
- TLS server cert/key (kubernetes.io/tls) - Server TLS termination, Platform/cert-manager, Configurable rotation
- TLS client CA cert (Opaque) - mTLS client verification, Platform/cert-manager, Configurable rotation
- Downstream TLS certs (Opaque) - TLS/mTLS to backend services, Platform/cert-manager, Configurable rotation

Authentication & Authorization:
- API endpoints (/api/v1/*, /api/v2/*):
  - Header passthrough: Configurable headers forwarded to backends
  - Optional: X-Forwarded-Access-Token rewrite to Bearer token
- Health endpoints (/health, /info): No authentication
- Downstream gRPC services: mTLS client certificates
- Downstream HTTP services: Header passthrough

Operator Features:
- Enabled: disconnected, fips-compliant
- Disabled: proxy-aware, tls-profiles, token-auth-aws, token-auth-azure, token-auth-gcp
