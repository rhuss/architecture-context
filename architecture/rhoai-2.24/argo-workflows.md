# Component: Argo Workflows

## Metadata
- **Repository**: https://github.com/red-hat-data-services/argo-workflows.git
- **Version**: 2.24.0 (branch: rhoai-2.24, commit: 7a94b6395)
- **Distribution**: RHOAI
- **Languages**: Go 1.23.1, TypeScript (UI)
- **Deployment Type**: Workflow Engine (Controller + API Server + Executor)
- **Generated By**: Claude Opus 4.6 on 2026-03-20

## Purpose
**Short**: Argo Workflows is a container-native workflow engine for orchestrating parallel jobs on Kubernetes, used as the execution backend for Data Science Pipelines in RHOAI.

**Detailed**: Argo Workflows implements a Kubernetes-native workflow engine that defines workflows as Custom Resources (CRDs). It enables multi-step, DAG-based orchestration of containerized tasks on Kubernetes. In the RHOAI distribution, Argo Workflows serves as the core execution engine for Data Science Pipelines, responsible for running pipeline steps as Kubernetes pods.

The component ships two container images for RHOAI: the workflow controller (`odh-data-science-pipelines-argo-workflowcontroller`) and the executor (`odh-data-science-pipelines-argo-argoexec`). The workflow controller watches Workflow CRDs and orchestrates pod creation, scheduling, and lifecycle management. The argoexec executor runs as a sidecar/init container within workflow pods, managing artifact collection, log capture, and container lifecycle. Both images are built with FIPS-compliant Go (`GOEXPERIMENT=strictfipsruntime`) for the RHOAI distribution. The Argo Server (API server with UI) is also part of the codebase but is not separately shipped as a RHOAI container image.

## Architecture Components

| Component | Type | Purpose |
|-----------|------|---------|
| workflow-controller | Go Binary (Deployment) | Watches Workflow CRDs, creates/manages pods for workflow steps, handles CronWorkflow scheduling, leader election, artifact GC |
| argoexec | Go Binary (Sidecar/Init container) | Runs inside workflow pods to manage container lifecycle, collect artifacts, capture logs, and report task results |
| argo-server | Go Binary (Deployment) | REST/gRPC API server with web UI for workflow management (not shipped as separate RHOAI image) |
| UI | TypeScript/React SPA | Web-based workflow visualization and management interface, served by argo-server |

## APIs Exposed

### Custom Resource Definitions (CRDs)

| Group | Version | Kind | Scope | Purpose |
|-------|---------|------|-------|---------|
| argoproj.io | v1alpha1 | Workflow | Namespaced | Defines a workflow execution with steps, DAGs, and templates |
| argoproj.io | v1alpha1 | WorkflowTemplate | Namespaced | Reusable workflow template definitions |
| argoproj.io | v1alpha1 | ClusterWorkflowTemplate | Cluster | Cluster-scoped reusable workflow templates |
| argoproj.io | v1alpha1 | CronWorkflow | Namespaced | Scheduled workflow execution via cron expressions |
| argoproj.io | v1alpha1 | WorkflowEventBinding | Namespaced | Maps events to workflow submissions |
| argoproj.io | v1alpha1 | WorkflowTaskSet | Namespaced | Agent-mode task coordination between controller and executor |
| argoproj.io | v1alpha1 | WorkflowTaskResult | Namespaced | Reports results from executor tasks back to controller |
| argoproj.io | v1alpha1 | WorkflowArtifactGCTask | Namespaced | Tracks artifact garbage collection tasks |

### gRPC Services

| Service | Port | Protocol | Encryption | Auth | Purpose |
|---------|------|----------|------------|------|---------|
| WorkflowService | 2746/TCP | gRPC/HTTP2 | TLS (configurable) | Bearer Token / SSO / Server SA | CRUD operations on Workflows |
| WorkflowTemplateService | 2746/TCP | gRPC/HTTP2 | TLS (configurable) | Bearer Token / SSO / Server SA | Manage WorkflowTemplates |
| ClusterWorkflowTemplateService | 2746/TCP | gRPC/HTTP2 | TLS (configurable) | Bearer Token / SSO / Server SA | Manage ClusterWorkflowTemplates |
| CronWorkflowService | 2746/TCP | gRPC/HTTP2 | TLS (configurable) | Bearer Token / SSO / Server SA | Manage CronWorkflows |
| EventService | 2746/TCP | gRPC/HTTP2 | TLS (configurable) | Bearer Token / SSO / Server SA | Submit events to trigger workflows |
| EventSourceService | 2746/TCP | gRPC/HTTP2 | TLS (configurable) | Bearer Token / SSO / Server SA | Manage Argo Events EventSources |
| SensorService | 2746/TCP | gRPC/HTTP2 | TLS (configurable) | Bearer Token / SSO / Server SA | Manage Argo Events Sensors |
| InfoService | 2746/TCP | gRPC/HTTP2 | TLS (configurable) | Bearer Token / SSO / Server SA | Server version and configuration info |
| ArchivedWorkflowService | 2746/TCP | gRPC/HTTP2 | TLS (configurable) | Bearer Token / SSO / Server SA | Query archived workflow history |

### HTTP Endpoints

| Path | Method | Port | Protocol | Encryption | Auth | Purpose |
|------|--------|------|----------|------------|------|---------|
| /api/v1/* | Various | 2746/TCP | HTTP/HTTPS | TLS (configurable) | Bearer Token / SSO | REST API via grpc-gateway proxy |
| /artifacts/* | GET/PUT | 2746/TCP | HTTP/HTTPS | TLS (configurable) | Bearer Token / SSO | Artifact upload/download |
| /metrics | GET | 9090/TCP | HTTP | plaintext | None | Prometheus metrics (workflow-controller) |
| /healthz | GET | 6060/TCP | HTTP | plaintext | None | Liveness probe (workflow-controller) |
| / | GET | 2746/TCP | HTTPS | TLS | None | Readiness probe (argo-server) |

## Dependencies

### External Dependencies

| Component | Version | Required | Purpose |
|-----------|---------|----------|---------|
| Kubernetes API | v1.24+ | Yes | Core platform for pod management and CRD operations |
| PostgreSQL | Any | No | Optional workflow archival persistence |
| MySQL | Any | No | Optional workflow archival persistence |
| S3-compatible storage | Any | No | Artifact storage (MinIO, AWS S3, GCS, Azure Blob) |
| OIDC provider | Any | No | SSO authentication for argo-server |
| HDFS | Any | No | Optional artifact storage backend |
| Git | Any | No | Optional artifact storage backend |

### Internal ODH Dependencies

| Component | Interaction Type | Purpose |
|-----------|------------------|---------|
| Data Science Pipelines Operator | CRD consumer | DSP Operator deploys and configures workflow-controller and argoexec |
| Data Science Pipelines API Server | Workflow CR creation | DSP API server creates Workflow CRs that the controller processes |
| ML Metadata (MLMD) | Sidecar in workflow pods | Tracks pipeline metadata and lineage |

## Network Architecture

### Services

| Service Name | Type | Port | Target Port | Protocol | Encryption | Auth | Exposure |
|--------------|------|------|-------------|----------|------------|------|----------|
| argo-server | ClusterIP | 2746/TCP | 2746 | HTTPS | TLS (configurable) | Bearer/SSO/Server | Internal |
| workflow-controller (metrics) | — | 9090/TCP | 9090 | HTTP | plaintext | None | Internal (metrics scraping) |
| workflow-controller (pprof/healthz) | — | 6060/TCP | 6060 | HTTP | plaintext | None | Internal (probes) |

### Ingress

No static ingress resources are defined in the manifests. In RHOAI, the Data Science Pipelines Operator manages ingress to the pipeline services. The argo-server is typically accessed only cluster-internally through the DSP API server.

### Egress

| Destination | Port | Protocol | Encryption | Auth | Purpose |
|-------------|------|----------|------------|------|---------|
| Kubernetes API server | 443/TCP | HTTPS | TLS 1.2+ | Service Account Token | Pod CRUD, CRD operations, leader election |
| S3/MinIO artifact storage | 443/TCP or 9000/TCP | HTTPS/HTTP | TLS (configurable) | Access Key / IAM | Artifact upload/download |
| PostgreSQL (optional) | 5432/TCP | TCP | TLS (configurable) | Username/Password | Workflow archival persistence |
| MySQL (optional) | 3306/TCP | TCP | TLS (configurable) | Username/Password | Workflow archival persistence |
| OIDC provider (optional) | 443/TCP | HTTPS | TLS 1.2+ | Client credentials | SSO token validation |

## Security

### RBAC - Cluster Roles

| Role Name | API Group | Resources | Verbs |
|-----------|-----------|-----------|-------|
| argo-cluster-role | "" | pods, pods/exec | create, get, list, watch, update, patch, delete |
| argo-cluster-role | "" | configmaps | get, watch, list |
| argo-cluster-role | "" | persistentvolumeclaims, persistentvolumeclaims/finalizers | create, update, delete, get |
| argo-cluster-role | argoproj.io | workflows, workflows/finalizers, workflowtasksets, workflowtasksets/finalizers, workflowartifactgctasks | get, list, watch, update, patch, delete, create |
| argo-cluster-role | argoproj.io | workflowtemplates, workflowtemplates/finalizers, clusterworkflowtemplates, clusterworkflowtemplates/finalizers | get, list, watch |
| argo-cluster-role | argoproj.io | workflowtaskresults | list, watch, deletecollection |
| argo-cluster-role | "" | serviceaccounts | get, list |
| argo-cluster-role | argoproj.io | cronworkflows, cronworkflows/finalizers | get, list, watch, update, patch, delete |
| argo-cluster-role | "" | events | create, patch |
| argo-cluster-role | policy | poddisruptionbudgets | create, get, delete |
| argo-cluster-role | "" | secrets (resourceNames: argo-workflows-agent-ca-certificates) | get |
| argo-server-cluster-role | "" | configmaps | get, watch, list |
| argo-server-cluster-role | "" | secrets | get, create |
| argo-server-cluster-role | "" | pods, pods/exec, pods/log | get, list, watch, delete |
| argo-server-cluster-role | "" | events | watch, create, patch |
| argo-server-cluster-role | "" | serviceaccounts | get, list, watch |
| argo-server-cluster-role | argoproj.io | eventsources, sensors, workflows, workfloweventbindings, workflowtemplates, cronworkflows, clusterworkflowtemplates | create, get, list, watch, update, patch, delete |
| argo-aggregate-to-view | argoproj.io | workflows, workfloweventbindings, workflowtemplates, cronworkflows, clusterworkflowtemplates, workflowtaskresults (+ finalizers) | get, list, watch |
| argo-aggregate-to-edit | argoproj.io | workflows, workfloweventbindings, workflowtemplates, cronworkflows, clusterworkflowtemplates, workflowtaskresults (+ finalizers) | create, delete, deletecollection, get, list, patch, update, watch |
| argo-aggregate-to-admin | argoproj.io | workflows, workfloweventbindings, workflowtemplates, cronworkflows, clusterworkflowtemplates, workflowtasksets, workflowtaskresults (+ finalizers) | create, delete, deletecollection, get, list, patch, update, watch |

### RBAC - Role Bindings

| Binding Name | Namespace | Role | Service Account |
|--------------|-----------|------|-----------------|
| argo-binding | (cluster) | argo-cluster-role | argo |
| argo-server-binding | (cluster) | argo-server-cluster-role | argo-server |

### Secrets

| Secret Name | Type | Purpose | Provisioned By | Auto-Rotate |
|-------------|------|---------|----------------|-------------|
| argo-workflows-agent-ca-certificates | Opaque | CA certificates for agent executor pods | Operator/Admin | No |
| argo-server-sso (optional) | Opaque | SSO client ID and client secret | Admin | No |
| Database credentials (optional) | Opaque | PostgreSQL/MySQL username and password for persistence | Admin | No |
| workflow-controller-configmap | ConfigMap | Controller configuration including artifact repos, persistence, SSO | Admin/Operator | No |

### Authentication & Authorization

| Endpoint | Methods | Auth Mechanism | Enforcement Point | Policy |
|----------|---------|----------------|-------------------|--------|
| gRPC/REST API (argo-server) | All | Bearer Token (SA Token) | Gatekeeper interceptor | Kubernetes RBAC impersonation |
| gRPC/REST API (argo-server) | All | SSO (OIDC JWT) | Gatekeeper interceptor | OIDC + optional RBAC mapping |
| gRPC/REST API (argo-server) | All | Server mode (controller SA) | Gatekeeper interceptor | Uses server's own service account |
| Metrics endpoint | GET | None | N/A | Unauthenticated |

## Data Flows

### Flow 1: Workflow Execution

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | DSP API Server / User | Kubernetes API | 443/TCP | HTTPS | TLS 1.2+ | Service Account Token |
| 2 | Kubernetes API (watch) | workflow-controller | N/A | HTTPS | TLS 1.2+ | Service Account Token |
| 3 | workflow-controller | Kubernetes API (create pod) | 443/TCP | HTTPS | TLS 1.2+ | Service Account Token (argo) |
| 4 | argoexec (init/sidecar in pod) | Kubernetes API | 443/TCP | HTTPS | TLS 1.2+ | Service Account Token |
| 5 | argoexec | S3/MinIO (artifact upload) | 443/TCP | HTTPS | TLS (configurable) | Access Key |
| 6 | argoexec | Kubernetes API (WorkflowTaskResult) | 443/TCP | HTTPS | TLS 1.2+ | Service Account Token |

### Flow 2: Workflow API Access (via argo-server)

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | Client (UI/CLI) | argo-server | 2746/TCP | HTTPS | TLS | Bearer Token / SSO |
| 2 | argo-server | Kubernetes API | 443/TCP | HTTPS | TLS 1.2+ | Impersonated SA Token |
| 3 | argo-server | PostgreSQL/MySQL (optional) | 5432/3306/TCP | TCP | TLS (configurable) | Username/Password |

### Flow 3: CronWorkflow Scheduling

| Step | Source | Destination | Port | Protocol | Encryption | Auth |
|------|--------|-------------|------|----------|------------|------|
| 1 | workflow-controller (cron scheduler) | Kubernetes API | 443/TCP | HTTPS | TLS 1.2+ | Service Account Token (argo) |
| 2 | workflow-controller | Kubernetes API (create Workflow CR) | 443/TCP | HTTPS | TLS 1.2+ | Service Account Token (argo) |

## Integration Points

| Component | Interaction Type | Port | Protocol | Encryption | Purpose |
|-----------|------------------|------|----------|------------|---------|
| Kubernetes API | REST API | 443/TCP | HTTPS | TLS 1.2+ | Core platform operations (pods, CRDs, events, leader election) |
| S3/MinIO | S3 API | 443/TCP or 9000/TCP | HTTPS/HTTP | TLS (configurable) | Artifact storage and retrieval |
| PostgreSQL | SQL | 5432/TCP | TCP | TLS (configurable) | Workflow archive persistence |
| MySQL | SQL | 3306/TCP | TCP | TLS (configurable) | Workflow archive persistence |
| Prometheus | HTTP scrape | 9090/TCP | HTTP | plaintext | Metrics collection |
| OIDC Provider | HTTPS | 443/TCP | HTTPS | TLS 1.2+ | SSO authentication for argo-server |
| Data Science Pipelines | CRD (Workflow) | N/A | N/A | N/A | DSP creates Workflow CRs for pipeline execution |

## Build Information

### RHOAI Container Images

| Image | Dockerfile | FIPS | Base Image |
|-------|-----------|------|------------|
| odh-data-science-pipelines-argo-workflowcontroller | argo-workflowcontroller/Dockerfile.konflux | Yes (strictfipsruntime, CGO_ENABLED=1) | ubi9/ubi-minimal |
| odh-data-science-pipelines-argo-argoexec | argo-argoexec/Dockerfile.konflux | Yes (strictfipsruntime + non-FIPS static binary) | ubi9/ubi-minimal |

Notes:
- The argoexec image ships two binaries: `argoexec` (non-FIPS, statically linked, CGO_ENABLED=0) and `argoexec-fips` (FIPS-compliant, CGO_ENABLED=1)
- Both images run as non-root users (8737 for workflowcontroller, 2000 for argoexec)
- Built on UBI9 with Go 1.23 toolset via Konflux pipelines

## Recent Changes

| Version | Date | Changes |
|---------|------|---------|
| rhoai-2.24 | 2025+ | - Merge upstream/main into rhoai-2.24<br>- Enable ARM (aarch64/ppc64le) builds with manifest list UBI images<br>- Update rpms.in.yaml for aarch64 support<br>- Sync pipelineruns with konflux-central (multiple iterations)<br>- Update go-toolset base image digests (1.23)<br>- Retry Konflux builds for RHOAIENG-31914 |

## Source References

### Files Analyzed

| File | Lines | Sections Informed |
|------|-------|-------------------|
| go.mod | 1-99 | Metadata, Dependencies, Architecture Components |
| version.go | 1-60 | Metadata |
| config/config.go | 1-277 | Architecture Components, Dependencies, Network Architecture, Security |
| config/sso.go | 1-33 | Security (Authentication) |
| server/auth/mode.go | 1-45 | Security (Authentication) |
| server/auth/gatekeeper.go | 1-60 | Security (Authentication) |
| server/apiserver/argoserver.go | 1-350 | APIs Exposed, Network Architecture, Architecture Components, gRPC Services |
| cmd/workflow-controller/main.go | 1-80 | Architecture Components |
| cmd/argoexec/main.go | 1-36 | Architecture Components |
| workflow/controller/controller.go | 1-80 | Architecture Components, Dependencies |
| manifests/base/argo-server/argo-server-deployment.yaml | 1-47 | Network Architecture, Architecture Components |
| manifests/base/argo-server/argo-server-service.yaml | 1-12 | Network Architecture (Services) |
| manifests/base/workflow-controller/workflow-controller-deployment.yaml | 1-52 | Network Architecture, Architecture Components |
| manifests/base/workflow-controller/workflow-controller-configmap.yaml | 1-5 | Security (Secrets) |
| manifests/base/workflow-controller/workflow-controller-priorityclass.yaml | 1-6 | Architecture Components |
| manifests/base/crds/full/argoproj.io_workflows.yaml | 1-30 | CRDs |
| manifests/base/crds/full/argoproj.io_clusterworkflowtemplates.yaml | (metadata) | CRDs |
| manifests/base/crds/full/argoproj.io_cronworkflows.yaml | (metadata) | CRDs |
| manifests/base/crds/full/argoproj.io_workflowartifactgctasks.yaml | (metadata) | CRDs |
| manifests/base/crds/full/argoproj.io_workfloweventbindings.yaml | (metadata) | CRDs |
| manifests/base/crds/full/argoproj.io_workflowtaskresults.yaml | (metadata) | CRDs |
| manifests/base/crds/full/argoproj.io_workflowtasksets.yaml | (metadata) | CRDs |
| manifests/base/crds/full/argoproj.io_workflowtemplates.yaml | (metadata) | CRDs |
| manifests/cluster-install/workflow-controller-rbac/workflow-controller-clusterrole.yaml | 1-113 | RBAC - Cluster Roles |
| manifests/cluster-install/workflow-controller-rbac/workflow-controller-clusterrolebinding.yaml | 1-12 | RBAC - Role Bindings |
| manifests/cluster-install/workflow-controller-rbac/workflow-aggregate-roles.yaml | 1-95 | RBAC - Cluster Roles |
| manifests/cluster-install/argo-server-rbac/argo-server-clusterole.yaml | 1-66 | RBAC - Cluster Roles |
| manifests/cluster-install/argo-server-rbac/argo-server-clusterolebinding.yaml | 1-12 | RBAC - Role Bindings |
| argo-workflowcontroller/Dockerfile.konflux | 1-49 | Build Information, Metadata |
| argo-argoexec/Dockerfile.konflux | 1-56 | Build Information, Metadata |
| pkg/apiclient/workflow/workflow.proto | 1-60 | APIs Exposed (gRPC Services) |
| ui/package.json | 1-30 | Architecture Components |

### Grep/Search Results Used

| Search Pattern | Files Matched | Sections Informed |
|----------------|---------------|-------------------|
| CRD metadata (group, scope, kind) | manifests/base/crds/full/*.yaml | CRDs |
| CRD versions (name: v) | manifests/base/crds/full/*.yaml | CRDs |
| MetricsConfig | config/config.go, workflow/controller/controller.go | Network Architecture, APIs Exposed |
| PersistConfig | config/config.go | Dependencies, Egress |
| AuthMode/Mode | server/auth/mode.go | Security (Authentication) |
| *.proto files | pkg/apiclient/*/*.proto | APIs Exposed (gRPC Services) |
| *networkpolic* | (none found) | Network Architecture |
| *secret*.yaml | manifests/quick-start/base/*.yaml | Security (Secrets) |
| Dockerfile.konflux | argo-argoexec/, argo-workflowcontroller/ | Build Information |

### Summary

- **Total files read**: 31
- **Total lines referenced**: ~1,400
- **Coverage**: All sections have direct source file backing. The "Internal ODH Dependencies" section is partially inferred from the RHOAI distribution context (DSP Operator integration) and Dockerfile labels confirming "Data Science Pipelines" usage. The "Ingress" section documents absence of static ingress resources based on exhaustive file search.

---
*Generated in 5m 49s (350s total)*
