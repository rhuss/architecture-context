FMS Guardrails Orchestrator - Security Network Diagram
Generated by: Claude Opus 4.6
======================================================================

┌──────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Clients / Applications)                                       │
│                                                                          │
│  [Client Application]                                                    │
│         │                                                                │
│         │ Port: 8033/TCP                                                 │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: TLS 1.2+ (optional, rustls)                        │
│         │ Auth: mTLS client certs (optional, WebPkiClientVerifier)       │
│         ▼                                                                │
├──────────────────────────────────────────────────────────────────────────┤
│  GUARDRAILS ORCHESTRATOR SERVICE                                         │
│                                                                          │
│  [Guardrails Server - Main API]                                          │
│  - Port: 8033/TCP (inbound)                                              │
│  - Protocol: HTTP/HTTPS                                                  │
│  - TLS Modes:                                                            │
│    1. No TLS (default, plaintext HTTP)                                   │
│    2. TLS (TLS_CERT_PATH + TLS_KEY_PATH set)                             │
│    3. mTLS (+ TLS_CLIENT_CA_CERT_PATH set)                               │
│  - Endpoints:                                                            │
│    POST /api/v1/task/classification-with-text-generation                 │
│    POST /api/v1/task/server-streaming-classification-with-text-generation │
│    POST /api/v2/text/detection/content                                   │
│    POST /api/v2/text/detection/stream-content                            │
│    POST /api/v2/text/generation-detection                                │
│    POST /api/v2/text/detection/chat                                      │
│    POST /api/v2/text/detection/context                                   │
│    POST /api/v2/text/detection/generated                                 │
│    POST /api/v2/chat/completions-detection                               │
│    POST /api/v2/text/completions-detection                               │
│                                                                          │
│  [Health Server]                                                         │
│  - Port: 8034/TCP (inbound)                                              │
│  - Protocol: HTTP                                                        │
│  - Encryption: plaintext (no TLS)                                        │
│  - Auth: None                                                            │
│  - Endpoints:                                                            │
│    GET /health (liveness check)                                          │
│    GET /info (backend client health status)                               │
│         │                                                                │
│         │ OUTBOUND CONNECTIONS                                           │
│         │                                                                │
│    ┌────┼────────────────┬──────────────────┬──────────────┐             │
│    │    │                │                  │              │             │
│    ▼    ▼                ▼                  ▼              ▼             │
├──────────────────────────────────────────────────────────────────────────┤
│  BACKEND SERVICES (Internal)                                             │
│                                                                          │
│  [TGIS Generation Server]                                                │
│  - Port: configurable/TCP                                                │
│  - Protocol: gRPC (HTTP/2)                                               │
│  - Encryption: TLS/mTLS (configurable per named TLS profile)             │
│  - Auth: mTLS client certs (optional)                                    │
│  - Service: GenerationService (unary + streaming)                        │
│                                                                          │
│  [Caikit NLP Generation Server]                                          │
│  - Port: configurable/TCP                                                │
│  - Protocol: gRPC (HTTP/2)                                               │
│  - Encryption: TLS/mTLS (configurable per named TLS profile)             │
│  - Auth: mTLS client certs (optional)                                    │
│  - Service: NlpService (unary + streaming)                               │
│                                                                          │
│  [OpenAI-compatible Server (e.g. vLLM)]                                  │
│  - Port: 8080/TCP (default)                                              │
│  - Protocol: HTTP/HTTPS                                                  │
│  - Encryption: TLS (configurable)                                        │
│  - Auth: None (header passthrough)                                       │
│  - Endpoints: /v1/chat/completions, /v1/completions, /tokenize           │
│                                                                          │
│  [Content Safety Detector Services]                                      │
│  - Port: 8080/TCP (default)                                              │
│  - Protocol: HTTP/HTTPS                                                  │
│  - Encryption: TLS/mTLS (configurable per named TLS profile)             │
│  - Auth: None (header passthrough)                                       │
│  - Endpoints: /api/v1/text/contents, /api/v1/text/chat,                  │
│    /api/v1/text/context/doc, /api/v1/text/generation                     │
│                                                                          │
│  [Caikit Chunker Services]                                               │
│  - Port: configurable/TCP                                                │
│  - Protocol: gRPC (HTTP/2)                                               │
│  - Encryption: TLS/mTLS (configurable per named TLS profile)             │
│  - Auth: mTLS client certs (optional)                                    │
│  - Service: ChunkersService                                              │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│  OBSERVABILITY (Optional)                                                │
│                                                                          │
│  [OpenTelemetry Collector]                                               │
│  - Port: 4317/TCP (gRPC) or 4318/TCP (HTTP)                             │
│  - Protocol: gRPC or HTTP (OTLP)                                        │
│  - Encryption: configurable                                              │
│  - Auth: None                                                            │
│  - Purpose: Distributed tracing and metrics export                       │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No Kubernetes RBAC resources defined (standalone application service)
- No ClusterRoles, Roles, or RoleBindings
- No ServiceAccounts defined in this component

Service Mesh Configuration:
- No PeerAuthentication or AuthorizationPolicy defined
- Ingress managed externally by platform operator or service mesh
- The orchestrator is deployed as an internal service

Secrets:
- TLS server cert/key (PEM) - Server-side TLS for port 8033, provisioned externally (e.g. cert-manager), no auto-rotate
- TLS client CA cert (PEM) - mTLS client verification, provisioned externally, no auto-rotate
- Client TLS cert/key per service (PEM) - mTLS for outbound connections, provisioned externally, no auto-rotate

TLS Configuration Notes:
- Server TLS controlled by: TLS_CERT_PATH, TLS_KEY_PATH, TLS_CLIENT_CA_CERT_PATH
- Client TLS per-service: named TLS profiles in config.yaml (cert_path, key_path, ca_cert_path, insecure flag)
- TLS implementation: rustls with ring backend
- mTLS verification: WebPkiClientVerifier against provided CA trust store
