Security Network Diagram for ilab-on-ocp (InstructLab on OCP)
Generated by: Claude Opus 4.6
Source: architecture/rhoai-2.24/ilab-on-ocp.md

┌─────────────────────────────────────────────────────────────────────────────┐
│  PIPELINE RUNTIME (KFP v2 / Argo Workflow Steps)                           │
│                                                                             │
│  ServiceAccount: pipeline-runner-dspa                                       │
│  Namespace: {user-namespace}                                                │
│                                                                             │
│  ┌──────────────────────┐  ┌──────────────────────┐  ┌──────────────────┐  │
│  │ SDG Task             │  │ Training Task         │  │ Eval Tasks       │  │
│  │ (Synthetic Data Gen) │  │ (PyTorchJob Launcher) │  │ (MT-Bench/MMLU)  │  │
│  └──────┬───────────────┘  └──────┬────────────────┘  └──────┬───────────┘  │
│         │                         │                          │               │
│  ┌──────┴───────────────┐  ┌──────┴────────────────┐  ┌──────┴───────────┐  │
│  │ Utils Tasks          │  │ Prerequisites Task    │  │ Upload Task      │  │
│  │ (Model Import)       │  │ (Health Checks)       │  │ (OCI + Registry) │  │
│  └──────────────────────┘  └───────────────────────┘  └──────────────────┘  │
│         │                         │                          │               │
├─────────┼─────────────────────────┼──────────────────────────┼───────────────┤
│         │                         │                          │               │
│  KUBERNETES API (Internal)        │                          │               │
│         │                         │                          │               │
│         │ Port: 443/TCP           │                          │               │
│         │ Protocol: HTTPS         │                          │               │
│         │ Encryption: TLS 1.2+    │                          │               │
│         │ Auth: ServiceAccount Token                         │               │
│         ▼                         │                          │               │
│  [kubernetes.default.svc]         │                          │               │
│  - Purpose: Secret fetching, PyTorchJob CRUD, PVC mgmt      │               │
│                                   │                          │               │
├───────────────────────────────────┼──────────────────────────┼───────────────┤
│  CLUSTER-INTERNAL (Service Mesh / mTLS)                      │               │
│                                                              │               │
│  ┌────────────────────────────────────────────────────────┐  │               │
│  │  PyTorchJob (Distributed Training)                     │  │               │
│  │  Namespace: {user-namespace}                           │  │               │
│  │                                                        │  │               │
│  │  [Master Pod] ◄──────► [Worker Pod(s)]                │  │               │
│  │  - Port: MASTER_PORT/TCP                               │  │               │
│  │  - Protocol: TCP (NCCL/Gloo rendezvous)               │  │               │
│  │  - Encryption: plaintext (cluster-internal)            │  │               │
│  │  - Auth: None                                          │  │               │
│  │  - Image: RHEL AI InstructLab                          │  │               │
│  │  - PVCs: sdg-input (RWX), model-cache (RWX),         │  │               │
│  │          output (RWX)                                  │  │               │
│  └────────────────────────────────────────────────────────┘  │               │
│                                                              │               │
│  ┌────────────────────────────────────────────────────────┐  │               │
│  │  Local vLLM Server (Eval Tasks - localhost only)       │  │               │
│  │  - Port: dynamic/TCP                                   │  │               │
│  │  - Protocol: HTTP                                      │  │               │
│  │  - Encryption: plaintext                               │  │               │
│  │  - Auth: None                                          │  │               │
│  │  - Scope: localhost only (within eval pod)             │  │               │
│  └────────────────────────────────────────────────────────┘  │               │
│                                                              │               │
│  ┌────────────────────────────────────────────────────────┐  │               │
│  │  KServe-Managed Model Services                         │  │               │
│  │  - mixtral (Teacher): 8080/TCP, TLS via Service Mesh  │  │               │
│  │  - prometheus (Judge): 8080/TCP, TLS via Service Mesh │  │               │
│  │  - Auth: Bearer Token                                  │  │               │
│  │  - Exposure: Internal via Istio Gateway                │  │               │
│  └────────────────────────────────────────────────────────┘  │               │
│                                                              │               │
│  ┌────────────────────────────────────────────────────────┐  │               │
│  │  NFS Server (Optional - Dev Only)                      │  │               │
│  │  - Namespace: nfs                                      │  │               │
│  │  - Port: 2049/TCP (NFS), 111/UDP (RPC)                │  │               │
│  │  - Protocol: NFS/RPC                                   │  │               │
│  │  - Encryption: plaintext                               │  │               │
│  │  - Auth: None                                          │  │               │
│  └────────────────────────────────────────────────────────┘  │               │
│                                                              │               │
├──────────────────────────────────────────────────────────────┼───────────────┤
│  EGRESS TO EXTERNAL SERVICES                                 │               │
│                                                              │               │
│  [Teacher Model Server - configurable]                       │               │
│  - Port: varies                                              │               │
│  - Protocol: HTTPS                                           │               │
│  - Encryption: TLS 1.2+ (custom CA support via ssl context) │               │
│  - Auth: Bearer Token (from teacher-secret)                  │               │
│  - Purpose: Synthetic data generation requests               │               │
│  - Endpoint: /v1/chat/completions                            │               │
│                                                              │               │
│  [Judge Model Server - configurable]                         │               │
│  - Port: varies                                              │               │
│  - Protocol: HTTPS                                           │               │
│  - Encryption: TLS 1.2+ (custom CA support via ssl context) │               │
│  - Auth: Bearer Token (from judge-secret)                    │               │
│  - Purpose: Model evaluation (MT-Bench, MT-Bench Branch)     │               │
│  - Endpoint: /v1/chat/completions                            │               │
│                                                              │               │
│  [OCI Container Registry - configurable]                     │               │
│  - Port: 443/TCP                                             │               │
│  - Protocol: HTTPS                                           │               │
│  - Encryption: TLS 1.2+                                      │               │
│  - Auth: dockerconfigjson                                    │               │
│  - Purpose: Push trained ModelCar image                      │               │
│                                                              │               │
│  [Git Repository - configurable]                             │               │
│  - Port: 443/TCP (HTTPS) or 22/TCP (SSH)                    │               │
│  - Protocol: HTTPS or SSH                                    │               │
│  - Encryption: TLS 1.2+ or SSH                               │               │
│  - Auth: basic-auth (PAT) or SSH key                         │               │
│  - Purpose: Clone taxonomy repository                        │               │
│                                                              │               │
│  [Model Registry API - configurable]                         │               │
│  - Port: 80 or 443/TCP                                       │               │
│  - Protocol: HTTP/HTTPS                                      │               │
│  - Encryption: TLS 1.2+                                      │               │
│  - Auth: ServiceAccount Token                                │               │
│  - Purpose: Register trained model version                   │               │
│                                                              │               │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

  Supplementary Manifest Roles:
  - Role: mixtral-view-role
    - API Group: serving.kserve.io
    - Resources: inferenceservices (mixtral)
    - Verbs: get

  Role Bindings:
  - mixtral-view → mixtral-view-role → SA: mixtral-sa (user namespace)
  - nfs-server-sa-anyuid → system:openshift:scc:anyuid → SA: nfs-server-sa (nfs)
  - nfs-server-sa-privileged → system:openshift:scc:privileged → SA: nfs-server-sa (nfs)

  Pipeline Runtime Requirements (SA: pipeline-runner-dspa):
  - API Group: ""           Resources: secrets                  Verbs: get
  - API Group: ""           Resources: namespaces               Verbs: get
  - API Group: kubeflow.org Resources: pytorchjobs              Verbs: create, get, list, watch, delete
  - API Group: ""           Resources: persistentvolumeclaims   Verbs: create, delete

Secrets:
  - teacher-secret (Opaque) - Teacher model API credentials (api_token, endpoint, model_name) - User provisioned, no auto-rotate
  - judge-secret (Opaque) - Judge model API credentials (api_token, endpoint, model_name) - User provisioned, no auto-rotate
  - taxonomy-repo-secret (basic-auth or ssh-auth) - Git credentials for taxonomy - User provisioned, no auto-rotate
  - OCI registry secret (dockerconfigjson) - Push credentials for OCI model output - User provisioned, no auto-rotate
  - hugging-face-token (Opaque) - HuggingFace API token for model download - User provisioned, no auto-rotate
  - registry-user-{model-registry-name} (RBAC Role) - Model Registry write access - Admin provisioned, no auto-rotate

Authentication & Authorization:
  - Teacher model /v1/chat/completions → Bearer Token (from secret) → Model server → API key validation
  - Judge model /v1/chat/completions → Bearer Token (from secret) → Model server → API key validation
  - Kubernetes API (secrets) → ServiceAccount Token → kube-apiserver → RBAC
  - Model Registry API → ServiceAccount Token → Model Registry → RBAC
  - OCI Registry → dockerconfigjson → Registry → Token auth
  - Git Repository → basic-auth (PAT) or SSH key → Git server → Credential-based
