// C4 Context Diagram: Guardrails Regex Detector
// Generated by: Claude Opus 4.6
// Source: architecture/rhoai-2.24/guardrails-regex-detector.md

workspace {
    model {
        orchestrator = softwareSystem "FMS Guardrails Orchestrator" "Orchestrates content safety guardrails for AI/ML inference pipelines; routes text to detector services" "Internal RHOAI"

        regexDetector = softwareSystem "Guardrails Regex Detector" "Lightweight Rust-based HTTP microservice that detects PII (email, SSN, credit card) and custom regex patterns in text" {
            server = container "regex-detector" "Axum HTTP server; listens on port 8080 for detection requests" "Rust (Axum + Tokio)"
            detectors = container "Detectors Module" "Built-in PII detectors (email, SSN, credit card) and custom regex matching" "Rust Module"
        }

        kubelet = softwareSystem "Kubernetes Kubelet" "Node agent that runs health probes against pods" "Infrastructure"

        // Relationships
        orchestrator -> regexDetector "Sends text for PII/regex detection" "HTTP/8080 plaintext"
        kubelet -> regexDetector "Health check probes" "HTTP GET /health 8080"

        // Internal relationships
        server -> detectors "Delegates regex matching" "In-process"
    }

    views {
        systemContext regexDetector "SystemContext" {
            include *
            autoLayout
        }

        container regexDetector "Containers" {
            include *
            autoLayout
        }

        styles {
            element "Software System" {
                background #438dd5
                color #ffffff
            }
            element "Internal RHOAI" {
                background #7ed321
                color #ffffff
            }
            element "Infrastructure" {
                background #999999
                color #ffffff
            }
            element "Container" {
                background #438dd5
                color #ffffff
            }
        }
    }
}
