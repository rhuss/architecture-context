Security Network Diagram: vllm-orchestrator-gateway
Generated by: Claude Opus 4.6
Source: architecture/rhoai-2.24/vllm-orchestrator-gateway.md
Date: 2026-03-20

┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Client / API Consumer]                                                 │
│         │                                                                │
│         │ Port: 8090/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: plaintext                                          │
│         │ Auth: Authorization header (optional, passthrough)             │
│         │ Headers: x-forwarded-* (passthrough)                           │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  GATEWAY SERVICE                                                         │
│                                                                          │
│  [vllm-orchestrator-gateway]                                             │
│  - Language: Rust 1.84.0 (axum framework)                               │
│  - Listen: 0.0.0.0:8090/TCP (configurable via HOST, HTTP_PORT)          │
│  - Image: quay.io/rhoai/odh-trustyai-vllm-orchestrator-gateway-rhel9   │
│  - Base: registry.access.redhat.com/ubi9/ubi-minimal:latest             │
│  - Binary: /app/bin/vllm-orchestrator-gateway                           │
│  - Config: /app/config/config.yaml (GATEWAY_CONFIG env)                 │
│                                                                          │
│  Dynamic Routes (from config.yaml):                                      │
│  - /{route_name}/v1/chat/completions (POST)                             │
│  - e.g., /pii/v1/chat/completions                                       │
│  - e.g., /passthrough/v1/chat/completions                                │
│                                                                          │
│  Auth Model: PASSTHROUGH ONLY                                            │
│  - Gateway does NOT enforce authentication                               │
│  - Forwards Authorization header to orchestrator                         │
│  - Forwards x-forwarded-* headers to orchestrator                        │
│  - Auth enforcement delegated to orchestrator/upstream                    │
│                                                                          │
│  Detection Handling:                                                      │
│  - If detections found + fallback_message configured → substitute reply  │
│  - Otherwise → forward orchestrator response directly                    │
│         │                                                                │
│         │ Port: 8085/TCP (configurable)                                  │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: Optional mTLS                                      │
│         │   - Auto-detected based on certificate presence                │
│         │   - HTTPS when client cert + key present                       │
│         │   - HTTP when certificates absent                              │
│         │ Auth: mTLS client certificate + forwarded Authorization header │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  BACKEND SERVICES (Internal)                                             │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Endpoint: /api/v2/chat/completions-detection (POST)                  │
│  - Port: 8085/TCP (configurable via orchestrator.port in config.yaml)   │
│  - Purpose: Content detection and guardrailed generation                 │
│  - Auth: Receives mTLS client identity + forwarded Authorization header │
│         │                          │                                     │
│         │                          │                                     │
│         ▼                          ▼                                     │
│  [vLLM / LLM Runtime]      [Detector Service(s)]                        │
│  - e.g., Qwen2.5-1.5B      - e.g., regex-language                      │
│  - Model inference          - PII detection                              │
│  - Port/Protocol: Varies   - Content filtering                           │
│                             - Port/Protocol: Varies                      │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

TLS Certificate Configuration:
┌─────────────────────────────────────────────────────────────────────────┐
│  Certificate          │ Path                         │ Provisioner      │
│  ─────────────────────┼──────────────────────────────┼────────────────  │
│  CA Certificate       │ /etc/tls/ca/service-ca.crt   │ OpenShift        │
│                       │                              │ service-ca       │
│  Client Certificate   │ /etc/tls/private/tls.crt     │ OpenShift        │
│                       │                              │ service-ca /     │
│                       │                              │ cert-manager     │
│  Client Private Key   │ /etc/tls/private/tls.key     │ OpenShift        │
│                       │                              │ service-ca /     │
│                       │                              │ cert-manager     │
│                                                                          │
│  Behavior:                                                               │
│  - If client cert AND key exist → HTTPS with mTLS to orchestrator       │
│  - If only CA cert exists → HTTPS (server verification only)             │
│  - If no certs exist → HTTP (plaintext)                                  │
│  - Auto-rotation depends on provisioner (service-ca / cert-manager)      │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC resources defined
- This is a standalone service without Kubernetes API access
- No ServiceAccount, ClusterRole, or RoleBinding resources

Security Notes:
- Gateway listens on plaintext HTTP (8090/TCP) — relies on network/mesh for ingress encryption
- No built-in authentication enforcement — purely passthrough
- mTLS to orchestrator is optional (depends on certificate mount)
- No Kubernetes Ingress/Route defined — exposure managed externally
- Part of TrustyAI guardrails stack within RHOAI ecosystem
