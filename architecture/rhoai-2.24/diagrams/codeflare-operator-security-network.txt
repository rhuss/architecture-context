CodeFlare Operator - Security Network Diagram
Generated by: Claude Opus 4.6

┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [User / kubectl]                    [User Browser]         [Ray Client]     │
│         │                                  │                      │          │
│         │ Port: 443/TCP                    │ Port: 443/TCP        │ 443/TCP  │
│         │ Protocol: HTTPS                  │ Protocol: HTTPS      │ HTTPS    │
│         │ Encryption: TLS 1.2+             │ Encryption: TLS 1.2+ │ mTLS    │
│         │ Auth: Bearer Token/kubeconfig    │ Auth: None (→OAuth)  │ Client   │
│         │                                  │                      │ Cert     │
│         ▼                                  ▼                      ▼          │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS LAYER                                                               │
│                                                                              │
│  [Kubernetes API Server]              [OpenShift Router]                      │
│  - Port: 443/TCP (external)           - Port: 443/TCP (external)             │
│  - TLS Termination: TLS 1.2+          - ray-dashboard-{cluster}: Reencrypt   │
│  - Auth: Bearer Token, kubeconfig     - rayclient-{cluster}: Passthrough     │
│         │                                  │                 │               │
│         │ Port: 9443/TCP                   │ Port: 8443/TCP  │ 10001/TCP    │
│         │ Protocol: HTTPS                  │ Protocol: HTTPS │ HTTPS        │
│         │ Encryption: TLS                  │ Encryption: TLS │ mTLS         │
│         │ Auth: API Server CA              │ (reencrypt)     │ (passthrough)│
│         ▼                                  ▼                 ▼              │
├──────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (opendatahub)                                            │
│                                                                              │
│  [codeflare-operator-manager]                                                │
│  - Deployment: codeflare-operator-controller-manager                         │
│  - ServiceAccount: codeflare-operator-controller-manager                     │
│  - Namespace: opendatahub                                                    │
│                                                                              │
│  ┌─ Webhook Server (9443/TCP) ──────────────────────────────────┐            │
│  │  /mutate-ray-io-v1-raycluster          POST  HTTPS  TLS     │            │
│  │  /validate-ray-io-v1-raycluster        POST  HTTPS  TLS     │            │
│  │  /mutate-workload-...-appwrapper       POST  HTTPS  TLS     │            │
│  │  /validate-workload-...-appwrapper     POST  HTTPS  TLS     │            │
│  │  Cert: codeflare-operator-webhook-server-cert (auto-rotated) │            │
│  └──────────────────────────────────────────────────────────────┘            │
│                                                                              │
│  ┌─ Health/Metrics ─────────────────────────────────────────────┐            │
│  │  /healthz   GET  8081/TCP  HTTP  None  None (liveness)       │            │
│  │  /readyz    GET  8081/TCP  HTTP  None  None (readiness)      │            │
│  │  /metrics   GET  8080/TCP  HTTP  None  None (Prometheus)     │            │
│  └──────────────────────────────────────────────────────────────┘            │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount Token                                         │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  USER NAMESPACE ({raycluster-namespace})                                     │
│                                                                              │
│  [RayCluster Head Pod]                                                       │
│  ┌─ Sidecar: oauth-proxy ──────────────────────────────────┐                 │
│  │  - Port: 8443/TCP  HTTPS  TLS (serving-cert)            │                 │
│  │  - Auth: OpenShift OAuth (cookie-based)                  │                 │
│  │  - Upstream: http://localhost:8265                        │                 │
│  │  - SAR: "get pods" in namespace                          │                 │
│  └──────────────────────────────────────────────────────────┘                │
│  ┌─ Init: create-cert ─────────────────────────────────────┐                 │
│  │  - Generates TLS certs from CA secret for mTLS          │                 │
│  └──────────────────────────────────────────────────────────┘                │
│  ┌─ Ray Head Process ──────────────────────────────────────┐                 │
│  │  - Dashboard: 8265/TCP  HTTP (localhost only)            │                 │
│  │  - Client API: 10001/TCP  mTLS (RAY_USE_TLS=1)          │                 │
│  │  - Inter-node: Various ports  mTLS                       │                 │
│  └──────────────────────────────────────────────────────────┘                │
│                                                                              │
│  [RayCluster Worker Pods]                                                    │
│  ┌─ Init: create-cert ─────────────────────────────────────┐                 │
│  │  - Generates TLS certs from CA secret for mTLS          │                 │
│  └──────────────────────────────────────────────────────────┘                │
│  ┌─ Ray Worker Process ────────────────────────────────────┐                 │
│  │  - Inter-node: Various ports  mTLS (RAY_USE_TLS=1)      │                 │
│  └──────────────────────────────────────────────────────────┘                │
│                                                                              │
│  ┌─ NetworkPolicy: {cluster}-head ─────────────────────────┐                 │
│  │  Allow: Same cluster pods (all ports)                    │                 │
│  │  Allow: Namespace pods on 10001/TCP, 8265/TCP            │                 │
│  │  Allow: kuberay-operator on 8265/TCP, 10001/TCP          │                 │
│  │  Allow: openshift-monitoring on 8080/TCP                 │                 │
│  │  Allow: 8443/TCP (OAuth), 10001/TCP (mTLS client)        │                 │
│  └──────────────────────────────────────────────────────────┘                │
│  ┌─ NetworkPolicy: {cluster}-workers ──────────────────────┐                 │
│  │  Allow: Same cluster pods only                           │                 │
│  └──────────────────────────────────────────────────────────┘                │
│                                                                              │
│  [Services]                                                                  │
│  - {cluster}-oauth: ClusterIP 443→8443 HTTPS (fronts oauth-proxy)           │
│                                                                              │
│  [Secrets]                                                                   │
│  - {cluster}-oauth-config: OAuth proxy cookie secret                         │
│  - {cluster}-proxy-tls-secret: OAuth proxy TLS cert (serving-cert-signer)   │
│  - ca-secret-{cluster}: Self-signed CA for mTLS between Ray nodes           │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  [Kubernetes API Server - kube-apiserver]                                     │
│  - Port: 443/TCP  HTTPS  TLS 1.2+                                           │
│  - Auth: ServiceAccount Token                                                │
│  - Purpose: CRD CRUD, webhook registration, resource management              │
│                                                                              │
│  [OpenShift Config API]                                                       │
│  - Port: 443/TCP  HTTPS  TLS 1.2+                                           │
│  - Auth: ServiceAccount Token                                                │
│  - Purpose: Read cluster ingress domain (config.openshift.io/ingresses)      │
│                                                                              │
│  [Prometheus / OpenShift Monitoring]                                          │
│  - Port: 8080/TCP  HTTP  None                                                │
│  - Purpose: Scrapes operator metrics                                         │
│                                                                              │
│  [OpenShift serving-cert-signer]                                              │
│  - Annotation-based TLS cert provisioning for oauth-proxy service            │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: codeflare-operator-manager-role
  - ServiceAccount: codeflare-operator-controller-manager (opendatahub)
  - API Groups: ray.io, workload.codeflare.dev, route.openshift.io,
                networking.k8s.io, rbac.authorization.k8s.io,
                admissionregistration.k8s.io, apiextensions.k8s.io,
                authentication.k8s.io, authorization.k8s.io,
                config.openshift.io, dscinitialization.opendatahub.io,
                apps, batch, kubeflow.org, scheduling.sigs.k8s.io,
                scheduling.x-k8s.io, jobset.x-k8s.io, core ("")
  - Key Resources: rayclusters, appwrappers, routes, ingresses,
                   networkpolicies, secrets, services, serviceaccounts,
                   clusterrolebindings, nodes, events, deployments,
                   statefulsets, jobs, pytorchjobs, podgroups, jobsets
  - Key Verbs: get, list, watch, create, update, patch, delete

- ClusterRole: codeflare-operator-appwrapper-editor-role
  - Aggregated to: admin, edit
  - Resources: appwrappers, appwrappers/status
  - Verbs: create, delete, get, list, patch, update, watch

- ClusterRole: codeflare-operator-viewer-role
  - Aggregated to: view
  - Resources: appwrappers, appwrappers/status
  - Verbs: get, list, watch

- Dynamic: {cluster}-{ns}-auth ClusterRoleBinding
  - Role: system:auth-delegator
  - SA: {cluster}-oauth-proxy (per RayCluster namespace)

Secrets:
- codeflare-operator-webhook-server-cert (kubernetes.io/tls)
  - Purpose: Webhook server TLS certificates
  - Provisioned by: cert-controller (auto-rotated)

- {cluster}-oauth-config (Opaque, per RayCluster)
  - Purpose: OAuth proxy cookie secret
  - Provisioned by: Controller (generated)

- {cluster}-proxy-tls-secret (kubernetes.io/tls, per RayCluster)
  - Purpose: OAuth proxy TLS serving cert
  - Provisioned by: OpenShift serving-cert-signer (auto-rotated)

- ca-secret-{cluster} (Opaque, per RayCluster)
  - Purpose: CA certificate for mTLS between Ray nodes
  - Provisioned by: Controller (self-signed CA generated)
