Argo Workflows - Security Network Diagram
Generated by: Claude Opus 4.6

┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [Client (UI/CLI/DSP API)]                                               │
│         │                                                                │
│         │ Port: 2746/TCP                                                 │
│         │ Protocol: gRPC/HTTP2 + REST (grpc-gateway)                     │
│         │ Encryption: TLS (configurable)                                 │
│         │ Auth: Bearer Token (SA Token) / SSO (OIDC JWT)                 │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  API LAYER                                                               │
│                                                                          │
│  [argo-server]                                                           │
│  - Service: argo-server (ClusterIP)                                      │
│  - Port: 2746/TCP                                                        │
│  - Protocol: HTTPS (gRPC + REST via grpc-gateway)                        │
│  - Encryption: TLS (configurable)                                        │
│  - Auth: Gatekeeper interceptor                                          │
│  - Modes: Bearer Token / SSO / Server SA                                 │
│  - ServiceAccount: argo-server                                           │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Impersonated SA Token                                    │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  CONTROL PLANE (Internal)                                                │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Service Account Token                                           │
│         │                                                                │
│         │ Watch events (Workflow CRDs)                                   │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Service Account Token (argo)                             │
│         ▼                                                                │
│  [workflow-controller]                                                   │
│  - ServiceAccount: argo                                                  │
│  - Metrics: 9090/TCP HTTP plaintext (unauthenticated)                    │
│  - Health: 6060/TCP HTTP plaintext (unauthenticated)                     │
│  - Runs as UID: 8737 (non-root)                                         │
│  - FIPS: strictfipsruntime, CGO_ENABLED=1                                │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Service Account Token (argo)                             │
│         ▼                                                                │
│  [Kubernetes API Server] ──► Create/manage workflow pods                 │
│         │                                                                │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  DATA PLANE (Workflow Pods)                                              │
│                                                                          │
│  [Workflow Pod + argoexec sidecar/init]                                  │
│  - Namespace: {user-namespace}                                           │
│  - Runs as UID: 2000 (non-root)                                         │
│  - FIPS: dual binary (argoexec + argoexec-fips)                          │
│         │                                                                │
│         ├──► [Kubernetes API] Port: 443/TCP, HTTPS, TLS 1.2+            │
│         │    Auth: Service Account Token                                 │
│         │    Purpose: Status updates, WorkflowTaskResult CRs             │
│         │                                                                │
│         │ Port: 443/TCP or 9000/TCP                                      │
│         │ Protocol: HTTPS/HTTP                                           │
│         │ Encryption: TLS (configurable)                                 │
│         │ Auth: Access Key / IAM                                         │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [S3/MinIO - Artifact Storage]                                           │
│  - Port: 443/TCP or 9000/TCP                                            │
│  - Protocol: HTTPS/HTTP                                                  │
│  - Encryption: TLS (configurable)                                        │
│  - Auth: Access Key / IAM credentials                                    │
│  - Purpose: Artifact upload/download                                     │
│                                                                          │
│  [PostgreSQL - Optional Archive]                                         │
│  - Port: 5432/TCP                                                        │
│  - Protocol: TCP                                                         │
│  - Encryption: TLS (configurable)                                        │
│  - Auth: Username/Password                                               │
│  - Purpose: Workflow archival persistence                                │
│                                                                          │
│  [MySQL - Optional Archive]                                              │
│  - Port: 3306/TCP                                                        │
│  - Protocol: TCP                                                         │
│  - Encryption: TLS (configurable)                                        │
│  - Auth: Username/Password                                               │
│  - Purpose: Workflow archival persistence                                │
│                                                                          │
│  [OIDC Provider - Optional SSO]                                          │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Client credentials                                              │
│  - Purpose: SSO token validation for argo-server                         │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

ClusterRole: argo-cluster-role (bound to SA: argo)
  - "" / pods, pods/exec: create, get, list, watch, update, patch, delete
  - "" / configmaps: get, watch, list
  - "" / persistentvolumeclaims, persistentvolumeclaims/finalizers: create, update, delete, get
  - argoproj.io / workflows, workflows/finalizers, workflowtasksets, workflowtasksets/finalizers, workflowartifactgctasks: get, list, watch, update, patch, delete, create
  - argoproj.io / workflowtemplates, workflowtemplates/finalizers, clusterworkflowtemplates, clusterworkflowtemplates/finalizers: get, list, watch
  - argoproj.io / workflowtaskresults: list, watch, deletecollection
  - "" / serviceaccounts: get, list
  - argoproj.io / cronworkflows, cronworkflows/finalizers: get, list, watch, update, patch, delete
  - "" / events: create, patch
  - policy / poddisruptionbudgets: create, get, delete
  - "" / secrets (argo-workflows-agent-ca-certificates): get

ClusterRole: argo-server-cluster-role (bound to SA: argo-server)
  - "" / configmaps: get, watch, list
  - "" / secrets: get, create
  - "" / pods, pods/exec, pods/log: get, list, watch, delete
  - "" / events: watch, create, patch
  - "" / serviceaccounts: get, list, watch
  - argoproj.io / eventsources, sensors, workflows, workfloweventbindings, workflowtemplates, cronworkflows, clusterworkflowtemplates: create, get, list, watch, update, patch, delete

Aggregate Roles:
  - argo-aggregate-to-view: get, list, watch on argoproj.io resources
  - argo-aggregate-to-edit: full CRUD on argoproj.io resources
  - argo-aggregate-to-admin: full CRUD on argoproj.io resources (incl. workflowtasksets)

Authentication & Authorization:
  - argo-server gRPC/REST API:
    - Bearer Token mode: Kubernetes RBAC impersonation via SA token
    - SSO mode: OIDC JWT validation + optional RBAC mapping
    - Server mode: Uses server's own service account (argo-server)
    - Enforcement: Gatekeeper interceptor
  - Metrics endpoint (9090): Unauthenticated
  - Health endpoint (6060): Unauthenticated

Secrets:
  - argo-workflows-agent-ca-certificates (Opaque) - CA certs for agent executor, Operator/Admin, No auto-rotate
  - argo-server-sso (Opaque, optional) - SSO client ID/secret, Admin, No auto-rotate
  - Database credentials (Opaque, optional) - PostgreSQL/MySQL credentials, Admin, No auto-rotate
  - workflow-controller-configmap (ConfigMap) - Controller config (artifact repos, persistence, SSO), Admin/Operator, No auto-rotate

Container Security:
  - workflow-controller: runs as UID 8737 (non-root), UBI9 base, FIPS-compliant (strictfipsruntime)
  - argoexec: runs as UID 2000 (non-root), UBI9 base, dual FIPS/non-FIPS binaries
