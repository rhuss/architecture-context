Security Network Diagram: Guardrails Regex Detector
Generated by: Claude Opus 4.6
Source: architecture/rhoai-2.24/guardrails-regex-detector.md
================================================================================

┌──────────────────────────────────────────────────────────────────────────────┐
│  CLUSTER INTERNAL (Trusted)                                                  │
│                                                                              │
│  [FMS Guardrails Orchestrator]                                               │
│  - Namespace: (configured by operator)                                       │
│  - Role: Upstream caller / orchestration layer                               │
│         │                                                                    │
│         │ Port: 8080/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None (plaintext)                                       │
│         │ Auth: None (relies on network-level controls)                      │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  SERVICE (Internal - ClusterIP)                                              │
│                                                                              │
│  [regex-detector]                                                            │
│  - Service Type: ClusterIP (internal only)                                   │
│  - Port: 8080/TCP → 8080/TCP                                                │
│  - Protocol: HTTP                                                            │
│  - Encryption: None (plaintext)                                              │
│  - Auth: None                                                                │
│  - Base Image: registry.access.redhat.com/ubi9/ubi-minimal                   │
│  - Runtime: Rust (Axum + Tokio)                                              │
│  - FIPS Compliant: Yes (operator feature enabled)                            │
│  - Non-root: Yes (shadow-utils user management)                              │
│                                                                              │
│  Exposed Endpoints:                                                          │
│  ┌────────────────────────────────────────────────────────────────┐           │
│  │ GET  /health                 → Liveness/readiness probe       │           │
│  │ POST /api/v1/text/contents   → PII/regex detection            │           │
│  └────────────────────────────────────────────────────────────────┘           │
│                                                                              │
│  Environment Variables:                                                      │
│  - HTTP_PORT=8080 (configurable)                                             │
│  - HOST=0.0.0.0                                                              │
│         ▲                                                                    │
│         │ Port: 8080/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None (plaintext)                                       │
│         │ Auth: None                                                         │
│         │                                                                    │
│  [Kubernetes Kubelet]                                                        │
│  - Purpose: Health check probes (liveness/readiness)                         │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  None - No egress connections.                                               │
│  All regex matching is performed in-memory.                                  │
│  Service is fully stateless with no external dependencies at runtime.        │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

INGRESS:
  - No Ingress resources defined
  - Service is internal-only (ClusterIP)
  - Accessible only within the cluster by the FMS Guardrails Orchestrator

EGRESS:
  - No outbound network connections
  - All processing is performed in-memory

RBAC Summary:
  - No ClusterRoles defined
  - No ClusterRoleBindings defined
  - No ServiceAccounts with special permissions
  - Service does not interact with the Kubernetes API

Secrets:
  - None required
  - No credentials, certificates, or API keys

Authentication & Authorization:
  - /health (GET): No authentication - open health probe
  - /api/v1/text/contents (POST): No built-in authentication
    - Security relies on network-level controls:
      - NetworkPolicy (if configured by operator)
      - Service Mesh mTLS (if deployed with Service Mesh)
      - ClusterIP service type (not externally accessible)

Container Security:
  - Base image: registry.access.redhat.com/ubi9/ubi-minimal (Red Hat supported)
  - FIPS compliant: Yes (operator feature enabled)
  - Disconnected support: Yes (operator feature enabled)
  - Non-root user: Yes (shadow-utils for user management)
  - Security scanning: Trivy (MEDIUM, HIGH, CRITICAL severity)
  - Build context: Konflux (RHOAI production builds)
