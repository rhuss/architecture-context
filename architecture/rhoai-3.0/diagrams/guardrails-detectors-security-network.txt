Security Network Diagram: Guardrails Detectors
Generated by: Claude Opus 4.6
================================================================================

┌──────────────────────────────────────────────────────────────────────────────┐
│  INTERNAL SERVICES (Callers)                                                │
│                                                                              │
│  [FMS Guardrails Orchestrator]                                               │
│  - Purpose: Orchestrates detector invocations for content analysis           │
│         │                              │                                     │
│         │ Port: 8080/TCP               │ Port: 8000/TCP                      │
│         │ Protocol: HTTP               │ Protocol: HTTP                      │
│         │ Encryption: plaintext        │ Encryption: Istio mTLS (STRICT)     │
│         │ Auth: None                   │ Auth: mTLS client certificate       │
│         ▼                              ▼                                     │
├──────────────────────────────────────────────────────────────────────────────┤
│  DETECTOR SERVICES (This Component)                                          │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  [Built-in Detector]                                                    │ │
│  │  - Namespace: (deployment namespace)                                    │ │
│  │  - Image: odh-built-in-detector-rhel9                                   │ │
│  │  - Base: registry.access.redhat.com/ubi9/ubi-minimal                    │ │
│  │  - Port: 8080/TCP                                                       │ │
│  │  - Protocol: HTTP (plaintext)                                           │ │
│  │  - Service Type: ClusterIP (via Pod)                                    │ │
│  │  - Istio Sidecar: No                                                    │ │
│  │  - Endpoints:                                                           │ │
│  │    - POST /api/v1/text/contents (detection)                             │ │
│  │    - GET  /health (health check)                                        │ │
│  │    - GET  /registry (list detectors)                                    │ │
│  │    - GET  /metrics (Prometheus)                                         │ │
│  │  - Security: Static code analysis for custom detectors                  │ │
│  │    - Forbidden imports: os, subprocess, sys, shutil                     │ │
│  │    - Forbidden calls: eval, exec, open, compile, input                  │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  [HuggingFace Detector]                                                 │ │
│  │  - Namespace: (deployment namespace)                                    │ │
│  │  - Image: odh-guardrails-detector-huggingface-runtime-rhel9             │ │
│  │  - Base: registry.access.redhat.com/ubi9/ubi-minimal                    │ │
│  │  - Port: 8000/TCP                                                       │ │
│  │  - Protocol: HTTP (app-level plaintext; mTLS via Istio sidecar)         │ │
│  │  - Service Type: ClusterIP (via KServe)                                 │ │
│  │  - Deployment: KServe InferenceService (RawDeployment mode)             │ │
│  │  - Istio Sidecar: Yes (sidecar.istio.io/inject: 'true')                │ │
│  │  - Annotations:                                                         │ │
│  │    - security.opendatahub.io/enable-auth: 'true'                        │ │
│  │    - opendatahub.io/dashboard: 'true'                                   │ │
│  │  - Endpoints:                                                           │ │
│  │    - POST /api/v1/text/contents (detection)                             │ │
│  │    - GET  /health (health check)                                        │ │
│  │    - GET  /metrics (Prometheus)                                         │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │  [LLM Judge Detector]                                                   │ │
│  │  - Namespace: (deployment namespace)                                    │ │
│  │  - Image: (llm-judge container)                                         │ │
│  │  - Base: registry.access.redhat.com/ubi9/ubi-minimal                    │ │
│  │  - Port: 8000/TCP                                                       │ │
│  │  - Protocol: HTTP (app-level plaintext; mTLS via Istio sidecar)         │ │
│  │  - Service Type: ClusterIP (via KServe)                                 │ │
│  │  - Deployment: KServe InferenceService (RawDeployment mode)             │ │
│  │  - Istio Sidecar: Yes (sidecar.istio.io/inject: 'true')                │ │
│  │  - Annotations:                                                         │ │
│  │    - security.opendatahub.io/enable-auth: 'true'                        │ │
│  │    - opendatahub.io/dashboard: 'true'                                   │ │
│  │  - Endpoints:                                                           │ │
│  │    - POST /api/v1/text/contents (detection)                             │ │
│  │    - POST /api/v1/text/generation (prompt+generation eval)              │ │
│  │    - GET  /api/v1/metrics (list eval metrics)                           │ │
│  │    - GET  /health (health check)                                        │ │
│  │    - GET  /metrics (Prometheus)                                         │ │
│  │         │                                                               │ │
│  │         │ Port: 8080/TCP                                                │ │
│  │         │ Protocol: HTTP                                                │ │
│  │         │ Encryption: Istio mTLS (within mesh)                          │ │
│  │         │ Auth: None (service-to-service within mesh)                   │ │
│  │         ▼                                                               │ │
│  └─────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS TARGETS                                                              │
│                                                                              │
│  [vLLM Server (e.g., qwen2-predictor)]                                       │
│  - Port: 8080/TCP                                                            │
│  - Protocol: HTTP (OpenAI-compatible API)                                    │
│  - Encryption: Istio mTLS (within mesh)                                      │
│  - Auth: None                                                                │
│  - Purpose: LLM Judge sends evaluation prompts for LLM-as-judge scoring     │
│                                                                              │
│  [S3/Minio Model Storage]                                                    │
│  - Port: 9000/TCP                                                            │
│  - Protocol: HTTP                                                            │
│  - Encryption: plaintext                                                     │
│  - Auth: AWS IAM credentials (AccessKey/SecretKey)                           │
│  - Purpose: KServe downloads HuggingFace model artifacts at startup          │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  MONITORING                                                                  │
│                                                                              │
│  [Prometheus]                                                                │
│  - Scrapes: /metrics on 8080/TCP (built-in) and 8000/TCP (hf, judge)        │
│  - Protocol: HTTP                                                            │
│  - Encryption: plaintext                                                     │
│  - Metrics: trustyai_guardrails_detections, _requests, _errors, _runtime     │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No custom RBAC roles defined by this component
- Example binding: user-one-view → ClusterRole/view (get, list, watch)
- ServiceAccount: user-one (example deployment namespace)

Service Mesh Configuration:
- PeerAuthentication: STRICT mTLS (inferred from sidecar.istio.io/inject: 'true')
- Platform Auth: security.opendatahub.io/enable-auth: 'true' (RHOAI platform auth)
- Applies to: HuggingFace Detector, LLM Judge Detector (KServe-deployed)
- Built-in Detector: No Istio sidecar (plaintext HTTP only)

Secrets:
- aws-connection-minio-data-connection-guardrails-guardian (Opaque)
  - Contents: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, endpoint, bucket, region
  - Purpose: S3/Minio credentials for model storage
  - Provisioned by: Manual / ODH Dashboard
  - Auto-rotate: No

Custom Detector Security:
- Static AST analysis at startup for user-provided Python detectors
- Forbidden imports: os, subprocess, sys, shutil
- Forbidden function calls: eval, exec, open, compile, input
- Application refuses to start if forbidden patterns detected
