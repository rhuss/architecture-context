KubeRay Operator - Security Network Diagram
Generated by: Claude Opus 4.6

┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [User / Browser]                                                            │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: None (Gateway terminates TLS)                                │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Platform Gateway)                                                  │
│                                                                              │
│  [data-science-gateway (Envoy)]                                              │
│  - Namespace: openshift-ingress                                              │
│  - Port: 443/TCP (external) → 8265/TCP (internal)                          │
│  - TLS Termination: TLS 1.2+                                                │
│  - Path: /ray/{namespace}/{cluster}/*                                        │
│  - URL Rewrite: strips /ray/{ns}/{cluster} prefix                           │
│         │                                                                    │
│         │ Port: 8265/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: plaintext                                              │
│         │ Auth: None (internal cluster network)                              │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (redhat-ods-applications)                                │
│                                                                              │
│  [kuberay-operator]                                                          │
│  - ServiceAccount: kuberay-operator                                          │
│  - Metrics: 8080/TCP HTTP plaintext                                          │
│  - Health: 8081/TCP HTTP plaintext (/healthz, /readyz)                      │
│  - Webhook: 9443/TCP HTTPS (OpenShift serving certs)                        │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount token                                         │
│         ▼                                                                    │
│  [Kubernetes API Server]                                                     │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  RAY CLUSTER NAMESPACE ({user-namespace})                                    │
│                                                                              │
│  ┌─────────────────────────────────────────────────┐                        │
│  │ [Ray Head Pod]                                   │                        │
│  │ - SCC: run-as-ray-user (UID 1000)               │                        │
│  │ - Dashboard: 8265/TCP HTTP plaintext             │                        │
│  │ - Client: 10001/TCP TCP plaintext                │                        │
│  │ - GCS: 6379/TCP TCP plaintext                    │                        │
│  │ - Metrics: 8080/TCP HTTP plaintext               │                        │
│  │                                                   │                        │
│  │ ┌───────────────────────────────────┐            │                        │
│  │ │ [kube-rbac-proxy sidecar]         │            │                        │
│  │ │ - Port: 8443/TCP HTTPS TLS        │            │                        │
│  │ │ - Auth: TokenReview + SAR          │            │                        │
│  │ │ - Requires: bearer token +         │            │                        │
│  │ │   get verb on pods resource        │            │                        │
│  │ └───────────────────────────────────┘            │                        │
│  └─────────────────────────────────────────────────┘                        │
│         │                                                                    │
│         │ mTLS (when enabled via cert-manager)                               │
│         │ Head cert: ray-head-secret (kubernetes.io/tls)                     │
│         │ Worker cert: ray-worker-secret (kubernetes.io/tls)                 │
│         │ CA: ca-secret-{uid} (Opaque)                                       │
│         ▼                                                                    │
│  [Ray Worker Pods] (1..N)                                                    │
│  - SCC: run-as-ray-user (UID 1000)                                          │
│  - Headless service: {cluster}-headless (multi-host workers)                │
│                                                                              │
│  ┌─────────────────────────────────────────────────┐                        │
│  │ NetworkPolicies (odh.ray.io/secure-trusted-network=true)                 │
│  │                                                   │                        │
│  │ {cluster}-head-networkpolicy:                     │                        │
│  │  1. Intra-cluster: all ports from same cluster    │                        │
│  │  2. Same-namespace: ports 10001, 8265             │                        │
│  │  3. Operator NS: ports 8265, 10001                │                        │
│  │  4. Monitoring NS: port 8080 (optional)           │                        │
│  │  5. Auth proxy: port 8443 from anywhere           │                        │
│  │                                                   │                        │
│  │ {cluster}-worker-networkpolicy:                   │                        │
│  │  Intra-cluster only (all ports, same cluster)     │                        │
│  └─────────────────────────────────────────────────┘                        │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  PLATFORM NAMESPACE (redhat-ods-applications)                                │
│                                                                              │
│  [HTTPRoute: {ns}-{cluster}]                                                 │
│  - Parent: data-science-gateway (openshift-ingress)                          │
│  - Path: /ray/{namespace}/{cluster}                                          │
│  - Backend: {cluster}-head-svc:8265 (cross-namespace)                       │
│                                                                              │
│  [ReferenceGrant: kuberay-gateway-access]                                    │
│  - Allows cross-namespace service references                                 │
│  - One shared per user namespace                                             │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  [Redis (optional)]                                                          │
│  - Port: 6379/TCP                                                            │
│  - Protocol: TCP                                                             │
│  - Encryption: TLS optional                                                  │
│  - Auth: Password/Username                                                   │
│  - Purpose: GCS fault tolerance external storage                             │
│                                                                              │
│  [cert-manager]                                                              │
│  - Purpose: mTLS PKI for Ray cluster communication                           │
│  - Creates: Issuer, Certificate CRs → TLS Secrets                           │
│  - Auto-Rotate: Yes                                                          │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

ClusterRole: kuberay-operator
  - core: pods, pods/status, pods/proxy → get, list, watch, create, update, patch, delete, deletecollection
  - core: services, services/status, services/proxy → get, list, watch, create, update, patch, delete
  - core: configmaps, events, serviceaccounts → get, list, watch, create, update, patch, delete
  - core: secrets → get, list, watch, delete
  - core: endpoints → get, list, watch
  - apps: deployments → get, list, watch, update, patch
  - batch: jobs → get, list, watch, create, update, patch, delete
  - ray.io: rayclusters, rayjobs, rayservices → get, list, watch, create, update, patch, delete
  - ray.io: */status → get, update, patch
  - ray.io: */finalizers → update
  - networking.k8s.io: ingresses, networkpolicies → get, list, watch, create, update, patch, delete
  - route.openshift.io: routes → get, list, watch, create, update, patch, delete
  - cert-manager.io: certificates, issuers → get, list, watch, create, update, patch, delete
  - coordination.k8s.io: leases → get, list, create, update
  - rbac.authorization.k8s.io: roles → get, list, watch, create, update, delete
  - rbac.authorization.k8s.io: rolebindings → get, list, watch, create, delete
  - authentication.k8s.io: tokenreviews → create
  - authorization.k8s.io: subjectaccessreviews → create
  - config.openshift.io: authentications, oauths → get, list, watch

ClusterRole: kuberay-operator-gateway-api
  - gateway.networking.k8s.io: gateways → get, list, watch
  - gateway.networking.k8s.io: httproutes, referencegrants → get, list, watch, create, update, patch, delete

Role Bindings:
  - kuberay-operator (Cluster-scoped) → ClusterRole/kuberay-operator → SA: kuberay-operator
  - kuberay-operator-gateway-api (Cluster-scoped) → ClusterRole/kuberay-operator-gateway-api → SA: kuberay-operator (redhat-ods-applications)

SecurityContextConstraints:
  - run-as-ray-user: MustRunAs UID 1000, Drop ALL capabilities, no privilege escalation

Secrets:
  - kuberay-webhook-server-cert (kubernetes.io/tls) → Webhook TLS, OpenShift service-ca, auto-rotate
  - ray-head-secret (kubernetes.io/tls) → Head mTLS cert, cert-manager, auto-rotate
  - ray-worker-secret (kubernetes.io/tls) → Worker mTLS cert, cert-manager, auto-rotate
  - ca-secret-{uid} (Opaque) → CA cert for cluster mTLS PKI, cert-manager, auto-rotate

Authentication & Authorization:
  - Ray dashboard (via HTTPRoute): kube-rbac-proxy (TokenReview + SAR), requires bearer token + get on pods
  - Webhook endpoints: API server client certificate only
  - Operator metrics (:8080): None (optional NetworkPolicy protection)
