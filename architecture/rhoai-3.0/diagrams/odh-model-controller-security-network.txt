Security Network Diagram - odh-model-controller
Generated by: Claude Opus 4.6
================================================================================

┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS Edge or Reencrypt                                  │
│         │ Auth: Optional (annotation-based)                                  │
│         ▼                                                                    │
│  [OpenShift Route] (per InferenceService, dynamic)                           │
│  - TLS Mode: Edge (no auth) / Reencrypt (auth enabled)                      │
│  - Annotation: security.opendatahub.io/enable-auth                          │
│         │                                                                    │
│         │                                                                    │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: AuthPolicy-enforced (Kuadrant)                               │
│         ▼                                                                    │
│  [Gateway: openshift-ai-inference] (in openshift-ingress NS)                 │
│  - AuthPolicy (Kuadrant) targeting Gateway                                   │
│  - EnvoyFilter (Istio) for SSL auth                                          │
│  - HTTPRoute → backend Service                                              │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (Trusted - Kubernetes Network)                             │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐       │
│  │  Namespace: redhat-ods-applications (controller namespace)         │       │
│  │                                                                    │       │
│  │  [odh-model-controller Pod]                                        │       │
│  │  - ServiceAccount: odh-model-controller                            │       │
│  │  - Image: quay.io/modh/odh-model-controller                       │       │
│  │                                                                    │       │
│  │  Ports:                                                            │       │
│  │  ┌──────────────────────────────────────────────────────────────┐  │       │
│  │  │ 8080/TCP - Metrics (HTTP, plaintext, no auth)               │  │       │
│  │  │ Service: odh-model-controller-metrics-service (ClusterIP)   │  │       │
│  │  │ Target: Prometheus scraping                                  │  │       │
│  │  ├──────────────────────────────────────────────────────────────┤  │       │
│  │  │ 8081/TCP - Health probes (HTTP, plaintext, no auth)         │  │       │
│  │  │ /healthz - Liveness probe                                    │  │       │
│  │  │ /readyz - Readiness probe                                    │  │       │
│  │  ├──────────────────────────────────────────────────────────────┤  │       │
│  │  │ 9443/TCP - Webhook server (HTTPS, TLS via service-ca)       │  │       │
│  │  │ Service: odh-model-controller-webhook-service (ClusterIP)   │  │       │
│  │  │ External port: 443/TCP → Target: 9443/TCP                   │  │       │
│  │  │ Auth: API server mTLS (client cert)                          │  │       │
│  │  │ Cert: odh-model-controller-webhook-cert (service-ca)         │  │       │
│  │  │                                                              │  │       │
│  │  │ Webhook Endpoints:                                           │  │       │
│  │  │   POST /mutate--v1-pod                                       │  │       │
│  │  │   POST /mutate-serving-kserve-io-v1beta1-inferenceservice    │  │       │
│  │  │   POST /mutate-serving-kserve-io-v1alpha1-inferencegraph     │  │       │
│  │  │   POST /validate-nim-opendatahub-io-v1-account               │  │       │
│  │  │   POST /validate-serving-kserve-io-v1alpha1-llminference...  │  │       │
│  │  │   POST /validate-serving-kserve-io-v1alpha1-inferencegraph   │  │       │
│  │  │   POST /validate-serving-kserve-io-v1beta1-inferenceservice  │  │       │
│  │  └──────────────────────────────────────────────────────────────┘  │       │
│  └────────────────────────────────────────────────────────────────────┘       │
│         │                                                                    │
│         │ Port: 6443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount token                                         │
│         ▼                                                                    │
│  [Kubernetes API Server]                                                     │
│  - Controller-runtime watch/list/create/update/delete                        │
│  - Creates: Routes, AuthPolicies, EnvoyFilters, ServiceMonitors,            │
│    PodMonitors, NetworkPolicies, ClusterRoleBindings, KEDA resources,        │
│    Secrets, ConfigMaps, Templates                                            │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐       │
│  │  Dynamic per-namespace resources (user namespaces)                 │       │
│  │                                                                    │       │
│  │  [metrics Service] (per InferenceService, dynamic)                 │       │
│  │  - Type: ClusterIP                                                 │       │
│  │  - Port: {prometheus-port}/TCP                                     │       │
│  │  - Protocol: HTTP, plaintext                                       │       │
│  │                                                                    │       │
│  │  [storage-config Secret] (aggregated data connections)             │       │
│  │  - Type: Opaque                                                    │       │
│  │  - Contains: S3/storage credentials                                │       │
│  │                                                                    │       │
│  │  [ray-tls Secret] (per-pod Ray TLS, multi-node serving)           │       │
│  │  - Type: Opaque                                                    │       │
│  │  - Contains: Self-signed TLS cert + key for Pod IP                 │       │
│  └────────────────────────────────────────────────────────────────────┘       │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  [NVIDIA NGC API - api.ngc.nvidia.com]                                       │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: API key (Bearer token)                                              │
│  - Purpose: NIM API key validation and model catalog retrieval               │
│                                                                              │
│  [Model Registry - internal service]                                         │
│  - Port: varies                                                              │
│  - Protocol: HTTPS/HTTP                                                      │
│  - Encryption: TLS 1.2+ (configurable, skip TLS optional)                   │
│  - Auth: Bearer Token                                                        │
│  - Purpose: InferenceService state synchronization                           │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
=============

ClusterRole: odh-model-controller-role
  ServiceAccount: odh-model-controller (bound via odh-model-controller-rolebinding-opendatahub)

  Core API:
  - configmaps, secrets, serviceaccounts, services: create, delete, get, list, patch, update, watch
  - endpoints, namespaces, pods: create, get, list, patch, update, watch
  - events: create, patch

  serving.kserve.io:
  - inferenceservices: get, list, patch, update, watch
  - inferenceservices/finalizers: create, delete, get, list, patch, update, watch
  - servingruntimes: create, get, list, update, watch
  - llminferenceservices: get, list, patch, post, update, watch
  - llminferenceservices/finalizers: patch, update
  - llminferenceservices/status: get, patch, update
  - llminferenceserviceconfigs, inferencegraphs: get, list, watch

  nim.opendatahub.io:
  - accounts: get, list, patch, update, watch
  - accounts/finalizers: update
  - accounts/status: get, list, update, watch

  route.openshift.io:
  - routes: create, delete, get, list, patch, update, watch
  - routes/custom-host: create

  rbac.authorization.k8s.io:
  - clusterrolebindings, rolebindings, roles: create, delete, get, list, patch, update, watch

  networking.k8s.io:
  - networkpolicies: create, delete, get, list, patch, update, watch
  - ingresses: get, list, watch

  monitoring.coreos.com:
  - podmonitors, servicemonitors: create, delete, get, list, patch, update, watch

  gateway.networking.k8s.io:
  - gateways: get, list, patch, update, watch
  - gateways/finalizers: patch, update
  - httproutes: get, list, watch

  kuadrant.io:
  - authpolicies: create, delete, get, list, patch, update, watch
  - authpolicies/status: get, patch, update

  networking.istio.io:
  - envoyfilters: create, delete, get, list, patch, update, watch

  keda.sh:
  - triggerauthentications: create, delete, get, list, patch, update, watch

  template.openshift.io:
  - templates: create, delete, get, list, patch, update, watch

  datasciencecluster.opendatahub.io:
  - datascienceclusters: get, list, watch

  dscinitialization.opendatahub.io:
  - dscinitializations: get, list, watch

  config.openshift.io:
  - authentications: get, list, watch

  metrics.k8s.io:
  - nodes, pods: get, list, watch

Dynamic RBAC:
  prometheus-ns-access (ClusterRole) → RoleBinding per user namespace
    SA: prometheus-custom (monitoring NS)
    Purpose: Prometheus namespace access for metrics federation

  inference-prometheus-auth (Role) → RoleBinding per user namespace (KEDA)
    SA: inference-prometheus-auth
    Purpose: KEDA autoscaling Prometheus access

Secrets:
========

| Secret Name                          | Type                          | Provisioned By              | Auto-Rotate |
|--------------------------------------|-------------------------------|-----------------------------|-------------|
| odh-model-controller-webhook-cert    | kubernetes.io/tls             | OpenShift service-ca        | Yes         |
| storage-config (per NS, dynamic)     | Opaque                        | odh-model-controller        | No          |
| ray-ca-tls                           | Opaque                        | odh-model-controller        | No          |
| ray-tls (per NS, dynamic)            | Opaque                        | odh-model-controller        | No          |
| NIM pull secret (per Account)        | kubernetes.io/dockerconfigjson| odh-model-controller        | Yes         |

Authentication & Authorization:
===============================

| Endpoint                              | Auth Mechanism                        | Enforcement Point          |
|---------------------------------------|---------------------------------------|----------------------------|
| Webhook endpoints (/validate-*,      | mTLS (API server client cert)         | OpenShift service-ca TLS   |
|   /mutate-*)                          |                                       |                            |
| InferenceService Routes (with auth)   | TLS Reencrypt                         | OpenShift Router           |
| LLMInferenceService (Gateway API)     | AuthPolicy (Kuadrant)                 | Gateway API + Kuadrant     |
| LLMInferenceService (Gateway SSL)     | EnvoyFilter (Istio)                   | Istio Envoy sidecar        |
