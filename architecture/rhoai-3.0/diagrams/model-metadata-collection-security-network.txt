================================================================================
  SECURITY NETWORK DIAGRAM - Model Metadata Collection
  Generated by: Claude Opus 4.6
  Component Type: Data Container (static catalog data)
  Note: Build-time tool only; no runtime network connections
================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  BUILD-TIME ENVIRONMENT (CI/CD - Konflux / GitHub Actions)             │
│                                                                         │
│  ┌─────────────────────────────────────────────┐                       │
│  │  model-extractor (Go CLI)                    │                       │
│  │  - Runs during CI/CD pipeline                │                       │
│  │  - NOT shipped in production container       │                       │
│  │  - UID: build user (CI context)              │                       │
│  └──────────┬──────────────┬────────────────────┘                       │
│             │              │                                            │
│    ┌────────┘              └────────┐                                   │
│    │                                │                                   │
│    │ Port: 443/TCP                  │ Port: 443/TCP                     │
│    │ Protocol: HTTPS                │ Protocol: HTTPS                   │
│    │ Encryption: TLS 1.2+           │ Encryption: TLS 1.2+             │
│    │ Auth: None (public API)        │ Auth: Docker registry auth       │
│    │                                │       (~/.docker/config.json)     │
│    ▼                                ▼                                   │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Build-Time Only)                                    │
│                                                                         │
│  [HuggingFace API - huggingface.co]                                    │
│  - Purpose: Model discovery, metadata enrichment, README fetching      │
│  - Endpoints:                                                           │
│    - /api/users/RedHatAI/collections (GET)                             │
│    - /api/collections/{slug} (GET)                                     │
│    - /api/models/{name} (GET)                                          │
│    - /{model}/raw/main/README.md (GET)                                 │
│  - Auth: None (public, rate-limited)                                   │
│  - Encryption: TLS 1.2+                                                │
│                                                                         │
│  [Red Hat Container Registry - registry.redhat.io]                     │
│  - Purpose: Fetch OCI manifests and model card layers                  │
│  - Protocol: OCI Distribution Spec v2                                  │
│  - Auth: Docker credentials via BuildKit secrets                       │
│  - Encryption: TLS 1.2+                                                │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  CONTAINER BUILD (Konflux CI/CD)                                        │
│                                                                         │
│  [Konflux PipelineRun]                                                  │
│  - Trigger: Push to rhoai-3.0 branch                                   │
│  - Build: Dockerfile.konflux (hermetic: true)                          │
│  - Prefetch: gomod                                                     │
│         │                                                               │
│         │ Port: 443/TCP                                                 │
│         │ Protocol: HTTPS                                               │
│         │ Encryption: TLS 1.2+                                          │
│         │ Auth: Konflux service account                                 │
│         ▼                                                               │
│  [quay.io/rhoai/odh-model-metadata-collection-rhel9]                   │
│  - Multi-arch: amd64, arm64, ppc64le, s390x                           │
│  - Base: ubi9-micro:latest                                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│  RUNTIME ENVIRONMENT (RHOAI Cluster)                                    │
│                                                                         │
│  *** NO NETWORK CONNECTIONS AT RUNTIME ***                              │
│                                                                         │
│  [odh-model-metadata-collection container]                              │
│  - Base: registry.access.redhat.com/ubi9-micro:latest                  │
│  - CMD: sleep infinity                                                  │
│  - UID: 1001 (non-root, "catalog" user)                                │
│  - No listening ports                                                   │
│  - No inbound connections                                               │
│  - No outbound connections                                              │
│  - No Kubernetes Services                                               │
│  - No Ingress                                                           │
│         │                                                               │
│         │ Data access: Volume mount                                     │
│         │ Path: /app/data/                                              │
│         │ Protocol: File I/O (local)                                    │
│         │ No network involved                                           │
│         ▼                                                               │
│  [Model Catalog UI (ODH Dashboard)]                                     │
│  - Reads: models-catalog.yaml                                           │
│  - Reads: validated-models-catalog.yaml                                 │
│  - Access: Volume mount from init/sidecar container                    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No Kubernetes RBAC required
- Component is a static data container with no API access
- No ClusterRoles, Roles, or RoleBindings

Service Mesh Configuration:
- Not applicable (no network services, no ingress/egress at runtime)

Secrets:
- Docker config (~/.docker/config.json)
  - Type: Docker credentials
  - Purpose: Authenticate to registry.redhat.io during build
  - Provisioned By: Manual / CI
  - Auto-Rotate: No
  - Scope: Build-time only

- QUAY_USERNAME / QUAY_PASSWORD
  - Type: CI secret (GitHub Secrets)
  - Purpose: Push built images to quay.io/opendatahub (upstream CI only)
  - Provisioned By: GitHub Secrets
  - Auto-Rotate: No
  - Scope: Build-time only (upstream path)

Container Security:
- Runs as non-root (UID 1001, "catalog" user)
- Minimal base image (ubi9-micro - no shell, no package manager)
- No Go binaries shipped (build tools excluded from production image)
- Static data only (YAML files)
- Hermetic build (Konflux)
