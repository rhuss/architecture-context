FMS Guardrails Orchestrator - Security Network Diagram
Generated by: Claude Opus 4.6
=============================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted / Cluster Consumers)                               │
│                                                                          │
│  [Client Application]                                                    │
│         │                                                                │
│         │ Port: 8033/TCP                                                 │
│         │ Protocol: HTTP/HTTPS                                           │
│         │ Encryption: TLS 1.2+ (optional, rustls/ring)                   │
│         │ Auth: Header passthrough (X-Forwarded-Access-Token → Bearer)   │
│         │       mTLS client cert (optional, WebPKI verifier)             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  ORCHESTRATOR SERVICE                                                    │
│                                                                          │
│  [fms-guardrails-orchestr8 - Main Server]                                │
│  - Port: 8033/TCP (API endpoints)                                        │
│  - Framework: Rust/axum                                                  │
│  - TLS: rustls with ring crypto provider (FIPS-compliant)                │
│  - TLS Modes:                                                            │
│    * Plaintext: No TLS env vars (default)                                │
│    * TLS: TLS_KEY_PATH + TLS_CERT_PATH                                   │
│    * mTLS: TLS + TLS_CLIENT_CA_CERT_PATH                                 │
│  - Auth: Passthrough headers to backends                                 │
│                                                                          │
│  [Health Server]                                                         │
│  - Port: 8034/TCP                                                        │
│  - Protocol: HTTP (plaintext)                                            │
│  - Auth: None (unauthenticated health probes)                            │
│  - Endpoints: /health (liveness), /info (client status)                  │
│         │                                                                │
│         │ Egress connections to backend services                         │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  BACKEND SERVICES (Internal / Platform-managed)                          │
│                                                                          │
│  ┌─────────────────────────────────────────────┐                         │
│  │ [TGIS Generation Server]                    │                         │
│  │ - Port: Configurable (default 8033/TCP)     │                         │
│  │ - Protocol: gRPC/HTTP2                      │                         │
│  │ - Encryption: TLS/mTLS (configurable)       │                         │
│  │ - Auth: Client certificates (configurable)  │                         │
│  │ - RPCs: Generate, GenerateStream            │                         │
│  └─────────────────────────────────────────────┘                         │
│                                                                          │
│  ┌─────────────────────────────────────────────┐                         │
│  │ [Caikit NLP Server]                         │                         │
│  │ - Port: Configurable                        │                         │
│  │ - Protocol: gRPC/HTTP2                      │                         │
│  │ - Encryption: TLS/mTLS (configurable)       │                         │
│  │ - Auth: Client certificates (configurable)  │                         │
│  └─────────────────────────────────────────────┘                         │
│                                                                          │
│  ┌─────────────────────────────────────────────┐                         │
│  │ [vLLM / OpenAI-compatible Server]           │                         │
│  │ - Port: Configurable (default 8080/TCP)     │                         │
│  │ - Protocol: HTTP/HTTPS                      │                         │
│  │ - Encryption: TLS (configurable)            │                         │
│  │ - Auth: Bearer token (forwarded from client)│                         │
│  │ - Endpoints: /v1/chat/completions,          │                         │
│  │              /v1/completions                 │                         │
│  └─────────────────────────────────────────────┘                         │
│                                                                          │
│  ┌─────────────────────────────────────────────┐                         │
│  │ [Detector Services (HAP, toxicity, etc.)]   │                         │
│  │ - API Port: Configurable (default 8080/TCP) │                         │
│  │ - Health Port: Configurable (default 8081)  │                         │
│  │ - Protocol: HTTP/HTTPS                      │                         │
│  │ - Encryption: TLS/mTLS (configurable)       │                         │
│  │ - Auth: Client certs / Header passthrough   │                         │
│  │ - Endpoints: /api/v1/text/contents,         │                         │
│  │   /api/v1/text/chat, /api/v1/text/context/  │                         │
│  │   doc, /api/v1/text/generation              │                         │
│  └─────────────────────────────────────────────┘                         │
│                                                                          │
│  ┌─────────────────────────────────────────────┐                         │
│  │ [Chunker Services (Caikit-based)]           │                         │
│  │ - Port: Configurable (default 8085/TCP)     │                         │
│  │ - Protocol: gRPC/HTTP2                      │                         │
│  │ - Encryption: TLS/mTLS (configurable)       │                         │
│  │ - Auth: Client certificates (configurable)  │                         │
│  │ - RPCs: ChunkerTokenizationTaskPredict,     │                         │
│  │   BidiStreamingChunkerTokenizationTaskPred. │                         │
│  └─────────────────────────────────────────────┘                         │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  OBSERVABILITY (Telemetry Sink)                                          │
│                                                                          │
│  [OpenTelemetry Collector]                                               │
│  - Port: 4317/TCP (gRPC) or 4318/TCP (HTTP)                             │
│  - Protocol: OTLP (gRPC or HTTP)                                         │
│  - Encryption: Configurable                                              │
│  - Auth: None                                                            │
│  - Data: Traces and metrics                                              │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No Kubernetes RBAC resources defined by this component
- RBAC is managed at platform level by the deploying operator
- This is a standalone service, not a Kubernetes operator

Secrets:
- TLS server cert/key (kubernetes.io/tls, external) - Server TLS encryption
  Provisioned by: Platform/Operator, Rotation: Depends on provisioner
- TLS client certs (Opaque, external) - mTLS client authentication for upstream services
  Provisioned by: Platform/Operator, Rotation: Depends on provisioner
- TLS CA certificates (Opaque, external) - CA verification for client connections
  Provisioned by: Platform/Operator, Rotation: Depends on provisioner

TLS Configuration Summary:
- Server TLS: Optional (Plaintext | TLS | mTLS) via env vars
- Client TLS: Per-service in YAML config (cert_path, key_path, client_ca_cert_path)
- Named TLS configs: Reusable TLS configurations across multiple services
- Crypto Provider: ring (FIPS-compliant)
- Insecure mode: Available per-service client connection (flag: insecure)

Authentication Flow:
- v1 API: X-Forwarded-Access-Token header → converted to Bearer token for backends
- v2 API: Configurable passthrough_headers list forwarded to backends
- Health endpoints (/health, /info): Unauthenticated
- mTLS mode: Client must present valid certificate signed by configured CA

Operator Features:
- disconnected: Enabled (air-gapped deployment support)
- fips-compliant: Enabled (ring crypto provider)
- proxy-aware: Disabled
- tls-profiles: Disabled
