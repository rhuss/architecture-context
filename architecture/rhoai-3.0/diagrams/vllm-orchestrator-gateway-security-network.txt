Security Network Diagram: vllm-orchestrator-gateway
Generated by: Claude Opus 4.6
================================================================================

┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [Client - OpenAI-compatible]                                                │
│         │                                                                    │
│         │ Port: 8090/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: plaintext                                              │
│         │ Auth: Authorization header (Bearer token, pass-through)            │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  GATEWAY SERVICE                                                             │
│                                                                              │
│  [vllm-orchestrator-gateway]                                                 │
│  - Image: rhoai/odh-trustyai-vllm-orchestrator-gateway-rhel9                │
│  - Base: registry.access.redhat.com/ubi9/ubi-minimal                        │
│  - Language: Rust 1.84.0 (axum 0.7.9)                                       │
│  - Listen: 0.0.0.0:8090/TCP (configurable via HTTP_PORT)                    │
│  - Config: GATEWAY_CONFIG env → config/config.yaml                          │
│  - Entry: /app/bin/vllm-orchestrator-gateway                                │
│                                                                              │
│  Dynamic Routes (from config):                                               │
│  - /{route_name}/v1/chat/completions (POST)                                 │
│  - Example: /pii/v1/chat/completions                                        │
│  - Example: /passthrough/v1/chat/completions                                │
│                                                                              │
│  Processing:                                                                 │
│  - Resolves route → detector config                                          │
│  - Injects detector specs into request payload                               │
│  - Applies fallback_message on detection trigger                             │
│         │                                                                    │
│         │ Port: 8085/TCP (configurable)                                      │
│         │ Protocol: HTTP or HTTPS                                            │
│         │ Encryption: Optional mTLS (TLS 1.2+ via native-tls/openssl)       │
│         │ Auth: mTLS client cert (if present) + forwarded Authorization      │
│         │       + forwarded x-forwarded-* headers                            │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INTERNAL ODH SERVICES                                                       │
│                                                                              │
│  [FMS Guardrails Orchestrator]                                               │
│  - Endpoint: /api/v2/chat/completions-detection                             │
│  - Port: 8085/TCP (configurable via orchestrator.port)                      │
│  - Purpose: LLM generation with content detection pipeline                   │
│  - Auth: Receives forwarded Authorization + mTLS identity                    │
│         │                                                                    │
│         │ (internal orchestrator routing)                                     │
│         ▼                                                                    │
│  [Guardrails Detectors]                                                      │
│  - e.g., regex-detector (PII detection)                                      │
│  - Managed by orchestrator configuration                                     │
│  - No direct communication from gateway                                      │
│         │                                                                    │
│         │ (internal orchestrator routing)                                     │
│         ▼                                                                    │
│  [vLLM / LLM Backend]                                                        │
│  - LLM inference service                                                     │
│  - Managed by orchestrator                                                   │
│  - No direct communication from gateway                                      │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

TLS Configuration:
================================================================================
  Path                              Purpose                     Required
  ─────────────────────────────────────────────────────────────────────────
  /etc/tls/private/tls.crt          Client TLS certificate      No (HTTP fallback)
  /etc/tls/private/tls.key          Client TLS private key       No (HTTP fallback)
  /etc/tls/ca/service-ca.crt        Custom CA certificate        No

  Behavior:
  - Cert + Key present → HTTPS scheme, mTLS client identity
  - CA only present → Added as trusted root, HTTP scheme
  - No certs → HTTP plaintext
  - localhost + custom CA → Relaxed hostname verification

RBAC Summary:
================================================================================
  None. This is a standalone service without Kubernetes API access.
  No ClusterRoles, Roles, or RoleBindings defined.

Secrets:
================================================================================
  Name/Mount                    Type                Purpose                     Provisioned By          Rotation
  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────
  /etc/tls/private/             kubernetes.io/tls   mTLS client identity        cert-manager or         Unknown
                                                    for orchestrator            service-ca-operator
  /etc/tls/ca/                  Opaque              Custom CA for               service-ca-operator     Unknown
                                                    orchestrator TLS

Authentication & Authorization:
================================================================================
  - Gateway does NOT enforce authentication
  - Authorization header: forwarded to orchestrator (pass-through)
  - x-forwarded-* headers: forwarded to orchestrator
  - Auth enforcement: Delegated entirely to orchestrator backend

Environment Variables:
================================================================================
  Variable          Default             Purpose
  ──────────────────────────────────────────────────────
  GATEWAY_CONFIG    config/config.yaml  Path to YAML config file
  HTTP_PORT         8090                HTTP listening port
  HOST              0.0.0.0             Bind address

Supported Architectures: amd64, arm64, ppc64le, s390x
OCP Versions: v4.19, v4.20
FIPS Compliant: Yes
Disconnected Support: Yes
