Security Network Diagram: Guardrails Regex Detector
Generated by: Claude Opus 4.6
Source: architecture/rhoai-3.0/guardrails-regex-detector.md

┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CLUSTER (Trusted - Service Mesh)                              │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Namespace: (orchestrator namespace)                                   │
│  - Role: Routes detection requests to detector backends                  │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: Platform-level (mTLS via service mesh)             │
│         │ Auth: None (application-level)                                 │
│         │ Method: POST /api/v1/text/contents                             │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  SERVICE (Internal Only - No External Ingress)                          │
│                                                                          │
│  [Guardrails Regex Detector]                                             │
│  - Service Type: ClusterIP (expected)                                    │
│  - Port: 8080/TCP (configurable via HTTP_PORT env)                      │
│  - Bind: 0.0.0.0 (configurable via HOST env)                           │
│  - Image: Built on registry.access.redhat.com/ubi9/ubi-minimal:latest  │
│  - Runtime: Rust binary (stateless)                                      │
│  - User: Non-root                                                        │
│  - CPU Architectures: amd64, arm64, ppc64le, s390x                     │
│                                                                          │
│  Endpoints:                                                              │
│  ┌─────────────────────────────────────────────────────────┐            │
│  │ GET  /health                  - Health/liveness probe   │            │
│  │   Port: 8080/TCP                                        │            │
│  │   Encryption: None (plaintext)                          │            │
│  │   Auth: None                                            │            │
│  ├─────────────────────────────────────────────────────────┤            │
│  │ POST /api/v1/text/contents    - Regex detection API     │            │
│  │   Port: 8080/TCP                                        │            │
│  │   Encryption: None (plaintext)                          │            │
│  │   Auth: None (secured by platform/mesh)                 │            │
│  └─────────────────────────────────────────────────────────┘            │
│                                                                          │
│  Processing: Local only (no outbound connections)                       │
│  Built-in Detectors:                                                     │
│  - email → EmailAddress (pii)                                           │
│  - ssn → SocialSecurity (pii)                                           │
│  - credit-card → CreditCard (pii)                                       │
│  - (custom regex patterns)                                               │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  HEALTH CHECKS                                                           │
│                                                                          │
│  [Kubernetes Kubelet]                                                    │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None                                               │
│         │ Auth: None                                                     │
│         │ Method: GET /health                                            │
│         ▼                                                                │
│  [Guardrails Regex Detector] /health                                    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

EGRESS:
- None. This service has no outbound network dependencies.
  All text processing is performed locally using regex patterns.

INGRESS:
- No external ingress resources defined.
- Service is internal-only, accessed by FMS Guardrails Orchestrator within the cluster.

RBAC Summary:
- No ClusterRoles defined
- No ClusterRoleBindings defined
- No ServiceAccounts defined in this repository
- Service has no Kubernetes API interactions (stateless HTTP service)

Secrets:
- None defined. The service does not reference any secrets or credentials.

Authentication & Authorization:
- No application-level authentication on any endpoint
- Security is delegated to platform-level controls:
  - Service mesh mTLS for transport encryption
  - Network policies for access control
  - FIPS compliance enabled at operator/platform level

Security Notes:
- Runs as non-root user in UBI 9 minimal container
- compat-openssl11 package installed for OpenSSL compatibility
- shadow-utils package installed for user management
- No TLS at application level (delegated to service mesh)
- FIPS compliance enabled at operator/platform level
- Supported OCP Versions: v4.19, v4.20
