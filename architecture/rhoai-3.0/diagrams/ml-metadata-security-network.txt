ML Metadata (MLMD) - Security Network Diagram
Generated by: Claude Opus 4.6
================================================================

┌──────────────────────────────────────────────────────────────────────────────┐
│  CLUSTER INTERNAL (No External Ingress)                                      │
│                                                                              │
│  No ingress resources defined. MLMD is an internal-only cluster service.     │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  SERVICE LAYER                                                               │
│                                                                              │
│  [Kubeflow Pipelines / Data Science Pipelines Backend]                       │
│  - Primary consumer of MLMD                                                  │
│  - Namespace: {dsp-namespace}                                                │
│         │                                                                    │
│         │ Port: 8080/TCP                                                     │
│         │ Protocol: gRPC/HTTP2                                               │
│         │ Encryption: Optional TLS (server cert/key)                         │
│         │ Auth: Optional mTLS (client_verify with custom CA)                 │
│         ▼                                                                    │
│  ┌──────────────────────────────────────────────────────────┐                │
│  │  [MLMD gRPC Server - metadata_store_server]              │                │
│  │  - Service Type: ClusterIP                                │                │
│  │  - Port: 8080/TCP (configurable)                          │                │
│  │  - Protocol: gRPC/HTTP2                                   │                │
│  │  - Encryption: Optional TLS via SSLConfig                 │                │
│  │    - server_key: Server private key                       │                │
│  │    - server_cert: Server certificate                      │                │
│  │    - custom_ca: Custom CA for client verification         │                │
│  │    - client_verify: Enable mTLS client cert validation    │                │
│  │  - Auth: None (application-level) or mTLS                 │                │
│  │  - Container: odh-mlmd-grpc-server-rhel9                  │                │
│  │  - Base Image: UBI9-minimal                               │                │
│  │  - Run as: non-root (UID 65534:65534)                     │                │
│  │  - API: 50+ gRPC RPC methods (MetadataStoreService)       │                │
│  └──────────────────────────┬───────────────────────────────┘                │
│                              │                                                │
│              ┌───────────────┼───────────────┐                                │
│              │               │               │                                │
│              ▼               ▼               ▼                                │
│  ┌──────────────────┐ ┌──────────────┐ ┌──────────────────┐                  │
│  │ MySQL / MariaDB  │ │  PostgreSQL  │ │     SQLite       │                  │
│  │ Port: 3306/TCP   │ │ Port: 5432   │ │  (in-memory or   │                  │
│  │ Protocol: MySQL  │ │ Protocol:    │ │   file-based)    │                  │
│  │ Encryption:      │ │ PostgreSQL   │ │ No network       │                  │
│  │  Optional SSL    │ │ Encryption:  │ │                  │                  │
│  │  - key           │ │  Optional SSL│ │                  │                  │
│  │  - cert          │ │  - sslmode   │ │                  │                  │
│  │  - ca            │ │  - sslcert   │ │                  │                  │
│  │  - capath        │ │  - sslkey    │ │                  │                  │
│  │  - cipher        │ │  - sslroot   │ │                  │                  │
│  │  - verify_server │ │  - sslpass   │ │                  │                  │
│  │ Auth: user/pass  │ │ Auth: user/  │ │                  │                  │
│  │                  │ │ pass/passfile │ │                  │                  │
│  └──────────────────┘ └──────────────┘ └──────────────────┘                  │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC resources defined by this component
- MLMD is a standalone gRPC service; does not interact with Kubernetes API
- RBAC is managed by the deploying operator (Data Science Pipelines operator)

Service Mesh Configuration:
- No service mesh configuration defined by this component
- Service mesh integration (if any) is managed by the deploying operator
- mTLS can be configured at the application level via SSLConfig

Secrets:
- Database credentials (Opaque)
  - MySQL/PostgreSQL connection: host, port, user, password
  - Provisioned by: Platform operator / DSP operator
  - Auto-rotate: No

- Server TLS cert/key (kubernetes.io/tls)
  - Purpose: Optional gRPC server TLS certificate and private key
  - Provisioned by: Platform operator / cert-manager
  - Auto-rotate: Depends on provisioner

- Client TLS cert (kubernetes.io/tls)
  - Purpose: Optional mTLS client certificate verification
  - Provisioned by: Platform operator
  - Auto-rotate: Depends on provisioner

- Database SSL certs (Opaque)
  - Purpose: Optional database connection SSL certificates (ca, cert, key)
  - Provisioned by: Platform operator
  - Auto-rotate: Depends on provisioner

Container Security:
- Run as non-root: Yes (UID 65534:65534)
- Base image: registry.access.redhat.com/ubi9/ubi-minimal
- Build image: registry.access.redhat.com/ubi9/ubi
- FIPS compliant: Yes (enabled operator feature)

Authentication Mechanisms:
┌────────────────────────────┬──────────────────────────┬─────────────────────────┐
│ Endpoint                   │ Auth Mechanism           │ Enforcement Point       │
├────────────────────────────┼──────────────────────────┼─────────────────────────┤
│ MetadataStoreService       │ Optional mTLS            │ gRPC server (BoringSSL) │
│ (all RPCs)                 │ (server_key, server_cert,│ SSLConfig in            │
│                            │  custom_ca, client_verify│ MetadataStoreServerCfg  │
├────────────────────────────┼──────────────────────────┼─────────────────────────┤
│ Database (MySQL)           │ Username/password +      │ MySQL server            │
│                            │ optional SSL client certs│ SSLOptions              │
├────────────────────────────┼──────────────────────────┼─────────────────────────┤
│ Database (PostgreSQL)      │ Username/password/       │ PostgreSQL server       │
│                            │ passfile + optional SSL  │ SSLOptions (sslmode:    │
│                            │                          │ disable/allow/verify-ca/│
│                            │                          │ verify-full)            │
└────────────────────────────┴──────────────────────────┴─────────────────────────┘
