Security Network Diagram: KServe V2 REST Proxy
Generated by: Claude Opus 4.6
Source: architecture/rhoai-3.2/rest-proxy.md
================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (via ModelMesh Service)                                       │
│                                                                         │
│  [Client]                                                               │
│         │                                                               │
│         │ Port: 8008/TCP                                                │
│         │ Protocol: HTTP or HTTPS                                       │
│         │ Encryption: Optional TLS (configurable via                    │
│         │             MM_TLS_KEY_CERT_PATH / MM_TLS_PRIVATE_KEY_PATH)   │
│         │ Auth: None at proxy level (delegated to ModelMesh)            │
│         ▼                                                               │
├─────────────────────────────────────────────────────────────────────────┤
│  MODELMESH SERVING POD (Sidecar Container)                              │
│                                                                         │
│  [REST Proxy Server]                                                    │
│  - Deployment Type: Sidecar container in ModelMesh Serving pod          │
│  - Listen Port: 8008/TCP (REST_PROXY_LISTEN_PORT)                       │
│  - Protocol: HTTP/HTTPS (KServe V2 REST Predict Protocol)               │
│  - TLS: Optional server TLS                                             │
│    - Certificate: MM_TLS_KEY_CERT_PATH                                  │
│    - Private Key: MM_TLS_PRIVATE_KEY_PATH                               │
│  - FIPS: GOEXPERIMENT=strictfipsruntime (FIPS-compliant crypto)         │
│  - Max Message Size: 16 MB (REST_PROXY_GRPC_MAX_MSG_SIZE_BYTES)         │
│  - Auth: None (delegated to ModelMesh infrastructure)                   │
│                                                                         │
│  Endpoints:                                                             │
│  - GET  /v2/models/{name}                          (Model Metadata)     │
│  - GET  /v2/models/{name}/versions/{version}       (Model Metadata)     │
│  - POST /v2/models/{model_name}/infer              (Model Inference)    │
│  - POST /v2/models/{model_name}/versions/{ver}/infer (Model Inference)  │
│         │                                                               │
│         │ Port: 8033/TCP                                                │
│         │ Protocol: gRPC                                                │
│         │ Encryption: Optional TLS (configurable via REST_PROXY_USE_TLS)│
│         │ Verify: REST_PROXY_SKIP_VERIFY (default: false)               │
│         │ Auth: None                                                    │
│         │ Connection: localhost (pod-internal)                           │
│         ▼                                                               │
│                                                                         │
│  [ModelMesh gRPC Server]                                                │
│  - Port: 8033/TCP (REST_PROXY_GRPC_PORT)                                │
│  - Protocol: gRPC (GRPCInferenceService)                                │
│  - Services:                                                            │
│    - GRPCInferenceService.ModelMetadata                                  │
│    - GRPCInferenceService.ModelInfer                                     │
│  - Encryption: Optional TLS                                             │
│  - Auth: None (internal pod communication)                              │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC required. This component is a stateless proxy binary
  with no Kubernetes API interactions.

Secrets:
- TLS certificate (path via MM_TLS_KEY_CERT_PATH)
  - Type: TLS certificate
  - Purpose: HTTPS listener certificate
  - Provisioned By: ModelMesh Serving / cert-manager
  - Auto-Rotate: Unknown
- TLS private key (path via MM_TLS_PRIVATE_KEY_PATH)
  - Type: TLS private key
  - Purpose: HTTPS listener private key
  - Provisioned By: ModelMesh Serving / cert-manager
  - Auto-Rotate: Unknown

Authentication & Authorization:
- All /v2/* endpoints: No authentication at proxy level
- Authentication is delegated to ModelMesh Serving infrastructure
- Enforcement point: ModelMesh Serving (upstream)

FIPS Compliance:
- Build flag: GOEXPERIMENT=strictfipsruntime
- Build flag: -tags strictfipsruntime
- CGO_ENABLED=1
- Ensures FIPS-compliant cryptographic operations

Configuration:
- REST_PROXY_LISTEN_PORT=8008        (HTTP listener port)
- REST_PROXY_GRPC_PORT=8033          (Backend gRPC port)
- REST_PROXY_GRPC_MAX_MSG_SIZE_BYTES=16777216 (16 MB max message)
- REST_PROXY_USE_TLS=(unset/false)   (TLS for gRPC backend)
- REST_PROXY_SKIP_VERIFY=false       (Skip TLS cert verification)
- MM_TLS_KEY_CERT_PATH=(unset)       (TLS cert path for HTTPS)
- MM_TLS_PRIVATE_KEY_PATH=(unset)    (TLS key path for HTTPS)
