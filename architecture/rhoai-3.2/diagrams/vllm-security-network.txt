vLLM CUDA Serving Runtime - Security Network Diagram
Generated by: Claude Opus 4.6
=====================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [External Client]                                                       │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Bearer Token / OAuth (optional)                          │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Managed by KServe)                                            │
│                                                                          │
│  [Gateway / Istio Ingress]                                               │
│  - Type: HTTPRoute (Gateway API) or Istio VirtualService                │
│  - Hosts: *.apps.cluster                                                 │
│  - Port: 443/TCP (external) → 8000/TCP (internal)                      │
│  - TLS Termination: TLS 1.2+, mode SIMPLE                              │
│  - Auth: Delegated to Istio / KServe platform                          │
│         │                                                                │
│         │ Port: 8000/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: plaintext (in-cluster)                             │
│         │ Auth: None (delegated to KServe)                               │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (KServe-managed pods)                                 │
│                                                                          │
│  [vLLM CUDA Serving Runtime Pod]                                         │
│  - Namespace: {user-namespace}                                           │
│  - Container: odh-vllm-cuda-rhel9                                        │
│  - User ID: 2000 (non-root)                                              │
│  - Entrypoint: vllm-tgis-adapter                                        │
│                                                                          │
│  Exposed Ports:                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ HTTP API - Port 8000/TCP                                         │   │
│  │ - Protocol: HTTP                                                  │   │
│  │ - Encryption: plaintext (in-cluster)                              │   │
│  │ - Auth: None at container level (delegated to KServe/Istio)      │   │
│  │ - Endpoints:                                                      │   │
│  │   POST /v1/completions       (text completions)                   │   │
│  │   POST /v1/chat/completions  (chat completions)                   │   │
│  │   GET  /v1/models            (list models)                        │   │
│  │   POST /v1/embeddings        (text embeddings)                    │   │
│  │   GET  /health               (health check)                       │   │
│  │   GET  /metrics              (Prometheus metrics)                 │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ gRPC API - Port 8033/TCP                                         │   │
│  │ - Protocol: gRPC                                                  │   │
│  │ - Encryption: plaintext (in-cluster)                              │   │
│  │ - Auth: None at container level (delegated to KServe/Istio)      │   │
│  │ - Service: TGIS GenerationService                                │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  Internal Connections:                                                   │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ GPU Access - NVIDIA CUDA                                         │   │
│  │ - Protocol: CUDA (local device)                                   │   │
│  │ - No network port (device-level access)                           │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  Inbound from:                                                           │
│  - KServe Controller: Pod lifecycle management (Kubernetes API)         │
│  - ModelMesh: gRPC/8033 (inference routing, plaintext)                  │
│  - Prometheus: HTTP/8000 /metrics scrape (plaintext)                    │
│         │                                                                │
│         │ (Egress at startup)                                            │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [S3-compatible Storage]                                                 │
│  - Purpose: Model weight / artifact storage                              │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: AWS credentials (from Secret)                                   │
│                                                                          │
│  [Hugging Face Hub - huggingface.co]                                     │
│  - Purpose: Download models/tokenizers if not pre-cached                │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: HF Token (optional, from Secret)                                │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- This component does not define RBAC resources.
- RBAC is managed by KServe and the RHOAI operator for inference pod service accounts.

Secrets:
- (model storage credentials) (Opaque) - S3/storage access for model downloads
  - Provisioned by: User / RHOAI operator
  - Auto-Rotate: No
- (HF token, optional) (Opaque) - Hugging Face Hub authentication
  - Provisioned by: User
  - Auto-Rotate: No

Container Security:
- User ID: 2000 (non-root)
- DISABLE_LOGPROBS_DURING_SPEC_DECODING=false (security override for spec decoding)

Authentication & Authorization:
- HTTP API (8000/TCP): No auth at container level; delegated to KServe / Istio / Gateway API
- gRPC API (8033/TCP): No auth at container level; delegated to KServe / Istio / Gateway API
- All external-facing auth is handled at the platform ingress layer

Network Policy Notes:
- Services are created dynamically by KServe or ModelMesh (not by this component)
- Ingress is managed by KServe via Gateway API HTTPRoutes or Istio VirtualServices
- In-cluster communication is plaintext (mTLS may be enforced by service mesh sidecar)
