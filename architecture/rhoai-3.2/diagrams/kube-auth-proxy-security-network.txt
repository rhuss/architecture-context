Security Network Diagram: kube-auth-proxy
Generated by: Claude Opus 4.6
Date: 2026-03-19

================================================================================
NETWORK TOPOLOGY - kube-auth-proxy (Dual-Mode Authentication/Authorization Proxy)
================================================================================

┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [Client Browser]                                                            │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: None (pre-authentication)                                    │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS LAYER (Edge TLS Termination)                                        │
│                                                                              │
│  [OpenShift Route / Gateway API HTTPRoute]                                   │
│  - Port: 443/TCP (external) → 4180/TCP or 8443/TCP (internal)              │
│  - TLS Termination: Edge (Route) or Passthrough (RBAC mode)                 │
│  - Auth: N/A (pass-through to sidecar)                                      │
│         │                                         │                          │
│         │ (auth mode)                              │ (rbac mode)             │
│         ▼                                         ▼                          │
├──────────────────────────────────────────────────────────────────────────────┤
│  POD (Sidecar Pattern - Pod-Internal Network)                                │
│                                                                              │
│  ┌─────────────────────────────────────┐  ┌────────────────────────────────┐ │
│  │  kube-auth-proxy (auth mode)        │  │  kube-rbac-proxy (rbac mode)   │ │
│  │  - Port: 4180/TCP                   │  │  - Port: 8443/TCP              │ │
│  │  - Protocol: HTTP                   │  │  - Protocol: HTTPS             │ │
│  │  - Encryption: None (edge-          │  │  - Encryption: TLS 1.2+        │ │
│  │    terminated by Route/Gateway)     │  │  - Cipher suites: configurable │ │
│  │  - Auth: Cookie/Bearer/OIDC         │  │  - Auth: Bearer (TokenReview)  │ │
│  │                                     │  │          mTLS (x509 client)    │ │
│  │  Endpoints:                         │  │          OIDC JWT              │ │
│  │  - /ping (health, no auth)          │  │                                │ │
│  │  - /ready (health, no auth)         │  │  Endpoints:                    │ │
│  │  - /oauth2/sign_in (sign-in page)   │  │  - /* (RBAC-protected proxy)   │ │
│  │  - /oauth2/sign_out (sign-out)      │  │  - /healthz (no auth)          │ │
│  │  - /oauth2/start (OAuth initiate)   │  │                                │ │
│  │  - /oauth2/callback (OAuth code)    │  │                                │ │
│  │  - /oauth2/auth (ext_authz: 200/401)│  │                                │ │
│  │  - /oauth2/userinfo (user info)     │  │                                │ │
│  │  - /* (reverse proxy to upstream)   │  │                                │ │
│  └──────────────┬──────────────────────┘  └──────────────┬─────────────────┘ │
│                 │                                         │                   │
│                 │ Port: configurable                      │ Port: configurable│
│                 │ Protocol: HTTP                          │ Protocol: HTTP/h2c│
│                 │ Encryption: None (pod-internal)         │ Encryption: None  │
│                 │ Auth: X-Forwarded-User/Email/Groups     │ Auth: x-remote-   │
│                 │       headers                           │   user/groups hdrs│
│                 ▼                                         ▼                   │
│  ┌──────────────────────────────────────────────────────────────────────────┐ │
│  │  [Upstream Application]                                                  │ │
│  │  - Receives pre-authenticated requests                                   │ │
│  │  - Trusts X-Forwarded-* / x-remote-* headers from sidecar              │ │
│  └──────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  ENVOY ext_authz INTEGRATION                                                 │
│                                                                              │
│  [Envoy Gateway]                                                             │
│         │                                                                    │
│         │ Port: 4180/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: Pod-internal                                           │
│         │ Auth: Cookie/Bearer forwarded                                      │
│         │ Path: GET /oauth2/auth                                             │
│         │ Response: 200 OK (allow) or 401 Unauthorized (deny)               │
│         ▼                                                                    │
│  [kube-auth-proxy /oauth2/auth endpoint]                                     │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS - AUTHENTICATION PROVIDERS                                           │
│                                                                              │
│  [OIDC Provider (IssuerURL)]                                                 │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: Client ID/Secret                                                    │
│  - Purpose: OIDC discovery, token exchange, userinfo                         │
│                                                                              │
│  [OpenShift OAuth Server]                                                    │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: Client ID/Secret (ServiceAccount token)                             │
│  - Discovery: /.well-known/oauth-authorization-server                        │
│  - Purpose: OAuth authorization, token redemption                            │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS - KUBERNETES API                                                     │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: ServiceAccount token                                                │
│  - APIs used:                                                                │
│    - authentication.k8s.io/v1 TokenReview (kube-rbac-proxy)                 │
│    - authorization.k8s.io/v1 SubjectAccessReview (kube-rbac-proxy)          │
│    - user.openshift.io/v1 Users (OpenShift provider user info)              │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS - OPTIONAL SERVICES                                                  │
│                                                                              │
│  [Redis (optional)]                                                          │
│  - Port: 6379/TCP                                                            │
│  - Protocol: TCP (Redis protocol)                                            │
│  - Encryption: TLS (optional, configurable)                                  │
│  - Auth: Password (optional)                                                 │
│  - Purpose: Distributed session storage (Sentinel/Cluster supported)         │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  MONITORING                                                                  │
│                                                                              │
│  [Prometheus]                                                                │
│  - Port: configurable                                                        │
│  - Protocol: HTTP or HTTPS                                                   │
│  - Encryption: Configurable                                                  │
│  - Auth: None                                                                │
│  - Path: /metrics                                                            │
│  - Purpose: Metrics scraping                                                 │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

================================================================================
RBAC REQUIREMENTS
================================================================================

The sidecar pod's ServiceAccount requires:

  Role (required by pod SA):
  ┌──────────────────────────────┬──────────────────────────┬──────────────┐
  │ API Group                    │ Resources                │ Verbs        │
  ├──────────────────────────────┼──────────────────────────┼──────────────┤
  │ authentication.k8s.io        │ tokenreviews             │ create       │
  │ authorization.k8s.io         │ subjectaccessreviews     │ create       │
  │ user.openshift.io            │ users                    │ get          │
  └──────────────────────────────┴──────────────────────────┴──────────────┘

  Note: No ClusterRoles are defined in the repository. RBAC permissions
  are determined by the ServiceAccount of the pod running the sidecar.

================================================================================
SECRETS
================================================================================

  ┌─────────────────────────┬────────────────────────┬──────────────────────────┬──────────────────┬──────────────┐
  │ Secret Name             │ Type                   │ Purpose                  │ Provisioned By   │ Auto-Rotate  │
  ├─────────────────────────┼────────────────────────┼──────────────────────────┼──────────────────┼──────────────┤
  │ cookie-secret           │ Opaque                 │ Session cookie           │ Manual/Operator  │ No           │
  │                         │                        │ encryption key           │                  │              │
  ├─────────────────────────┼────────────────────────┼──────────────────────────┼──────────────────┼──────────────┤
  │ client-secret           │ Opaque                 │ OAuth2 client secret     │ Manual/Operator  │ No           │
  │                         │                        │ for OIDC/OpenShift       │                  │              │
  ├─────────────────────────┼────────────────────────┼──────────────────────────┼──────────────────┼──────────────┤
  │ tls-cert/key            │ kubernetes.io/tls      │ TLS cert/key for HTTPS   │ cert-manager/    │ Depends      │
  │                         │                        │ listeners                │ Manual           │              │
  ├─────────────────────────┼────────────────────────┼──────────────────────────┼──────────────────┼──────────────┤
  │ jwt_signing_key.pem     │ Opaque (file)          │ JWT signing key          │ Build            │ No           │
  │                         │                        │ (empty default)          │                  │              │
  ├─────────────────────────┼────────────────────────┼──────────────────────────┼──────────────────┼──────────────┤
  │ ServiceAccount token    │ k8s.io/sa-token        │ Auto-detected client     │ Kubernetes       │ Yes          │
  │                         │                        │ secret for OpenShift     │                  │              │
  └─────────────────────────┴────────────────────────┴──────────────────────────┴──────────────────┴──────────────┘

================================================================================
FIPS COMPLIANCE
================================================================================

  - Build flags: CGO_ENABLED=1 GOEXPERIMENT=strictfipsruntime -tags strictfipsruntime
  - Base image: registry.access.redhat.com/ubi9/ubi-minimal (FIPS-validated crypto)
  - Build image: registry.access.redhat.com/ubi9/go-toolset:1.25
  - TLS minimum: TLS 1.2+ (configurable, default VersionTLS12)
  - Validation: check-payload CI workflow
  - Supported CPU architectures: amd64, arm64, ppc64le, s390x

================================================================================
INTEGRATION POINTS
================================================================================

  ┌────────────────────────────┬──────────────┬────────────┬───────────────┬──────────────────────────────────┐
  │ Component                  │ Port         │ Protocol   │ Encryption    │ Purpose                          │
  ├────────────────────────────┼──────────────┼────────────┼───────────────┼──────────────────────────────────┤
  │ RHOAI Dashboard            │ 4180/8443    │ HTTP/HTTPS │ TLS 1.2+      │ Auth/authz for dashboard         │
  │ Notebook Controllers       │ 8443/TCP     │ HTTPS      │ TLS 1.2+      │ RBAC for notebook access         │
  │ Model Serving              │ 8443/TCP     │ HTTPS      │ TLS 1.2+      │ RBAC for inference endpoints     │
  │ Envoy / Gateway API        │ 4180/TCP     │ HTTP       │ Pod-internal  │ ext_authz for Gateway API        │
  │ OpenShift OAuth Server     │ 443/TCP      │ HTTPS      │ TLS 1.2+      │ User authentication              │
  │ External OIDC Provider     │ 443/TCP      │ HTTPS      │ TLS 1.2+      │ BYOIDC support                   │
  │ Redis (optional)           │ 6379/TCP     │ TCP/TLS    │ Configurable  │ Distributed session storage      │
  │ Prometheus                 │ configurable │ HTTP/HTTPS │ Configurable  │ Metrics collection               │
  └────────────────────────────┴──────────────┴────────────┴───────────────┴──────────────────────────────────┘
