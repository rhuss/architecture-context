Security Network Diagram: vllm-orchestrator-gateway
Generated by: Claude Opus 4.6
Source: architecture/rhoai-3.2/vllm-orchestrator-gateway.md

┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CLIENTS (Cluster-Internal)                                    │
│                                                                          │
│  [Client Workloads / API Consumers]                                      │
│         │                                                                │
│         │ Port: 8090/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: plaintext                                          │
│         │ Auth: Authorization header (pass-through to orchestrator)      │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  GATEWAY SERVICE                                                         │
│                                                                          │
│  [vLLM Orchestrator Gateway]                                             │
│  - Language: Rust 1.84.0 (Axum/Tokio)                                   │
│  - Service Type: ClusterIP (assumed)                                     │
│  - Port: 8090/TCP (configurable via HTTP_PORT)                          │
│  - Host: 0.0.0.0 (configurable via HOST)                                │
│  - Image: quay.io/rhoai/odh-trustyai-vllm-orchestrator-gateway-rhel9   │
│  - Base Image: ubi9/ubi-minimal                                          │
│  - Endpoints: /{route_name}/v1/chat/completions (POST)                  │
│  - Route names: Configured via config.yaml                               │
│  - Modes: Streaming (SSE) and Non-Streaming                             │
│  - Auth at gateway: NONE (pass-through only)                             │
│         │                                                                │
│         │ Port: 8085/TCP (configurable via config.yaml)                  │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: Optional mTLS (TLS 1.2+)                           │
│         │   - Client cert: /etc/tls/private/tls.crt                      │
│         │   - Client key:  /etc/tls/private/tls.key                      │
│         │   - CA cert:     /etc/tls/ca/service-ca.crt                    │
│         │ Auth: Client certificate (mTLS) or none                        │
│         │ Headers forwarded: Authorization, X-Forwarded-*                │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  BACKEND SERVICES (Cluster-Internal)                                     │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Endpoint: POST /api/v2/chat/completions-detection                    │
│  - Port: 8085/TCP (configurable)                                        │
│  - Purpose: Detection-augmented chat completions                         │
│  - Encryption: Optional mTLS                                             │
│         │                                                                │
│         ├──► [Content Detectors (e.g., regex-detector)]                  │
│         │    - Registered with orchestrator                              │
│         │    - Referenced by name from gateway config                     │
│         │    - Detects: PII, content policy violations, etc.             │
│         │                                                                │
│         └──► [vLLM / LLM Backend]                                       │
│              - Chat completion inference                                 │
│              - Accessed indirectly via orchestrator                      │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC manifests defined in repository
- RBAC is expected to be managed externally (RHOAI operator)
- No ServiceAccount, ClusterRole, or RoleBinding defined

Secrets:
- TLS cert at /etc/tls/private/tls.crt (kubernetes.io/tls expected)
  - Purpose: Client TLS certificate for mTLS with orchestrator
  - Provisioned by: External (cert-manager or service-ca operator)
  - Auto-rotate: Unknown
- TLS key at /etc/tls/private/tls.key (kubernetes.io/tls expected)
  - Purpose: Client TLS private key for mTLS with orchestrator
  - Provisioned by: External (cert-manager or service-ca operator)
  - Auto-rotate: Unknown
- CA cert at /etc/tls/ca/service-ca.crt (Opaque expected)
  - Purpose: Trusted CA for validating orchestrator's TLS certificate
  - Provisioned by: External (service-ca operator)
  - Auto-rotate: Unknown

Authentication & Authorization:
- Gateway-level auth: NONE
- Authorization header: Forwarded to orchestrator (pass-through)
- X-Forwarded-* headers: Forwarded to orchestrator
- Auth enforcement: Delegated entirely to orchestrator

TLS Configuration:
- Inbound (client → gateway): plaintext HTTP on 8090/TCP
- Outbound (gateway → orchestrator): Optional mTLS
  - When /etc/tls/private/tls.crt + tls.key present: HTTPS with client cert
  - When /etc/tls/ca/service-ca.crt present: Custom CA trust
  - When connecting to localhost with custom CA: hostname verification relaxed
  - When no TLS certs found: plain HTTP

Network Policies:
- No NetworkPolicy manifests defined in repository
- Network policies expected to be managed externally

Configuration:
- GATEWAY_CONFIG env var → path to config.yaml (default: config/config.yaml)
- Config defines: orchestrator host/port, detector configs, route mappings
- Routes are dynamic: each route creates /{name}/v1/chat/completions endpoint
