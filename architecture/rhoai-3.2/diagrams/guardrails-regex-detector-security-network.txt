Security Network Diagram - Guardrails Regex Detector
Generated by: Claude Opus 4.6
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│  CLUSTER INTERNAL (Trusted - No Ingress)                                    │
│                                                                              │
│  No external ingress defined.                                                │
│  Service is consumed only by internal cluster components.                    │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  INTERNAL SERVICES                                                           │
│                                                                              │
│  [FMS Guardrails Orchestrator]                                               │
│  - Role: Primary (and only expected) consumer                                │
│  - Namespace: (deployment-dependent)                                         │
│         │                                                                    │
│         │ Port: 8080/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: plaintext (cluster-internal)                           │
│         │ Auth: None (relies on network-level access control)                │
│         ▼                                                                    │
│  [Regex Detector Service]                                                    │
│  - Service Type: ClusterIP (assumed)                                         │
│  - Port: 8080/TCP → 8080/TCP                                                │
│  - Protocol: HTTP                                                            │
│  - Exposure: Internal only                                                   │
│  - Binary: /app/bin/regex-detector                                           │
│  - Base Image: ubi9/ubi-minimal:latest                                       │
│  - Bind Address: 0.0.0.0 (configurable via HOST env)                        │
│  - Listen Port: 8080 (configurable via HTTP_PORT env)                       │
│                                                                              │
│  Endpoints:                                                                  │
│  ┌──────────────────────────────────────────────────────────────────┐        │
│  │ POST /api/v1/text/contents  │ 8080/TCP │ HTTP │ No Auth        │        │
│  │ GET  /health                │ 8080/TCP │ HTTP │ No Auth        │        │
│  └──────────────────────────────────────────────────────────────────┘        │
│                                                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  EGRESS                                                                      │
│                                                                              │
│  None. The service is entirely self-contained.                               │
│  All regex processing is performed in-memory with no external calls.         │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- No Kubernetes API interactions
- Standalone microservice without RBAC requirements

Secrets:
- None defined
- No credentials or TLS certificates required by the service

Authentication & Authorization:
- No built-in authentication mechanism
- All endpoints are open (no auth required)
- Security relies on:
  1. NetworkPolicies (cluster-level network access control)
  2. Service mesh mTLS (if deployed with service mesh sidecar)
  3. Namespace isolation

Environment Variables:
- HTTP_PORT (default: 8080) - Configurable HTTP listening port
- HOST (default: 0.0.0.0) - Configurable bind address

Container Security:
- Multi-arch support: amd64, arm64, ppc64le, s390x
- FIPS-compliant operator feature enabled
- Base image: Red Hat UBI 9 Minimal (security-hardened)
- Runtime dependencies: shadow-utils, compat-openssl11

Network Security Notes:
- Service exposes HTTP (plaintext) only - no TLS termination at service level
- In production RHOAI deployments, network-level security is expected:
  * NetworkPolicies to restrict access to authorized callers
  * Service mesh mTLS for transport encryption
  * The FMS Guardrails Orchestrator is the primary authorized caller
