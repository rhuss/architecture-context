┌─────────────────────────────────────────────────────────────────────────┐
│  USER ENVIRONMENT (Untrusted)                                           │
│                                                                          │
│  [User / CI Pipeline]                                                    │
│  - Runs CLI binary directly (kubectl plugin) or via container image     │
│  - Provides kubeconfig with cluster credentials                          │
│         │                                                                │
│         │ Execution: Local process / Container ENTRYPOINT                │
│         │ Auth: kubeconfig (Bearer Token / Client Certificate / OIDC)   │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  CLI PROCESS (odh-cli / kubectl-odh / rhai-cli)                         │
│                                                                          │
│  [kubectl-odh Binary]                                                    │
│  - FIPS-compliant (CGO_ENABLED=1, strictfipsruntime)                    │
│  - No network listener (no inbound ports)                                │
│  - Reads kubeconfig from ~/.kube/config or KUBECONFIG env              │
│  - QPS: 50, Burst: 100 (configurable)                                   │
│  - Timeout: 10m (configurable)                                           │
│         │                                                                │
│         │ Port: 6443/TCP                                                 │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+ (system crypto policy, FIPS mode)        │
│         │ Auth: Bearer Token / Client Certificate / OIDC                │
│         │ Direction: EGRESS ONLY                                         │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE (Trusted)                                      │
│                                                                          │
│  [Kubernetes API Server - kube-apiserver]                                │
│  - Port: 6443/TCP                                                        │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - AuthN: Bearer Token, Client Certificate, OIDC                        │
│  - AuthZ: RBAC (user's existing permissions)                             │
│                                                                          │
│  ┌─── API Groups Accessed (Read) ──────────────────────────────────┐   │
│  │                                                                    │  │
│  │  Platform APIs:                                                    │  │
│  │  - datasciencecluster.opendatahub.io (DSC - singleton GET)       │  │
│  │  - dscinitialization.opendatahub.io (DSCI - singleton GET)       │  │
│  │  - apiextensions.k8s.io (CRDs - GET, LIST)                      │  │
│  │  - config.openshift.io (ClusterVersion - GET)                    │  │
│  │                                                                    │  │
│  │  OLM APIs:                                                         │  │
│  │  - operators.coreos.com (Subscriptions, CSVs - GET, LIST)        │  │
│  │  - packages.operators.coreos.com (PackageManifests - GET, LIST)  │  │
│  │                                                                    │  │
│  │  Workload APIs:                                                    │  │
│  │  - kubeflow.org (Notebooks, PyTorchJobs)                         │  │
│  │  - serving.kserve.io (InferenceServices, ServingRuntimes)        │  │
│  │  - kueue.x-k8s.io (ClusterQueues, LocalQueues)                  │  │
│  │  - ray.io (RayClusters, RayJobs)                                 │  │
│  │  - workload.codeflare.dev (AppWrappers)                          │  │
│  │  - trustyai.opendatahub.io (GuardrailsOrchestrators)            │  │
│  │  - datasciencepipelinesapplications.opendatahub.io (DSPAs)      │  │
│  │                                                                    │  │
│  │  Core APIs:                                                        │  │
│  │  - core (ConfigMaps, Secrets, Namespaces, Pods, Services, PVCs)  │  │
│  │  - apps (Deployments, StatefulSets, ReplicaSets, DaemonSets)    │  │
│  │  - batch (Jobs, CronJobs)                                        │  │
│  │  - image.openshift.io (ImageStreams, ImageStreamTags)            │  │
│  │                                                                    │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                                                          │
│  ┌─── API Groups Accessed (Write - Migration Only) ─────────────┐     │
│  │                                                                  │    │
│  │  - operators.coreos.com/subscriptions (CREATE)                  │    │
│  │  - datasciencecluster.opendatahub.io/datascienceclusters (UPDATE) │  │
│  │  - core/configmaps (UPDATE)                                      │   │
│  │                                                                  │    │
│  │  Write operations use:                                           │    │
│  │  - Exponential backoff with conflict retry                      │    │
│  │  - Dry-run support (--dry-run flag)                             │    │
│  │  - Interactive confirmation (--yes to skip)                     │    │
│  │                                                                  │    │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  LOCAL FILESYSTEM (Backup Output Only)                                   │
│                                                                          │
│  [Backup Output Directory]                                               │
│  - Portable YAML files organized by namespace                            │
│  - Cluster-specific metadata stripped (status, resourceVersion, uid)    │
│  - May contain sensitive data (Secrets exported during backup)          │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

NETWORK SUMMARY:
- Ingress: NONE (CLI tool, no network listener)
- Egress: Kubernetes API server only (6443/TCP, HTTPS, TLS 1.2+)
- Service Mesh: N/A (not deployed as a pod)
- No inter-pod communication
- No external service calls (all queries are to in-cluster K8s API)

RBAC SUMMARY (Required Permissions):

  Read-Only (lint + backup):
  - 23+ API groups with GET, LIST verbs
  - Spans platform CRDs, OLM resources, workload CRDs, core resources
  - No cluster-admin required for read operations

  Write (migration only):
  - operators.coreos.com/subscriptions: CREATE
  - datasciencecluster.opendatahub.io/datascienceclusters: UPDATE
  - core/configmaps: UPDATE

AUTHENTICATION:
- Mechanism: kubeconfig-based (Bearer Token / Client Certificate / OIDC)
- Enforcement: kube-apiserver RBAC
- No service accounts created (uses caller's identity)
- No secrets created or managed

FIPS COMPLIANCE:
- Build: CGO_ENABLED=1 GOEXPERIMENT=strictfipsruntime -tags strictfipsruntime
- TLS: System crypto policy via client-go (FIPS via OpenSSL on UBI9)
- No custom tls.Config, no InsecureSkipVerify
- No weak crypto (no MD5, RC4, DES usage detected)

SECRETS HANDLING:
- Backup command reads existing Secrets for export
- No secrets created or modified by the CLI
- Backup output files may contain secret values (user responsibility to secure)
