┌─────────────────────────────────────────────────────────────────────────┐
│  CLUSTER INTERNAL (Trusted - Network Policy / Service Mesh)             │
│                                                                          │
│  ┌──────────────────────────────────────┐                                │
│  │  FMS Guardrails Orchestrator         │                                │
│  │  (upstream caller)                   │                                │
│  └──────────────┬───────────────────────┘                                │
│                 │                                                         │
│                 │ Port: 8080/TCP                                          │
│                 │ Protocol: HTTP                                          │
│                 │ Encryption: None (plaintext)                            │
│                 │ Auth: None                                              │
│                 │ Method: POST /api/v1/text/contents                     │
│                 │ Body: { contents: [...], detector_params: { regex } }  │
│                 ▼                                                         │
│  ┌──────────────────────────────────────┐                                │
│  │  Guardrails Regex Detector           │                                │
│  │  ─────────────────────────────────   │                                │
│  │  Image: regex-detector (UBI 9)       │                                │
│  │  Language: Rust (Axum 0.7.9)         │                                │
│  │  Bind: 0.0.0.0:8080                  │                                │
│  │  Env: HOST, HTTP_PORT                │                                │
│  │  User: root (no USER directive)      │                                │
│  │                                      │                                │
│  │  Endpoints:                          │                                │
│  │    POST /api/v1/text/contents        │                                │
│  │    GET  /health                      │                                │
│  │                                      │                                │
│  │  Built-in Detectors:                 │                                │
│  │    - EmailAddress (PII)              │                                │
│  │    - SSN (PII)                       │                                │
│  │    - CreditCard (PII)               │                                │
│  │    - Custom regex (user-supplied)    │                                │
│  └──────────────▲───────────────────────┘                                │
│                 │                                                         │
│                 │ Port: 8080/TCP                                          │
│                 │ Protocol: HTTP                                          │
│                 │ Encryption: None                                        │
│                 │ Auth: None                                              │
│                 │ Method: GET /health                                     │
│                 │                                                         │
│  ┌──────────────┴───────────────────────┐                                │
│  │  Kubernetes Kubelet                  │                                │
│  │  (liveness/readiness probes)         │                                │
│  └──────────────────────────────────────┘                                │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS                                                                  │
│                                                                          │
│  None — the service makes no outbound network calls.                     │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (External)                                                      │
│                                                                          │
│  None — no ingress resources. Service is internal-only (ClusterIP).      │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
  None — the service does not interact with the Kubernetes API.
  No ClusterRoles, Roles, or RoleBindings.

Secrets:
  None — no secrets consumed by the service.

Authentication & Authorization:
  - POST /api/v1/text/contents: Unauthenticated
  - GET /health: Unauthenticated
  - Security relies entirely on network-level isolation
    (NetworkPolicy, Service Mesh mTLS, or kube-rbac-proxy sidecar)

FIPS Compliance:
  - No TLS — serves plaintext HTTP only
  - No cryptographic operations performed
  - compat-openssl11 installed in runtime image (check-payload compliance)
  - FIPS compliance depends on infrastructure layer (Service Mesh mTLS)

Security Findings:
  - ⚠ No TLS termination on service port (plaintext HTTP)
  - ⚠ No authentication mechanism on endpoints
  - ⚠ Container runs as root (no USER directive in Dockerfile)
  - ⚠ No Konflux Dockerfile — may be built outside standard pipeline
  - ⚠ microdnf install without version pinning (supply-chain gap)
  - ✓ Stateless — no persistent data, no database
  - ✓ No outbound network calls (zero egress)
  - ✓ No Kubernetes API interaction (zero RBAC surface)
  - ✓ Cargo.lock present (hermetic Rust dependency resolution)
