┌─────────────────────────────────────────────────────────────────────────┐
│  PLATFORM SECURITY BOUNDARY                                             │
│                                                                          │
│  Security is NOT implemented at the application level.                   │
│  All security hardening is delegated to platform infrastructure:         │
│  - Kubernetes Network Policies                                           │
│  - Service Mesh (mTLS)                                                   │
│  - kube-rbac-proxy sidecars                                              │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  INTERNAL (Cluster Network)                                              │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Namespace: {guardrails-namespace}                                     │
│  - Role: Detection request originator                                    │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (application-level)                           │
│         │              Platform may provide mTLS via service mesh        │
│         │ Auth: None (application-level)                                 │
│         │       Platform may enforce via kube-rbac-proxy sidecar         │
│         │ Method: POST /api/v1/text/contents                             │
│         ▼                                                                │
│                                                                          │
│  [Guardrails Regex Detector]                                             │
│  - Service: regex-detector (ClusterIP)                                   │
│  - Namespace: {guardrails-namespace}                                     │
│  - Container Port: 8080/TCP                                              │
│  - Image: ubi9/ubi-minimal based                                         │
│  - Language: Rust (axum framework)                                       │
│  - Container User: root (no USER directive in Dockerfile)                │
│  - Endpoints:                                                            │
│    - POST /api/v1/text/contents (detection)                              │
│    - GET  /health (health check)                                         │
│  - TLS: None                                                             │
│  - Auth: None                                                            │
│  - Rate Limiting: None                                                   │
│  - Input Validation: Minimal (checks non-empty regex list only)          │
│  - No size limits on content payloads                                    │
│         │                                                                │
│         │ HTTP Response                                                  │
│         │ Detection results (JSON)                                       │
│         ▼                                                                │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Receives detection results                                            │
│  - Aggregates with other detector responses                              │
│  - Enforces content safety policies                                      │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  None — This component has NO external egress.                           │
│  It is a pure compute service with no outbound dependencies.             │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS                                                                 │
│                                                                          │
│  None — This component is NOT exposed outside the cluster.               │
│  No Ingress, Route, or Gateway resources.                                │
│  Access is limited to cluster-internal traffic only.                     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC roles or bindings defined
- No ServiceAccount configured (uses default)
- No Kubernetes API access required
- The component does not interact with the Kubernetes API server

Service Mesh Configuration:
- No application-level service mesh configuration
- Platform-level mTLS may be enforced externally (not configured by this component)
- No PeerAuthentication or AuthorizationPolicy resources

Secrets:
- No secrets defined or consumed
- No TLS certificates
- No credentials or API keys

Security Observations:
- Container runs as root (no USER directive in Dockerfile) — potential hardening gap
- No input size limits on POST body — could accept arbitrarily large payloads
- No rate limiting at application level
- Regex engine (Rust `regex` crate) guarantees linear-time matching — safe against ReDoS
- Custom regex patterns compiled per-request — no caching, but no security risk
- All security (AuthN, AuthZ, encryption, network isolation) delegated to platform

FIPS Compliance:
- No cryptographic operations performed by the application
- compat-openssl11 installed in image for runtime library compatibility only
- FIPS compliance inherited from platform network security layer
- Rust binary uses dynamic linking (glibc), compatible with check-payload validation
