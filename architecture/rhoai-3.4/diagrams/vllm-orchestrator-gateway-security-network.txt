Security Network Diagram: vllm-orchestrator-gateway
Generated by: Claude Opus 4.6
Source: architecture/rhoai-3.4/vllm-orchestrator-gateway.md

================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL / UPSTREAM (Untrusted)                                        │
│                                                                          │
│  [Client Application]                                                    │
│  - Any OpenAI-compatible client                                          │
│         │                                                                │
│         │ Port: 8090/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: plaintext                                          │
│         │ Auth: Authorization header (passthrough)                       │
│         │       X-Forwarded-* headers (passthrough)                      │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  GATEWAY SERVICE (vllm-orchestrator-gateway)                            │
│                                                                          │
│  [vllm-orchestrator-gateway]                                             │
│  - Language: Rust 1.84.0 (axum framework)                               │
│  - Listen: 0.0.0.0:8090/TCP (configurable via HTTP_PORT)                │
│  - Image: rhoai/odh-trustyai-vllm-orchestrator-gateway-rhel9            │
│  - Base: UBI 9 Minimal                                                   │
│  - Endpoints: POST /{route_name}/v1/chat/completions                    │
│  - Routes: Dynamically configured from YAML                             │
│    Example: /pii/v1/chat/completions                                    │
│    Example: /passthrough/v1/chat/completions                            │
│                                                                          │
│  Processing:                                                             │
│  - Injects detector config into request payload                         │
│  - Forwards Authorization + X-Forwarded-* headers only                  │
│  - All other headers are dropped                                        │
│  - Replaces content with fallback_message on detection                  │
│  - Supports SSE streaming (text/event-stream)                           │
│                                                                          │
│  TLS Client Configuration:                                               │
│  - Client cert: /etc/tls/private/tls.crt (PEM)                         │
│  - Client key:  /etc/tls/private/tls.key (PEM)                         │
│  - CA cert:     /etc/tls/ca/service-ca.crt (PEM)                       │
│  - PKCS#12 identity built in-memory from PEM cert/key (OpenSSL)        │
│  - Auto-detection: If TLS files present → HTTPS; otherwise → HTTP      │
│         │                                                                │
│         │ Port: 8085/TCP (configurable)                                  │
│         │ Protocol: HTTP or HTTPS                                        │
│         │ Encryption: Optional mTLS (TLS with client cert)              │
│         │ Auth: mTLS client certificate +                                │
│         │       Authorization header (forwarded from client)            │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  BACKEND SERVICES (Internal)                                             │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Port: 8085/TCP (configurable via orchestrator.port)                  │
│  - Endpoint: POST /api/v2/chat/completions-detection                    │
│  - Purpose: Chat completion with content detection                      │
│  - Encryption: TLS (when gateway uses mTLS)                             │
│         │                                                                │
│         ├─── [Guardrails Detectors]                                      │
│         │    - e.g., regex-language (PII detection)                      │
│         │    - Configured in orchestrator                                │
│         │    - Referenced by name in gateway config                      │
│         │                                                                │
│         └─── [vLLM Model Server]                                         │
│              - LLM inference backend                                     │
│              - e.g., Qwen/Qwen2.5-1.5B-Instruct                        │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

================================================================================
RBAC Summary
================================================================================

No Kubernetes RBAC defined. This is a standalone HTTP service.
- No ClusterRoles
- No ClusterRoleBindings
- No ServiceAccounts defined in repository

================================================================================
Secrets / Certificates
================================================================================

| Secret Path                    | Type             | Purpose                                          | Provisioned By                      | Auto-Rotate |
|-------------------------------|------------------|--------------------------------------------------|-------------------------------------|-------------|
| /etc/tls/private/tls.crt      | TLS cert (PEM)   | Client certificate for mTLS to orchestrator      | Platform/Service Mesh (vol mount)   | Unknown     |
| /etc/tls/private/tls.key      | TLS key (PEM)    | Client private key for mTLS to orchestrator      | Platform/Service Mesh (vol mount)   | Unknown     |
| /etc/tls/ca/service-ca.crt    | CA cert (PEM)    | Trusted CA for orchestrator server cert           | OpenShift service-ca operator       | Unknown     |

================================================================================
Authentication & Authorization
================================================================================

- Gateway does NOT authenticate clients directly
- Authorization header: forwarded as-is to orchestrator
- X-Forwarded-* headers: forwarded as-is to orchestrator
- All other request headers are dropped (not forwarded)
- mTLS to orchestrator: automatic when TLS cert/key files exist at mounted paths

================================================================================
Network Exposure
================================================================================

- No Kubernetes Ingress, Route, HTTPRoute, or Gateway resources defined
- Service is internal-only (no external exposure from this component)
- Receives traffic from upstream components
- Listen address: 0.0.0.0:8090/TCP (configurable)

================================================================================
Configuration
================================================================================

| Variable       | Default            | Purpose                           |
|----------------|--------------------|-----------------------------------|
| GATEWAY_CONFIG | config/config.yaml | Path to gateway YAML config file  |
| HTTP_PORT      | 8090               | HTTP listen port                  |
| HOST           | 0.0.0.0            | HTTP listen address               |
