%% Data Flow Diagram: Guardrails Regex Detector
%% Generated by: Claude Opus 4.6
%% Source: architecture/rhoai-3.4/guardrails-regex-detector.md

sequenceDiagram
    participant Orchestrator as FMS Guardrails<br/>Orchestrator
    participant Detector as Regex Detector<br/>8080/TCP HTTP
    participant Kubelet as Kubernetes<br/>Kubelet

    Note over Orchestrator,Detector: Flow 1: PII Detection Request

    Orchestrator->>Detector: POST /api/v1/text/contents<br/>HTTP 8080/TCP (plaintext)<br/>Auth: None
    Note right of Detector: Parse JSON request<br/>Extract contents[] and<br/>detector_params.regex[]

    alt Built-in Detectors
        Note right of Detector: Apply email regex pattern
        Note right of Detector: Apply SSN regex pattern
        Note right of Detector: Apply credit card regex pattern
    end

    alt Custom Regex
        Note right of Detector: Apply user-supplied<br/>regex patterns
    end

    Detector-->>Orchestrator: 200 OK<br/>JSON: [[{start, end, text,<br/>detection_type, detection,<br/>score: 1.0}]]

    Note over Kubelet,Detector: Flow 2: Health Check

    Kubelet->>Detector: GET /health<br/>HTTP 8080/TCP (plaintext)<br/>Auth: None
    Detector-->>Kubelet: 200 OK
