Security Network Diagram for llm-d-kv-cache
Generated by: Claude Opus 4.6
============================================================================

┌──────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL SERVICES (Outbound)                                            │
│                                                                          │
│  [HuggingFace Hub - huggingface.co]                                      │
│  - Purpose: Tokenizer model downloads                                    │
│  - Auth: Bearer Token (HF_TOKEN from vllm-p2p-secrets)                   │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [OTLP Collector]                                                        │
│  - Purpose: Distributed tracing export                                   │
│  - Auth: None                                                            │
│  - Encryption: plaintext (configurable)                                  │
│                                                                          │
│  [Redis/Valkey Server] (optional)                                        │
│  - Purpose: Distributed KV-block index backend                           │
│  - Auth: Optional via URL credentials                                    │
│  - Encryption: Optional TLS (valkeys://)                                 │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
         ▲                    ▲                    ▲
         │ 443/TCP            │ 4317/TCP           │ 6379/TCP
         │ HTTPS              │ gRPC               │ Redis protocol
         │ TLS 1.2+           │ plaintext           │ Optional TLS
         │ Bearer Token       │ None               │ Optional auth
         │                    │                    │
┌──────────────────────────────────────────────────────────────────────────┐
│  KUBERNETES CLUSTER (Internal)                                           │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │  KV-CACHE MANAGER POD (StatefulSet)                                │  │
│  │                                                                    │  │
│  │  ┌────────────────────────────────┐  ┌──────────────────────────┐  │  │
│  │  │  Main Container (Go)           │  │  UDS Tokenizer Sidecar   │  │  │
│  │  │                                │  │  (Python 3.12)           │  │  │
│  │  │  - kvcache.Indexer             │  │                          │  │  │
│  │  │  - kvevents.Pool               │  │  - TokenizationService   │  │  │
│  │  │  - PodReconciler               │  │  - Port: UDS only        │  │  │
│  │  │  - Metrics: Prometheus         │  │  - Health: 8082/TCP      │  │  │
│  │  │                                │  │  - User: 65532 (nonroot) │  │  │
│  │  │  ZMQ SUB: 5557/TCP (inbound)   │  │                          │  │  │
│  │  │  gRPC: 50051/TCP (optional)    │  │                          │  │  │
│  │  │  HTTP: 8080/TCP (service)      │  │                          │  │  │
│  │  │                                │  │                          │  │  │
│  │  │           gRPC over UDS        │  │                          │  │  │
│  │  │  ─────────────────────────────►│  │                          │  │  │
│  │  │  /tmp/tokenizer/tokenizer-     │  │                          │  │  │
│  │  │  uds.socket (shared volume)    │  │                          │  │  │
│  │  └────────────────────────────────┘  └──────────────────────────┘  │  │
│  │                                                                    │  │
│  └────────────────────────────────────────────────────────────────────┘  │
│         │                    ▲                                            │
│         │                    │                                            │
│         │ 443/TCP            │ 5557/TCP                                   │
│         │ HTTPS              │ ZMQ PUB/SUB                                │
│         │ TLS 1.2+           │ plaintext                                  │
│         │ SA token           │ None                                       │
│         ▼                    │                                            │
│  ┌──────────────┐     ┌─────┴──────────────────────────────────────┐     │
│  │ Kubernetes    │     │  vLLM Pod Fleet                            │     │
│  │ API Server    │     │                                            │     │
│  │ :443/TCP      │     │  [vLLM Pod 1] ──► ZMQ PUB :5557           │     │
│  │ TLS 1.2+      │     │  [vLLM Pod 2] ──► ZMQ PUB :5557           │     │
│  │ SA token auth  │     │  [vLLM Pod N] ──► ZMQ PUB :5557           │     │
│  └──────────────┘     │                                            │     │
│                        │  Topic: kv@ (KVEvents)                     │     │
│                        │  Events: BlockStored, BlockRemoved,        │     │
│                        │          AllBlocksCleared                   │     │
│                        └────────────────────────────────────────────┘     │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐     │
│  │  llm-d-inference-scheduler (Go library consumer)                 │     │
│  │  - Imports kvcache.Indexer as Go library                         │     │
│  │  - Calls GetPodScores() in-process                               │     │
│  │  - No network communication (library mode)                       │     │
│  └──────────────────────────────────────────────────────────────────┘     │
│                                                                          │
│  [Prometheus] ──► HTTP scrape (controller-runtime metrics endpoint)       │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- Role: kv-cache-manager (namespace-scoped)
  - API Groups: "" (core)
  - Resources: pods
  - Verbs: get, list, watch

- ClusterRole: kv-cache-manager (cluster-scoped, optional)
  - API Groups: "" (core)
  - Resources: pods
  - Verbs: get, list, watch
  - Note: Required only when podNamespace is empty (all-namespace mode)

- RoleBinding: kv-cache-manager
  - Namespace: configurable
  - Role: kv-cache-manager
  - ServiceAccount: operator-controller-manager

Authentication & Authorization:
- IndexerService gRPC (50051/TCP): No authentication (internal cluster only)
- TokenizationService gRPC (UDS): No auth; file system permissions on socket
  - Runs as non-root user 65532
  - Socket at /tmp/tokenizer/tokenizer-uds.socket (shared volume)
- Health endpoint (8082/TCP): No auth (pod-local probes only)
- ZMQ connections (5557/TCP): No authentication (plaintext, internal cluster)

Secrets:
- vllm-p2p-secrets (Opaque) - HuggingFace API token for tokenizer model access
  - Provisioned by: User/Helm
  - Auto-Rotate: No
  - Contains: HF_TOKEN

Network Policies:
- No explicit NetworkPolicies defined in deployment manifests
- Relies on cluster-level network policies and namespace isolation

Encryption Summary:
- External (HuggingFace Hub): TLS 1.2+ (HTTPS)
- External (Redis/Valkey): Optional TLS (valkeys:// scheme)
- Kubernetes API: TLS 1.2+ (HTTPS)
- ZMQ PUB/SUB: plaintext (internal cluster)
- gRPC IndexerService: plaintext (internal cluster)
- gRPC UDS Tokenizer: plaintext (pod-local UDS)
- OTLP export: plaintext (configurable)
