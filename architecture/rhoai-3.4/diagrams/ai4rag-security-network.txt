┌─────────────────────────────────────────────────────────────────────────┐
│  IN-PROCESS (Library Boundary)                                          │
│                                                                          │
│  ai4rag is a Python library, not a deployed service.                     │
│  All internal communication is in-process (function calls).              │
│                                                                          │
│  [User Code / KFP Pipeline Component]                                    │
│         │                                                                │
│         │ Method: AI4RAGExperiment.search()                              │
│         │ Transport: In-process (Python function call)                   │
│         │ Encryption: N/A                                                │
│         │ Auth: N/A                                                      │
│         ▼                                                                │
│  [AI4RAGExperiment]                                                      │
│  - Orchestrates full HPO lifecycle                                       │
│  - Manages ThreadPoolExecutor (10 threads, concurrent I/O)              │
│         │                                                                │
│         ├── [ModelsPreSelector] ─── In-process ──► [ChromaVectorStore]   │
│         │       Uses in-memory ChromaDB for fast model screening         │
│         │       No network calls for Chroma (embedded DB)                │
│         │                                                                │
│         ├── [GAMOptimizer / RandomOptimizer] ─── In-process              │
│         │       Pure computation (pygam, scikit-learn)                    │
│         │       No network calls                                         │
│         │                                                                │
│         ├── [SimpleRAG Pipeline] ─── In-process                          │
│         │       │                                                        │
│         │       ├── [LangChainChunker] ─── In-process                   │
│         │       │       Pure computation (text splitting)                │
│         │       │                                                        │
│         │       ├── [OGXEmbeddingModel] ─── See EGRESS below            │
│         │       ├── [OGXVectorStore] ─── See EGRESS below               │
│         │       ├── [Retriever] ─── See EGRESS below                    │
│         │       └── [OGXFoundationModel] ─── See EGRESS below           │
│         │                                                                │
│         └── [UnitxtEvaluator] ─── In-process                            │
│                 Pure computation (metrics calculation)                    │
│                 No network calls                                         │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS (Outbound Connections)                                           │
│                                                                          │
│  All external communication goes to a single destination:                │
│  the OGX Server, via the ogx-client library.                             │
│                                                                          │
│  [OGXFoundationModel]                                                    │
│         │                                                                │
│         │ Endpoint: {BASE_URL}/chat/completions                          │
│         │ Port: Configurable (BASE_URL)                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS (via ogx-client / httpx)                       │
│         │ Auth: API Key (Bearer Token)                                   │
│         │ Purpose: Foundation model text generation                      │
│         ▼                                                                │
│  [OGXEmbeddingModel]                                                     │
│         │                                                                │
│         │ Endpoint: {BASE_URL}/embeddings                                │
│         │ Port: Configurable (BASE_URL)                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS (via ogx-client / httpx)                       │
│         │ Auth: API Key (Bearer Token)                                   │
│         │ Purpose: Text embedding generation                             │
│         ▼                                                                │
│  [OGXVectorStore]                                                        │
│         │                                                                │
│         │ Endpoint: {BASE_URL}/vector_stores/*                           │
│         │           {BASE_URL}/vector_io/*                               │
│         │ Port: Configurable (BASE_URL)                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS (via ogx-client / httpx)                       │
│         │ Auth: API Key (Bearer Token)                                   │
│         │ Purpose: Vector store CRUD, document insert, similarity query  │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [OGX Server]                                                            │
│  - Host: Configurable via BASE_URL environment variable                  │
│  - Port: Configurable (typically 443 for HTTPS)                          │
│  - Protocol: HTTPS (REST API)                                            │
│  - Encryption: TLS (handled by ogx-client / httpx / Python SSL)         │
│  - Auth: API Key (Bearer Token via API_KEY env var)                      │
│  - APIs consumed:                                                        │
│    • chat.completions.create — Foundation model inference                │
│    • embeddings.create — Text embedding generation                      │
│    • vector_stores.create/search/delete — Vector store management       │
│    • vector_io.insert/query — Document indexing and retrieval            │
│  - Concurrency: Up to 10 concurrent requests (ThreadPoolExecutor)       │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No Kubernetes RBAC resources — ai4rag is a Python library, not a K8s operator.
- No ClusterRoles, Roles, or RoleBindings.
- When consumed by a KFP pipeline component, RBAC is managed by the
  consuming component's ServiceAccount and Kubeflow Pipelines RBAC.

Secrets / Credentials:
- API_KEY (environment variable / .env file)
  - Type: API Key (Bearer Token)
  - Purpose: Authentication to OGX Server
  - Provisioned by: User (manual configuration)
  - Auto-Rotate: No
  - Risk: If leaked, grants access to OGX Server resources

- BASE_URL (environment variable / .env file)
  - Type: URL
  - Purpose: OGX Server endpoint
  - Provisioned by: User (manual configuration)
  - Auto-Rotate: No

Service Mesh Configuration:
- N/A — ai4rag is a library, not a deployed service.
- When running inside a KFP pod with service mesh sidecar,
  outbound HTTPS to OGX Server would traverse the mesh egress.

FIPS Compliance:
- Pure Python library — no direct cryptographic operations.
- TLS delegated to ogx-client (httpx internally).
- hashlib.sha256 used for document deduplication (FIPS-approved).
- No non-FIPS crypto risks detected.
