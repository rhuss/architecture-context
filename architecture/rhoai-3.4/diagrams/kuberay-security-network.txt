┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External User / Data Scientist]                                            │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: None (pre-auth, handled downstream)                          │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Gateway API - DMZ)                                                 │
│                                                                              │
│  [RHOAI Gateway (data-science-gateway)]                                      │
│  - Namespace: opendatahub (platform namespace)                               │
│  - Port: 443/TCP (external) → HTTPRoute to backend                          │
│  - TLS Termination: TLS 1.2+ (Gateway-terminated)                            │
│  - Cross-namespace: HTTPRoute in platform NS → Service in user NS            │
│  - Requires: ReferenceGrant (managed by AuthenticationController)            │
│         │                                                                    │
│         │ Port: 8443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (cert-manager certificates)                        │
│         │ Auth: Bearer Token forwarded from client                           │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  AUTH PROXY (Trust Boundary)                                                 │
│                                                                              │
│  [kube-rbac-proxy sidecar] (OIDC mode)                                       │
│  - Container in head pod                                                     │
│  - Port: 8443/TCP (frontend, TLS)                                            │
│  - Upstream: 127.0.0.1:8265 (Ray Dashboard, plaintext)                       │
│  - Validates: Bearer token via TokenReview API                               │
│  - Authorizes: SubjectAccessReview against RBAC rules                        │
│         │                                                                    │
│     ┌───┤                                                                    │
│     │   │ Port: 443/TCP (TokenReview)                                        │
│     │   │ Protocol: HTTPS                                                    │
│     │   │ Auth: ServiceAccount Token                                         │
│     │   ▼                                                                    │
│     │  [Kubernetes API Server]                                               │
│     │   │ Returns: Token identity + authorization decision                   │
│     │   ▼                                                                    │
│     │                                                                        │
│  OR                                                                          │
│                                                                              │
│  [oauth-proxy sidecar] (IntegratedOAuth mode)                                │
│  - Container in head pod                                                     │
│  - Delegates to OpenShift OAuth server                                       │
│  - Cookie-based session management                                           │
│         │                                                                    │
│         │ Port: 8265/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None (localhost / 127.0.0.1)                            │
│         │ Auth: None (pre-authorized by proxy)                               │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (Operator Namespace)                                       │
│                                                                              │
│  [KubeRay Operator - ray-operator]                                           │
│  - Namespace: opendatahub                                                    │
│  - ServiceAccount: kuberay-operator                                          │
│  - Image: quay.io/opendatahub/kuberay-operator:v1.4.4                        │
│  - Ports exposed:                                                            │
│    - 8080/TCP: Prometheus metrics (HTTP, no auth)                            │
│    - 8081/TCP: Health probes (/healthz, /readyz, HTTP, no auth)              │
│    - 9443/TCP: Webhook server (HTTPS, OpenShift serving certs)               │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount Token (Bearer)                                │
│         ▼                                                                    │
│  [Kubernetes API Server]                                                     │
│  - CRD CRUD (RayCluster, RayJob, RayService)                                │
│  - Pod/Service/Secret management                                             │
│  - Leader election via Leases                                                │
│                                                                              │
│  [kuberay-webhook-service]                                                   │
│  - Port: 443/TCP (external) → 9443/TCP (target)                             │
│  - Protocol: HTTPS                                                           │
│  - TLS: OpenShift serving cert signer (auto-provisioned)                     │
│  - Auth: Kubernetes API server (mutual TLS)                                  │
│  - Endpoints:                                                                │
│    - POST /mutate-ray-io-v1-raycluster                                       │
│    - POST /validate-ray-io-v1-raycluster                                     │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (User Namespace - Ray Workloads)                           │
│                                                                              │
│  [{cluster}-head-svc] ClusterIP                                              │
│  - 8265/TCP: Ray Dashboard (HTTP, no auth or proxied via sidecar)            │
│  - 10001/TCP: Ray Client (gRPC, none or mTLS)                                │
│  - 6379/TCP: Ray GCS (Redis protocol, none or mTLS)                          │
│  - 8080/TCP: Metrics (HTTP, no auth)                                         │
│                                                                              │
│  [{cluster}-serve-svc] ClusterIP                                             │
│  - 8000/TCP: Ray Serve endpoint (HTTP, no auth)                              │
│                                                                              │
│  [{cluster}-headless] Headless (ClusterIP=None)                              │
│  - Multi-host worker peer discovery                                          │
│                                                                              │
│  [Head Pod]                                                                  │
│  - ServiceAccount: {cluster} or custom SA                                    │
│  - Containers: ray-head + optional auth sidecar                              │
│  - mTLS: Optional (cert-manager certificates with pod IP SANs)              │
│         │                                                                    │
│         │ Port: 6379/TCP, 10001/TCP                                          │
│         │ Protocol: Ray GCS / Ray Client                                     │
│         │ Encryption: None or mTLS (cert-manager)                            │
│         │ Auth: None                                                         │
│         ▼                                                                    │
│  [Worker Pods]                                                               │
│  - Init container: GCS health check (polls 6379 until ready)                 │
│  - ServiceAccount: {cluster} or custom SA                                    │
│  - mTLS: Optional (cert-manager certificates with pod IP SANs)              │
│                                                                              │
│  [NetworkPolicies] (annotation-gated)                                        │
│  - Head pod policy: allows ingress from monitoring NS, worker pods           │
│  - Worker pod policy: allows ingress from head pod                           │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  [cert-manager]                                                              │
│  - Purpose: mTLS certificate lifecycle (Issuer, Certificate CRs)             │
│  - Port: 443/TCP (Kubernetes API for CR management)                          │
│  - Encryption: TLS 1.2+                                                      │
│  - Certificate duration: 90 days, renewal at 15 days before expiry           │
│                                                                              │
│  [Redis] (optional - GCS fault tolerance)                                    │
│  - Purpose: External storage for Ray Global Control Store                    │
│  - Port: 6379/TCP                                                            │
│  - Protocol: Redis protocol                                                  │
│  - Encryption: Optional TLS                                                  │
│  - Auth: Optional username/password                                          │
│                                                                              │
│  [Prometheus]                                                                │
│  - Purpose: Metrics scraping                                                 │
│  - Port: 8080/TCP (operator), 8080/TCP (head pod)                            │
│  - Protocol: HTTP                                                            │
│  - Auth: None (NetworkPolicy allows monitoring namespace)                    │
│                                                                              │
│  [OpenShift OAuth Server] (IntegratedOAuth mode)                             │
│  - Purpose: OAuth delegation for dashboard authentication                    │
│  - Auth: oauth-proxy sidecar delegates to OAuth server                       │
│                                                                              │
│  [OpenShift API (config.openshift.io)]                                       │
│  - Purpose: Read Authentication/OAuth cluster configuration                  │
│  - Port: 443/TCP, HTTPS, TLS 1.2+                                           │
│  - Auth: ServiceAccount Token (read-only)                                    │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: kuberay-operator
  - ray.io: rayclusters, rayjobs, rayservices (full CRUD + finalizers + status)
  - core: pods (create, delete, deletecollection, get, list, patch, update, watch)
  - core: services, serviceaccounts, configmaps, events, secrets (CRUD)
  - apps: deployments (get, list, patch, update, watch)
  - batch: jobs (full CRUD)
  - cert-manager.io: certificates, issuers (full CRUD)
  - networking.k8s.io: networkpolicies, ingresses (full CRUD)
  - gateway.networking.k8s.io: httproutes, referencegrants (full CRUD), gateways (read)
  - route.openshift.io: routes (full CRUD)
  - authentication.k8s.io: tokenreviews (create)
  - authorization.k8s.io: subjectaccessreviews (create)
  - config.openshift.io: authentications, oauths (read)
  - operator.openshift.io: kubeapiservers (read)
  - rbac.authorization.k8s.io: roles, rolebindings (create, delete, get, list, watch)
  - coordination.k8s.io: leases (create, get, list, update)

- ClusterRole: kuberay-operator-gateway-api
  - gateway.networking.k8s.io: gateways (read), httproutes, referencegrants (full CRUD)

- Per-RayCluster Role (namespaced):
  - core: pods (get, list, watch)
  - ray.io: rayclusters (get, patch)

Secrets:
- kuberay-webhook-server-cert (kubernetes.io/tls) - Webhook TLS, OpenShift serving cert signer, auto-rotate
- {cluster}-ca-secret-{uid} (kubernetes.io/tls) - CA root cert, cert-manager self-signed, 90d/15d renewal
- ray-head-secret-{cluster} (kubernetes.io/tls) - Head pod TLS with DNS/IP SANs, cert-manager, 90d/15d
- ray-worker-secret-{cluster} (kubernetes.io/tls) - Worker pod TLS with DNS/IP/wildcard SANs, cert-manager, 90d/15d
- {cluster}-oauth-config (Opaque) - OAuth proxy cookie secret, AuthenticationController, no auto-rotate
- {cluster}-oidc-config (Opaque) - OIDC proxy configuration, AuthenticationController, no auto-rotate

Service Mesh / Network Policy Configuration:
- NetworkPolicy: head pod - allows ingress from monitoring namespace + worker pods
- NetworkPolicy: worker pod - allows ingress from head pod
- Activation: annotation-gated (ray.io/enable-network-policy)
- Mutating webhook forces: enableIngress=false, ray.io/secure-trusted-network=true on OpenShift

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking
- Base image: UBI 9 minimal (OpenSSL included)
- TLS: Delegated to cert-manager and kube-rbac-proxy (no direct crypto/tls usage)
- No non-FIPS crypto detected (no MD5, RC4, DES, InsecureSkipVerify)
