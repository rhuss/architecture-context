Security Network Diagram: pipelines-components
Generated by: Claude Opus 4.6

Note: This component is a KFP component library. It does NOT expose any
services or ingress. All network traffic is EGRESS from ephemeral KFP task pods.

================================================================================

┌─────────────────────────────────────────────────────────────────────────┐
│  KUBEFLOW PIPELINES RUNTIME (Trusted)                                   │
│                                                                          │
│  [KFP Pipeline Runner]                                                   │
│  - Namespace: {pipeline-namespace}                                       │
│  - ServiceAccount: KFP runner SA                                         │
│  - Orchestrates pipeline tasks                                           │
│         │                                                                │
│         │ Kubernetes Pod scheduling                                      │
│         │ Auth: KFP ServiceAccount                                       │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EPHEMERAL KFP TASK PODS (Pipeline Namespace)                           │
│                                                                          │
│  ┌─────────────────────────────┐  ┌─────────────────────────────┐       │
│  │  dataset_download Pod       │  │  automl_data_loader Pod     │       │
│  │  - Downloads datasets       │  │  - Loads CSV from S3        │       │
│  │  - Sources: HF/S3/HTTP/PVC  │  │  - Max 1GB sampling         │       │
│  └────────────┬────────────────┘  └────────────┬────────────────┘       │
│               │                                 │                        │
│  ┌─────────────────────────────┐  ┌─────────────────────────────┐       │
│  │  train_model Pod            │  │  models_selection Pod       │       │
│  │  - Submits training jobs    │  │  - AutoGluon ensemble       │       │
│  │  - Via Kubeflow Trainer SDK │  │  - Stacking/bagging         │       │
│  └────────────┬────────────────┘  └─────────────────────────────┘       │
│               │                                                          │
│  ┌─────────────────────────────┐  ┌─────────────────────────────┐       │
│  │  universal_llm_evaluator    │  │  kubeflow_model_registry    │       │
│  │  - lm-eval + vLLM backend   │  │  - Registers trained models │       │
│  │  - GPU required             │  │  - Provenance tracking      │       │
│  └─────────────────────────────┘  └────────────┬────────────────┘       │
│                                                  │                       │
│  All pods mount Shared PVC for inter-component data sharing              │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS CONNECTIONS                                                      │
│                                                                          │
│  dataset_download Pod ──────────────────────────────────────────────►    │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Bearer Token (HF_TOKEN)                                  │
│         │ Destination: huggingface.co                                    │
│         │ Purpose: Download datasets and models                          │
│                                                                          │
│  dataset_download Pod / automl_data_loader Pod ────────────────────►    │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: AWS IAM (Access Key / Secret Key)                        │
│         │ Destination: S3 / MinIO                                        │
│         │ Purpose: Load training data from S3-compatible storage         │
│                                                                          │
│  dataset_download Pod ──────────────────────────────────────────────►    │
│         │ Port: 443/TCP or 80/TCP                                        │
│         │ Protocol: HTTPS or HTTP                                        │
│         │ Encryption: TLS 1.2+ or plaintext                              │
│         │ Auth: None                                                     │
│         │ Destination: Arbitrary HTTP/HTTPS URLs                         │
│         │ Purpose: Download datasets from arbitrary sources              │
│                                                                          │
│  train_model Pod ───────────────────────────────────────────────────►    │
│         │ Port: 6443/TCP                                                 │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Bearer Token (KUBERNETES_AUTH_TOKEN)                      │
│         │ Destination: Kubernetes API Server                             │
│         │ Purpose: Submit training jobs via Kubeflow Trainer             │
│                                                                          │
│  Training Worker Pods ──────────────────────────────────────────────►    │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Docker config.json (OCI_PULL_SECRET_MODEL_DOWNLOAD)      │
│         │ Destination: OCI Container Registries                          │
│         │ Purpose: Pull OCI model images via skopeo                      │
│                                                                          │
│  kubeflow_model_registry Pod ───────────────────────────────────────►    │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: *** PLAINTEXT - NO ENCRYPTION ***                  │
│         │ Auth: *** NONE ***                                             │
│         │ Destination: Kubeflow Model Registry                           │
│         │ Purpose: Register trained models                               │
│         │ ⚠ SECURITY NOTE: Insecure HTTP, no authentication             │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                       │
│                                                                          │
│  [HuggingFace Hub - huggingface.co]                                      │
│  - Purpose: Dataset and model downloads                                  │
│  - Auth: HF_TOKEN (optional, required for gated/private resources)       │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [AWS S3 / MinIO]                                                        │
│  - Purpose: Training data storage (S3-compatible)                        │
│  - Auth: AWS IAM (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)              │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [OCI Container Registries]                                              │
│  - Purpose: OCI model image pulls                                        │
│  - Auth: Docker config.json                                              │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Purpose: Training job submission via Kubeflow Trainer                 │
│  - Auth: Bearer Token (KUBERNETES_AUTH_TOKEN)                            │
│  - Encryption: TLS 1.2+                                                  │
│                                                                          │
│  [Kubeflow Model Registry]                                               │
│  - Purpose: Model registration with provenance                          │
│  - Auth: None                                                            │
│  - Encryption: None (HTTP plaintext)                                     │
│  - ⚠ Security Risk: No auth or encryption                               │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles or Roles are defined by this component
- Components run under the KFP pipeline runner ServiceAccount
- Required SA permissions:
  - Create and manage Pods (for KFP tasks)
  - Read Secrets (for credential injection)
  - Create PVCs (for workspace storage)
  - Submit TrainingJobs (for Kubeflow Trainer integration)

Secrets:
- hf-token (Opaque) - HuggingFace authentication token, User/Admin provisioned, No auto-rotate
- s3-secret (Opaque) - AWS S3 credentials (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY), User/Admin provisioned, No auto-rotate
- kubernetes-credentials (Opaque) - K8s API credentials (KUBERNETES_SERVER_URL, KUBERNETES_AUTH_TOKEN), User/Admin provisioned, No auto-rotate
- oci-pull-secret-model-download (Opaque) - Docker config.json for OCI model pulls, User/Admin provisioned, No auto-rotate
- git_auth_secret (Opaque) - Git auth for Konflux build pipeline, Konflux/PaC provisioned, No auto-rotate

Security Observations:
- ⚠ Model Registry connection uses HTTP plaintext with no authentication
- ⚠ No secrets have auto-rotation configured
- ⚠ HTTP data sources allow plaintext downloads (dataset_download component)
- All other external connections use TLS 1.2+ encryption
- GPU resources (nvidia.com/gpu) required for training and evaluation workloads
