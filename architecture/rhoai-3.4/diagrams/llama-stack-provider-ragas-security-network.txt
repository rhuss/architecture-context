┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [Client / Data Scientist]                                                   │
│         │                                                                    │
│         │ Port: 8321/TCP                                                     │
│         │ Protocol: HTTP                                                     │
│         │ Encryption: None (TLS via ingress if configured)                   │
│         │ Auth: None (delegated to ingress)                                  │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  LLAMA STACK DISTRIBUTION (Component Boundary)                               │
│                                                                              │
│  [Llama Stack Distribution Server]                                           │
│  - Service Type: ClusterIP                                                   │
│  - Port: 8321/TCP (HTTP)                                                     │
│  - Encryption: None (TLS via ingress)                                        │
│  - Auth: None (delegated to ingress)                                         │
│  - Managed by: Llama Stack Operator (LlamaStackDistribution CR)              │
│         │                                                                    │
│         │ In-process delegation                                              │
│         ▼                                                                    │
│  ┌────────────────────────────────────────────────────────────────────┐       │
│  │  [llama-stack-provider-ragas]                                     │       │
│  │  - Image: rhoai/odh-trustyai-ragas-lls-provider-dsp-rhel9         │       │
│  │  - Language: Python 3.12 (UBI9 base)                              │       │
│  │                                                                    │       │
│  │  ┌─────────────────────┐  ┌──────────────────────────┐            │       │
│  │  │  Inline Evaluator   │  │  Remote Evaluator        │            │       │
│  │  │  - In-process LLM   │  │  - KFP pipeline submit   │            │       │
│  │  │  - In-process embed │  │  - S3 result retrieval   │            │       │
│  │  │  - max_workers=1    │  │  - Async job tracking    │            │       │
│  │  └─────────────────────┘  └──────────┬───────────────┘            │       │
│  └──────────────────────────────────────┼────────────────────────────┘       │
│                                          │                                   │
├──────────────────────────────────────────┼───────────────────────────────────┤
│  EGRESS TO EXTERNAL SERVICES             │                                   │
│                                          │                                   │
│  ┌───────────────────────────────────────┼──────────────────────────────┐    │
│  │                                       │                              │    │
│  │  ┌───────────────────────┐            │                              │    │
│  │  │ Kubeflow Pipelines API│◄───────────┘                              │    │
│  │  │ (Data Science Pipes)  │                                           │    │
│  │  │ Port: 443/TCP         │                                           │    │
│  │  │ Protocol: HTTPS       │                                           │    │
│  │  │ Encryption: TLS 1.2+  │                                           │    │
│  │  │ Auth: Bearer Token    │                                           │    │
│  │  │ (OpenShift user token)│                                           │    │
│  │  └───────────┬───────────┘                                           │    │
│  │              │                                                        │    │
│  │              │ Creates pipeline pods                                   │    │
│  │              ▼                                                        │    │
│  │  ┌───────────────────────┐     ┌─────────────────────────────┐       │    │
│  │  │ KFP Pipeline Pod      │────▶│ S3-compatible Storage       │       │    │
│  │  │ (retrieve_data +      │     │ Port: 443/TCP               │       │    │
│  │  │  run_ragas_evaluation) │     │ Protocol: HTTPS             │       │    │
│  │  │ Image: ragas-provider  │     │ Encryption: TLS 1.2+        │       │    │
│  │  │ Port: N/A (batch job) │     │ Auth: AWS IAM credentials   │       │    │
│  │  └───────────┬───────────┘     │ (AccessKeyId/SecretAccessKey)│       │    │
│  │              │                  └──────────────▲──────────────┘       │    │
│  │              │ HTTP 8321                       │                      │    │
│  │              ▼                                 │                      │    │
│  │  ┌───────────────────────┐                     │                      │    │
│  │  │ Llama Stack Server    │                     │                      │    │
│  │  │ (from KFP pods)       │                     │                      │    │
│  │  │ Port: 8321/TCP HTTP   │                     │                      │    │
│  │  │ Auth: None            │                     │                      │    │
│  │  └───────────────────────┘                     │                      │    │
│  │                                                │                      │    │
│  │  Remote Evaluator ─────────────────────────────┘                      │    │
│  │  (reads results from S3, HTTPS 443, AWS IAM)                          │    │
│  │                                                                        │    │
│  └────────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐      │
│  │ Ollama / vLLM Inference Server                                     │      │
│  │ Port: 8443/TCP or 11434/TCP                                        │      │
│  │ Protocol: HTTP/HTTPS                                               │      │
│  │ Encryption: Configurable (VLLM_TLS_VERIFY flag)                    │      │
│  │ Auth: Bearer Token (optional)                                      │      │
│  │ Purpose: LLM completions and embedding generation                  │      │
│  └────────────────────────────────────────────────────────────────────┘      │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐      │
│  │ Kubernetes API Server                                              │      │
│  │ Port: 443/TCP                                                      │      │
│  │ Protocol: HTTPS                                                    │      │
│  │ Encryption: TLS 1.2+                                               │      │
│  │ Auth: ServiceAccount token / kubeconfig                            │      │
│  │ Purpose: Read ConfigMaps (trustyai-service-operator-config),       │      │
│  │          load kubeconfig for KFP token retrieval                    │      │
│  └────────────────────────────────────────────────────────────────────┘      │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

Secrets Summary:
- kubeflow-pipelines-token (Opaque) - Bearer token for KFP API, manual, NO auto-rotation
  ⚠️  Uses OpenShift user token (oc whoami -t) with limited TTL
- aws-credentials (Opaque) - AWS AccessKeyId/SecretAccessKey for S3, manual, NO auto-rotation
- llama-stack-inference-model-secret (Opaque) - Model name, vLLM URL, TLS verify, API token, manual
- KUBEFLOW_S3_CREDENTIALS_SECRET_NAME (Opaque) - AWS creds mounted in KFP pods, manual

RBAC Summary:
- No ClusterRoles or Roles defined by this component
- RBAC is managed by the Llama Stack Operator and Data Science Pipelines operator

Authentication & Authorization:
- Kubeflow Pipelines API: Bearer Token (OpenShift user token or KUBEFLOW_PIPELINES_TOKEN)
  - User must have pipeline run creation permissions in target namespace
- S3 storage: AWS IAM credentials from Kubernetes Secret
  - Write access to configured S3 prefix required
- Llama Stack API (remote mode): None (network-level access from KFP pods)

Security Considerations:
- ⚠️  VLLM_TLS_VERIFY=false option disables TLS verification (demo only, non-production)
- ⚠️  KFP token uses OpenShift user token with limited TTL (remote eval fails after expiry)
- ⚠️  All secrets are user-provisioned with no auto-rotation
- FIPS: Inherited from UBI9 base image OpenSSL; cryptography==46.0.2 uses OpenSSL backend

FIPS Compliance:
- Python component on UBI9 — FIPS compliance from base image's OpenSSL
- cryptography library (v46.0.2) uses OpenSSL backend (FIPS-validated on UBI9)
- No application-level crypto anti-patterns detected (no MD5, RC4, DES, custom RNG)
