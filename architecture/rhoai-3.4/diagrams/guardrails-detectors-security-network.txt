Guardrails Detectors - Security Network Diagram
Generated by: Claude Opus 4.6
==========================================================================

┌──────────────────────────────────────────────────────────────────────────┐
│  INTERNAL SERVICES (Cluster-Only)                                        │
│                                                                          │
│  [FMS Guardrails Orchestrator]                                           │
│  - Role: Primary consumer of all detector APIs                           │
│         │                                                                │
│         │ Port: 8000/TCP (HF, LLM Judge) or 8080/TCP (Built-in)         │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plaintext)                                   │
│         │ Auth: None (HF, Built-in) / Header: detector_id (LLM Judge)   │
│         ▼                                                                │
├──────────────────────────────────────────────────────────────────────────┤
│  DETECTOR SERVICES (Internal Microservices - ClusterIP)                  │
│                                                                          │
│  ┌────────────────────────────────────────────────────────┐              │
│  │  [HuggingFace Detector]                                 │              │
│  │  - Service: huggingface-detector (ClusterIP)            │              │
│  │  - Port: 8000/TCP                                       │              │
│  │  - Protocol: HTTP (plaintext)                           │              │
│  │  - Auth: None                                           │              │
│  │  - Image: odh-guardrails-detector-huggingface-runtime   │              │
│  │  - Arch: amd64, ppc64le, s390x                          │              │
│  │  - Endpoints:                                           │              │
│  │    POST /api/v1/text/contents                           │              │
│  │    GET  /health                                         │              │
│  │    GET  /metrics                                        │              │
│  │         │                                               │              │
│  │         │ Filesystem (local volume)                     │              │
│  │         │ Path: MODEL_DIR                               │              │
│  │         ▼                                               │              │
│  │  [Local Model Files - Mounted Volume]                   │              │
│  └────────────────────────────────────────────────────────┘              │
│                                                                          │
│  ┌────────────────────────────────────────────────────────┐              │
│  │  [LLM Judge Detector]                                   │              │
│  │  - Service: llm-judge-detector (ClusterIP)              │              │
│  │  - Port: 8000/TCP                                       │              │
│  │  - Protocol: HTTP (plaintext)                           │              │
│  │  - Auth: Header: detector_id (required)                 │              │
│  │  - Endpoints:                                           │              │
│  │    POST /api/v1/text/contents                           │              │
│  │    POST /api/v1/text/generation                         │              │
│  │    GET  /api/v1/metrics                                 │              │
│  │    GET  /health                                         │              │
│  │    GET  /metrics                                        │              │
│  │         │                                               │              │
│  │         │ Port: Configurable (VLLM_BASE_URL)           │              │
│  │         │ Protocol: HTTP/HTTPS                         │              │
│  │         │ Encryption: Depends on endpoint              │              │
│  │         │ Auth: API key (optional)                     │              │
│  │         ▼                                               │              │
│  │  [vLLM / OpenAI-compatible LLM Server] ──── EGRESS     │              │
│  └────────────────────────────────────────────────────────┘              │
│                                                                          │
│  ┌────────────────────────────────────────────────────────┐              │
│  │  [Built-in Detector]                                    │              │
│  │  - Service: built-in-detector (ClusterIP)               │              │
│  │  - Port: 8080/TCP                                       │              │
│  │  - Protocol: HTTP (plaintext)                           │              │
│  │  - Auth: None                                           │              │
│  │  - Image: odh-built-in-detector-rhel9                   │              │
│  │  - Arch: amd64, arm64, ppc64le, s390x                   │              │
│  │  - Endpoints:                                           │              │
│  │    POST /api/v1/text/contents                           │              │
│  │    GET  /registry                                       │              │
│  │    GET  /health                                         │              │
│  │    GET  /metrics                                        │              │
│  │  - Security Controls:                                   │              │
│  │    * Static AST analysis on custom detector code        │              │
│  │    * Forbidden: os, subprocess, sys, shutil imports     │              │
│  │    * Forbidden: eval, exec, open, compile, input calls  │              │
│  │    * Allowed: os.environ, os.getenv                     │              │
│  └────────────────────────────────────────────────────────┘              │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│  MONITORING                                                              │
│                                                                          │
│  [Prometheus Scraper]                                                    │
│         │                                                                │
│         │ Port: 8000/TCP (HF, LLM Judge) or 8080/TCP (Built-in)         │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (plaintext)                                   │
│         │ Auth: None                                                     │
│         │ Metrics: trustyai_guardrails_{detections,requests,errors,      │
│         │          runtime} counters                                     │
│         ▼                                                                │
│  [All Detector /metrics endpoints]                                       │
│                                                                          │
├──────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL EGRESS                                                         │
│                                                                          │
│  [vLLM / OpenAI-compatible LLM Server]                                   │
│  - Purpose: LLM-as-a-judge evaluation                                    │
│  - URL: Configurable via VLLM_BASE_URL                                   │
│  - Protocol: HTTP/HTTPS                                                  │
│  - Auth: API key (optional)                                              │
│  - Consumer: LLM Judge Detector only                                     │
└──────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No RBAC resources defined by this component
- RBAC managed by deploying platform (KServe, Guardrails Orchestrator)

Authentication Summary:
- Endpoint-level auth: None (except LLM Judge detector_id header)
- Platform-level auth: Expected to be enforced by orchestrator/platform
- Optional TLS: ssl_ca_certs, ssl_certfile, ssl_keyfile via config.yaml

Security Controls:
- Custom Detector Static Code Analysis:
  - AST analysis blocks forbidden imports: os, subprocess, sys, shutil
  - AST analysis blocks forbidden calls: eval, exec, open, compile, input
  - Exception: os.environ and os.getenv are explicitly allowed
  - Only non-underscore-prefixed functions are exposed

Secrets:
- No secrets defined directly by the component
- External secrets (model files, API keys) managed by deploying platform

Prometheus Metrics:
- trustyai_guardrails_detections (Counter) - positive detections per function
- trustyai_guardrails_requests (Counter) - requests per function
- trustyai_guardrails_errors (Counter) - errors per function
- trustyai_guardrails_runtime (Counter) - runtime per function
- Labels: detector_kind, detector_name
