┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [External Client / User Application]                                       │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS (KServe-managed)                                  │
│         │ TLS Mode: SIMPLE                                                  │
│         │ Auth: Per-ISVC (kube-rbac-proxy / OAuth proxy if enabled)         │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (KServe Gateway)                                                   │
│                                                                             │
│  [KServe InferenceService HTTPRoute]                                        │
│  - Port: 443/TCP (external) → internal service ports                       │
│  - TLS Termination: SIMPLE                                                  │
│  - Per-ISVC hostname routing                                                │
│         │                                                                   │
│         │ Port: 8080/TCP (built-in), 8000/TCP (hf, llm-judge)              │
│         │ Protocol: HTTP                                                    │
│         │ Encryption: mTLS (Istio STRICT)                                   │
│         │ Auth: Istio service identity                                       │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  SERVICE MESH (Trusted - Istio mTLS STRICT)                                 │
│                                                                             │
│  [FMS Guardrails Orchestrator]                                              │
│  - Invokes detectors on LLM input/output                                    │
│  - IBM Detector API specification                                           │
│         │                                                                   │
│         ├─── Port: 8080/TCP ──────────────────────────────────────┐         │
│         │    Protocol: HTTP                                       │         │
│         │    Encryption: mTLS (Istio sidecar)                     │         │
│         │    Auth: Istio mTLS                                     │         │
│         ▼                                                         │         │
│  [Built-in Detector]                                              │         │
│  - Namespace: (deploy namespace)                                  │         │
│  - Service: ClusterIP                                             │         │
│  - Port: 8080/TCP                                                 │         │
│  - Image: odh-built-in-detector-rhel9                             │         │
│  - Workers: 4 (uvicorn)                                           │         │
│  - No external dependencies                                      │         │
│                                                                   │         │
│         ├─── Port: 8000/TCP ──────────────────────────────────────┤         │
│         │    Protocol: HTTP                                       │         │
│         │    Encryption: mTLS (Istio sidecar)                     │         │
│         │    Auth: Istio mTLS                                     │         │
│         ▼                                                         │         │
│  [HuggingFace Detector]                                           │         │
│  - Namespace: (deploy namespace)                                  │         │
│  - Service: ClusterIP (via KServe ISVC)                           │         │
│  - Port: 8000/TCP                                                 │         │
│  - Image: odh-guardrails-detector-huggingface-runtime-rhel9       │         │
│  - Workers: 1 (GPU-bound)                                         │         │
│  - Istio sidecar: injected (sidecar.istio.io/inject: 'true')     │         │
│  - Auth annotation: security.opendatahub.io/enable-auth: 'true'  │         │
│         │                                                         │         │
│         │ Port: 9000/TCP                                          │         │
│         │ Protocol: HTTP (S3 API)                                 │         │
│         │ Encryption: None (cluster-internal)                     │         │
│         │ Auth: AWS access key / secret key                       │         │
│         ▼                                                         │         │
│  [S3/MinIO - minio-guardrails-guardian]                           │         │
│  - Service: ClusterIP                                             │         │
│  - Port: 9000/TCP                                                 │         │
│  - Purpose: Model artifact storage for HF detector                │         │
│                                                                   │         │
│         ├─── Port: 8000/TCP ──────────────────────────────────────┘         │
│         │    Protocol: HTTP                                                 │
│         │    Encryption: mTLS (Istio sidecar)                               │
│         │    Auth: Istio mTLS                                               │
│         ▼                                                                   │
│  [LLM Judge Detector]                                                       │
│  - Namespace: (deploy namespace)                                            │
│  - Service: ClusterIP (via KServe ISVC)                                     │
│  - Port: 8000/TCP                                                           │
│  - Image: guardrails-detector-llm-judge                                     │
│  - Istio sidecar: injected (sidecar.istio.io/inject: 'true')               │
│  - Auth annotation: security.opendatahub.io/enable-auth: 'true'            │
│  - Note: No Konflux build (pre-GA / community-only)                        │
│         │                                                                   │
│         │ Port: 8080/TCP                                                    │
│         │ Protocol: HTTP (OpenAI-compatible)                                │
│         │ Encryption: None (cluster-internal)                               │
│         │ Auth: None                                                        │
│         ▼                                                                   │
│  [vLLM Inference Server]                                                    │
│  - Purpose: External LLM for judge evaluation                              │
│  - Port: 8080/TCP                                                           │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  MONITORING                                                                 │
│                                                                             │
│  [Prometheus]                                                               │
│  - Scrapes /metrics on 8080/TCP (built-in) and 8000/TCP (hf)               │
│  - Annotations: prometheus.io/port, prometheus.io/path                      │
│  - Metrics: request_count, detection_count, error_count, runtime_latency   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No ClusterRoles defined by this component
- Relies on KServe and platform RBAC
- Example RoleBinding: user-one-view → ClusterRole/view → SA: user-one

Authentication & Authorization:
- /api/v1/text/contents (POST): Istio mTLS + security.opendatahub.io/enable-auth annotation
  - Enforcement: Istio sidecar (injected via sidecar.istio.io/inject: 'true')
  - When auth annotation set: platform injects kube-rbac-proxy or OAuth proxy
- /health (GET): Unauthenticated health probe
- /metrics (GET): Unauthenticated Prometheus scrape

Secrets:
- aws-connection-minio-data-connection-guardrails-guardian (Opaque)
  - Contents: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, endpoint, bucket, region
  - Purpose: S3 connection credentials for model storage
  - Provisioned By: Manual
  - Auto-Rotate: No

FIPS Compliance:
- Python applications on UBI 9 inherit FIPS-compliant OpenSSL from base image
- ssl stdlib module delegates to system OpenSSL (FIPS-validated on RHEL 9)
- HuggingFace Dockerfile pins openssl-libs-3.5.1-5.el9_7
- No non-FIPS crypto detected (no MD5, RC4, or other non-FIPS algorithms)

Build Hermeticity:
- hermetic: false (set explicitly in Tekton PipelineRuns)
- No lock files (no pip compile / uv.lock)
- Python deps pinned via exact versions in requirements.txt
- Supply chain hermeticity gap: transitive dependencies not locked

Custom Detector Security:
- Static code analysis sandbox for user-defined Python detectors
- Forbidden imports: os, subprocess, sys, shutil
- Forbidden functions: eval, exec, open, compile, input
- WARNING: AST-based analysis — bypassable via dynamic imports (NOT a security boundary)
