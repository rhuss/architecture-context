┌─────────────────────────────────────────────────────────────────────────────────┐
│  BUILD-TIME ENVIRONMENT (CI Pipeline / Developer Workstation)                   │
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐                │
│  │  model-extractor (Go CLI)                                    │                │
│  │  - User: developer / CI service account                      │                │
│  │  - No Kubernetes RBAC required                               │                │
│  │  - Network: outbound HTTPS only                              │                │
│  │  - Concurrency: up to 5 concurrent jobs (--max-concurrent)   │                │
│  └──────────┬──────────────────────────┬────────────────────────┘                │
│             │                          │                                         │
│             │ Port: 443/TCP            │ Port: 443/TCP                           │
│             │ Protocol: HTTPS          │ Protocol: HTTPS                         │
│             │ Encryption: TLS 1.2+     │ Encryption: TLS 1.2+                   │
│             │ Auth: Bearer Token       │ Auth: None (public)                     │
│             │   (HF_TOKEN, optional)   │                                         │
│             │ Rate limiting applies    │                                         │
│             │   without token          │                                         │
│             ▼                          ▼                                         │
├─────────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Build-Time Egress Only)                                     │
│                                                                                 │
│  ┌──────────────────────────────────┐  ┌──────────────────────────────────────┐ │
│  │  HuggingFace                     │  │  Red Hat Container Registry          │ │
│  │  - Host: huggingface.co          │  │  - Host: registry.redhat.io          │ │
│  │  - Port: 443/TCP                 │  │  - Port: 443/TCP                     │ │
│  │  - Protocol: HTTPS               │  │  - Protocol: HTTPS (OCI v2)          │ │
│  │  - Encryption: TLS 1.2+          │  │  - Encryption: TLS 1.2+              │ │
│  │  - Auth: Bearer Token (optional) │  │  - Auth: None (public registry)      │ │
│  │                                   │  │                                       │ │
│  │  Endpoints accessed:              │  │  Endpoints accessed:                  │ │
│  │  - /api/collections/{id}         │  │  - /v2/{repo}/manifests/{tag}         │ │
│  │  - /api/models/{id}              │  │  - /v2/{repo}/blobs/{digest}          │ │
│  │  - /{org}/{model}/raw/main/...   │  │                                       │ │
│  │                                   │  │  Data accessed:                       │ │
│  │  Data accessed:                   │  │  - OCI manifests                      │ │
│  │  - Model collections             │  │  - Image config blobs                 │ │
│  │  - Model metadata                │  │  - Container layers (modelcard.md)    │ │
│  │  - README.md (YAML frontmatter)  │  │  - Architecture metadata              │ │
│  │  - Repository tags               │  │  - Timestamp metadata                 │ │
│  └──────────────────────────────────┘  └──────────────────────────────────────┘ │
│                                                                                 │
│  ⚠ Note: ALL external egress occurs at BUILD TIME only.                        │
│    The deployed data container has ZERO network access.                          │
│                                                                                 │
├─────────────────────────────────────────────────────────────────────────────────┤
│  BUILD OUTPUT (Artifact)                                                        │
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐                │
│  │  Pre-generated YAML Catalog Files (data/)                    │                │
│  │  - models-catalog.yaml                                       │                │
│  │  - validated-models-catalog.yaml                             │                │
│  │  - other-models-catalog.yaml                                 │                │
│  │  - redhat-mcp-servers-catalog.yaml                           │                │
│  │  - partner-mcp-servers-catalog.yaml                          │                │
│  │  - community-mcp-servers-catalog.yaml                        │                │
│  └──────────┬──────────────────────────────────────────────────┘                │
│             │                                                                   │
│             │ Konflux CI/CD                                                     │
│             │ Dockerfile.konflux: COPY data/*.yaml /app/data/                   │
│             ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────────┤
│  RUNTIME (Deployed to OpenShift / Kubernetes)                                   │
│                                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐                │
│  │  odh-model-metadata-collection Container                     │                │
│  │  - Image: managed-open-data-hub/                             │                │
│  │           odh-model-metadata-collection-rhel9                │                │
│  │  - Base: registry.access.redhat.com/ubi9-micro:latest        │                │
│  │  - CMD: ["sleep", "infinity"]                                │                │
│  │  - User: UID 1001 (non-root)                                │                │
│  │  - Network: NONE (no ports, no ingress, no egress)           │                │
│  │  - RBAC: NONE (no ClusterRoles, no RoleBindings)             │                │
│  │  - Executable code: NONE (data-only image)                   │                │
│  │                                                               │                │
│  │  Volume: /app/data/                                           │                │
│  │  - models-catalog.yaml                                       │                │
│  │  - validated-models-catalog.yaml                             │                │
│  │  - other-models-catalog.yaml                                 │                │
│  │  - redhat-mcp-servers-catalog.yaml                           │                │
│  │  - partner-mcp-servers-catalog.yaml                          │                │
│  │  - community-mcp-servers-catalog.yaml                        │                │
│  │  - benchmarks/                                               │                │
│  └──────────┬──────────────────────────────────────────────────┘                │
│             │                                                                   │
│             │ Access: Volume mount / file copy                                  │
│             │ Protocol: File I/O (no network)                                   │
│             │ Auth: N/A (filesystem access via pod spec)                        │
│             ▼                                                                   │
│  ┌─────────────────────────────────────────────────────────────┐                │
│  │  RHOAI Dashboard                                             │                │
│  │  - Reads YAML catalog files                                  │                │
│  │  - Displays model and MCP server info to users               │                │
│  └─────────────────────────────────────────────────────────────┘                │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘

Secrets:
- HF_TOKEN (Opaque, env var) - HuggingFace API authentication
  - Purpose: Access private collections and gated models
  - Provisioned By: Manual (developer/CI)
  - Auto-Rotate: No
  - Usage: Authorization: Bearer header (optional, unauthenticated allowed but rate-limited)

RBAC Summary:
- No ClusterRoles defined
- No RoleBindings defined
- Data container runs as non-root UID 1001
- No Kubernetes API access required

FIPS Compliance:
- Runtime: N/A (data-only container, no executable code, no crypto)
- Build-time: Go stdlib net/http with default TLS config (FIPS-compliant on FIPS-enabled toolchain)
- No custom crypto, no MD5/RC4/DES usage
- Base image: ubi9-micro (minimal, no OpenSSL in image)

Service Mesh Configuration:
- Not applicable (no network services exposed, no ingress, no egress at runtime)

Build Hermeticity:
- go.sum: Present (Go dependency lock)
- rpms.lock.yaml: Absent (N/A — no packages installed in Dockerfile)
- artifacts.lock.yaml: Absent
- Hermeto prefetch: Absent
- Note: Dockerfile.konflux only copies static YAML files, no go build or dnf install
