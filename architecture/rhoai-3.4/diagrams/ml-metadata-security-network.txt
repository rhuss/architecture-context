ML Metadata (MLMD) - Security Network Diagram
Generated by: Claude Opus 4.6
Source: architecture/rhoai-3.4/ml-metadata.md

┌─────────────────────────────────────────────────────────────────────────┐
│  INTERNAL CLIENTS (Cluster-Internal)                                    │
│                                                                          │
│  [Data Science Pipelines (ds-pipeline-*)]                                │
│  - Namespace: {dsp-namespace}                                            │
│  - Purpose: Store/retrieve pipeline run metadata, artifact lineage       │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: gRPC/HTTP2                                           │
│         │ Encryption: Optional TLS (SSLConfig)                           │
│         │ Auth: Optional mTLS (client_verify)                            │
│         ▼                                                                │
│  [Kubeflow Pipelines SDK (Python Client)]                                │
│  - Purpose: Programmatic metadata access                                 │
│         │                                                                │
│         │ Port: 8080/TCP                                                 │
│         │ Protocol: gRPC/HTTP2                                           │
│         │ Encryption: Optional TLS (SSLConfig)                           │
│         │ Auth: Optional mTLS (client_verify)                            │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  MLMD SERVICE (Cluster-Internal)                                        │
│                                                                          │
│  [MLMD gRPC Server - metadata_store_server]                              │
│  - Image: quay.io/rhoai/odh-mlmd-grpc-server-rhel9:rhoai-3.4           │
│  - Base: registry.access.redhat.com/ubi9/ubi-minimal                    │
│  - User: 65534:65534 (nobody, non-root)                                 │
│  - Port: 8080/TCP (configurable via GRPC_PORT)                          │
│  - Protocol: gRPC/HTTP2 (MetadataStoreService)                          │
│  - TLS: Optional (SSLConfig: server_key, server_cert, custom_ca)        │
│  - Auth: Optional mTLS (SSLConfig.client_verify=true)                   │
│  - API: 40+ RPC methods (CRUD for artifacts, executions, contexts,      │
│         events, lineage)                                                 │
│         │                                                                │
│         │ Database Connection (one of):                                   │
│         │                                                                │
│         ├──── MySQL/MariaDB ────────────────────────────────────┐        │
│         │     Port: 3306/TCP                                    │        │
│         │     Protocol: MySQL wire protocol                     │        │
│         │     Encryption: Optional TLS                          │        │
│         │       (MySQLDatabaseConfig.SSLOptions)                │        │
│         │       verify_server_cert option                       │        │
│         │     Auth: Username/password                           │        │
│         │                                                       │        │
│         └──── PostgreSQL ───────────────────────────────────┐   │        │
│               Port: 5432/TCP                                │   │        │
│               Protocol: PostgreSQL wire protocol            │   │        │
│               Encryption: Optional TLS                      │   │        │
│                 (PostgreSQLDatabaseConfig.SSLOptions)        │   │        │
│                 sslmode: disable/allow/verify-ca/verify-full │   │        │
│                 sslcert, sslkey, sslrootcert                │   │        │
│               Auth: Username/password or passfile           │   │        │
│                                                             │   │        │
│         ▼                                                   ▼   ▼        │
├─────────────────────────────────────────────────────────────────────────┤
│  DATABASE TIER (Cluster-Internal or External)                           │
│                                                                          │
│  [MySQL / MariaDB]                                                       │
│  - Port: 3306/TCP                                                        │
│  - Purpose: Persistent metadata storage backend                          │
│  - Auth: Username/password                                               │
│  - Encryption: Optional TLS (SSLOptions)                                 │
│                                                                          │
│  [PostgreSQL]                                                            │
│  - Port: 5432/TCP                                                        │
│  - Purpose: Persistent metadata storage backend                          │
│  - Auth: Username/password or passfile                                   │
│  - Encryption: Optional TLS (sslmode configurable)                       │
│                                                                          │
│  [SQLite] (optional, embedded)                                           │
│  - In-memory or file-based                                               │
│  - Purpose: Lightweight/testing metadata storage                         │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- No Kubernetes RBAC resources defined
- This is a standalone gRPC service with no Kubernetes API interactions
- Access control is handled at the gRPC transport layer (optional mTLS)

Service Mesh Configuration:
- No service mesh configuration defined in this component
- May inherit namespace-level service mesh policies if deployed in a meshed namespace

Secrets:
- Database credentials (Opaque) - MySQL/PostgreSQL user, password, SSL certs
  - Provisioned by: Platform operator / DSP controller
  - Auto-Rotate: No
  - Injected via: MetadataStoreServerConfig protobuf config or CLI flags

- gRPC SSL certs (Opaque) - server_key, server_cert, custom_ca for gRPC TLS
  - Provisioned by: Platform operator
  - Auto-Rotate: No
  - Injected via: MetadataStoreServerConfig.SSLConfig

Container Security:
- Runs as non-root (USER 65534:65534 - nobody)
- Built on UBI9-minimal (minimal attack surface)
- Multi-stage build (build artifacts excluded from final image)
- Multi-arch: amd64, arm64, ppc64le

Network Exposure:
- Internal only (no ingress resources defined)
- No external network access required (only connects to internal databases)
- Accessed by DSP components within the cluster
