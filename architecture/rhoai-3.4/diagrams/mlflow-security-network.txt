┌─────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                   │
│                                                                          │
│  [ML SDK Client]  (Notebook / Data Science Pipeline)                     │
│  [Browser]        (RHOAI Dashboard user)                                 │
│         │                                                                │
│         │ Port: 443/TCP                                                  │
│         │ Protocol: HTTPS                                                │
│         │ Encryption: TLS 1.2+                                           │
│         │ Auth: Bearer Token (K8s SA) / OAuth session                    │
│         │ Headers: X-MLFLOW-WORKSPACE={namespace}                        │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Platform Gateway)                                              │
│                                                                          │
│  [RHOAI Data Science Gateway (Envoy)]                                    │
│  - Type: HTTPRoute (Gateway API)                                         │
│  - Path: /mlflow                                                         │
│  - Port: 443/TCP (external) → 5000/TCP (internal)                       │
│  - TLS Termination: TLS 1.2+ (SIMPLE mode)                              │
│  - Auth: Passthrough (token forwarded to backend)                        │
│         │                                                                │
│         │ Port: 5000/TCP                                                 │
│         │ Protocol: HTTP                                                 │
│         │ Encryption: None (cluster-internal plaintext)                   │
│         │ Auth: Bearer Token passed through from gateway                  │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  APPLICATION (MLflow Server Pod)                                         │
│                                                                          │
│  [kube-rbac-proxy sidecar]  (operator-managed)                           │
│  - TLS termination and auth enforcement in front of MLflow               │
│         │                                                                │
│         ▼                                                                │
│  [MLflow Tracking Server]                                                │
│  - Namespace: {operator-managed namespace}                               │
│  - ServiceAccount: mlflow-server                                         │
│  - Image: RHOAI MLflow container (UBI9 ubi-minimal:9.7)                  │
│  - Service: mlflow-server (ClusterIP, 5000/TCP)                          │
│  - Framework: FastAPI (ASGI) + Flask (WSGI via WSGIMiddleware)           │
│                                                                          │
│  Security Middleware:                                                     │
│  - Host header validation (allowlist)                                    │
│  - CORS blocking (no cross-origin by default)                            │
│  - CSP headers (Content Security Policy)                                 │
│  - Content-Type validation                                               │
│                                                                          │
│  [kubernetes-auth Plugin]                                                │
│  - Extracts Bearer Token from Authorization header                       │
│  - Resolves workspace from X-MLFLOW-WORKSPACE header                     │
│  - Validates via SelfSubjectAccessReview                                  │
│  - Token cache: 60s TTL, thread-safe locking                             │
│  - Pseudo-resources: mlflow.kubeflow.org API group                       │
│                                                                          │
│  [Envelope Encryption Engine]                                            │
│  - Algorithm: AES-256-GCM                                                │
│  - KEK derivation: PBKDF2-HMAC-SHA256 (600k iterations)                 │
│  - Per-secret random DEKs                                                │
│  - AAD binding prevents ciphertext substitution                          │
│  - KEK rotation: atomic via `mlflow crypto rotate-kek`                   │
│         │                                                                │
│         │ Multiple egress paths:                                         │
│         ▼                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS - Kubernetes API                                                 │
│                                                                          │
│  [Kubernetes API Server]                                                 │
│  - Port: 6443/TCP                                                        │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: Bearer Token (Service Account)                                  │
│  - Operations:                                                           │
│    - POST SelfSubjectAccessReview (authorization checks)                 │
│    - GET/LIST/WATCH namespaces (workspace enumeration)                   │
│    - GET/LIST/WATCH MLflowConfig CRs (per-namespace config)              │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS - Database                                                       │
│                                                                          │
│  [PostgreSQL Database]                                                   │
│  - Port: 5432/TCP                                                        │
│  - Protocol: PostgreSQL wire protocol                                    │
│  - Encryption: TLS (configurable)                                        │
│  - Auth: Database credentials (connection string)                        │
│  - Purpose: Tracking store, model registry, auth DB, workspace metadata  │
│                                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│  EGRESS - Object Storage (Optional)                                      │
│                                                                          │
│  [S3-compatible Storage / GCS / Azure Blob]                              │
│  - Port: 443/TCP                                                         │
│  - Protocol: HTTPS                                                       │
│  - Encryption: TLS 1.2+                                                  │
│  - Auth: AWS IAM / GCS Service Account / Azure credentials               │
│  - Purpose: Artifact storage read/write (when --serve-artifacts enabled) │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: (Operator-managed)
  - API Groups: ""
  - Resources: namespaces
  - Verbs: get, list, watch

- ClusterRole: (Operator-managed)
  - API Groups: authorization.k8s.io
  - Resources: selfsubjectaccessreviews
  - Verbs: create

- ClusterRole: (Operator-managed)
  - API Groups: mlflow.kubeflow.org
  - Resources: MLflowConfig
  - Verbs: get, list, watch

- ClusterRoleBinding: (Operator-managed)
  - Scope: cluster-scoped
  - Service Account: mlflow-server SA

Authentication & Authorization:
- /api/2.0/mlflow/* → Bearer Token (K8s SA) → kubernetes-auth plugin → SelfSubjectAccessReview
- /ajax-api/* → Bearer Token (K8s SA) → kubernetes-auth plugin → SelfSubjectAccessReview
- /v1/traces → Bearer Token (K8s SA) → kubernetes-auth plugin (experiment-scoped via x-mlflow-experiment-id)
- /health, /version → No auth (public health check)
- /static-files/*, / → No auth (static asset serving)

Secrets:
- MLFLOW_CRYPTO_KEK_PASSPHRASE (Opaque env var) - KEK passphrase for envelope encryption, Manual provisioning, No auto-rotate
- MLFLOW_FLASK_SERVER_SECRET_KEY (Opaque env var) - Flask session signing for CSRF, Manual provisioning, No auto-rotate
- Backend store URI (Opaque env var) - PostgreSQL connection string with credentials, Operator/manual, No auto-rotate

Crypto Configuration:
- Envelope encryption: AES-256-GCM with per-secret random DEKs
- KEK derivation: PBKDF2-HMAC-SHA256, 600,000 iterations
- Crypto library: cryptography>=43.0.0,<47 (OpenSSL backend, FIPS-capable on UBI9)
- TLS: Client-configurable via MLFLOW_TRACKING_SERVER_CERT_PATH
- No non-FIPS crypto risks detected (no MD5/RC4/DES)

Disabled Features (Attack Surface Reduction):
- MLFLOW_ENABLE_AI_GATEWAY=false
- MLFLOW_ENABLE_ASSISTANT=false
- MLFLOW_SERVER_ENABLE_JOB_EXECUTION=false
