================================================================================
  SECURITY NETWORK DIAGRAM: kube-auth-proxy
  Generated by: Claude Opus 4.6
  Source: architecture/rhoai-3.4/kube-auth-proxy.md
================================================================================

┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                              │
│  [User Browser]                    [API Client]                              │
│         │                                │                                   │
│         │ Port: 4180/TCP                 │ Port: 8443/TCP                    │
│         │ Protocol: HTTP/HTTPS           │ Protocol: HTTPS                   │
│         │ Encryption: configurable TLS   │ Encryption: TLS 1.2+             │
│         │ Auth: OAuth2/OIDC session      │ Auth: Bearer Token / X509 cert   │
│         ▼                                ▼                                   │
├──────────────────────────────────────────────────────────────────────────────┤
│  POD (Sidecar Container)                                                     │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │  entrypoint (Go Binary Multiplexer)                                 │     │
│  │  Selects proxy mode via PROXY_MODE env var or CLI arg               │     │
│  │  Default mode: rbac                                                 │     │
│  └─────────────┬───────────────────────────────┬───────────────────────┘     │
│                │ PROXY_MODE=auth                │ PROXY_MODE=rbac            │
│                ▼                                ▼                             │
│  ┌──────────────────────────┐    ┌──────────────────────────────┐           │
│  │  kube-auth-proxy         │    │  kube-rbac-proxy             │           │
│  │  Port: 4180/TCP          │    │  Port: 8443/TCP              │           │
│  │  Protocol: HTTP (or TLS) │    │  Protocol: HTTPS             │           │
│  │  Encryption: configurable│    │  Encryption: TLS 1.2+        │           │
│  │                          │    │                              │           │
│  │  Auth methods:           │    │  Auth methods:               │           │
│  │  - OAuth2/OIDC (cookie)  │    │  - Delegating (TokenReview)  │           │
│  │  - Bearer Token (JWT)    │    │  - OIDC (JWT)                │           │
│  │  - htpasswd (Basic Auth) │    │  - X509 (client cert)        │           │
│  │  - K8s SA Token          │    │                              │           │
│  │    (TokenReview)         │    │  Authz: SubjectAccessReview  │           │
│  │                          │    │                              │           │
│  │  Endpoints:              │    │  Endpoints:                  │           │
│  │  /oauth2/sign_in  (GET)  │    │  /* (ANY) - RBAC proxy       │           │
│  │  /oauth2/sign_out (GET)  │    │  /healthz (GET)              │           │
│  │  /oauth2/start    (GET)  │    │                              │           │
│  │  /oauth2/callback (GET)  │    └──────────────┬───────────────┘           │
│  │  /oauth2/auth     (GET)  │                   │                            │
│  │  /oauth2/userinfo (GET)  │                   │ Port: configurable         │
│  │  /ping            (GET)  │                   │ Protocol: HTTP/H2C         │
│  │  /ready           (GET)  │                   │ Encryption: configurable   │
│  │  /* (ANY) - reverse prxy │                   │ Auth: x-remote-user,       │
│  └──────────────┬───────────┘                   │       x-remote-groups      │
│                 │                                │                            │
│                 │ Port: configurable             │                            │
│                 │ Protocol: HTTP                 │                            │
│                 │ Auth: Forwarded headers        │                            │
│                 ▼                                ▼                            │
│  ┌──────────────────────────────────────────────────────────────────────┐    │
│  │  [Upstream Application]                                              │    │
│  │  Receives authenticated/authorized requests with identity headers    │    │
│  └──────────────────────────────────────────────────────────────────────┘    │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS (Outbound from proxy)                                                │
│                                                                              │
│  kube-auth-proxy egress:                                                     │
│         │                                                                    │
│         ├──► [Kubernetes API Server]                                         │
│         │    Port: 443/TCP                                                   │
│         │    Protocol: HTTPS                                                 │
│         │    Encryption: TLS 1.2+                                            │
│         │    Auth: ServiceAccount Token                                      │
│         │    Purpose: TokenReview API                                        │
│         │                                                                    │
│         ├──► [OpenShift OAuth Server]                                        │
│         │    Port: 443/TCP                                                   │
│         │    Protocol: HTTPS                                                 │
│         │    Encryption: TLS 1.2+                                            │
│         │    Auth: OAuth2 Client Credentials                                 │
│         │    Purpose: OAuth authorization code flow, token exchange          │
│         │                                                                    │
│         ├──► [OIDC Identity Provider]                                        │
│         │    Port: 443/TCP                                                   │
│         │    Protocol: HTTPS                                                 │
│         │    Encryption: TLS 1.2+                                            │
│         │    Auth: OAuth2 Client Credentials                                 │
│         │    Purpose: OIDC discovery, token exchange, JWKS retrieval         │
│         │                                                                    │
│         └──► [Redis] (optional)                                              │
│              Port: 6379/TCP                                                  │
│              Protocol: TCP                                                   │
│              Encryption: configurable TLS                                    │
│              Auth: Password/ACL                                              │
│              Purpose: Session storage                                        │
│                                                                              │
│  kube-rbac-proxy egress:                                                     │
│         │                                                                    │
│         └──► [Kubernetes API Server]                                         │
│              Port: 443/TCP                                                   │
│              Protocol: HTTPS                                                 │
│              Encryption: TLS 1.2+                                            │
│              Auth: ServiceAccount Token                                      │
│              Purpose: TokenReview + SubjectAccessReview                      │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (Inbound to proxy)                                                  │
│                                                                              │
│  [Envoy Gateway] ──► kube-auth-proxy /oauth2/auth                            │
│    Port: 4180/TCP                                                            │
│    Protocol: HTTP                                                            │
│    Encryption: plaintext (pod-internal)                                      │
│    Auth: Bearer Token / Cookie                                               │
│    Purpose: ext_authz decision (200 OK or 401/403)                           │
│                                                                              │
│  [Prometheus] ──► kube-auth-proxy                                            │
│    Port: 4180/TCP                                                            │
│    Protocol: HTTP                                                            │
│    Encryption: configurable                                                  │
│    Auth: None                                                                │
│    Purpose: Metrics scraping                                                 │
│                                                                              │
│  Note: No Ingress/Route/HTTPRoute resources created by this component.       │
│  Other RHOAI components create HTTPRoute/Route pointing to Services          │
│  fronted by kube-auth-proxy sidecars.                                        │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
─────────────
kube-auth-proxy mode (K8s token validation enabled):
  - API Group: authentication.k8s.io
    Resources: tokenreviews
    Verbs: create

kube-rbac-proxy mode:
  - API Group: authentication.k8s.io
    Resources: tokenreviews
    Verbs: create
  - API Group: authorization.k8s.io
    Resources: subjectaccessreviews
    Verbs: create

Note: No ClusterRole manifests shipped. Component requires these
permissions on the ServiceAccount of the pod it runs in.

Secrets:
────────
- OAuth Client Secret (Opaque) - OIDC/OAuth2 client secret, Admin-provisioned, No auto-rotate
- Cookie Secret (Opaque) - HMAC key for cookie encryption/signing, Admin-provisioned, No auto-rotate
- TLS Certificate/kube-auth-proxy (kubernetes.io/tls) - HTTPS listener, cert-manager/Admin, Depends
- TLS Certificate/kube-rbac-proxy (kubernetes.io/tls) - Secure listener, cert-manager/Admin, Yes (reloader)
- JWT Signing Key (Opaque) - Optional JWT signing, Admin-provisioned, No auto-rotate
- Service Account Token (kubernetes.io/service-account-token) - API access, Kubernetes, Yes (auto)
- Redis Password (Opaque) - Optional Redis auth, Admin-provisioned, No auto-rotate

FIPS Compliance:
────────────────
- Build: GOEXPERIMENT=strictfipsruntime, -tags strictfipsruntime
- CGO: CGO_ENABLED=1 (required for FIPS)
- Base Image: registry.access.redhat.com/ubi9/ubi-minimal
- TLS Minimum: TLS 1.2
- Validation: Automated check-payload verification in CI/CD

Authentication & Authorization Matrix:
───────────────────────────────────────
  Endpoint             │ Auth Mechanism                      │ Enforcement
  ─────────────────────┼─────────────────────────────────────┼──────────────────
  auth-proxy /*        │ OAuth2/OIDC, Bearer JWT, htpasswd,  │ kube-auth-proxy
                       │ K8s SA Token (TokenReview)          │
  auth-proxy /auth     │ Cookie/Bearer (ext_authz)           │ kube-auth-proxy
  rbac-proxy /*        │ Delegating, OIDC, X509              │ kube-rbac-proxy
  rbac-proxy /* (authz)│ SubjectAccessReview                 │ Kubernetes API
