┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [Browser / External Client]                                                │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.3                                               │
│         │ Auth: Bearer token or session cookie                              │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (RHOAI Gateway / Envoy)                                            │
│                                                                             │
│  [Envoy Proxy - Gateway API]                                                │
│  - Port: 443/TCP (external)                                                 │
│  - TLS Termination: TLS 1.3                                                 │
│  - ext_authz delegation to kube-auth-proxy                                  │
│         │                                                                   │
│         │ Port: 8080/TCP                                                    │
│         │ Protocol: HTTP                                                    │
│         │ Encryption: None (in-pod localhost)                                │
│         │ Auth: Bearer token forwarded via headers                          │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  SIDECAR CONTAINER (Pod-level trust boundary)                               │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────┐        │
│  │  kube-auth-proxy (auth mode)                                    │        │
│  │  - Image: quay.io/rhoai/odh-kube-auth-proxy-rhel9              │        │
│  │  - FIPS: strictfipsruntime + CGO_ENABLED=1                      │        │
│  │  - Port: 8080/TCP (HTTP) or 8443/TCP (HTTPS)                   │        │
│  │  - TLS: 1.2+ minimum, configurable cipher suites               │        │
│  │  - Session: Cookie (AES-GCM encrypted) or Redis                │        │
│  │  - AuthN: OIDC, OpenShift OAuth, JWT bearer, K8s SA token,     │        │
│  │           HTTP basic auth, session cookie                       │        │
│  │  - Endpoints:                                                   │        │
│  │    /oauth2/auth   - Envoy ext_authz (202/401)                  │        │
│  │    /oauth2/start  - Initiate OAuth2 flow                       │        │
│  │    /oauth2/callback - OAuth2 code exchange                     │        │
│  │    /oauth2/sign_out - Logout (deletes OAuthAccessToken)        │        │
│  │    /oauth2/userinfo - Current user info (JSON)                 │        │
│  │    /ping, /ready  - Health checks (no auth)                    │        │
│  │    /metrics       - Prometheus metrics (no auth)               │        │
│  └─────────────────────────────────────────────────────────────────┘        │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────┐        │
│  │  kube-rbac-proxy (rbac mode, default)                           │        │
│  │  - Image: quay.io/rhoai/odh-kube-auth-proxy-rhel9 (same)      │        │
│  │  - FIPS: strictfipsruntime + CGO_ENABLED=1                      │        │
│  │  - Port: 8443/TCP (HTTPS)                                      │        │
│  │  - TLS: 1.2+ minimum, hot-reloadable certs                     │        │
│  │  - AuthN: Bearer token (TokenReview), OIDC JWT, client cert    │        │
│  │  - AuthZ: SubjectAccessReview (Kubernetes RBAC)                │        │
│  │  - Endpoints:                                                   │        │
│  │    /*        - Authorized reverse proxy (Bearer/cert)          │        │
│  │    /metrics  - Hardcoded allow for prometheus-k8s SA           │        │
│  │    /healthz  - Health check (proxy-endpoints-port)             │        │
│  │  - HTTP/2: Disableable (CVE-2023-44487 mitigation)            │        │
│  └─────────────────────────────────────────────────────────────────┘        │
│         │                                                                   │
│         │ Port: Configurable                                                │
│         │ Protocol: HTTP / HTTPS / H2C                                      │
│         │ Encryption: Configurable                                          │
│         │ Auth: None (identity headers injected:                            │
│         │       X-Forwarded-User, X-Forwarded-Email,                        │
│         │       X-Forwarded-Groups, x-remote-user, x-remote-groups)        │
│         ▼                                                                   │
│  [Upstream Application - RHOAI Component Pod]                               │
│  - Same pod as sidecar (localhost)                                          │
│  - Receives pre-authenticated/authorized requests                          │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  EGRESS (Outbound from sidecar)                                             │
│                                                                             │
│  ┌────────────────────────────────────────────────┐                         │
│  │  [Kubernetes API Server]                        │                         │
│  │  - Port: 443/TCP                                │                         │
│  │  - Protocol: HTTPS                              │                         │
│  │  - Encryption: TLS 1.2+                         │                         │
│  │  - Auth: Service account token (automounted)    │                         │
│  │  - APIs:                                        │                         │
│  │    authentication.k8s.io/v1 TokenReview         │                         │
│  │    authorization.k8s.io/v1 SubjectAccessReview  │                         │
│  └────────────────────────────────────────────────┘                         │
│                                                                             │
│  ┌────────────────────────────────────────────────┐                         │
│  │  [OIDC Provider (Keycloak)]                     │                         │
│  │  - Port: 443/TCP                                │                         │
│  │  - Protocol: HTTPS                              │                         │
│  │  - Encryption: TLS 1.2+                         │                         │
│  │  - Auth: OAuth2 client credentials              │                         │
│  │  - Purpose: Token exchange, JWKS retrieval,     │                         │
│  │             OIDC discovery, token validation     │                         │
│  └────────────────────────────────────────────────┘                         │
│                                                                             │
│  ┌────────────────────────────────────────────────┐                         │
│  │  [OpenShift OAuth Server]                       │                         │
│  │  - Port: 443/TCP                                │                         │
│  │  - Protocol: HTTPS                              │                         │
│  │  - Encryption: TLS 1.2+                         │                         │
│  │  - Auth: Service account token                  │                         │
│  │  - Purpose: OAuth token exchange,               │                         │
│  │    OAuthAccessToken deletion on logout           │                         │
│  │  - Auto-discovers from K8s API .well-known      │                         │
│  └────────────────────────────────────────────────┘                         │
│                                                                             │
│  ┌────────────────────────────────────────────────┐                         │
│  │  [Redis (Optional)]                             │                         │
│  │  - Port: 6379/TCP                               │                         │
│  │  - Protocol: TCP or TLS                         │                         │
│  │  - Encryption: Optional TLS                     │                         │
│  │  - Auth: Password                               │                         │
│  │  - Purpose: Distributed session storage         │                         │
│  └────────────────────────────────────────────────┘                         │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  MONITORING                                                                  │
│                                                                             │
│  [Prometheus - openshift-monitoring]                                        │
│  - Port: 8443/TCP                                                           │
│  - Protocol: HTTPS                                                          │
│  - Encryption: TLS 1.2+                                                     │
│  - Auth: Hardcoded allow for                                                │
│    system:serviceaccount:openshift-monitoring:prometheus-k8s                │
│  - Purpose: Metrics scraping (/metrics)                                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- Required ClusterRole permissions (managed by rhods-operator):
  - API Group: authentication.k8s.io
    - Resources: tokenreviews
    - Verbs: create
  - API Group: authorization.k8s.io
    - Resources: subjectaccessreviews
    - Verbs: create
  - API Group: oauth.openshift.io (OpenShift mode only)
    - Resources: oauthaccesstokens
    - Verbs: get, delete

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, -tags strictfipsruntime
- Runtime: UBI 9 ubi-minimal base with OpenSSL
- TLS: 1.2+ minimum (configurable to 1.3)
- Crypto: Go stdlib crypto/tls via OpenSSL (FIPS mode), no MD5/RC4/DES detected
- CI: check-payload validation on every PR
- HTTP/2 disableable (CVE-2023-44487 mitigation)

Secrets:
- TLS cert/key (kubernetes.io/tls) - Server TLS, deploying operator/cert-manager, hot-reloadable
- Cookie secret (Opaque) - AES-GCM session cookie encryption, deploying operator, no auto-rotate
- Client secret (Opaque) - OAuth2 client credentials, deploying operator, no auto-rotate
- JWT signing key (Opaque) - /etc/ssl/private/jwt_signing_key.pem, build-time placeholder, no auto-rotate
- Redis password (Opaque) - Redis session store auth, deploying operator, no auto-rotate

Security Notes:
- InsecureSkipVerify available but opt-in (annotated with nosec)
- Session cookie encrypted with AES-GCM
- PKCE used for OAuth2 authorization code flow
- CSRF protection via double-submit cookie pattern
- OpenShift sha256~ token format supported
- Hardcoded metrics authorization for openshift-monitoring prometheus-k8s SA
- No CRDs or operator RBAC - all RBAC managed by rhods-operator
