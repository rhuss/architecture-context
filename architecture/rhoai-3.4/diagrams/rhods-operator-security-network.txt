RHOAI Operator (rhods-operator) - Security Network Diagram
Generated by: Claude Opus 4.6
==============================================================================

┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [User Browser]                                                              │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: None (initial), Cookie-based session (subsequent)            │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (DMZ) - Two Modes                                                   │
│                                                                              │
│  ┌─ LoadBalancer Mode ─────────────────────────────────────────────┐        │
│  │  [Gateway LB Service]                                            │        │
│  │  - Namespace: openshift-ingress                                  │        │
│  │  - Port: 443/TCP (external) → 443/TCP (internal)               │        │
│  │  - TLS Termination: at Gateway (Envoy)                           │        │
│  │  - Hostname: rh-ai.{cluster-domain}                              │        │
│  └──────────────────────────────────────────────────────────────────┘        │
│                                                                              │
│  ┌─ OcpRoute Mode ─────────────────────────────────────────────────┐        │
│  │  [OpenShift Router]                                              │        │
│  │  - Port: 443/TCP → [Gateway ClusterIP Service] 443/TCP         │        │
│  │  - TLS Mode: Reencrypt                                           │        │
│  │  - Route: data-science-gateway (route.openshift.io/v1)          │        │
│  │  - Hostname: rh-ai.{cluster-domain}                              │        │
│  └──────────────────────────────────────────────────────────────────┘        │
│                                                                              │
│         │                                                                    │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  GATEWAY LAYER (openshift-ingress namespace)                                 │
│                                                                              │
│  [Gateway - data-science-gateway]                                            │
│  - GatewayClass: data-science-gateway-class                                  │
│  - Controller: openshift.io/gateway-controller/v1                            │
│  - Listener: HTTPS/443 (TLS Terminate, SIMPLE mode)                         │
│  - EnvoyFilter: data-science-authn-filter (ext_authz + Lua)                 │
│  - DestinationRule: data-science-tls-rule (SIMPLE TLS, ports 443, 8443)     │
│         │                                                                    │
│         │ Port: 8443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (OpenShift service-CA)                             │
│         │ Auth: ext_authz check                                              │
│         ▼                                                                    │
│  [kube-auth-proxy]                                                           │
│  - Namespace: openshift-ingress                                              │
│  - Port: 8443/TCP (HTTPS, service-CA TLS)                                   │
│  - Port: 9000/TCP (metrics, HTTP, plaintext)                                │
│  - Auth: OAuth2 (OpenShift) or OIDC (external)                              │
│  - Session: Cookie-based (_oauth2_proxy cookie)                             │
│  - K8s SA Token validation: optional (enableK8sTokenValidation)             │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: OAuth2 Client Credentials / OIDC Client Credentials         │
│         ▼                                                                    │
│  [OpenShift OAuth Server / External OIDC Provider]                           │
│                                                                              │
│  ── After auth ──                                                            │
│                                                                              │
│  [Gateway (Envoy)] ──► [Backend Component Services]                         │
│  - Protocol: HTTP/HTTPS (per-component)                                      │
│  - Auth: Bearer Token (injected by Lua filter)                               │
│  - Routing: via HTTPRoute CRs referencing data-science-gateway              │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR LAYER (system namespace)                                           │
│                                                                              │
│  [RHOAI Operator - controller-manager]                                       │
│  - ServiceAccount: controller-manager                                        │
│  - Port: 8080/TCP (metrics, HTTP, plaintext)                                │
│  - Port: 8081/TCP (healthz/readyz, HTTP, plaintext)                         │
│  - Port: 9443/TCP (webhooks, HTTPS, TLS)                                    │
│         │                                                                    │
│         │ Port: 6443/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount Token                                         │
│         ▼                                                                    │
│  [Kubernetes API Server]                                                     │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  DASHBOARD REDIRECT LAYER                                                    │
│                                                                              │
│  [dashboard-redirect nginx]                                                  │
│  - Port: 8080/TCP (HTTP, plaintext)                                          │
│  - Purpose: 301 redirect from legacy dashboard URLs to rh-ai.{domain}       │
│  - OCP Routes: rhods-dashboard (legacy), data-science-gateway (old subdomain)│
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  [OpenShift OAuth Server]                                                    │
│  - Purpose: User authentication (IntegratedOAuth mode)                       │
│  - Port: 443/TCP, HTTPS, TLS 1.2+                                           │
│  - Auth: OAuth2 Client Credentials                                           │
│                                                                              │
│  [External OIDC Provider]                                                    │
│  - Purpose: User authentication (ROSA/external IdP)                          │
│  - Port: 443/TCP, HTTPS, TLS 1.2+                                           │
│  - Auth: OIDC Client Credentials                                             │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Purpose: Controller reconciliation, resource management                   │
│  - Port: 6443/TCP, HTTPS, TLS 1.2+                                          │
│  - Auth: ServiceAccount Token                                                │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

NETWORK POLICIES:
================================================================================
  Name: kube-auth-proxy
  Namespace: openshift-ingress
  Pod Selector: app: kube-auth-proxy
  Ingress Rules:
    - Allow from gateway pods → port 8443/TCP
    - Allow from openshift-monitoring → port 9000/TCP
    - Allow from openshift-user-workload-monitoring → port 9000/TCP

RBAC SUMMARY:
================================================================================
  ClusterRole: controller-manager-role
  ServiceAccount: controller-manager (system namespace)
  Binding: controller-manager-rolebinding (ClusterRoleBinding)

  Key Permissions:
  - datasciencecluster.opendatahub.io:
    Resources: datascienceclusters, /status, /finalizers
    Verbs: get, list, watch, create, update, patch, delete, deletecollection

  - dscinitialization.opendatahub.io:
    Resources: dscinitializations, /status, /finalizers
    Verbs: get, list, watch, create, update, patch, delete

  - components.platform.opendatahub.io:
    Resources: dashboards, workbenches, kserves, datasciencepipelines,
               modelregistries, kueues, rays, trustyais, trainingoperators,
               trainers, feastoperators, llamastackoperators, mlflowoperators,
               sparkoperators, modelsasservices, modelcontrollers (+/status, /finalizers)
    Verbs: get, list, watch, create, update, patch, delete

  - services.platform.opendatahub.io:
    Resources: auths, monitorings, gatewayconfigs (+/status, /finalizers)
    Verbs: get, list, watch, create, update, patch, delete

  - gateway.networking.k8s.io:
    Resources: gateways, gatewayclasses, httproutes
    Verbs: get, list, watch, create, update, patch, delete

  - networking.istio.io:
    Resources: envoyfilters, destinationrules
    Verbs: get, list, watch, create, update, patch, delete

  - route.openshift.io:
    Resources: routes, routes/custom-host
    Verbs: get, list, watch, create, update, patch, delete

  - oauth.openshift.io:
    Resources: oauthclients
    Verbs: get, list, watch, create, update, patch, delete

  - core (""):
    Resources: secrets, configmaps, services, serviceaccounts, namespaces,
               pods, events, endpoints, pvs, pvcs, nodes
    Verbs: get, list, watch, create, update, patch, delete

  - apps:
    Resources: deployments, replicasets, statefulsets
    Verbs: get, list, watch, create, update, patch, delete

  - rbac.authorization.k8s.io:
    Resources: roles, rolebindings, clusterroles, clusterrolebindings
    Verbs: * (all)

  - apiextensions.k8s.io:
    Resources: customresourcedefinitions
    Verbs: get, list, watch, create, patch, delete, update

  - monitoring.coreos.com:
    Resources: servicemonitors, podmonitors, prometheusrules, prometheuses,
               alertmanagers (+/status, /finalizers)
    Verbs: get, list, watch, create, update, patch, delete, deletecollection

  - security.openshift.io:
    Resources: securitycontextconstraints
    Verbs: get, list, watch, create, patch, use

  - serving.kserve.io:
    Resources: inferenceservices, servingruntimes, clusterservingruntimes,
               trainedmodels, predictors, inferencegraphs, etc. (+/status, /finalizers)
    Verbs: get, list, watch, create, update, patch, delete

  - kuadrant.io:
    Resources: authpolicies, tokenratelimitpolicies, ratelimitpolicies, telemetrypolicies
    Verbs: get, list, watch, create, update, patch, delete

SECRETS:
================================================================================
  Name: kube-auth-proxy-creds
  Type: Opaque
  Contents: OAuth2 proxy client ID, client secret, cookie secret
  Provisioned By: Gateway Controller (dynamic)
  Auto-Rotate: No (persisted once created)

  Name: kube-auth-proxy-tls
  Type: kubernetes.io/tls
  Contents: TLS cert for kube-auth-proxy service
  Provisioned By: OpenShift service-CA (annotation-based)
  Auto-Rotate: Yes

  Name: data-science-gatewayconfig-tls
  Type: kubernetes.io/tls
  Contents: TLS cert for Gateway HTTPS listener
  Provisioned By: Gateway Controller (from default ingress / self-signed / user-provided)
  Auto-Rotate: Depends on source type

  Name: data-science-gateway-service-tls
  Type: kubernetes.io/tls
  Contents: TLS cert for Gateway ClusterIP service (OcpRoute mode)
  Provisioned By: OpenShift service-CA
  Auto-Rotate: Yes

AUTHENTICATION & AUTHORIZATION:
================================================================================
  Gateway API (all routes):
    Methods: GET, POST, PUT, DELETE
    Auth: OAuth2 (OpenShift) or OIDC (external)
    Enforcement: EnvoyFilter ext_authz -> kube-auth-proxy
    Policy: Cookie-based session + Bearer token passthrough

  Gateway API (/oauth2/*):
    Methods: GET, POST
    Auth: OAuth2/OIDC callback
    Enforcement: HTTPRoute -> kube-auth-proxy
    Policy: OAuth2 callback flow

  K8s API (operator):
    Methods: All
    Auth: ServiceAccount Token
    Enforcement: Kubernetes RBAC
    Policy: ClusterRole controller-manager-role

  Webhook endpoints:
    Methods: POST
    Auth: k8s API server TLS client cert
    Enforcement: Kubernetes API server
    Policy: ValidatingWebhookConfiguration / MutatingWebhookConfiguration

  K8s Service Account Tokens:
    Auth: Bearer / TokenReview API validation
    Enforcement: kube-auth-proxy (enableK8sTokenValidation=true)
    Policy: SA can authenticate alongside OAuth/OIDC
