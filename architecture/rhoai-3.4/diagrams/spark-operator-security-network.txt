┌─────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                       │
│                                                                             │
│  [User / Data Scientist]                                                    │
│         │                                                                   │
│         │ Port: 443/TCP                                                     │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS 1.2+                                              │
│         │ Auth: Bearer token / kubeconfig                                   │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES CONTROL PLANE (Trusted)                                         │
│                                                                             │
│  [Kubernetes API Server]                                                    │
│  - Port: 443/TCP (external + internal)                                     │
│  - TLS Termination: TLS 1.2+                                               │
│  - Auth: Bearer token, client certificates, SA tokens                      │
│         │                                                                   │
│         │ Port: 9443/TCP                                                    │
│         │ Protocol: HTTPS                                                   │
│         │ Encryption: TLS (self-signed CA or cert-manager)                  │
│         │ Auth: API server client certificate                               │
│         │ Direction: API server → Webhook (admission review)                │
│         ▼                                                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│  OPERATOR NAMESPACE (redhat-ods-applications)                               │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  [spark-operator-webhook]                                          │    │
│  │  - Deployment: spark-operator-webhook                              │    │
│  │  - ServiceAccount: spark-operator-webhook                          │    │
│  │  - Service: spark-operator-webhook-svc (ClusterIP)                 │    │
│  │  - Port: 443/TCP → 9443/TCP (HTTPS, TLS self-signed/cert-manager) │    │
│  │  - Port: 8080/TCP (HTTP, metrics, no auth)                        │    │
│  │  - Port: 8081/TCP (HTTP, health probes, no auth)                  │    │
│  │  - Webhook endpoints:                                              │    │
│  │    - POST /mutate--v1-pod                                          │    │
│  │    - POST /mutate-sparkoperator-k8s-io-v1beta2-sparkapplication    │    │
│  │    - POST /mutate-...-scheduledsparkapplication                    │    │
│  │    - POST /validate-sparkoperator-k8s-io-v1beta2-sparkapplication  │    │
│  │    - POST /validate-...-scheduledsparkapplication                  │    │
│  │  - TLS Config: HTTP/2 disabled (NextProtos: ["http/1.1"])          │    │
│  │  - Cert: spark-operator-webhook-certs secret                      │    │
│  │    - 2048-bit RSA, self-signed CA, 10-year validity                │    │
│  │    - 180-day renewal threshold                                     │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  [spark-operator-controller]                                       │    │
│  │  - Deployment: spark-operator-controller                           │    │
│  │  - ServiceAccount: spark-operator-controller                       │    │
│  │  - Port: 8080/TCP (HTTP, metrics, no auth)                        │    │
│  │  - Port: 8081/TCP (HTTP, health probes, no auth)                  │    │
│  │  - Contains: spark-submit (in-process execution)                  │    │
│  │  - Leader election: via Lease in operator namespace                │    │
│  │         │                                                          │    │
│  │         │ Port: 443/TCP                                            │    │
│  │         │ Protocol: HTTPS                                          │    │
│  │         │ Encryption: TLS 1.2+                                     │    │
│  │         │ Auth: ServiceAccount token                               │    │
│  │         │ Purpose: CRD reconciliation, pod/svc/cm/ingress CRUD     │    │
│  │         ▼                                                          │    │
│  │  [Kubernetes API Server]                                           │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
│  NetworkPolicy: spark-operator-allow-internal                               │
│  PodMonitor: opendatahub.io/monitoring: "true"                             │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  USER NAMESPACES (per SparkApplication)                                     │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  [Driver Pod]                                                      │    │
│  │  - ServiceAccount: spark-operator-spark                            │    │
│  │  - Port: 7078/TCP (Driver RPC, TCP, no encryption, no auth)       │    │
│  │  - Port: 7079/TCP (BlockManager, TCP, no encryption, no auth)     │    │
│  │  - Optional sidecar: JMX Exporter (:8090/TCP HTTP)                │    │
│  │         │                                                          │    │
│  │         │ Port: 443/TCP                                            │    │
│  │         │ Protocol: HTTPS                                          │    │
│  │         │ Encryption: TLS 1.2+                                     │    │
│  │         │ Auth: spark-operator-spark SA token                      │    │
│  │         │ Purpose: Create executor pods                            │    │
│  │         ▼                                                          │    │
│  │  [Kubernetes API Server]                                           │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  [Executor Pods]                                                   │    │
│  │  - Optional sidecar: JMX Exporter (:8090/TCP HTTP)                │    │
│  │         │                                                          │    │
│  │         │ Port: 7078/TCP (Driver RPC)                              │    │
│  │         │ Port: 7079/TCP (BlockManager)                            │    │
│  │         │ Protocol: TCP                                            │    │
│  │         │ Encryption: None                                         │    │
│  │         │ Auth: None                                               │    │
│  │         ▼                                                          │    │
│  │  [Driver Pod]                                                      │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │  [SparkConnect Server Pod]  (when SparkConnect CR is used)         │    │
│  │  - Service: [sparkconnect]-server (ClusterIP)                     │    │
│  │  - Port: 7078/TCP (Driver RPC, TCP, no encryption)                │    │
│  │  - Port: 7079/TCP (BlockManager, TCP, no encryption)              │    │
│  │  - Port: 4040/TCP (Web UI, HTTP, no encryption)                   │    │
│  │  - Port: 15002/TCP (SparkConnect protocol, gRPC/HTTP2, no enc)    │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                                                             │
│  [Web UI Service] (per SparkApplication)                                    │
│  - Port: 4040/TCP (HTTP, no encryption, no auth)                          │
│  - Optional Ingress: [app]-ing (HTTP/HTTPS, optional TLS)                 │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  MONITORING                                                                 │
│                                                                             │
│  [Prometheus]                                                               │
│  - Scrapes controller :8080/TCP HTTP (spark_application_*, metrics)        │
│  - Scrapes webhook :8080/TCP HTTP (operator metrics)                       │
│  - Scrapes JMX Exporter sidecars :8090/TCP HTTP (JVM metrics)             │
│  - No encryption, no auth on metrics endpoints                             │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  OPTIONAL BATCH SCHEDULERS                                                  │
│                                                                             │
│  [Volcano]                                                                  │
│  - Integration: PodGroup CRD creation via Kubernetes API                   │
│  - Port: 443/TCP (HTTPS, TLS 1.2+, SA token)                             │
│  - Purpose: Gang scheduling for Spark driver + executors                   │
│                                                                             │
│  [Apache YuniKorn]                                                          │
│  - Integration: Pod annotations (no direct API call)                       │
│  - Purpose: Task group annotations for gang scheduling                     │
│                                                                             │
│  [Kube-Scheduler Plugins]                                                   │
│  - Integration: PodGroup CRD via Kubernetes API                            │
│  - Port: 443/TCP (HTTPS, TLS 1.2+, SA token)                             │
│  - Purpose: Scheduler framework PodGroup batch scheduling                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

Service Accounts:
- spark-operator-controller (operator namespace)
  - ClusterRole: spark-operator-controller
  - Permissions: pods (CRUD), configmaps (CRUD), services (CRUD),
    persistentvolumeclaims (list/watch), events (create/patch/update),
    nodes (get), resourcequotas (get/list/watch),
    customresourcedefinitions (get), ingresses (CRUD),
    sparkapplications (CRUD + status + finalizers),
    scheduledsparkapplications (get/list/watch + status + finalizers),
    sparkconnects (get/list/watch + status)
  - Role: spark-operator-controller-leader-election (leader election in namespace)

- spark-operator-webhook (operator namespace)
  - ClusterRole: spark-operator-webhook
  - Permissions: mutatingwebhookconfigurations (get/list/update/watch),
    validatingwebhookconfigurations (get/list/update/watch)

- spark-operator-spark (operator namespace)
  - Role: spark-operator-role
  - Permissions: pods, configmaps, persistentvolumeclaims, services
    (get/list/watch/create/update/patch/delete/deletecollection)

Aggregated Roles (user-facing):
- sparkapplication-viewer (aggregated to view): sparkapplications (get/list/watch)
- sparkapplication-editor (aggregated to edit, admin): sparkapplications (CRUD)
- scheduledsparkapplication-viewer (aggregated to view): scheduledsparkapplications (get/list/watch)
- scheduledsparkapplication-editor (aggregated to edit, admin): scheduledsparkapplications (CRUD)

Secrets:
- spark-operator-webhook-certs (Opaque)
  - Contents: ca-key.pem, ca-cert.pem, server-key.pem, server-cert.pem
  - Provisioned by: Internal certificate provider (self-signed) or cert-manager
  - Key size: 2048-bit RSA
  - Validity: 10 years
  - Renewal: 180-day threshold
  - Auto-rotate: Yes

FIPS Compliance:
- WARNING: CGO_ENABLED=0 (static linking) - uses pure-Go crypto, NOT FIPS-compliant
- Will FAIL check-payload validation on FIPS-enabled clusters
- TLS hardening: HTTP/2 disabled (NextProtos: ["http/1.1"])
- Java 17 (OpenJDK) in image uses its own TLS implementation

Network Policy:
- spark-operator-allow-internal: Restricts traffic within operator namespace
