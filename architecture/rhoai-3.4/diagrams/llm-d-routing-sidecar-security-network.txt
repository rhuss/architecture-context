┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [llm-d-inference-scheduler / EPP]                                           │
│  - Upstream routing layer that selects prefill targets                        │
│  - Sets x-prefiller-host-port header for disaggregated P/D                   │
│         │                                                                    │
│         │ Port: 8000/TCP                                                     │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+ (curated cipher suites)                       │
│         │   Ciphers: TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256,                  │
│         │            TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384,                  │
│         │            TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256,                │
│         │            TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384,                │
│         │            TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256,             │
│         │            TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256            │
│         │ Auth: None (relies on network policy / service mesh)                │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  SIDECAR POD (Decoder Pod Boundary)                                          │
│                                                                              │
│  [llm-d-routing-sidecar]                                                     │
│  - Container: sidecar alongside vLLM decoder                                 │
│  - Listening: 8000/TCP (HTTPS, TLS 1.2+)                                     │
│  - Image: ghcr.io/llm-d/llm-d-routing-sidecar:{VERSION}                     │
│  - ServiceAccount: parameterized (SSRF allowlist SA)                         │
│  - Build: CGO_ENABLED=0, static linking (non-FIPS upstream)                  │
│                                                                              │
│  ┌─────────────────────────────────────────────────┐                         │
│  │  SSRF PROTECTION (Optional)                     │                         │
│  │                                                 │                         │
│  │  AllowlistValidator:                            │                         │
│  │  - Watches InferencePool CR label selectors     │                         │
│  │  - Builds pod IP/hostname allowlist             │                         │
│  │  - Blocks unauthorized prefill targets (403)    │                         │
│  │  - Host-only validation (any port allowed)      │                         │
│  │  - 30-second resync period                      │                         │
│  │  - Disabled by default                          │                         │
│  └─────────────────────────────────────────────────┘                         │
│         │                                                                    │
│         │ EGRESS 1: Local Decoder                                            │
│         │ Port: 8001/TCP (configurable)                                      │
│         │ Protocol: HTTP or HTTPS                                            │
│         │ Encryption: Optional TLS 1.2+                                      │
│         │ Auth: None                                                         │
│         │ Path: localhost (same pod)                                          │
│         ▼                                                                    │
│                                                                              │
│  [vLLM Decoder]                                                              │
│  - Container: main inference server                                          │
│  - Listening: 8001/TCP (default)                                             │
│  - Receives decode requests with KV cache transfer params                    │
│  - Loads pre-computed KV cache blocks via NIXL                               │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER NETWORK (Pod-to-Pod)                                                │
│                                                                              │
│  EGRESS 2: Remote Prefiller                                                  │
│         │                                                                    │
│         │ Port: Configurable (from x-prefiller-host-port header)             │
│         │ Protocol: HTTP or HTTPS                                            │
│         │ Encryption: Optional TLS 1.2+ (--secure-prefiller flag)            │
│         │   InsecureSkipVerify: Available via flag (disabled by default)      │
│         │ Auth: None                                                         │
│         │ Target: Validated against SSRF allowlist (if enabled)               │
│         ▼                                                                    │
│                                                                              │
│  [vLLM Prefiller Pod(s)]                                                     │
│  - Remote prefill workers in the cluster                                     │
│  - Selected by llm-d-inference-scheduler                                     │
│  - Receives modified request with kv_transfer_params                         │
│  - Returns NIXL block IDs / engine metadata for KV cache transfer            │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  KUBERNETES API SERVER                                                       │
│                                                                              │
│  EGRESS 3: Kubernetes API (Conditional — SSRF protection only)               │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS                                                    │
│         │ Auth: ServiceAccount token (automounted)                           │
│         ▼                                                                    │
│                                                                              │
│  [Kubernetes API Server]                                                     │
│  - Watches: InferencePool (inference.networking.x-k8s.io/v1alpha2)           │
│  - Watches: Pods (matching InferencePool selectors)                          │
│  - Purpose: Build dynamic SSRF allowlist of valid prefill targets            │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  OPENSHIFT INGRESS (Optional)                                                │
│                                                                              │
│  [OpenShift Route]                                                           │
│  - Name: ${PROJECT_NAME}-route                                               │
│  - Port: 443/TCP → 8080/TCP (Service)                                       │
│  - TLS Mode: Edge termination                                                │
│  - Exposure: External                                                        │
│                                                                              │
│  [ClusterIP Service]                                                         │
│  - Name: ${PROJECT_NAME}-service                                             │
│  - Port: 8080/TCP → 8080/TCP                                                │
│  - Type: ClusterIP (internal only)                                           │
│  - No encryption at service level                                            │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:

  Role: ${PROJECT_NAME}-exec-role
  - API Groups: ""
  - Resources: pods/exec
  - Verbs: create
  - Bound To: Group: system:authenticated (namespace-scoped)

  Role: ssrf-allowlist-role
  - API Groups: ""
    - Resources: pods
    - Verbs: get, watch, list
  - API Groups: inference.networking.x-k8s.io
    - Resources: inferencepools
    - Verbs: get, watch, list
  - Bound To: ServiceAccount (parameterized, namespace-scoped)

TLS Configuration:

  Server (Sidecar → Clients):
  - Default: TLS enabled (--secure-proxy=true)
  - Min Version: TLS 1.2
  - Cipher Suites:
    - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
    - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
    - TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256
    - TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384
    - TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256
    - TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256
  - Certificate: External (--cert-path) or self-signed (4096-bit RSA, testing only)

  Client (Sidecar → Prefiller):
  - Configurable TLS (--secure-prefiller flag)
  - InsecureSkipVerify available (disabled by default)

  Client (Sidecar → Decoder):
  - Configurable TLS (--secure-decoder flag)
  - InsecureSkipVerify available (disabled by default)

Secrets:

  - TLS certificate (optional, kubernetes.io/tls)
    - Purpose: TLS for secure proxy (tls.crt, tls.key)
    - Provisioned by: Manual or cert-manager
    - Auto-Rotate: No

FIPS Notes:

  - Upstream Dockerfile: CGO_ENABLED=0 (static linking, NOT FIPS-compliant)
  - No FIPS build tags or OpenSSL in image (ubi-micro base)
  - Crypto: Go stdlib crypto/tls (pure-Go implementation)
  - Downstream RHOAI build expected to use CGO_ENABLED=1 + FIPS build tags
  - Cipher suites are FIPS-compatible (AES-GCM, ChaCha20) but not FIPS-validated
