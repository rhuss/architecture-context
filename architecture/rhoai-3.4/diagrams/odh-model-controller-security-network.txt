┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [User / kubectl]                                                            │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: OIDC / Bearer Token                                          │
│         ▼                                                                    │
│  [External Client]                                                           │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (edge or re-encrypt, per-ISVC annotation)          │
│         │ Auth: Per odh.opendatahub.io/auth-enabled annotation               │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS                                                                     │
│                                                                              │
│  [OpenShift Route - {isvc-name}]                                             │
│  - Port: 443/TCP (external) → runtime port (internal)                       │
│  - TLS Mode: Edge or Re-encrypt (auth-dependent)                            │
│  - Auto-generated hostname                                                   │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (Trusted)                                                  │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │  odh-model-controller                                               │     │
│  │  Namespace: {applications-namespace}                                │     │
│  │  ServiceAccount: odh-model-controller                               │     │
│  │  Image: quay.io/opendatahub/odh-model-controller:stable             │     │
│  │                                                                     │     │
│  │  Ports:                                                             │     │
│  │  - 8081/TCP  HTTP   (health probes /healthz, /readyz)  No TLS      │     │
│  │  - 8443/TCP  HTTPS  (Prometheus metrics)               TLS         │     │
│  │  - 9443/TCP  HTTPS  (admission webhooks)               TLS         │     │
│  │                                                                     │     │
│  │  Webhooks (9443/TCP, cluster-internal only):                        │     │
│  │  - /mutate--v1-pod (Ray TLS cert injection)                        │     │
│  │  - /validate-nim-opendatahub-io-v1-account                         │     │
│  │  - /validate-serving-kserve-io-v1alpha1-inferencegraph             │     │
│  │  - /validate-serving-kserve-io-v1beta1-inferenceservice            │     │
│  │  - /mutate-serving-kserve-io-v1alpha1-inferencegraph               │     │
│  │  - /mutate-serving-kserve-io-v1beta1-inferenceservice              │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount Token                                         │
│         ▼                                                                    │
│  [Kubernetes API Server]                                                     │
│  - All controller-runtime operations                                         │
│  - CRD watch/create/update/delete                                            │
│  - RBAC operations (ClusterRoleBindings, RoleBindings)                       │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │  model-serving-api                                                  │     │
│  │  Namespace: {applications-namespace}                                │     │
│  │  ServiceAccount: model-serving-api                                  │     │
│  │  Image: quay.io/opendatahub/odh-model-controller:odh-model-        │     │
│  │         serving-api-stable                                          │     │
│  │                                                                     │     │
│  │  Ports:                                                             │     │
│  │  - 8443/TCP  HTTPS  (API: /api/v1/gateways, /healthz, /readyz)    │     │
│  │    TLS 1.2+ FIPS cipher suites: ECDHE+AES-GCM, P-256/P-384        │     │
│  │    Auth: Bearer Token (user-forwarded)                              │     │
│  │    Cert: OpenShift service-CA (auto-rotated via fsnotify)           │     │
│  │  - 8080/TCP  HTTPS  (Prometheus /metrics)                          │     │
│  │    TLS 1.2+, No auth                                               │     │
│  │                                                                     │     │
│  │  Service: model-serving-api                                         │     │
│  │  - 443/TCP → 8443 (API)                                            │     │
│  │  - 8080/TCP → 8080 (metrics)                                       │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS, TLS 1.2+                                          │
│         │ Auth: User Bearer Token (SSAR) + ServiceAccount Token              │
│         ▼                                                                    │
│  [Kubernetes API Server]                                                     │
│  - SelfSubjectAccessReview (user token)                                      │
│  - List Gateways (SA token)                                                  │
│  - Get Namespaces (SA token)                                                 │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │  webhook-service (ClusterIP)                                        │     │
│  │  - Port: 443/TCP → 9443 (controller webhooks)                      │     │
│  │  - TLS, cluster-internal only                                       │     │
│  │  - Called by: Kubernetes API Server                                  │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │  {isvc-name}-metrics (ClusterIP, per InferenceService)              │     │
│  │  - Port: runtime-specific                                           │     │
│  │  - Protocol: HTTP (no TLS)                                          │     │
│  │  - Scraped by: Prometheus via ServiceMonitor                        │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES                                                           │
│                                                                              │
│  [NVIDIA NGC API - nvcr.io]                                                  │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS, TLS 1.2+                                                │
│  - Auth: API Key (nvapi-*)                                                   │
│  - Purpose: NIM runtime token exchange for image pull credentials            │
│                                                                              │
│  [NVIDIA NGC Catalog API - api.ngc.nvidia.com]                               │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS, TLS 1.2+                                                │
│  - Auth: API Key (nvapi-*)                                                   │
│  - Purpose: NIM model catalog queries and API key validation                 │
│                                                                              │
│  [Kubeflow Model Registry]                                                   │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS, TLS 1.2+ (configurable skip via MR_SKIP_TLS_VERIFY)     │
│  - Auth: Bearer Token                                                        │
│  - Purpose: Bidirectional InferenceService sync                              │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
- ClusterRole: odh-model-controller-role
  - 20+ API Groups including: core, serving.kserve.io, nim.opendatahub.io,
    route.openshift.io, gateway.networking.k8s.io, kuadrant.io,
    networking.istio.io, monitoring.coreos.com, keda.sh, rbac.authorization.k8s.io,
    template.openshift.io, config.openshift.io, metrics.k8s.io
  - Full CRUD on: inferenceservices, servingruntimes, routes, authpolicies,
    envoyfilters, networkpolicies, servicemonitors, triggerauthentications, templates
  - Read-only on: datascienceclusters, dscinitializations, authentications,
    kuadrants, authorinos, gateways

- ClusterRole: model-serving-api
  - gateway.networking.k8s.io/gateways: list
  - core/namespaces: get

- ClusterRoleBindings:
  - odh-model-controller-rolebinding → SA: odh-model-controller
  - model-serving-api-binding → SA: model-serving-api
  - Dynamic per-namespace: {ns}-{sa}-auth-delegator → system:auth-delegator

Secrets:
- model-serving-api-tls (kubernetes.io/tls)
  Purpose: TLS cert for model-serving-api HTTPS
  Provisioned: OpenShift service-CA (annotation-based)
  Rotation: Yes (fsnotify cert reloader)

- storage-config (Opaque)
  Purpose: Aggregated S3 data connection credentials
  Provisioned: Controller (from data connection secrets)
  Rotation: No (reconciled on change)

- ray-ca-tls (Opaque)
  Purpose: Self-signed CA certificate for Ray multi-node TLS
  Provisioned: Controller (RSA 4096-bit, 5-year validity)
  Rotation: No (checked for expiry)

- ray-tls (Opaque)
  Purpose: Per-pod Ray TLS certificates with SAN IPs
  Provisioned: Controller (3-year validity)
  Rotation: No (regenerated on pod IP change)

- inference-prometheus-auth (kubernetes.io/service-account-token)
  Purpose: Bearer token for KEDA Prometheus access
  Provisioned: Kubernetes (auto-populated)
  Rotation: Yes (K8s managed)

- NIM pull secret (kubernetes.io/dockerconfigjson)
  Purpose: NGC container registry pull credentials
  Provisioned: Controller (from NIM API key exchange)
  Rotation: Yes (on reconcile)

FIPS Compliance:
- Build: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking via UBI9
- model-serving-api TLS: FIPS cipher suites (ECDHE+AES-GCM), NIST P-256/P-384 only
- NIM HTTP client: TLS 1.2+ with default cipher suites (not explicitly FIPS-restricted)

Network Policies:
- Controller creates NetworkPolicies for InferenceService isolation
- Applied per-ISVC in user namespaces
