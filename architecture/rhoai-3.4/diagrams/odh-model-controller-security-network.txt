================================================================================
  SECURITY NETWORK DIAGRAM - odh-model-controller
  Generated by: Claude Opus 4.6
  Date: 2026-03-19
================================================================================

┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [User / kubectl]                                                            │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: Bearer Token (k8s auth)                                      │
│         ▼                                                                    │
│  [External Client]                                                           │
│         │                                                                    │
│         ├──────────────────────────────────────────────┐                     │
│         │                                              │                     │
│         │ Port: 443/TCP                                │ Port: 443/TCP       │
│         │ Protocol: HTTPS                              │ Protocol: HTTPS     │
│         │ Encryption: TLS Edge/Reencrypt               │ Encryption: TLS 1.2+│
│         │ Auth: None (direct)                          │ Auth: Per AuthPolicy│
│         ▼                                              ▼                     │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS                                                                     │
│                                                                              │
│  [OpenShift Router]                          [Envoy Gateway]                 │
│  - Dynamic Routes per InferenceService       - Gateway API pattern           │
│  - TLS Edge or Reencrypt termination         - Kuadrant AuthPolicy enforced  │
│         │                                              │                     │
│         │ Port: 80/TCP or 443/TCP                      │ TLS (EnvoyFilter)   │
│         │ Protocol: HTTP/HTTPS                         │ Auth: mTLS          │
│         │ Encryption: plaintext/TLS                    ▼                     │
│         │ Auth: None                           [Authorino]                   │
│         ▼                                    - Auth decision service          │
│  [Predictor Service]                         - TLS configured via            │
│                                                EnvoyFilter                   │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (Controller Namespace)                                     │
│                                                                              │
│  [odh-model-controller Manager]                                              │
│  - Namespace: controller namespace (e.g., redhat-ods-applications)           │
│  - ServiceAccount: odh-model-controller                                      │
│  - Image: odh-model-controller:latest                                        │
│                                                                              │
│  Ports:                                                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │ 8080/TCP  │ HTTP  │ plaintext │ None           │ Prometheus metrics │     │
│  │ 8081/TCP  │ HTTP  │ plaintext │ None           │ Health probes      │     │
│  │ 9443/TCP  │ HTTPS │ TLS       │ k8s API server │ Webhook endpoints  │     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│                                                                              │
│  Webhook Endpoints (9443/TCP TLS):                                           │
│  - /mutate--v1-pod (Ray TLS init-container injection)                        │
│  - /mutate-serving-kserve-io-v1beta1-inferenceservice                        │
│  - /mutate-serving-kserve-io-v1alpha1-inferencegraph                         │
│  - /validate-nim-opendatahub-io-v1-account                                   │
│  - /validate-serving-kserve-io-v1beta1-inferenceservice                      │
│  - /validate-serving-kserve-io-v1alpha1-inferencegraph                       │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS 1.2+                                               │
│         │ Auth: ServiceAccount token                                         │
│         ▼                                                                    │
│  [Kubernetes API Server]                                                     │
│  - Watches: InferenceService, ServingRuntime, LLMInferenceService,           │
│             NIM Account, ConfigMap, Secret, Pod, Gateway, HTTPRoute,         │
│             Kuadrant, Authorino                                              │
│  - Creates: Route, ServiceMonitor, PodMonitor, ClusterRoleBinding,          │
│             TriggerAuthentication, AuthPolicy, EnvoyFilter, NetworkPolicy,   │
│             Secret, ConfigMap, Template                                      │
│                                                                              │
│  [Gateway Discovery Server] (separate binary)                                │
│  - ServiceAccount: odh-model-controller                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐     │
│  │ 8443/TCP  │ HTTPS │ TLS │ Bearer Token (k8s SA) │ Gateway discovery │     │
│  │ 9090/TCP  │ HTTPS │ TLS │ None                  │ Prometheus metrics│     │
│  └─────────────────────────────────────────────────────────────────────┘     │
│  - Endpoint: GET /api/v1/gateways                                            │
│  - Auth: TokenReview against k8s API                                         │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER SERVICES                                                            │
│                                                                              │
│  [odh-model-controller-metrics-service]                                      │
│  - Type: ClusterIP                                                           │
│  - Port: 8080/TCP → 8080                                                     │
│  - Protocol: HTTP                                                            │
│  - Encryption: plaintext                                                     │
│  - Auth: None                                                                │
│                                                                              │
│  [odh-model-controller-webhook-service]                                      │
│  - Type: ClusterIP                                                           │
│  - Port: 443/TCP → 9443                                                      │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS (OpenShift serving cert)                                  │
│  - Auth: k8s API server                                                      │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EXTERNAL SERVICES (Egress)                                                  │
│                                                                              │
│  [NVIDIA NGC API - api.ngc.nvidia.com]                                       │
│  - Purpose: API key validation, model catalog retrieval                      │
│  - Port: 443/TCP                                                             │
│  - Protocol: HTTPS                                                           │
│  - Encryption: TLS 1.2+                                                      │
│  - Auth: API Key (Bearer token)                                              │
│                                                                              │
│  [Model Registry Service]                                                    │
│  - Purpose: Optional InferenceService metadata sync                          │
│  - Port: configurable (443/TCP or 80/TCP)                                    │
│  - Protocol: HTTP/HTTPS (configurable)                                       │
│  - Encryption: TLS (configurable)                                            │
│  - Auth: Bearer token                                                        │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

================================================================================
RBAC Summary
================================================================================

ClusterRole: odh-model-controller-role
  ServiceAccount: odh-model-controller (cluster-scoped binding)

  Core Resources:
  - "" / configmaps, secrets, serviceaccounts, services:
      create, delete, get, list, patch, update, watch
  - "" / endpoints, namespaces, pods:
      create, get, list, patch, update, watch
  - "" / events: create, patch

  KServe Resources:
  - serving.kserve.io / inferenceservices, inferenceservices/finalizers:
      get, list, watch, update, patch, create, delete
  - serving.kserve.io / servingruntimes, servingruntimes/finalizers:
      create, get, list, update, watch
  - serving.kserve.io / llminferenceservices, llminferenceservices/status,
    llminferenceservices/finalizers:
      get, list, watch, update, patch, post
  - serving.kserve.io / inferencegraphs, inferencegraphs/finalizers,
    llminferenceserviceconfigs:
      get, list, watch, update

  NIM Resources:
  - nim.opendatahub.io / accounts, accounts/status, accounts/finalizers:
      get, list, watch, update, patch

  OpenShift Resources:
  - route.openshift.io / routes, routes/custom-host:
      create, delete, get, list, patch, update, watch
  - template.openshift.io / templates:
      create, delete, get, list, patch, update, watch
  - config.openshift.io / authentications:
      get, list, watch

  Gateway API Resources:
  - gateway.networking.k8s.io / gateways, gateways/finalizers:
      get, list, watch, update, patch
  - gateway.networking.k8s.io / httproutes:
      get, list, watch

  Kuadrant/Authorino Resources:
  - kuadrant.io / authpolicies, authpolicies/status, kuadrants:
      create, delete, get, list, patch, update, watch
  - operator.authorino.kuadrant.io / authorinos:
      get, list, watch

  Istio Resources:
  - networking.istio.io / envoyfilters:
      create, delete, get, list, patch, update, watch

  Network Resources:
  - networking.k8s.io / networkpolicies:
      create, delete, get, list, patch, update, watch
  - networking.k8s.io, extensions / ingresses:
      get, list, watch

  RBAC Resources:
  - rbac.authorization.k8s.io / clusterrolebindings, roles, rolebindings:
      create, delete, get, list, patch, update, watch

  Monitoring Resources:
  - monitoring.coreos.com / servicemonitors, podmonitors:
      create, delete, get, list, patch, update, watch
  - keda.sh / triggerauthentications:
      create, delete, get, list, patch, update, watch
  - metrics.k8s.io / pods, nodes:
      get, list, watch

  Platform Resources:
  - datasciencecluster.opendatahub.io / datascienceclusters:
      get, list, watch
  - dscinitialization.opendatahub.io / dscinitializations:
      get, list, watch

Dynamic ClusterRole: prometheus-ns-access
  - Created dynamically for Prometheus namespace access
  - Bound in monitoring/user namespaces for SA: prometheus-custom

================================================================================
Secrets
================================================================================

| Secret Name                         | Type                          | Provisioned By                     | Auto-Rotate |
|-------------------------------------|-------------------------------|------------------------------------|-------------|
| odh-model-controller-webhook-cert   | kubernetes.io/tls             | OpenShift service-ca (annotation)  | Yes         |
| ray-ca-tls                          | Opaque                        | odh-model-controller (self-signed) | No          |
| ray-tls                             | Opaque                        | odh-model-controller (per-pod)     | No*         |
| storage-config (dynamic)            | Opaque                        | odh-model-controller (reconciled)  | No          |
| {nim-account}-pull-secret (dynamic) | kubernetes.io/dockerconfigjson| odh-model-controller (from NIM key)| No          |

* ray-tls certs are regenerated on pod IP changes

================================================================================
Authentication & Authorization
================================================================================

| Endpoint                       | Auth Mechanism                    | Enforcement Point     |
|--------------------------------|-----------------------------------|-----------------------|
| Webhooks (9443/TCP)            | k8s API server TLS client auth    | kube-apiserver        |
| Discovery API (/api/v1/gateways)| Bearer Token (k8s SA)            | middleware.Auth        |
| ISVC Routes (dynamic)          | None (direct) or TLS reencrypt    | OpenShift Router      |
| LLMInferenceService Gateways   | Kuadrant AuthPolicy               | Authorino + EnvoyFilter|
