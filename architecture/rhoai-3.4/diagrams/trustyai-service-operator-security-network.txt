┌──────────────────────────────────────────────────────────────────────────────┐
│  EXTERNAL (Untrusted)                                                        │
│                                                                              │
│  [External Clients / Data Scientists]                                        │
│         │                                                                    │
│         │ Port: 443/TCP                                                      │
│         │ Protocol: HTTPS                                                    │
│         │ Encryption: TLS (OpenShift Router)                                 │
│         │ Auth: Bearer Token (optional, SAR-enforced)                        │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  INGRESS (OpenShift Routes - Reencrypt TLS)                                  │
│                                                                              │
│  [TAS Route] ──────────── 443/TCP → {tas}-tls:8443 (Reencrypt)              │
│  [GORCH Route] ────────── 443/TCP → {gorch}-service:8432 (Reencrypt)        │
│  [GORCH Health Route] ─── 443/TCP → {gorch}-service:8034 (Edge)             │
│  [GORCH Gateway Route] ── 443/TCP → {gorch}-service:8490 (Reencrypt)        │
│  [GORCH Built-in Route] ─ 443/TCP → {gorch}-service:8480 (Reencrypt)        │
│  [NeMo Route] ─────────── 443/TCP → {nemo}:8443 (Reencrypt)                │
│  [EvalHub Route] ──────── 443/TCP → {evalhub}:8443 (Reencrypt)              │
│         │                                                                    │
│         ▼                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│  CLUSTER INTERNAL (Trusted)                                                  │
│                                                                              │
│  ┌─ TrustyAI Service (TAS) ─────────────────────────────────────────┐       │
│  │  [kube-rbac-proxy] ── Port: 8443/TCP                             │       │
│  │    Protocol: HTTPS │ Encryption: TLS 1.2+ (FIPS ciphers only)    │       │
│  │    Auth: Bearer Token → SubjectAccessReview                      │       │
│  │    Ciphers: ECDHE_RSA/ECDSA_AES-GCM, RSA_AES-GCM               │       │
│  │         │                                                        │       │
│  │         ▼                                                        │       │
│  │  [TrustyAI Service] ── Port: 8080/TCP (HTTP, internal)           │       │
│  │                     ── Port: 4443/TCP (HTTPS, internal TLS)      │       │
│  │    SA: {tas}-proxy                                               │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│  ┌─ Guardrails Orchestrator (GORCH) ────────────────────────────────┐       │
│  │  [kube-rbac-proxy (orchestrator)] ── Port: 8432/TCP              │       │
│  │    Protocol: HTTPS │ Encryption: TLS │ Auth: SAR                  │       │
│  │         │                                                        │       │
│  │         ▼                                                        │       │
│  │  [Orchestrator] ── Port: 8032/TCP (HTTP, loopback)               │       │
│  │                                                                  │       │
│  │  [kube-rbac-proxy (gateway)] ── Port: 8490/TCP                   │       │
│  │    Protocol: HTTPS │ Encryption: TLS │ Auth: SAR                  │       │
│  │         │                                                        │       │
│  │         ▼                                                        │       │
│  │  [Gateway Sidecar] ── internal                                   │       │
│  │                                                                  │       │
│  │  [kube-rbac-proxy (built-in)] ── Port: 8480/TCP                  │       │
│  │    Protocol: HTTPS │ Encryption: TLS │ Auth: SAR                  │       │
│  │         │                                                        │       │
│  │         ▼                                                        │       │
│  │  [Built-in Regex Detector] ── Port: 8080/TCP (HTTP, loopback)    │       │
│  │                                                                  │       │
│  │  [Health] ── Port: 8034/TCP (HTTP, no auth)                      │       │
│  │    SA: {gorch}-serviceaccount                                    │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│  ┌─ NeMo Guardrails ────────────────────────────────────────────────┐       │
│  │  [kube-rbac-proxy] ── Port: 8443/TCP                             │       │
│  │    Protocol: HTTPS │ Encryption: TLS │ Auth: SAR                  │       │
│  │         │                                                        │       │
│  │         ▼                                                        │       │
│  │  [NeMo Server] ── internal                                       │       │
│  │    CA Bundle: ODH + OpenShift + user-provided CAs                │       │
│  │    SA: {nemo}-serviceaccount                                     │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│  ┌─ EvalHub ────────────────────────────────────────────────────────┐       │
│  │  [kube-rbac-proxy] ── Port: 8443/TCP                             │       │
│  │    Protocol: HTTPS │ Encryption: TLS (OpenShift serving cert)    │       │
│  │    Auth: Bearer Token → SAR + X-Tenant header                    │       │
│  │    Multi-tenant: per-namespace access control                    │       │
│  │         │                                                        │       │
│  │         ▼                                                        │       │
│  │  [EvalHub API Server] ── internal                                │       │
│  │    SA: {evalhub}-service                                         │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│  ┌─ Operator ───────────────────────────────────────────────────────┐       │
│  │  [trustyai-service-operator] ── Port: 8080/TCP (metrics, HTTP)   │       │
│  │                              ── Port: 8081/TCP (probes, HTTP)    │       │
│  │    SA: controller-manager                                        │       │
│  │    No external access (cluster-internal only)                    │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                              │
│  ┌─ LMEval Jobs (Ephemeral) ────────────────────────────────────────┐       │
│  │  [Evaluation Pod]                                                │       │
│  │    init: ta-lmes-driver → shared EmptyDir                        │       │
│  │    main: lm-eval + driver HTTP server                            │       │
│  │    Driver: Port 18080/TCP (localhost-only, no auth)               │       │
│  │    Controller polls via kubectl exec (SPDY/TLS)                  │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│                                                                              │
├──────────────────────────────────────────────────────────────────────────────┤
│  EGRESS (External Services)                                                  │
│                                                                              │
│  [KServe InferenceService endpoints]                                         │
│    Port: varies │ Protocol: HTTP/HTTPS │ Encryption: TLS (conditional)       │
│    Auth: None │ Purpose: TAS prediction logging, GORCH guardrailed traffic   │
│                                                                              │
│  [S3-compatible storage]                                                     │
│    Port: 443/TCP │ Protocol: HTTPS │ Encryption: TLS                         │
│    Auth: AWS IAM credentials (from Secret)                                   │
│    Purpose: LMES downloads offline HuggingFace assets                        │
│                                                                              │
│  [OCI Registries]                                                            │
│    Port: 443/TCP │ Protocol: HTTPS │ Encryption: TLS                         │
│    Auth: Username/Password or dockerConfigJson (from Secret)                 │
│    Purpose: LMES uploads evaluation result artifacts                         │
│                                                                              │
│  [Git Repositories]                                                          │
│    Port: 443/TCP │ Protocol: HTTPS │ Encryption: TLS                         │
│    Auth: None │ Purpose: LMES clones custom evaluation tasks (HTTPS-only)    │
│                                                                              │
│  [PostgreSQL Database]                                                       │
│    Port: configurable │ Protocol: TCP │ Encryption: TLS (optional, verify-ca)│
│    Auth: Username/Password (from Secret)                                     │
│    Purpose: TAS database storage mode                                        │
│                                                                              │
│  [MLFlow Server]                                                             │
│    Port: varies │ Protocol: HTTP/HTTPS │ Encryption: TLS (conditional)       │
│    Auth: ServiceAccount token                                                │
│    Purpose: EvalHub records experiment results                               │
│                                                                              │
│  [OTLP Collector]                                                            │
│    Port: configurable │ Protocol: gRPC/HTTP │ Encryption: TLS (conditional)  │
│    Auth: None │ Purpose: GORCH/EvalHub OpenTelemetry export                  │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘

RBAC Summary:
─────────────
ClusterRole: tas-manager-role
  - trustyai.opendatahub.io: trustyaiservices (get,list,watch,create,update,patch,delete)
  - apps: deployments (list,watch,get,create,update,patch,delete)
  - core: services,pods,configmaps,pvcs,secrets (get,list,watch,create,update,patch,delete)
  - route.openshift.io: routes (list,watch,get,create,update,patch,delete)
  - serving.kserve.io: inferenceservices (list,watch,get,update,patch,delete)
  - networking.istio.io: destinationrules,virtualservices (create,list,watch,get,update,patch,delete)
  - monitoring.coreos.com: servicemonitors (list,watch,create)

ClusterRole: lmes-manager-role
  - trustyai.opendatahub.io: lmevaljobs (get,list,watch,create,update,patch,delete)
  - core: pods,pods/exec (get,list,watch,create,delete)
  - core: configmaps,secrets (get,watch,list)

ClusterRole: gorch-manager-role
  - trustyai.opendatahub.io: guardrailsorchestrators (get,list,watch,create,update,patch,delete)
  - serving.kserve.io: servingruntimes (get,list,watch), inferenceservices (get,list)
  - apps: deployments (list,watch,get,create,update,patch,delete)
  - core: services,pods,configmaps,events,secrets,serviceaccounts (get,list,watch,create,update,patch,delete)

ClusterRole: nemo-guardrails-manager-role
  - trustyai.opendatahub.io: nemoguardrails (get,list,watch,create,update,patch,delete)
  - core: configmaps,services,secrets,serviceaccounts (get,list,watch,create,update,patch,delete)
  - apps: deployments (get,list,watch,create,update,patch,delete)

ClusterRole: evalhub-manager-role
  - trustyai.opendatahub.io: evalhubs,status-events (get,list,watch,create,update,patch,delete)
  - batch: jobs (get,list,watch,patch,delete)
  - rbac.authorization.k8s.io: roles,rolebindings,clusterrolebindings (get,list,watch,create,update,patch,delete)

ClusterRole: job-mgr-manager-role
  - kueue.x-k8s.io: workloads (get,list,watch,create,update,patch,delete)
  - kueue.x-k8s.io: resourceflavors,workloadpriorityclasses (get,list,watch)

ClusterRole: proxy-role
  - authentication.k8s.io: tokenreviews (create)
  - authorization.k8s.io: subjectaccessreviews (create)

Authentication & Authorization:
───────────────────────────────
- TrustyAI API (8443): Bearer Token → SAR (services/get on instance name) via kube-rbac-proxy
- GORCH orchestrator (8432): Bearer Token → SAR via kube-rbac-proxy (configurable template)
- GORCH gateway (8490): Bearer Token → SAR via kube-rbac-proxy (configurable template)
- GORCH built-in detector (8480): Bearer Token → SAR via kube-rbac-proxy (configurable template)
- NemoGuardrails (8443): Bearer Token → SAR via kube-rbac-proxy (configurable template)
- EvalHub API (8443): Bearer Token + X-Tenant header → per-namespace SAR via kube-rbac-proxy

TLS Configuration:
──────────────────
- kube-rbac-proxy: TLS 1.2 minimum, FIPS-approved cipher suites ONLY
  Ciphers: TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256, TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384,
           TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256, TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384,
           TLS_RSA_WITH_AES_128_GCM_SHA256, TLS_RSA_WITH_AES_256_GCM_SHA384
- OpenShift Routes: Reencrypt (TAS, GORCH, NeMo, EvalHub), Edge (GORCH health)
- Certificates: OpenShift Service CA auto-generated (auto-rotate: yes)

Secrets:
────────
- {tas}-internal (kubernetes.io/tls) - Internal TLS cert for TrustyAI (port 4443), OpenShift Service CA, auto-rotate
- {tas}-tls (kubernetes.io/tls) - kube-rbac-proxy TLS cert (port 8443), OpenShift Service CA, auto-rotate
- {tas}-db-credentials (Opaque) - Database credentials (user, password, host, port, name), manual
- {tas}-db-ca (Opaque) - Database TLS CA certificate (optional), manual
- {gorch}-tls (kubernetes.io/tls) - GORCH service TLS cert, OpenShift Service CA, auto-rotate
- {nemo}-tls (kubernetes.io/tls) - NeMo service TLS cert, OpenShift Service CA, auto-rotate
- {evalhub}-tls (kubernetes.io/tls) - EvalHub service TLS cert, OpenShift Service CA, auto-rotate
- S3 credentials (Opaque) - AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, region, bucket, endpoint, manual
- OCI credentials (Opaque) - Registry username/password or dockerConfigJson, manual

FIPS Compliance:
────────────────
- Operator: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking, UBI9 base
- LMES Driver: GOEXPERIMENT=strictfipsruntime, CGO_ENABLED=1, dynamic linking, UBI9 base
- Runtime: stdlib crypto/tls via OpenSSL (FIPS mode), FIPS-approved cipher suites only
- No non-FIPS crypto risks detected
